# SMES_CORE_PREP004_CHALLENGE_REPORT.md
# Independent adversarial challenge — round status

Session `[SMEPLUS-26-09-10-VDR-PREP-004]` · Layer: **LAYER 1 — CLEAN-ROOM.** Checkpoint 12.

---

## 1. Round status at this baseline

> **OPEN — no challenge has been conducted against Baseline B1.**

This file exists so that its emptiness is explicit. A missing challenge report and an unconducted
challenge are the same fact, and the previous round was criticised for leaving a reviewer to infer it.

## 2. Round protocol, adopted after `PMO4-01`

The previous round's freeze was broken because the round was treated as closed **on the inference that
all challengers had reported**, rather than on a recorded close. One challenger then resumed and read a
superseded baseline.

**For this round:**

| Rule | |
|------|--|
| Round opens | at the recorded freeze SHA, with a timestamp |
| Round closes | only by an **explicit recorded close with a timestamp** — never by inferring that reports have stopped arriving |
| Between open and close | **zero commits into the package path.** Self-found corrections are held outside it |
| If a correction is required | the round is **invalidated** and a new baseline created, per §16 — the frozen artefact is never edited |
| A challenger that resumes after the close | reads a superseded baseline, and its findings are dispositioned against the **current** one |

## 3. What the challenge must attack

Carried forward as the mandate, and shaped by where this round's own defects were found:

- **The runtime observation join** — the largest new claim. Can its predicate fail? Are the three evidence tiers honestly assigned, or does an indirect route get reported as element-level?
- **The `SECURITY` grade, corrected from 0 to 72.92%** — the previous round found this dimension *inverted*. Is the correction now right, or merely inverted the other way?
- **`SOURCE` at 53.78%** — graded by pointer resolution. Does the test actually fail on a bad pointer?
- **`OPTIONAL_FUNCTION` and `CONFIGURATION`** — both rose sharply on subject-level determinations inherited by elements. **Is inheriting a subject's determination to its elements legitimate, or is it the class-label defect wearing new clothes?**
- **The container rule** applied to 17 nodes — is 17 the right population?
- **Every zero and every 100%**, as always.
- **The `EDGE` exclusion of 1,014 items** whose condition uses detection-failure vocabulary — is excluding them right, or is it now too harsh?
- **The two gap closures** — are they genuinely complete, or closed because the round wanted a number?

## 4. Disposition

**No challenge findings exist to disposition.** The package's own self-found corrections for this round
are recorded in the machine registers outside the Layer 1 documents, and no correction has been applied
to this baseline after its freeze.
