> DOMAIN_01 — Accounting Core | Team A PART 2 (Sonnet) | Input: committed Part 1 evidence | No SMEsPlus design
> ADVANCEMENT_CANDIDATE — TEAM B DESIGN INPUT AFTER AUDIT. No implementation proposed anywhere below.

# 12 — REFERENCE-TO-ADVANCEMENT REGISTER

| ID | Reference capability | Evidence | Limitation / gap | Independent principle | Advancement objective | Measurement candidate | Status |
|---|---|---|---|---|---|---|---|
| ADV-01 | Entry balance validation | CF-01, MR-01 | App-only, suppressible, no aggregate DB backstop; 0 triggers exist | The double-entry identity must hold provably at persistence, not merely at the moment application code chooses to check it | Make the balance guarantee non-optional at the point data becomes durable | Zero unbalanced entries ever reach a queryable "committed" state, independent of which code path wrote them | **SUPPORTED** |
| ADV-02 | Ledger tamper-evidence | CF-02 | Opt-in per journal; default value unconfirmed (GAP-D01-14); Thai law confirms this is mandatory for e-Tax invoices specifically | Regulatory-grade documents require provable integrity; this should not depend on a configuration choice being remembered | Tamper-evidence as a default property, with the current opt-in model understood as the floor to beat, not the target | Every regulated document class has integrity coverage by default, not by administrator memory | **SUPPORTED** |
| ADV-03 | Period cutoff control | CF-03; NetSuite triangulation | 6 independent date fields + per-user variants + exception object + code-level bypass; peer (NetSuite) uses one 3-state object + 1 override permission | One authoritative answer to "is this period open for this posting?" is achievable and demonstrated by a peer | Reduce the number of independent, potentially-disagreeing controls governing one business question | Number of distinct mechanisms that must be inspected to answer "can I post here" (peer: ~2; reference: 8+) | **SUPPORTED** |
| ADV-04 | Correction of a posted entry | CF-04 + CF-06 + RC-03; SAP B1 triangulation | Reference system offers BOTH a sound pattern (reversal) and an unsound one (reset-to-draft posted history), and does not force the sound one; peer explicitly forbids the unsound one | Once committed and consumed downstream, a fact should be correctable only additively | Eliminate or gate the destructive correction path so the additive path is the only one available for committed, downstream-consumed facts | Percentage of corrections to committed entries that are additive (reversal-linked) vs. destructive (in-place edit) — target 100% additive for consumed facts | **SUPPORTED — HIGHEST PRIORITY** (06_STATE_EVENT_LOGIC_ANALYSIS identifies this as the domain's central weakness) |
| ADV-05 | Document typing (one table, many document kinds) | Part 1 F-12/MC-14 | `move_type` overloads one table for journal entries, invoices, bills and credit notes | Every consumer of the model must branch on type to know what it's looking at | Reduce the branching burden a correct system inherits when one storage shape serves semantically distinct business objects | Number of distinct type-dependent behaviours a downstream consumer must special-case | **SUPPORTED**, carried from Part 1, not independently re-evidenced this round |
| ADV-06 | Monetary column representation | CF-05, MR-02 | Three columns (debit/credit/balance) encode two degrees of freedom; **no CHECK constraint found tying `balance` to `debit − credit`** — a sharper finding than Part 1's general "redundancy" note | A single authoritative amount representation avoids the possibility of internal disagreement | Reduce the number of independently-writable columns representing one monetary fact | Whether the stored trio can ever disagree (currently: structurally possible, not disproven) | **SUPPORTED**, sharpened this round |
| ADV-07 | State/status representation | 06_STATE_EVENT_LOGIC_ANALYSIS | One `state` field conflates "is this committed yet" with "should this still count"; mutability is gated on raw status, not on whether the fact has been consumed downstream | The invariant that should gate mutability (external consumption) is not the invariant that does gate it (status) | Gate correction-path availability on what actually matters (has this been consumed downstream), not merely on a status enum | Whether "has this been consumed by anything external" is knowable and enforced before a destructive correction path is offered | **SUPPORTED, NEW THIS ROUND** — not present in Part 1's advancement candidates |
| ADV-08 | Currency remeasurement (IAS 21) | MR-04, INV-04b | Whether periodic remeasurement of monetary items is implemented is UNKNOWN, not evidenced either way | IAS 21 compliance requires remeasurement at each reporting date, not just recognition at transaction date | — (cannot form an advancement objective without first knowing whether the reference system already does this) | — | **RESEARCH REQUIRED** — not enough evidence yet to state a limitation, only an unknown |
