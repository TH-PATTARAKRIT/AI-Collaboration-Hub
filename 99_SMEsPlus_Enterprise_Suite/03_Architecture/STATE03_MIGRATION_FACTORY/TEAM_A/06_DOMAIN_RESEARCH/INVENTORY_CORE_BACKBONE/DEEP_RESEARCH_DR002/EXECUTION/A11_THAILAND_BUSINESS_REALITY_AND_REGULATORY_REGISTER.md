# A11 — Thailand Business Reality and Regulatory Register

| Item / Task | Owner | Evidence location | Timestamp | Reviewer / Verifier | Verification Status | Gate Impact |
|---|---|---|---|---|---|---|
| Triangulate Thailand-specific Inventory questions; classify per the 9-value Amendment vocabulary; identify what requires real-user validation | Claude (Team A, DR-002) | This artifact | 2026-08-31 | Independent Evidence Review (pending) | See per-row classification | No entry in this register is claimed above what its evidence supports |

**Governing rule (Amendment §6, restated)**: *One ERP/customer dataset is not Thailand-wide inventory practice.* Classification vocabulary used below: `VERIFIED — PRIMARY/AUTHORITATIVE`, `SUPPORTED — MULTIPLE INDEPENDENT SOURCES`, `PARTIALLY UNDERSTOOD`, `CONFLICTING PRACTICE`, `COMPANY DEPENDENT`, `INDUSTRY DEPENDENT`, `REQUIRES REAL USER VALIDATION`, `REGULATORY VERIFICATION REQUIRED`, `UNKNOWN`.

## Register

| ID | Question | Evidence | Classification | Note |
|---|---|---|---|---|
| TH-INV-01 | Does this codebase's "branch" concept correctly represent Thai statutory branch/tax-branch requirements for Inventory-relevant location scoping? | Two uncoordinated source implementations found (a child-`res.company` "branch" and an unrelated `res.partner` "Thai Tax Branch" field) — GROUP A frozen finding, reused DELTA-FIRST | `CONFLICTING PRACTICE` (within one dataset) — **not** `REGULATORY VERIFICATION REQUIRED` yet, since no statutory source was consulted this pass | The regulatory question ("what does Thai law actually require for branch-level inventory/VAT reporting") itself remains untested — this row only confirms the source's internal inconsistency, not the correct answer |
| TH-INV-02 | Is the Company/Branch hierarchy pattern observed in this dataset representative of how Thai SMEs generally structure legal entities for inventory purposes? | GROUP A frozen finding: `_accessible_branches()` called from both Sale and Purchase; explicitly flagged `UNKNOWN / REQUIRES REAL USER VALIDATION` by GROUP A itself | `REQUIRES REAL USER VALIDATION` (reused DELTA-FIRST, not re-derived) | One customer's implementation choice is not Thailand-wide truth per the governing rule |
| TH-INV-03 | Does Thai inventory-valuation/reporting practice require a specific costing method (standard/FIFO/average) or presentation not evidenced in this source? | `property_cost_method` (standard/fifo/average) and `property_valuation` (periodic/real_time) are configuration options in the source (A9 §3); no Thai-specific costing rule was found or searched for in an official source this pass | `UNKNOWN` | This is explicitly deferred to COA-G06 (Thailand Tax Accounting Controls) per the Learning Priority Matrix — Inventory registers the interface, does not resolve the Thai costing-method question itself |
| TH-INV-04 | Is the WHT PND form-code logic (referenced in GROUP A's carried-forward Medium#18) correct against current Thai Revenue Department rules? | GROUP A frozen finding, unresolved, carried forward unchanged | `REGULATORY VERIFICATION REQUIRED` (reused as-is) | Not Inventory-owned (WHT is a Purchase/Accounting concern) but registered here for completeness since it appears in the shared evidence chain this DR-002 reuses |
| TH-INV-05 | Does Thai district/sub-district address data reach the delivery/logistics workflow in a way that affects Inventory routing? | GROUP A frozen finding, carried forward as an open Medium item (#19), not independently re-tested this pass (delivery-carrier modules were registered out-of-scope in A1) | `UNKNOWN` | Carried forward unchanged; this pass did not open the `delivery`/carrier-connector modules |
| TH-INV-06 | Does the live embedded Thai Revenue Department SOAP/VAT web-service integration (GROUP A External Dependency register, Government/E-document category) have any Inventory-side trigger, or is it exclusively an Accounting/Tax-document concern? | GROUP A frozen finding — the integration's existence is `VERIFIED — PRIMARY/AUTHORITATIVE` (it is a literal, named government endpoint call found in source), but its Inventory-relevance was not independently re-tested this pass | `PARTIALLY UNDERSTOOD` | The web-service call itself is a confirmed fact; whether it is ever invoked from an Inventory-triggered event (vs. purely a Sales/AR e-tax-invoice concern) is unresolved |
| TH-INV-07 | Are the "many SME businesses expect X" style generalizations in the frozen Fit-Gap register (GROUP A candidate #15, flagged by Independent Review as IR-2, a wording qualifier) reliable? | Independent Evidence Review's own finding: unqualified general-business-practice claim, classified `CONTROLLED CARRY-FORWARD`, not Gate-blocking, and the Boss Evidence Gate explicitly requires treating it as `HYPOTHESIS / REQUIRES REAL USER VALIDATION` | `REQUIRES REAL USER VALIDATION` (Boss-directed classification, reused verbatim) | Directly governs how any Inventory-adjacent Fit-Gap item inherited from GROUP A must be labeled going forward |

## What this pass did NOT add

This DR-002 pass's own primary source research (stock/valuation, product/UOM, sale/purchase/mrp handoffs) was conducted entirely against **generic Odoo business logic**, not Thailand-specific customization code — none of the three research agents dispatched this pass were asked to search for Thailand-specific Inventory logic, because GROUP A's own register already establishes that Thailand customizations in this dataset attach to shared master-data/Accounting models (party, company/branch), **"never a fork of `stock.picking` itself"** (GROUP A's own governing statement, reused DELTA-FIRST). This pass's deepening therefore concentrated on the generic Inventory core and its Accounting interface (per the Amendment's explicit mandate), and correctly leaves the Thailand-specific register unchanged from GROUP A's own work rather than fabricating new Thailand findings without evidence.

No entry above is generalized from one customer/source system to Thailand-wide truth, per the governing rule.

No Evidence = No Progress. DELTA-FIRST.
