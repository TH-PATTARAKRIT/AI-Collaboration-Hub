# P07 — RESEARCH ERROR AND REVISION LOG

Session ID: `SMEPLUS-26-09-04-ACC-P07-TAX-TH-REV2-001`
Classification: `LAYER 2 — AUDIT QUARANTINE`
Date: `2026-09-04`

## 1. Purpose

This log records every correction made **to this package's own claims during its own
execution**, and every governance change absorbed mid-session. It exists because the
programme's recorded pattern is that authors do not catch their own errors; publishing the
ones that were caught, and how, is the only way to tell a reader which controls actually
worked.

## 2. Governance Absorbed Mid-Session

| ID | Instrument | When | Effect on this package |
|---|---|---|---|
| `REV-G-01` | `SMEPLUS-DR-EXIT-8C-001` — Very Deep Research 8-Criteria Universal Exit Constitution, effective 2026-09-04 | Read during scope bounding, before any finding was written | `13_P07_SOURCE_LINK_REGISTER.md` was written to discharge `EC-01` before findings; `11 §5` was written to discharge `EC-06`; `18_P07_PMO.md` records the eight-criteria assessment; the terminal recommendation is bounded by `EC-07`, which this single first pass cannot satisfy. |
| `REV-G-02` | `SMEPLUS-26-09-04-ACC-REV2-CORR1` — Scope-Aware Constitution Correction | Received **mid-authoring**, while `01_P07_THAI_TAX_REQUIREMENT_REGISTER.md` §7 was being written | Applied immediately without reset. See §3. |

## 3. Scope-Aware Correction — Delta Revalidation

Per the correction §6, only findings materially affected by the superseded
"Tenant + Company context everywhere" assumption were revalidated. All other evidence,
source links, contradictions and commit lineage were preserved unchanged.

| Original finding | Scope assumption used | Why it was over-constrained | Correct scope analysis | Updated classification | Architecture impact | Cross-process impact | Evidence required |
|---|---|---|---|---|---|---|---|
| `R-B-01` (as first drafted): "Tax master data must belong to exactly one company", assessed against `account.withholding.tax` | Blanket company ownership for all tax master data | The Thai rate and form catalogue is **national**, identical for every tenant and company. Requiring company context for it is an over-constraint that the canonical rule does not impose (`PLATFORM SCOPE -> company context NOT REQUIRED`). | The object holds **two** scopes in one record: a `PLATFORM` statutory reference (name, rate, form tags, type) and a `COMPANY` financial binding (`account_id`). One record cannot answer to two owners. | **Restated and strengthened** as a scope-conflation finding plus a separate unproven-ownership finding (`REQUIRED OWNERSHIP CANNOT BE PROVEN = DENY`). `P07-F-18`. | The remedy is a scope split, not a company-id fix. A company-id fix alone would harden the wrong half. | `product.template.wt_tax_id` is a `TENANT`-scope reference into this object (`20 §4` row 10) and cannot be validated until the split is resolved. | none further; source evidence is sufficient |
| Implicit assumption that the statutory catalogue must be company-owned | same | The catalogue is a `PLATFORM` reference | Instantiating it per company is permitted; the correction does not require platform data to be physically singular | **New, downgraded finding** `P07-F-30`: over-instantiation, recorded as a scope-design observation rather than a defect — with the material consequence that one company-mutable label is used as a statutory report predicate | none on its own | none | none |
| Branch handling, previously framed only as a data-consistency defect | Blanket company context | It is a **scope collision**, not merely inconsistency: the counterparty branch is `TENANT`-scope master data and the filing branch is a `COMPANY`-scope legal attribute | Three representations across two scopes, used interchangeably; the `COMPANY`-scope attribute is read by no report | **Re-scoped** `P07-F-06`; new `R-B-04` | The filing-unit boundary required by `S-15` cannot be built on a counterparty attribute | `H-05` added to the P07 handoff contract | none further |
| Cross-company tax-unit grouping, previously unexamined | — | New question raised by the correction: unrelated independent companies are separate tenants by default | Whether the grouping is tenant-contained could not be established: no tenant construct was selected by the declared patterns in the P07 population | **New** `HOLD — SCOPE EVIDENCE REQUIRED`, `P07-U-14`; negative bounded as `P07-N-09` class `B` | unresolved | `P07-D-24` routed to P11 | tenant model evidence from outside the P07 population |
| External Revenue Department enrichment service, previously unexamined | — | New question raised by the correction §4 q.3–5 | Scope is `TENANT` writing tenant master data via a `PLATFORM` external service; the egress boundary was not assessed | **New** `P07-U-15`, class `C — NOT YET SEARCHED` | unresolved | none | egress and data-protection assessment |

New deliverable produced by the correction: `20_P07_SCOPE_OWNERSHIP_MATRIX.md`, answering
the eight correction questions for twenty material P07 objects.

Registers updated per correction §6: `11_P07_CONTRADICTION_REGISTER.md` (`P07-C-02`),
`12_P07_DEPENDENCY_REGISTER.md` (`P07-D-24`), this log, and the new scope matrix.
No research was re-run. No prior evidence was discarded.

## 4. Errors This Session Made and Corrected

Recorded in full, including the two that were self-inflicted, because a package that
reports only its findings and not its own defect rate is not auditable.

| ID | Error | How it was caught | Correction | Status at publication |
|---|---|---|---|---|
| `REV-E-01` | The draft of `03_P07_WHT_EVENT_MODEL.md` asserted "PND2 and PND54 appear nowhere". | By **running the declared pattern** (`pnd`, case-insensitive, all file types) instead of trusting the draft, as the negative-claim rule requires. | PND 54 **is** provisioned: `213303 Tax Withheld - PND 54`, with a bilingual description naming the remittance form. The claim was replaced by a four-layer provisioning matrix (`03 §4.1`) and a sharper finding: the chart names an obligation the reporting layer cannot serve (`W-K-07`). `P07-N-11` re-classed to `A` for the reporting layers and `E` for the chart layer. | corrected before publication |
| `REV-E-02` | The draft of `08_P07_CORRECTION_ADJUSTMENT_MATRIX.md` §5 asserted that **every** element of the filing/close segment was absent. | By checking whether a base filing framework existed before publishing a system-wide negative. | The base set contains `account.return`, `account.return.type` and `account.return.check` with deadline periodicity, days delay and workflow states, provisioned by 118 localisation modules. The negative was **withdrawn** (`P07-N-15`, class `E`) and replaced by a measured provisioning gap, `P07-F-37`. `01 §5 R-V-20` and `07 §4 T-04` were corrected in the same pass. | corrected before publication |
| `REV-E-03` | A column-shift defect was hypothesised in the SMEsPlus VAT report handlers (declared columns vs emitted columns). | By counting both sides for all four reports. | **No misalignment exists.** The hypothesis was wrong and is recorded as such at `07 §3`. What does exist is an asymmetry between the sale-zero and purchase-zero column sets (`P07-F-35`). The disproved hypothesis is published so a later reviewer does not repeat the check. | corrected before publication |
| `REV-E-04` | The first run of the fiscal-position enumeration used `-maxdepth 3` and returned **zero** results, which would have supported a false "no peer ships this either" reading. | By noticing that a known-good example (`l10n_be/data/template/account.fiscal.position-be.csv`) sat at depth 4 and could not have been matched. | Re-run at `-maxdepth 4`: **113 of 138** chart-template directories ship a fiscal position template, and Thailand ships none. The finding `P07-F-38` rests on the corrected run. | corrected before publication |
| `REV-E-05` | A draft observation treated the divergence between the tax groups' settlement accounts (`213500`, `114401`) and the repartition accounts (`213301`, `213302`, `114300`) as an inconsistency. | By reading the account descriptions, which state a deliberate two-stage design. | **Not a defect.** Recorded explicitly at `03 §7` as inspected-and-coherent so that a later reviewer does not re-raise it. What remains open is the executor of the consolidation, `P07-N-13` / `P07-U-17`. | corrected before publication |
| `REV-E-06` | A draft treated the removal of `trl.refund_tax_id` from the SMEsPlus tax-report SQL as a grouping change. | By checking functional dependency: `refund_tax_id` is an attribute of the same repartition-line row already in the `GROUP BY`, so removing it cannot change the grouping. | **Not a defect.** Not published as a finding. | not published |
| `REV-E-07` | An initial statutory retrieval indicated the 7% VAT rate expires 30 September 2026, twenty-six days after this session's date — which would have made `P07-F-01` an imminent compliance cliff. | By searching for a later instrument before publishing a dated risk. | A further extension to 30 September 2027 was approved by Cabinet on 27 July 2026 and confirmed by a Revenue Department notice on 2 August 2026. No finding asserts an imminent lapse; the structural coupling finding stands on its own terms. `P07-C-21`. | corrected before use |

| `REV-E-08` | On finding that the cross-company tax-unit mechanism synchronises fiscal positions, the session inferred that the mechanism must therefore be broken for Thailand, since Thailand ships no fiscal position templates (`P07-F-38`) — an attractive inference that would have linked two findings into one. | By reading the method rather than its name. | **Refuted by the code's own docstring**: the mechanism creates its own fiscal positions and those positions carry no taxes, so it does not depend on localisation templates at all. The inference was discarded and is recorded as discarded at `20 §8`. `P07-F-38` and `P07-F-39` are independent. | discarded before publication |

Seven of the eight were caught by mechanically executing a declared control — running the
pattern, counting both sides, checking for a later instrument, reading the method — rather
than by re-reading the draft. Re-reading the draft caught nothing in this session.

## 4A. Errors Found by Independent Challenge, Not by the Author

The eight errors in §4 were caught before publication by the session's own controls. The
following were **not** caught by the session and were found by the four AAS-03 experts.
They are listed separately because the distinction is the whole point of the control.

| ID | Error | Found by | Correction | Where |
|---|---|---|---|---|
| `REV-E-09` | "`tax_period_date` is read by nothing." | `AAS-03/A` | A reader exists — a hidden list column. Narrowed to "no report, compute, domain or SQL reads it". The author had **read that view file earlier in the session** and still wrote the absolute. | `04 §3`, `11 P07-N-02` |
| `REV-E-10` | Files `06` and `08` asserted system-wide negatives with no class and no boundary, while `02`, `03` and `05` registered theirs. | `AAS-03/C` | Negative-claim registers added to both. Of the negatives left unregistered, three were then found wrong or over-stated; **no registered negative has failed**. The correlation is recorded as evidence that the control does real work. | `06 §6`, `08 §6` |
| `REV-E-11` | The declared dependency-closure pattern was never run, so the population was understated by ten members — one of which the package simultaneously cited as evidence and registered as a broken dependency. | `AAS-03/D` | Population corrected 15 → 25. Recorded as a `PATTERN` failure, not a `PATH SET` failure. | `13 §5.1` |
| `REV-E-12` | The PND token census, offered as proof of a denominator, did not reproduce: it had been taken without the declared exclusions. | `AAS-03/B` | Corrected counts published beside the originals. Qualitative conclusions unchanged. | `13 §5.2` |
| `REV-E-13` | "113 of 138" — the numerator counted files and the denominator counted something else. The defective ratio had already reached the Layer-1 file. | `AAS-03/D` | Corrected to 94 of 126, with both commands published. This is a `UNIT` failure inside the very finding whose closing note congratulated this session for catching a `-maxdepth` error in the same enumeration. | `12 §5`, `19 §5` |
| `REV-E-14` | The restatement check was certified as executed while naming three files that did not exist, and excluding the files where restatement actually happens. | `AAS-03/D` | Check executed properly; four class-`B`-to-unqualified restatements found, **all in the Layer-1 file**, plus a count error. All corrected. The Layer-1 file had passed the vendor-token scrub cleanly on the same content — the two controls are independent. | `11 §5.1` |
| `REV-E-15` | Seven further substantive claims were refuted and eight overstated, including "no guard prevents both withholding frameworks acting on one payment" (a guard exists, and it silently discards), "the zero/exempt taxes have no tax group" (they resolve to a withholding group), and "the Thai tax-invoice wording is a hard-coded English literal" (it is translated). | all four | Each corrected in the body; none left standing with a challenge attached. | `16 §3`, `§4` |

| `REV-E-16` | The findings register's own totals were **asserted, never executed**: 49 issued against 48 actual, and every severity and evidence-state cell wrong. Then the first attempt to correct them re-derived the counts with a second pattern, double-counted a dual-state cell, and produced a total that summed correctly by coincidence — breaking the project rule *enumerate by call site, then read* inside the correction itself. | P04, by raising the identical defect against its own register and telling P07 to recompute | Totals enumerated row by row and republished with the asserted figures beside them at `00 §3.1`. | corrected after publication |
| `REV-E-17` | The Class 2 tally in the method proposal read "nine instances, nine actors": P04's figure was inherited uncorrected, four P07 instances were counted as four actors when they were one, and the same number was labelled on both axes — **a unit conflation inside a standard about counting**. | P04 | Recomputed with the unit declared: 12 instances across 5 actors, the two actor sets disjoint. The original sentence is preserved verbatim at `§3.1b` of the proposal. | corrected after publication |

| `REV-E-18` | `REV-E-09` was recorded as if the search had been too narrow. It was not: the grep **printed the reader** — `views/view_tax_period.xml:30`, a live field declaration — and the published conclusion contradicted the output the author was looking at. Re-executed to confirm the line was in the original output; it was. This is not a bounded-enumeration defect, it is **tool output standing in for the file it points at**. | P04, by referring a P11 case that has the same shape and proposing the class extension | Reclassified. The method proposal's Class 1 is extended to cover tool output at `r3`, evidenced on P07's own instance rather than on the referred one. | corrected after publication |
| `REV-E-19` | The sentence declaring the corrected Class 1 split wrote "3 P04, 2 P07" for a table containing 2 P04 and 3 P07 — a third counting error, in three consecutive revisions of the file whose subject is counting. | **the author**, by parsing the table instead of reading it | Corrected before publication. The only self-caught counting error of the exchange, and it required executing the count. | corrected before publication |

| `REV-E-20` | r2 and r3 published a single joint cross-party tally ("12 instances across 5 actors"). A cross-party tally **cannot be executed by either party** — P07 cannot open P04's drafts, P04 has not read P11's register. Every joint figure produced in the exchange was wrong. | P11, raising `P11-G-02` | Replaced by declared halves, each executed by its owner, with no total. P04's offer to restate it as "14 across 5" was **declined** — the halves were adopted, the single number was not, because producing one repeats the defect. Obligation 6 added. | corrected after publication |

| `REV-M-08` | Not an error — a control outcome worth recording. P07 declined to count a peer's referred instance **on the class of the evidence, without any suspicion about its content**. The identifier in that referral was in fact wrong, and the refusal is what left room for its owner to correct it. P04's generalisation, adopted: *a discipline that only fires when you suspect a problem is not a discipline.* A control conditioned on suspicion is unavailable in exactly the case that matters. | — | Recorded as the rationale for why the proposal's obligations are obligations rather than judgement calls. | — |

| `REV-M-09` | Not a P07 error — a control outcome. P07 twice declined a figure for P11's half: once from P04 relaying it (2), once holding at P11's own direct statement (`≥1`). P11 then enumerated it and reported **5**, correcting its own published 2. Both the relay and the original were wrong in the same way, one step apart: **a figure inherited from a peer carries that peer's execution status, and neither party can see it.** P07 verified all five ids present at source before adopting. | — | Recorded as the operative warrant for obligation 6 of the method proposal. | — |

| `REV-M-10` | Not a P07 error. P11 refined `P11-G-02` after watching this package carry `P11: ≥1` **correctly**: the rule said publish declared halves but never said *when* a half was executed, so a peer-published half goes stale silently — and the rule's own prohibition on re-deriving another party's half is exactly what stops a consumer noticing. Halves are now stamped `value @ owner-SHA`; correcting staleness is the owner's obligation to push. | P11 | Applied at `§3.1c` and as obligation 6a. P04's half currently carries **no SHA**, which is P04's to stamp. | — |

| `REV-E-21` | `13 §7` declared that no database was queried, and treated that as a scope statement. It is a **capability claim**, and it was `ASSUMED`, not `TESTED` — no attempt was made to locate a readable dump for the declared source set, and the programme has a recorded instance of exactly this claim being false. | P04, by registering the same defect against itself (`P04-REV-19`) after asserting four times that it could not open peer registers that were one fetch away | `13 §7` entries now carry `TESTED` / `ASSUMED`. The database entry is marked `ASSUMED`; `U-02` now records the untested status of its own incapacity. No finding changes, but the boundary around them is now honest about its own basis. | corrected after publication |

| `REV-E-22` | r7 recorded a peer's declared half as "none supplied — cannot be distinguished from a stale one", reasoning that a SHA is the owner's to stamp. Half right: P07 must not *invent* a SHA. The other half was wrong — the peer's revision log is **published on the same remote**, states its total at `18_P04_REVISION_LOG.md:103`, and one fetch confirms it. **P07 held a row as unverifiable while the figure sat in a public artefact it had never opened.** | P11, by stating the bound after all three sessions had committed the error | Row stamped `9/4 @ c839bfe`, verified **as published**, classification not re-adjudicated. Obligation 6b added: a cross-party *tally* may be unexecutable; a cross-party *citation* is always verifiable, and obligation 6 never licensed declining to open a published artefact. | corrected after publication |

| `REV-M-11` | Not an error — a conflict held open. P04 and P11 published **discrimination tests that give opposite verdicts on one instance**: P11's routes "register never enumerated" to Class 2 (and P11 applied it against its own proposal); P04's routes "never enumerated at all, because you were generalising from the case in front of you" out of Class 2. `§4b`'s conclusion rests on P11's. P07 gave its reading only on the question it owns — what Class 2 covers — and left both tests unadopted, since **the two classes stay cleanly separated under either**. Offered to P11: the collisions may be an artefact of framing the candidate as a third *evidence-failure class* rather than as a *cause taxonomy*. | P04 | Recorded at `§4c`. `§4b` unchanged. | — |

| `REV-E-23` | P07's `§4a` characterised Class 2's mechanism as *"an assertion stood in for an execution"* while `§3.1a` said *"nobody's pattern was too narrow; the arithmetic was simply not performed"*. Read together they are **ambiguous about whether an execution must have been attempted** — and two peers read them in opposite directions, producing a live classification dispute. The contradiction P11 found in its own proposal existed in P07's file too. | P11, by logging the same contradiction against itself and routing the question to P07 | `§4a` corrected: an assertion standing in for an **owed** execution. Ruling given at `§4d`: the test is *was an execution owed*, not *was one attempted*. | corrected after publication |

| `REV-E-24` | `13 §7` declared "no database was queried" and `U-02` bounded every behavioural finding on it. **Tested, and false.** A 65 MB dump of the declared v19 generation sits inside the declared PATH SET and was printed by a directory listing this session ran in its first minutes — the `§2.1a` mechanism exactly: tool output plus a plausible reason not to open it (the session was enumerating modules; a `.dump` is not a module). Worse than the peer instance that prompted the test, because that peer's dumps were outside its declared scope and this one was inside. A second trap was avoided by one step: the host's default `pg_restore` 16.15 rejects the v1.16 archive, and **stopping there would have produced a `TESTED` incapacity that was still false** — `postgresql@18` is on the same host and reads it. | P04, by applying P07's own clause 2.4 to itself and finding four readable dumps | `22_P07_RUNTIME_EVIDENCE.md` written. `P07-F-01` and `P07-F-42` upgraded from inference to verified; `-F-03`, `-F-37`, `-F-40` verified; `-F-51` refined; `P07-U-20` closed; two new findings. | corrected after publication |

| `REV-E-25` | The runtime results were written into `22` and into this log, and **the two files where those findings are actually argued were left saying "not executed"** — `02 §5.1` for `P07-F-01` and `06 §3A` for `P07-F-42`, the latter still naming `P07-U-20` as open after it had closed. A reader arriving at either finding would have read a superseded boundary. | P04, whose closing note states the rule: **a revision log is not a correction; the edit is** — applied to P07 within minutes of arriving | Both passages corrected in place, each retaining what it previously said. | corrected after publication |
| `REV-M-12` | Not an error. P11 corrected its **own over-broad self-blame** — it had published that the classification dispute was "manufactured by P11's own file" when the ambiguity was jointly produced, half of it P07's (`REV-E-23`). Its principle, adopted here: *an over-broad self-blame is still an inaccurate record; taking on more fault than the evidence supports is not humility, it is the same failure to check as taking on less.* Checked against P07's own entries: `REV-E-24`'s claim to be "worse than the peer instance" is evidenced (inside the declared scope vs outside it) and stands. | P11 | Recorded as a standing check on this log. | — |

| `REV-E-26` | `22 §4.1` generalised from **one** database: *"the condition is not exceptional, it is the shipped state of a Thai deployment"*. Across four databases it holds in **two**. The universality claim is withdrawn; the finding's severity is not, because a defect that fires in half of deployments cannot be found by testing one that works. | P04, by reporting that its own dump enumeration had been bounded to one directory, which prompted P07 to test its own | `22 §7.1`. `P07-F-01` re-stated as deployment-dependent. | corrected after publication |
| `REV-E-27` | `22 §6` said *"the other three dumps"* — a self-describing count never executed, the seventh instance of that class in this session. Enumerated by magic bytes: **five distinct databases in nine files**, four now examined. | same | `22 §7.0`. Note the paired result: P07's *search bound* (`-size +1M`) survived its own test, because the excess is source fixtures. A bound that survives is published beside one that does not. | corrected after publication |
| `REV-M-13` | Not an error. Checking a second database **inverted the package's evidence ranking**: `P07-F-42`, held longest at `INF` and upgraded most reluctantly, is now verified in 4 of 4 databases across 6 independent company sets — better evidenced than `P07-F-01`, the headline. The reluctance was right and so was the checking. | — | Recorded so a later reader does not assume the headline is the best-evidenced finding. | — |

| `REV-M-14` | Not an error — a control that held. P11 found **10 of 86** findings marked `ACCEPTED — CORRECTED` in its challenge record that were never edited into its registers, including that round's only `CRITICAL`. P07 audited the same claim in `16`: **0 of 18 checks unrepaired**, run with a positive and a negative control because P11's own audit had failed on silently non-matching patterns. It held for one reason worth naming: every correction was made as an **in-place edit at the moment it was accepted**, never batched. | P11, by testing its own record and publishing the failure | Audit published at `16 §9`, stated without triumph — this package failed the same rule twice the same day (`REV-E-25`, `REV-E-27`). | — |
| `REV-M-15` | Research caveat from P04, and it lands harder here than there. Readability is per `(artefact, tool)` pair. Of the five databases, the **two in which `P07-F-01` fires are `v1.16` and cannot be opened by the host's default client**; the two in which it does not fire are `v1.14` and open with stock tooling. A reader reproducing the finding with default tooling can open **only the deployments where the defect is absent**, and would reasonably conclude the finding is wrong. | P04 | `22 §3.1` added and placed **before** the results, because it governs how they should be checked. | — |

| `REV-E-28` | `22 §7` counted **snapshots as database identities**. "Nine files holding five distinct databases" is five *snapshots* of **four identities**; and `P07-F-01`'s "2 of 4" is **1 identity of 3 examined**, because both firing snapshots are `iTEST02` a month apart. The conflation sat in the evidence base of this package's headline finding. | P04, by re-running its own dump enumeration on P07's method, finding its bound survived but its unit did not, and sending the method rather than the conclusion | `22 §7.4`. `P07-F-01`'s base restated as one identity observed twice — weaker than published, but establishing **persistence**, which it had not been credited for. `P07-F-42` restated as 3 of 3 identities and survives unchanged, so **the evidence inversion between the two is wider under the corrected unit than under the wrong one.** | corrected after publication |

| `REV-E-29` | `P07-F-60` claimed **no statutory withholding certificate has ever been issued** — a negative from a single 6-move configuration database. `iSMEs` holds **5,201**; `BK12MAY26` holds one. **WITHDRAWN.** Replaced by `P07-F-62`, which is stronger: a real population of 5,201 certificates carries the correct s.40 taxonomy that the statutory export ignores. | P11, by generalising the tooling caveat into a rule that every database-derived negative must state its client version and the generations opened | `22 §8.1` | corrected after publication |
| `REV-E-30` (**restated by `REV-E-32`**) | As first written: *this session built its entire runtime section on the smallest database available, and convenience of location determined the evidence base.* **Too broad — see `REV-E-32`.** The live finding is narrower: a **configuration** database was used to support **population** negatives. | P11, and P04 | `22 §8.3`, restated. | corrected, then re-corrected |
| `REV-E-32` | `REV-E-30` over-blamed. Ranked with the unit declared, `iTEST02` has **1,395 populated tables against `iSMEs`'s 651** — it is the **broadest module install**, and the right database for the configuration claims in `22 §4.1–4.5` and `§7`, which is what this session used it for. `iSMEs` is the **deepest** (447,384 move lines against 23) and was required only for the **population negatives** in `§4.6`/`§8`. So the error was not picking the smallest; it was **using a configuration database for population claims, and not distinguishing the two kinds of claim in one file**. P11's own principle applies: an over-broad self-blame is still an inaccurate record. | P11, whose independent ranking used a different unit and inverted the order; verified by P07 rather than adopted | `22 §8.3` rewritten. Corrective gains a clause: **rank before choosing, and declare the unit you ranked by** — the right database depends on the claim. | corrected after publication |

| `REV-E-31` | The `REV-E-28` unit correction was written into `22 §7.4` and **four assertions of the superseded unit were left standing in the same file** — §4.1 ("across four databases"), §6 ("five distinct databases"), §7.2 ("four of four databases"), §7.3 ("2 of 4 deployments"). Fourth instance of this class here; second of *corrected-in-one-place-only*. Worse than the peer instance that prompted the check: their stragglers were in sibling files, mine were in **the same document as the correction**. | P04, by reporting its own unit restatement recurring in three sibling locations **within one commit of naming it**, and supplying the rule: *the only step that catches it is a grep for the corrected phrase across the package, not a re-read* | All four corrected. **Corrections in this package now end with a package-wide grep for the superseded phrase**, run with a positive control. `22 §7.4`. | corrected after publication |

| `REV-E-33` | `REV-E-31` instituted a package-wide grep after every correction — and **that grep was an enumerated list of six literal phrases**, bounded in exactly the way the count it was fixing had been. Re-run with a broad pattern (any numeral or number-word within 40 characters of `database\|deployment\|snapshot\|identit`) plus a control on the sweep, it returned **five more survivors**: the **section heading** *"Four Databases, Not One"* — the most-read line in the section that corrects it — a sentence rendered **factually wrong** by §7.4 (*"the two deployments where the defect fires"*, when they are **one deployment observed twice**), and two cells of the §7.3 summary. | P04, by re-running its own sweep, finding the identical defect, and sending the method: *the fix for an enumeration defect was itself pattern-bounded* | All five corrected. The step is now **grep with a broad pattern and a positive control on the sweep**, not grep with a phrase list. `22 §7.4`. Third round of this class; all three found by a peer sending the method rather than the conclusion. | corrected after publication |

| `REV-M-16` | Not an error — a standing caveat added to the scope register itself. P11 generalised P07's account of its own selection failure into a principle: **a declared scope register bounds the evidence and in doing so also orders it; if the register is consulted before the ranking, the register *is* the ranking — and its ordering was chosen for containment, not relevance.** This package's `13 §2` did exactly that, and it cost `P07-F-60`. | P11, sharpening P07's own sentence | `13 §2.0` added: declaring a scope does not exempt selection **within** it from being declared, the register does not answer "which item first", and the right member depends on the kind of claim. | — |

| `REV-E-34` | `P07-F-51` asserted a **configuration** claim (the shipped chart provisions no withholding-account flag) and a **population** claim (accounts are flagged in practice) **in one sentence, without saying which evidence supported which** — and rested the population half on `iTEST02`, the *configuration* database, which `§8.3` had just established is the wrong kind for a population claim. | P04, by finding the identical compound-claim defect in its own `F-81` and sending it: *the selection being correct did not make the statement correct* | Split at `22 §4.5`, each half on the right evidence. The population half re-run across identities — 3 of 586, 1 of 339, 2 of 544 — **3 of 3**, which turns it from "this deployment provisioned it" into *every deployment examined performed a provisioning step the localisation does not perform*. Both halves stronger after the split. | corrected after publication |
| `REV-M-17` | P04's closing observation, recorded because it outlives both packages: across this entire exchange **neither session produced a single correction by looking harder at its own work.** Twelve or more between them, every one forced by the other's evidence. The rules that came out of it — obligation 0, the broad sweep, rank-with-a-declared-unit, the compound-claim split — are all downstream of that fact, and none of them substitutes for it. | P04 | Recorded as the exchange's most transferable result. | — |

| `REV-E-35` | `§3`'s generation claim was made from **one regex hit on one module row**, and was never extended to the databases added at `§7`. Re-run over `ir_module_module`: `iTEST02` and `BK12MAY26` are v19 (486 and 251 installed modules, `base 19.0.1.3`); **`iSMEs` is v16** (190 modules, `base 16.0.1.3`). So `§7.1` tested a **v19 code predicate against a v16 database** — invalid *when written*, not invalidated later — and `§8.3` recommended `iSMEs` as "the reference population for any operational claim", which is the ranking error in a new form: ranked by depth, never checked for **eligibility**. | P04, by reporting the same category in its own scope paragraph — *wrong at the time, in the one place a reader is entitled to trust without checking* | `22 §9`. No finding withdrawn; `P07-F-01` restated to **1 of 2 in-generation**, `P07-F-42` to **2 of 2 in-generation plus v16 corroboration**, `P07-F-62` labelled a v16 observation, and the `§8.3` recommendation withdrawn. Fifth distinct unit failure in one file and the first of *eligibility* rather than count. | corrected after publication |

| `REV-E-36` | Orphan check on `P07-F-nn` identifiers found **two**: `P07-F-61` was created in `22 §4.6` and **never added to the findings register**, and `P07-F-60` was **withdrawn without leaving a row**, so a reader looking up either found nothing. Both are precisely the defect that register was written to fix — 39 dangling identifiers — reappearing **in the register itself**. | P04, reporting its own identifier-formatting defect for the **third** time and noting that only the orphan check, never a re-read, has ever caught it | Both rows added; `P07-F-60`'s kept as a withdrawal tombstone so the identifier resolves. Orphan check documented at `00 §2` and now run before publication. | corrected after publication |
| `REV-E-37` | **Re-running the orphan check found a third orphan, created inside the correction for the first two.** `P07-F-63` was written into `22 §4.5` and not added to the register — ninety seconds old; the same pass also left `P07-F-60`'s row one column short. | the same check, re-run | Row added, column fixed. Check moved from *pre-publication gate* to *last step before every commit*. | corrected before publication |
| `REV-E-38` | **The runtime population was wrong in two units at once.** Snapshots were censused by extension, missing two `.zip`-borne `dump.sql` archives (5 → 7); identities were keyed on filename, so two different databases both named `iEVING` counted as one (4 → 5 by `database.uuid`). In-generation identities: 2 → **4**. `iEVING` sat in the same directory as three dumps that were read, all session. | P04, publishing an `ir_module_module` version table that named `iEVING` as v19 — an identity this package had recorded as unexamined | Population re-derived at `22 §10.2`; every runtime finding re-run against all 5 identities at `22 §10.3`. | corrected after publication |
| `REV-E-39` | **`P07-F-63` partly refuted ninety minutes after publication.** Published as *"3 of 3 identities independently performed it"*. The fourth in-generation identity has 237 accounts and **zero** flagged. Universality withdrawn. | the corrected population | Restated at `00 §2` and `22 §10.4`. The identity holds 0 move lines, so it is a fresh deployment — it becomes a **positive control** for the never-provisioned half, which is what survives. | corrected after publication |
| `REV-E-40` | `P07-F-01` prevalence corrected **1 of 2 → 1 of 4** in-generation identities. Wrong in the direction that flattered the headline. Severity argument untouched: a defect silent in three of four deployments cannot be found by testing a system that works. | the corrected population | `00 §2`, `22 §10.4` | corrected after publication |
| `REV-E-41` | `P07-F-42` strengthened **2 of 2 → 4 of 4** in-generation identities, **6 → 10 company sets**, plus v16. Second successive population correction to widen the gap between it and the headline finding. | the corrected population | `00 §2`, `22 §10.4` | corrected after publication |
| `REV-E-42` | **A filter that could not fire.** The zero-rate tax filter matched literal `0`/`0.0`/`0.000000`; the stored format is `0.0000`. It reported 0 where there were 12 — and would have been published as evidence **against** `P07-F-42`. Caught by a positive control printing the value distribution, not by inspection. | own positive control | Numeric comparison throughout; `22 §10.5` | corrected before publication |
| `REV-M-19` | **Eligibility is a fifth unit class, and it sits before the other four.** POPULATION + PATTERN + PATH SET + UNIT all operate on a set already assumed eligible; none looks at membership. Asymmetry recorded: P04's eligibility error *included* ineligible members and was visible in its own evidence; P07's *excluded* an eligible member and was invisible by construction — no control here would have found it. Mechanism in both: **a scope declared before the first result does not survive the population changing under it.** | P04 | `22 §10.6` | — |
| `REV-E-43` | **Seven malformed table rows, invisible to every check this package ran.** Four in `03 §6.1` — the section added *after independent challenge* — where the whole `Evidence` column was absent, so the citations rendered inside the Finding cell and the evidence column read empty. One in `07 §3` (`RPT-12`, `Partner handling` absent), one in `20 §5` (`P07-U-14`, `Class` absent), one in `22 §5`. | P04, reporting five of the same class in its own package and noting that the identifier check cannot see them | All seven repaired; per-table structural check added to the pre-commit sweep. | corrected after publication |
| `REV-E-44` | **The `iEVING` row was wrong twice, and the second way is worse.** Besides being a column short, its excluded-reason read *"not examined — different product line"*. That was never checked against `ir_module_module` and is **false** — it is `19.0.1.3`, the declared generation. `REV-E-38` is therefore not an oversight: **the identity was excluded from the population by an unverified assertion written into the evidence table**, which a reader auditing the population would have found and accepted. | the repair pass | `22 §5`; the rule stated there — *a negative about the evidence base needs the same authority as a negative about the subject* | corrected after publication |
| `REV-E-45` | `07 §3` `RPT-12` `Partner handling` is recorded as **omitted when written and not re-derived — open**, rather than filled in by inference. Small, but the alternative was to guess a cell in a traceability matrix. | the repair pass | `07 §3` | open |
| `REV-M-20` | **The check that catches a defect is the one whose UNIT matches the defect.** The identifier check (`REV-E-36/37`) and the structural check see disjoint defect sets: the identifier check returned CLEAN on a package holding seven broken rows. They do not substitute for each other, and "run the checks at commit time" is the wrong lesson on its own — *run the check whose unit matches* is the right one. P04 further reports its own structural checker taking the modal column count **per file** when table width is a **per-table** property: it returned two false positives and missed three real defects, including both governance-file rows. **A wrong-unit checker returns CLEAN, and clean is indistinguishable from correct.** This package's checker was per-table from the start but treated pipes inside code spans as non-splitting, which is more lenient than any renderer; re-run with strict renderer semantics (every unescaped pipe splits) it still reports 0 across 149 tables — the leniency was not exercised, which is luck, not a control. | P04 | `00 §2`, this log | — |
| `REV-E-46` | **`P07-F-42`'s `10 of 10 company sets` was never counted.** Carried from `6 of 6` by regex substitution across the register, in the same commit that corrected four other denominators. Enumerated: **8 of 8 in-generation** (1+1+3+3), 9 including v16. | P04's rule — *a total that survives a correction to its own unit has only failed to move; check composition, never the sum* — applied to this package's own output | `00 §2`, `22 §10.7`. Finding unaffected; the number supporting it was asserted. | corrected after publication |
| `REV-E-47` | **The census method claim was false though the count was right.** `22 §10.1` published the census as re-run *"by format (`file -b`)"*. The input was an extension glob; `file` merely reported the format of files already selected by extension. Re-run with no extension filter over two roots, testing content: **11 artefacts, 7 snapshots, 5 identities** — counts unchanged. | own audit, prompted by P04 reporting the mirror failure — a genuine magic-byte census that matched only `PGDMP` and missed both `.zip`-borne dumps | `22 §10.7`; signature set now declared. **An enumeration by magic bytes is only as complete as the set of signatures it enumerates**: "by format not extension" is one check *per format*. | corrected after publication |
| `REV-M-21` | **`manifest.json` states `version_info` directly** — `[19, 0, 0, 'final', 0, 'e']`, verified in both `.zip` artefacts. Preferred over inference from `ir_module_module` where it exists; both agree here. | P04 | `22 §10.7` | — |
| `REV-M-22` | **Having the rule, recalling the rule, and running the rule are three different things; only the third is a control.** Both defects above break rules this package carries **in its own text**, and both were committed *in the act of correcting other people's denominators*. P04 reports the identical shape: a memory naming both of its failure points, in its own index, unconsulted before publication. | P04, who stated it first as *recall is not application* | `22 §10.7` | — |
| `REV-E-48` | **An exclusion reason in the scope register was false, and it excluded the deployed source.** `13 §2.1` excluded `efaplus-custom` as *"Different products. Out of scope."* — never tested. That root supplies 24–35 of the **installed** modules in each in-generation database, including every Thai withholding and reporting module this package analyses; all four in-generation databases are owned by role `efaplus`. `P07-F-64`, `P07-F-65`, `P07-U-28`. | own audit, running P04's `P04-F-88` method against `13 §2.1` | `22 §11`, `13 §2.1` corrected in place with the false reason struck through, not deleted. | corrected after publication |
| `REV-E-49` | **Two survivors of the `§10` correction, found by re-running the propagation check over the file it corrected.** `22 §8.4` still read *"`P07-F-61` holds, 2 of 2 in-generation"*; `22 §8` still read *"Not opened: `iEVING` (`P07-U-27`)"*. Both corrected; `P07-U-27` CLOSED. The `iEVING` line is left standing with its correction attached rather than rewritten. | own re-run, prompted by P04 reporting five survivors of its own correction | `22 §8`, `§8.4` | corrected after publication |
| `REV-M-23` | **Adopted from P04: a check is only as good as its unit, and a sweep is only as good as the set of units it spans.** Their fourth-check defect — an integrity record disagreeing with the files it describes, at exactly one commit, while three clean checks reported the package sound — is a unit this package's sweep did have. Audited all 12 commits of this session: **0 stale manifest rows, 0 missing, 0 extra.** That is a clean negative, but it is structural luck, not a control: the manifest is regenerated by the same script that ends each edit. It is now **verified** against the committed blobs rather than assumed from regeneration. | P04 | `14`, pre-commit sweep | — |
| `REV-M-24` | **A caveat that gets weaker is the one least likely to be re-checked** (P04). Re-tested here: `22 §7`'s reproduction caveat said default tooling reaches only the non-firing cases. With the two `.zip` artefacts added, stock tooling reaches **5 of 7 snapshots** rather than 3 of 5 — the caveat weakened — but its load-bearing half is unchanged: **both snapshots in which `P07-F-01` fires are `v1.16` and still need `postgresql@18`.** Re-checked rather than assumed to have improved. | P04 | `22 §7` | — |
| `REV-E-50` | **A sixth identity, in a third generation, newer than everything enumerated, outside both searched directories.** `22 §10`'s census of 7 snapshots / 5 identities is marked **SUPERSEDED AND A LOWER BOUND**, not replaced — a full content sweep over `$HOME` and every mounted volume is still executing, and this package will not publish a second census it has not finished. `P07-F-66`. Battery re-run: `F-01` does not fire (v18, out of generation, prevalence unchanged at 1 of 4 in-generation); **`F-42` fires**, in a `TAX 1%` naming variant, so it now holds in **v16, v18 and v19**; `F-61` holds 6 of 6; `F-63` gains a fourth configuration (2 of 658); `F-62` gains a second real certificate population (332). | P04, re-running its census over a declared path set and finding it | `22 §12`, `00 §2` | corrected after publication |
| `REV-E-51` | **`P07-F-62` said "the 15-value s.40 taxonomy".** Fifteen is the field's **domain**; the **observed** distribution is three values (`5`, `6`, `2`) across 6,507 cert lines in two generations. Register made precise; the finding is unaffected and now rests on two generations. | own re-run on the v18 identity | `00 §2`, `22 §12.5` | corrected after publication |
| `REV-M-25` | **The ladder needs a fourth rung: having the rule, recalling it, running it, and running it on the right set.** P04 applied this package's `REV-E-44` correctly — raised an exclusion's authority from filename to internal `dbname`, executed it over all seven archives — and the exclusion was still wrong, because the **population it ranged over** was two undeclared directories. Their formulation, adopted: **raising the authority of a statement about a population does nothing for a population that was drawn wrongly.** The same defect is in this package, and worse in one respect: `PATH SET` was declared rigorously for the source evidence (`13`) and silently author-chosen for the runtime evidence, in the same package, by the same author. | P04 | `22 §12.2` | — |
| `REV-M-26` | **No check whose unit is the package can find evidence that is not in it.** The four-check sweep — identifiers, table structure, manifest hashes, Layer-1 scrub — all take the package as their unit, so none could have found `P07-F-66`. This is a property of the sweep, not a gap in it: **evidence-base completeness is not a package property**, and it needs a check whose unit is the *host*. | P04, whose four-check sweep had the same blind spot | `22 §12.2` | — |
| `REV-E-52` | **`P07-F-61` published as "present and empty in all 7 snapshots" is wrong at 15.** In one identity `account_tax_unit` is **absent from the archive TOC entirely** — the module is not installed — not present-and-empty. Correct: present and empty in **6 of 7 identities**, absent in the seventh. Conclusion unchanged. This is the empty-versus-absent distinction that `22 §10.4` introduced, published wrongly one section later. | own re-run at the finished census | `00 §2`, `22 §13.4` | corrected after publication |
| `REV-E-53` | **Census finished: 15 snapshots, 7 identities, 3 generations.** Roots `$HOME` + every entry in `/Volumes`, no extension filter, `PGDMP` + ZIP-containing-`dump.sql`, keyed on `database.uuid`. Published history for one population: **5·4 → 7·5 → 15·7**; every correction came from a peer, none from a check inside this package. `P07-F-42` now 7 of 7 identities / 3 of 3 generations (`P07-F-67`); `P07-F-63` gains a **second** never-transacted positive control in a second generation (0 of 152, v18; 0 of 237, v19); `P07-F-01` prevalence **unchanged at 1 of 4 in-generation** — the first denominator in this file to survive a census correction. | P04 | `22 §13` | — |
| `REV-M-27` | **A raw path count is not an artefact count, and the inflation is invisible in the total.** The sweep returned **57 path hits** for **15 artefacts**: one mounted root is a mirror of this machine containing `$HOME` and the working volume *twice*, once directly and once under `System/Volumes/Data`. 57 is perfectly reproducible and meaningless. **Before counting, test whether one root mirrors another; dedupe by content and key identity on `database.uuid`.** The converse also bit: seven files of one simulation-lab database are 7 snapshots of **1** identity, which is why the answer is `15 · 7` and not `15 · 13`. | own sweep | `22 §13.2`, `§13.3` | — |
| `REV-M-18` | P04's generalisation of the compound-claim split, adopted and confirmed here: **a compound claim does not merely under-describe its evidence — it can conceal the finding.** Splitting `P07-F-51` surfaced `P07-F-63`: every deployment independently performed an undocumented provisioning step the localisation never performs, **and each arrived at a different answer** (3 of 586, 1 of 339, 2 of 544). While the halves shared a sentence the configuration half read as a property of the chart rather than as consistent independent action by every operator. | P04 | `22 §4.5` | — |

Two of the reviewers' findings **escalated** the severity of the session's own headline
findings rather than reducing them (`P07-F-01`, `P07-F-42`), and both were reached
independently by two reviewers. The session's own severity assignment was too low in both
cases.

**Net for the round:** the author caught 8 errors in its own unpublished drafts; the
reviewers caught 15 in its published ones and contributed 31 new findings. Two further
errors (`REV-E-16`, `REV-E-17`) were caught **after publication, by a peer process**, and
both were counting defects in the artefacts that exist to make counting possible. No error
in this round was caught by the author re-reading its own published text.

**The `REV-E-16` / `REV-E-17` pair is the sharpest single lesson of the session.** Both were
found because P04 re-executed a table it had already published twice, in a package about
counting discipline, and then told P07 to do the same. Neither would have been found by any
control this session ran, because every one of those controls checked *claims* and neither
error was in a claim — they were in totals. A control that validates findings does not
validate the arithmetic that describes them.

## 5. Method Notes for the Next Round

| # | Note |
|---|---|
| `REV-M-01` | Every negative claim in this package was produced by a pattern that is recorded in `13 §4` **with its false-negative modes**. Two of the four patterns are name-based and would miss a Thai tax rule implemented with no Thai, tax or withholding token in its identifiers (`U-11`). |
| `REV-M-02` | No database and no runtime evidence was used (`U-02`). Every behavioural statement is derived from source. Statements about what a user would observe (`04 §5`) are derivations, not observations, and are labelled as such. |
| `REV-M-03` | The declared source set is the Account module's own source index. Which addon copy is deployed in production is `U-01` and was not determined. Findings bind to the declared set. |
| `REV-M-04` | Under `EC-07`, two consecutive clean independent passes are required before a Final Research Gate. This session is **one** pass with one round of independent challenge. It cannot and does not claim `EC-07`. |
| `REV-M-05` | `U-12` (excess-VAT carry-forward mechanism) was opened as an untraced unknown and **closed within the session** by reading the report expression tree. Closing it produced `P07-F-40`, which is more material than the unknown was. Two further inferences were tested during that trace and refuted: that the carry-forward was unimplemented, and that the return periodicity was simply unset rather than defaulted. Both are recorded here rather than published as findings. |
| `REV-M-06` | `P07-U-14` was opened as `HOLD — SCOPE EVIDENCE REQUIRED` and **closed as a decidable finding** during the round: the tenant boundary is specified in this very clone at status `NEW`, and no tenant ORM model exists anywhere on the volume. The routing that would have deferred it to P11 said the evidence lay "outside the P07 population" — it lay six directories up, in the same working tree. This is the programme's recorded "never declare no-access from a working-tree search" defect recurring in a milder form, and it was caught by a reviewer, not the author. |
| `REV-M-07` | The controls that worked in this round were, in order of yield: **running a declared pattern** (8 self-caught errors), and **disjoint adversarial review with an explicit instruction to report wrong paths in the brief** (15 corrections, 31 new findings, 2 severity escalations). The control that yielded nothing was re-reading the draft. A next round should spend its self-review budget on executing declared patterns rather than on re-reading. |
