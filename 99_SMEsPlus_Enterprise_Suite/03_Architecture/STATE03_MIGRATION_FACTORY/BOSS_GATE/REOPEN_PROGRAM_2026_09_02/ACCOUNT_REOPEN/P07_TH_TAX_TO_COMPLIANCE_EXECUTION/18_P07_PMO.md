# P07 — PMO CONTROL REPORT

Session ID: `SMEPLUS-26-09-04-ACC-P07-TAX-TH-REV2-001`
Classification: `LAYER 2 — AUDIT QUARANTINE`
Date: `2026-09-04`

## 1. Control Rule Applied

Per `AI_BOOTSTRAP_PACKAGE.md` (`SMEPLUS-AIOS-BOOTSTRAP-001`): AI creates output, PMO checks
process, Technical Team AI checks completeness, **Boss gives final approval**. This file
checks process only. It does not approve anything.

## 2. Eight-Criteria Assessment — `SMEPLUS-DR-EXIT-8C-001`

Assessed honestly against the constitution's own wording, not against what would be
convenient.

| Criterion | Assessment | Basis |
|---|---|---|
| `EC-01` **Scope Bounded** | **NOT SATISFIED** | `PATH SET` is declared, enumerated and independently reproduced — two reviewers ran the manifest counts and confirmed 62 / 1371 / 69, and no wrong path was found. But `POPULATION` was refuted (15 → 25, `13 §5.1`), `PATTERN` was declared and not run to closure, and `UNIT` was switched mid-count in one measurement (`12 §5`). Three of the rule's four clauses failed. |
| `EC-02` **Enumeration Converged** | **NOT SATISFIED** | The corrected population, the corrected token census and the corrected fiscal-position ratio are all material deltas produced in this round. Material-delta stability has not been demonstrated; by definition it cannot be after one round. |
| `EC-03` **Unknown Exhausted** | **NOT SATISFIED** | Nine unknowns carry a disposition; six statutory questions remain `HOLD — STATUTORY EVIDENCE REQUIRED`, and `P07-U-03` is gating for one forced design position (`17 §5`). No unknown is unclassified, which is the part that is satisfied. |
| `EC-04` **Tolerance-Zero Closed** | **NOT SATISFIED** | Tolerance-zero boundaries are open, not closed: company isolation (`P07-F-18`), the tenant boundary itself (`P07-F-50`, specified and not built), unauthorised deletion of a statutory document (`P07-F-19`), and silent under-recognition of a tax liability (`P07-F-16`). `CONDITIONAL PASS` may not bypass these. |
| `EC-05` **Contradiction Resolution Complete** | **PARTIALLY** | Twenty-four contradictions are registered with disposition and lineage (`11`). Ten remain `UNRESOLVED — architectural`, and all ten are explicitly Boss-level (`11 §6`), not unresolved differences of opinion. |
| `EC-06` **Negative Claim Controlled** | **SATISFIED, after correction** | Twenty-five negatives, each with a class letter and a declared boundary. One withdrawn as `E`. Two registers were added after challenge revealed that files `06` and `08` carried unregistered negatives — and every negative that had been registered survived challenge while three unregistered ones did not. The control demonstrably works; it was under-applied and is now applied. |
| `EC-07` **Two Consecutive Clean Independent Passes** | **NOT SATISFIED, and cannot be** | This is one pass with one challenge round. That round was not clean: seven refutations, eight overstatements, thirty-one new findings, two of them severity `S1` escalations. A second pass is required before this criterion can even be tested. |
| `EC-08` **Final Knowledge Package Complete** | **PARTIALLY** | Twenty-one files including every deliverable the directive names, plus a findings register and a scope matrix added in response to the correction and the challenge. Missing against the constitution's list: a verified Function Coverage Register, a Control Matrix, and Jira lineage (§6). |

**`0 of 8` criteria claimed as satisfied at gate level.** `EC-06` is satisfied; it is not
claimed as a gate pass because the constitution requires all eight.

## 3. Process Findings Against This Session

| # | Process finding | Evidence |
|---|---|---|
| `PMO-01` | The session declared four enumeration patterns and ran two to closure. The two it did not run are where the population defect and the undeclared-dependency defect were found. | `16 §7` |
| `PMO-02` | A control was certified as executed while naming three non-existent files as its coverage. When actually run it found four violations, all in the Layer-1 file. | `11 §5.1` |
| `PMO-03` | Thirty-nine finding identifiers were cited across four registers with no register defining any of them. Corrected by writing `00`. | `00 §0` |
| `PMO-04` | Two findings were escalated by reviewers from the class the author assigned, both from reporting-path to a more serious characterisation, and both reached independently by two reviewers. | `16 §3` `R2`, `16 §5` |
| `PMO-05` | The session caught eight of its own errors before publication, all by mechanically executing a declared control rather than by re-reading drafts. Re-reading drafts caught nothing. | `15 §4` |
| `PMO-06` | Every material correction to a *conclusion* came from independent review; every correction the author made was to its own *unpublished* draft. The programme's recorded pattern held again. | `15 §4`, `16 §3` |

`PMO-05` and `PMO-06` together are the process lesson: mechanical self-controls catch
author errors before publication; only independent review catches author errors after it.
Both are necessary and neither substitutes for the other.

## 4. Directive Compliance

| Directive requirement | Status |
|---|---|
| No evidence = no progress | Held. Six statutory questions are on `HOLD` and none is used to support a conclusion. |
| Never skip a gate | Held. No gate declared. |
| No repeated question without material delta | Held. No Boss question asked. |
| Delta-first; read Bootstrap governance first | Held. Bootstrap and the exit constitution were read before any finding was written. |
| No intermediate Boss questions | Held. Zero. |
| Classify findings precisely | Held after correction. `00` assigns severity and evidence state to all 47. |
| Clean-room mandatory | Held. Layer 2 marked on every file; the Layer-1 file passes the vendor-token scrub with zero hits, re-run after every edit. |
| Trace the full chain | Held. `02 §2-3`, `03 §1-2` trace it twice each, statute against implementation. |
| One tax fact → one canonical event → one accounting effect | Held as the analytical frame; four breaks recorded (`02 §5`), plus a third computation of one withholding amount (`03 §7.2`). |
| Attack duplicate tax recognition | Held. It produced `P07-F-16`, and the attack was itself corrected — the real defect is silent under-recognition at the posting layer and double counting at the reporting layer. |
| Parallel peer evidence consumed without waiting | Held. Peer processes recorded as `PEER DEPENDENCY OPEN`; none blocked this session. |
| All four AAS-03 experts challenge every level | Held. Four disjoint adversarial briefs (`16 §1`). |
| AAS+ consolidates | Held (`17`), marked `PROVISIONAL / NON-CANONICAL`. |
| Tenant / Company / Tax Unit boundaries must be proven | **Not proven — and that is the finding.** The tenant boundary is specified and not built (`P07-F-50`); the tax-unit mechanism carries no tenant constraint (`P07-F-39`); company ownership of withholding master data is taken from session context (`P07-F-18`). |
| Separate LAW / REGULATION / RD requirement / standard / practice / reference ERP / SMEsPlus policy | Held. `09` bands every source and records one rejected commercial source. |
| Authoritative evidence for legal claims | Held. Statutory text from the Revenue Department's own publication; one dated claim corrected before use. |
| Maintain all mandatory registers | Held, with two added after challenge. |
| No implementation or merge | Held. No merge. No implementation. `AASR-P07-VETO-01` blocks one design area. |
| Publish verifiable lineage | Held for GitHub. **Not held for Jira** — see §6. |

## 5. Scope-Aware Correction Compliance — `SMEPLUS-26-09-04-ACC-REV2-CORR1`

| Requirement | Status |
|---|---|
| Do not reset; do not restart from L1; do not discard evidence | Held. The correction arrived mid-authoring and was applied in place. |
| Supersede blanket tenant+company enforcement | Held. `01 §7` rewritten; `20` created. |
| Determine scope per object before requiring context | Held for twenty objects (`20 §3-§6`). |
| Do not automatically assume both contexts | Held. Three objects are assessed as `PLATFORM` and explicitly require neither. |
| Update contradiction, dependency, revision and scope registers | Held (`11 P07-C-02`, `12 P07-D-24`, `15 §3`, `20`). |
| Revalidate only materially affected findings | Held. Six revalidated; no other research re-run. |
| Peer dependency open, do not stop | Held. Seven peer dependencies recorded open. |
| Resolve scope from semantics and evidence, else `HOLD — SCOPE EVIDENCE REQUIRED` | Held, and one such HOLD was subsequently **closed** by evidence rather than left open (`P07-F-50`). |
| No Boss confirmation, no intermediate checkpoints | Held. |

## 6. Jira Lineage — Not Held

The directive requires publishable Jira lineage. **No Jira issue was created or transitioned
by this session.** The Atlassian connectors available to this environment are unauthorised
and cannot be authorised from a non-interactive session. This is recorded as a known
deficiency against `EC-08` rather than silently omitted. GitHub lineage is complete: branch,
commits and file paths are in `14_P07_EVIDENCE_MANIFEST.md`.

## 7. PMO Recommendation

`RECOMMEND HOLD`.

Basis: `EC-04` names tolerance-zero boundaries, and three of them are open in this
package's own subject matter — company isolation, the tenant boundary, and unauthorised
deletion of a statutory record. The constitution states that `CONDITIONAL PASS` may not
bypass a tolerance-zero risk, so `RECOMMEND CONDITIONAL PASS` is unavailable to P07
regardless of the quality of the rest of the work.

The terminal process state `READY FOR CORE ACCOUNTING RECONCILIATION` is asserted
separately and means only that P07 has produced the positions, contract and open questions
the reconciliation needs. It is not a gate outcome. Boss is the sole final approver.

## 8. Round Close — Checkpoint / Resume State

**Round state: `TERMINAL`.** No route is executing. No process belonging to this round is
running. This section is the resume state: anything not listed here as open was not left open.

### 8.1 Background task disposition

| Task | Route | Disposition |
|---|---|---|
| `btrz49256` | `P07-N-25` re-run — all `.py` under `/Volumes/iMacSys` **and** `$HOME`, with a positive control | **`TERMINATED — REDUNDANT CORROBORATIVE ROUTE / BLOCKED_ON_IO`** |

Diagnostic evidence and the exact reason are preserved at `22 §48.1`; the correction is
`REV-E-91`. Ownership was verified before termination — the two PIDs signalled were this
session's; two PIDs belonging to another session were identified and left untouched. Exit code
`144` (`128 + 16`) confirms the intended process, and only it, received `SIGTERM`.

**Not to be rerun.** The `P07-N-25` question was settled independently at `22 §30`/`§31` by the
`ir_model` registry route — 7 of 7 identities, 5,133 model rows, 3 generations, zero
tenant-shaped models, positive and negative controls both behaving. This walk was corroborative.
**`P07-N-25` is not reopened.**

### 8.2 What this round leaves open, deliberately

- **`P07-N-03`** — the second whole-product negative. Never re-run. Closes **unresolved**, and is
  not converted into anything stronger by silence.
- **`P07-U-32`** — the 36 artefacts under the excluded `~/Library` subtree: **not keyed, not
  read, not counted as identities.** The largest unexamined evidence set this package has named.
- **`22 §13`'s census** — **15 snapshots · 7 identities is a floor**, not a total. No number in
  it was amended in this round.
- **`P07-U-01`** — *which* copy of the diverging `l10n_th` modules is deployed remains
  undecidable. `P07-U-28` (whether that matters) is **CLOSED on evidence** at `22 §21`
  (`P07-F-78`); `P07-U-01` is not, and stays open.
- **The `§46`/`§47` estate-vocabulary measurement** — exists only in the scratchpad sweep script,
  **never registered**, and declared unregistered at `22 §48.3` so the gap is recorded rather
  than silent.
- **`EC-08` Jira lineage** — unheld, for the connector reason at `§6`.

### 8.3 Verdict at close

`RECOMMEND HOLD`. **0 of 8 exit criteria claimed. No blocker closed. No merge. No freeze.**
`AASR-P07-VETO-01` not discharged. **`P07-U-28` is the only open item this package closed on
evidence, and it is not a blocker** — closing it established that *which* copy is deployed cannot
change any published finding, not that any finding was resolved.

**No implementation, no merge — as the issuing prompt requires.** Boss is the sole final
approver.
