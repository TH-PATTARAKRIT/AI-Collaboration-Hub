# P07 — CONTRADICTION AND REVISION SUPPLEMENT

Round `SMEPLUS-26-09-06-…-002`. `LAYER 2 — AUDIT QUARANTINE`.
Supplement to `11_P07_CONTRADICTION_REGISTER.md` and `15_P07_REVISION_LOG.md`. **Nothing in
either is overwritten**; this file adds rows and points at the ones it changes.

## 1. New Findings — `P07-F-105` … `P07-F-112`

| ID | Sev | Statement | Basis | Argued at |
|---|---|---|---|---|
| `P07-F-105` | `S1` | **RESTATED AFTER CHALLENGE — two scope limits accepted from `CH-A` and `CH-C` before publication.** *(i)* The instrument counted **fields the set adds**, so the claim is **no new carrier**, not indifference to period placement — `CH-A`. *(ii)* The population is **the installed `l10n_th*` set**, which is **not the same as Thai tax code in this estate** — `scgl_tax_period_date`, which owns both tax-period carriers, carries no `l10n_` prefix and is outside it. The round's own `EP-3`/`EP-4a` found the carrier **outside** the searched set, which is the counter-example proving the distinction — `CH-C`. **The statutory tax point has no carrier added by the Thai localisation.** The installed set adds **18 fields across 8 shared accounting models** and **not one is a date, a period or a tax point** — every one is a withholding attribute or a branch string. The only tax-period carriers in the estate come from `scgl_tax_period_date`, a **non-localisation** custom module installed in 3 of 7 identities. `O-03` — *P07 owns tax-period membership and the implementation delegates it to the accounting date* — is now **measured** rather than argued. | `EP-2`, `EP-2b`, `EP-4b`; two instruments, controls firing | `…SUPPLEMENT.md §4`; `…DATE_PERIOD_MATRIX.md §3` |
| `P07-F-106` | `S3` | **The Thai localisation module count is not a constant: `10, 10, 10, 10, 9, 9, 1` over 7 identities.** P10 recorded nine in two documents and ten in a third (`G02-R-17`); **both are correct, for different databases, and neither is correct for "the estate".** The v16 set is **not a subset** of the v19 set — it carries `l10n_th_google_fonts`, which the v19 identities do not. A platform-scope fact counted per database yields different numbers per database without any being wrong. | `EP-1`, 7 identities, controls firing | `MATERIAL_DELTA…§4` |
| `P07-F-107` | `S2` | **The tax-period carrier exists at item level too, and both carriers are inert.** `account_move_line.tax_period_date` and `account_move.tax_period` are **co-present in exactly 3 of 7 identities and co-absent in the other 4**, because one module defines both. P10's `FACT VERIFIED` that the carrier is *absent from the item* is **CONTRADICTED**. Neither is read for tax-period selection (`04 §3`, `§4`). Presence is **not** a property of generation `19.0+e`: one of the three is **v16**. | `EP-3` + `EP-4a`, two independent routes agreeing (schema columns, module registry) | `…SUPPLEMENT.md §5`; `P07_TO_P10_HANDOFF.md §5` |
| `P07-F-108` | `S2` | **The two candidate bodies of `l10n_th_withholding_tax/models/account_move.py` disagree on `_compute_wht_amount`.** The declared copy writes `rec.wht_amount`; the `efaplus-custom` copy writes **`self.wht_amount` inside `for rec in self:`**, and the field is `store=True`. On a multi-record recompute the last record's value is written to every move in the batch and persisted. **This is the first claim in this package for which `P07-U-01` — which copy is deployed — is load-bearing.** Which body is deployed is `SUPPORTED INTERPRETATION` (`P07-F-64` points at `efaplus-custom`; `P07-F-65` proves no manifest can decide it), **not `FACT`.** | `EP-5`, both copies hashed and parsed | `…SUPPLEMENT.md §6` |
| `P07-F-109` | `S1` | **A stored statutory figure disagrees with its own transaction in 16.8% of the exercised population.** In `45a8e08e`: **199 of 1,186** non-zero `wht_amount` values differ from the amount recomputable from the move's own withholding lines, and **7** carry a non-zero amount with **no withholding-tax line at all**. Instrument validated by **980 exact reproductions**; `551ab874` is 4 of 4 clean, which at n=4 settles nothing. **The cause is not decidable from a database** — the `<B>` compute defect and ordinary staleness after line edits fit the evidence identically. `P07-U-35`, `EVIDENCE NEVER RECORDED`. | `EP-6`, positive control firing | `…SUPPLEMENT.md §7` |
| `P07-F-110` | `S3` | **Latent, not live: nothing installed reads the stored value.** Across 36 occurrences of `wht_amount` in both declared roots, the **only** consumer of the stored field is `print_payment_remittance_adviec` (`body.xml:184,187,207`, `amount_total - wht_amount`), which is **`uninstalled` in 6 of 7 identities and absent from the registry of the 7th**. Every other site — both PND SQL branches, `l10n_th_reports`, and the installed `print_voucher_request` — **recomputes locally**. A stored, exercised, divergent statutory figure that no installed consumer reads. | `EP-5` §6.1 + install-state query over 7 identities | `…SUPPLEMENT.md §6.1` |
| `P07-F-111` | `S2` | **One statutory number, four independent computations, three different bases, no shared definition.** `wht_amount`'s stored compute and the voucher/remittance Python both use `wt_tax_id.amount/100 × price_subtotal`; PND branch 1 uses `tax.amount × tax_base_amount/100`; PND branch 2, **35 lines later in the same file**, uses `tax.amount × price_subtotal/100`. This sits **on** `P07-F-11`'s surface without restating it: `P07-F-11` is about which rows each branch selects, this is about what arithmetic each site performs. Whether `tax_base_amount` and `price_subtotal` can actually diverge on a withholding line is **not tested** — `P07-U-36`. | `EP-5` §6.1 | `…SUPPLEMENT.md §8` |

| `P07-F-112` | `S1` | **`P07-F-111`'s inconsistency is not latent: one of the two bases is empty on the population that matters.** On lines carrying `wt_tax_id`, `tax_base_amount` is **null in 17,254 of 17,878 (96.5%)** and **`0.0` in 614 of the 624 populated cases**, while `price_subtotal` is populated in **100%**. A computation of the form `tax.amount * tax_base_amount / 100` therefore returns **null or zero for essentially every withholding-bearing line**, and the same computation on `price_subtotal` returns the real figure. **Two sites computing one statutory number over these two bases cannot agree.** Measured, not inferred; claims nothing about which PND branch selects which row — that is `P07-F-11`'s subject and is not restated. | `EP-8`, 2 identities, both columns verified present | `…SUPPLEMENT.md §8.1` |

## 2. New Open Items — `P07-U-34` … `P07-U-36`

| ID | Statement | Closes by |
|---|---|---|
| `P07-U-34` | `l10n_th_google_fonts` is installed in `45a8e08e` and its source is not present under either declared root, so `P07-F-105`'s zero covers **10 of 11** installed names. | `NOT ON THIS HOST` |
| `P07-U-35` | Whether `P07-F-109`'s 199 divergences were written by the `<B>` compute defect or are stale values after line edits. **A database records the result of a write, never the sequence.** | **`EVIDENCE NEVER RECORDED`** |
| `P07-U-36` | ~~Whether `tax_base_amount` and `price_subtotal` diverge on a withholding line — i.e. whether `P07-F-111`'s two computations can disagree.~~ **CLOSED ON EVIDENCE in the round that opened it — `P07-F-112`: one of the two bases is null on 96.5% of the population and zero on 98.4% of the rest.** The row is kept in definition form rather than struck out, because a struck-out definition is an orphan to every check that reads it. | *closed — `EP-8`* |

**Two opened, one closed in the same round.** `P07-U-36` was opened as `NOT YET READ` and then
closed by `EP-8`, because **`NOT YET READ` is an honest disposition only when the reading is out
of reach or out of scope, and this one was neither** — it sat under a declared Closure Question,
on data already extracted, one query away. `REV-M-99`.

**`P07-U-35` joins `P07-U-20` and `P07-U-29` as the third item in this package that no amount of
further research can close.** All three need one thing: a controlled execution, run twice, with
the result recorded. They should travel together if a runtime request is ever granted.

## 3. Contradictions — New and Changed

| ID | Contradiction | Position A | Position B | Resolution |
|---|---|---|---|---|
| `P07-C-26` | **A peer's `FACT VERIFIED` against this package's measurement.** | **P10** (`1fea562`): a tax-period carrier exists on the entry and is **absent from the item**, deployed 19.0+e | **P07** (`EP-3`, `EP-4a`): both carriers are **co-present in 3 of 7 identities and co-absent in 4**, one module defines both, and one of the three is **v16** | **RESOLVED in P07's favour, on evidence.** Two independent routes agree. Handoff published; P10 to correct its own register — **P07 does not edit a peer's package** |
| `P07-C-27` | **A discharge read wider than its scope.** | `P07-F-78`: *"which copy is deployed cannot change any published finding"* | `P07-F-108`: the two copies **disagree** on `_compute_wht_amount`, and a new claim turns on it | **NOT A CONTRADICTION — a scope boundary made explicit.** `P07-F-78` stands exactly as written: it tested **four cited claims** and both bodies agreed on all four. `account_move.py`'s compute was not among them because nothing cited it. **A discharge is scoped to the claims it tested.** `REV-M-96` |
| `P07-C-28` | **A stored figure against the transaction it describes.** | `account.move.wht_amount` as stored | the sum recomputable from the move's own withholding lines | **UNRESOLVED as to cause** (`P07-U-35`); **the divergence itself is `FACT VERIFIED`** at 199 of 1,186, and **latent in effect** (`P07-F-110`) |

| `P07-C-29` | **A disproof attempt against this round's most consequential finding, measured and failed.** | `CH-B`: the 199 divergences need no defect — one withholding-rate edit after posting would produce all of them, making `P07-F-109` an instrument artefact | `EP-6b`: **151 of 199 are moves whose own lines carry no withholding tax at all** — there is no rate to have changed; and the other 48 show **no dominant `stored ÷ own` ratio**, where one rate edit predicts one. The observed ratios match no ratio between any two rates present in the table | **CHALLENGE FAILED ON MEASUREMENT. `P07-F-109` stands and is stronger** — its most plausible alternative is now tested and excluded rather than acknowledged. `CH-B`'s residual disagreement (that the *cause* is still unproven) is upheld and is what `P07-U-35` records |

`P07-C-02` (four branch representations) and `DUP-03` (two withholding frameworks on one payment)
are **corroborated** this round — the first at field level by `EP-4b`, the second by P06's own
evidence (`MD-05`). **Neither moves. Corroboration is not a status change.**

## 4. Revision Entries

| ID | What was wrong or added | Found by | Effect |
|---|---|---|---|
| `REV-E-92` | **This package cites `smesplus_tax_period_date` throughout; the module actually installed is `scgl_tax_period_date`.** `smesplus_tax_period_date` is installed in **0 of 7** identities; `scgl_tax_period_date` in **3 of 7**. The citation named a module nobody runs. | `EP-4a`, run to answer P10's `Q07-4` | **No finding moves.** `P07-F-74` established all five copies hash to one tree, differing only in the manifest `author` — so the code cited *is* the code that runs. Corrected as an **attribution** defect, and the original citations are left standing with this note attached. |
| `REV-E-93` | **A draft reading of this round's own evidence was wrong and was caught before publication.** `EP-4b` reported `tax_ids` as a Many2many *added to* `account.move` by `l10n_th_withholding_tax`, and the draft treated it as a shadowing data carrier. Reading the definition shows `compute='get_tax_domain'`, **not stored** — a UI domain helper. | own check, prompted by the latent-vs-live rule | The alarming reading was deleted before it was written. **An AST field walk reports that a field exists; it does not report whether it holds anything.** |
| `REV-E-94` | **A second draft reading, also wrong, also caught before publication.** The draft had `P07-F-109` as a **live** divergence reaching a vendor-facing document, because `print_payment_remittance_adviec` reads the stored value. It is **uninstalled in 6 of 7 identities and absent from the 7th's registry**. `print_voucher_request` **is** installed in 3 of 7 — and recomputes. | own check: *measure reachability before ranking severity* | `P07-F-110` exists **because** the check was run. The finding is materially weaker and materially truer. **Two of this round's three self-caught errors were the same kind — reading a source fact as a runtime fact.** |
| `REV-M-95` | **P10 published a handoff because the route was empty** — a table cell reading *"routed to P07"* with no evidence and no question. **A declared route with an empty payload is worse than no route**, because it reads as coverage. Adopted: every handoff this package publishes carries the exact question, the evidence and the bound, or it is not published. | P10 (`G02-R-09`) | `P07_TO_P10_HANDOFF.md` is written to that standard |
| `REV-M-96` | **A discharge is scoped to the claims it tested, and its conclusion must never be generalised.** `P07-F-78` closed `P07-U-28` with *"which copy is deployed cannot change any published finding"* — true of the four claims it hashed, and read one sentence wider it would have suppressed `P07-F-108`. The general form: **when a check retires a question, record what it tested, not what it concluded** — because the conclusion is what the next reader carries forward. | own audit, on finding a fifth file the check never covered | `P07-C-27` |
| `REV-E-95` | **A widening control answered the wrong question.** `EP-1` reported *"no installed name carries a bare `th` token outside `l10n_`"* as evidence its prefix was not narrowing the population. `scgl_tax_period_date` carries no `th` token either — **the control proved the prefix captured every module *named* Thai, not every module *doing* Thai tax**, which is what the claim needed. | `CH-C` | The control is recorded as **insufficient**, not deleted; `P07-F-105` carries the scope limit it should have caught. A widening control must widen along the axis the claim travels on. |
| `REV-M-98` | **A claim's population and its subject can differ by a name.** `P07-F-105` is about *the Thai localisation*; the searched set was *modules named `l10n_th*`*. Those are the same set only if Thai tax code is always named Thai — and in this estate it is not, by the round's own counter-example. **Before publishing a claim about a domain, check that the population was selected by the property the claim is about and not by the naming convention that usually accompanies it.** This is `eligibility is the fifth unit` (`REV-M-25` line) arriving through the name rather than the path. | `CH-C` | `P07-F-105` scope note; `P07_TO_P10_HANDOFF.md §2` states the bound in the sentence itself |
| `REV-E-96` | **The pre-commit sweep was flat and the package had become nested.** This round added a sub-directory with 13 deliverables; `os.listdir` saw none of them, and unit `[0]` would have reported *"on disk 23, at HEAD 23, clean"* while **13 published files sat outside every unit of the sweep** — no set integrity, no identifier check, no table check, no manifest row, no Layer-1 scrub. **`P07-F-98` exactly: a set the check cannot see.** The sweep now walks recursively and unit `[0]` diffs against `git ls-tree -r`. | own check, on running the sweep against the new folder | The 13 files entered the manifest (23 → **35 rows**) and the identifier check immediately found three further defects that a flat sweep would have published |
| `REV-E-97` | **An identifier collision is invisible to an orphan check, and this round created two.** `P07-C-24` and `P07-C-25` were issued fresh in the round folder while **both already existed** in `11_P07_CONTRADICTION_REGISTER.md`. Cited and defined counts matched, so unit `[1]` reported `UNDEFINED: NONE` — **an orphan check counts, and a collision does not change the count.** Renumbered to `P07-C-26`…`P07-C-29` and a cross-file duplicate-definition check run: **0 ids defined in both the root and the round folder.** | own check, prompted by reading the register I was appending to rather than trusting the sweep | **P04's stem-collision class, recurring in the one shape the sweep cannot detect.** The new check is *is any id defined twice*, which is a different question from *is any id cited and never defined* |
| `REV-M-100` | **A check that counts cannot detect a duplicate.** Orphan detection compares the cited set to the defined set; a second definition of an existing id leaves both sets the same size and both memberships satisfied. **Every counting check has a corresponding collision the count conceals**, and the fix is never a bigger denominator — it is a second check whose unit is *identity* rather than *presence*. Added to the sweep's standing intent alongside `REV-M-90`'s *the family name is not the series*. | own audit of why `REV-E-97` passed unit `[1]` | `REV-E-97` |
| `REV-M-99` | **An open item under a declared Closure Question is a gap in that question, not a note.** This round opened `P07-U-36` as `NOT YET READ` while `CQ-P07-09` depended on it, on data already extracted, one query away — and only the closure-question disposition pass caught that *"not yet read"* was standing in for *"not attempted"*. **`Sufficient Evidence = Stop Searching` licenses stopping when the question is answered, never when it is merely deferred.** Closed as `P07-F-112`, which turned out to be `S1`. | the disposition pass in `P07_CLOSURE_QUESTION_REGISTER.md` | `P07-F-112` |
| `REV-M-97` | **A count disagreement between two documents can be two correct denominators.** P10 asked P07 to reconcile nine against ten. Neither was wrong: the estate holds `10, 10, 10, 10, 9, 9, 1`. **Before reconciling two numbers, ask whether they are counting the same population** — `MD-02` would have consumed a round if the answer had been assumed to be one number. | own execution of `MD-02` | `P07-F-106` |

## 5. Corrections This Round Did **Not** Make

Recorded because a supplement that only adds is not a review.

- **`P07-F-01`, `P07-F-42`, `P07-F-50`, `P07-F-11`** — no delta touched them. Not re-run, not
  restated, not re-evidenced. `No Material Delta = No Additional Research Round.`
- **`P07-N-25`** — settled last round by the `ir_model` registry route and **not reopened**; its
  terminated filesystem route is **not rerun**.
- **`P07-U-32`** — 20 unread artefacts. Class E for every declared Closure Question. Unchanged.
- **P10's own register.** `P07-C-26` is resolved in P07's favour and **P07 does not edit P10's
  package.** The handoff is published; the correction is P10's to make.
