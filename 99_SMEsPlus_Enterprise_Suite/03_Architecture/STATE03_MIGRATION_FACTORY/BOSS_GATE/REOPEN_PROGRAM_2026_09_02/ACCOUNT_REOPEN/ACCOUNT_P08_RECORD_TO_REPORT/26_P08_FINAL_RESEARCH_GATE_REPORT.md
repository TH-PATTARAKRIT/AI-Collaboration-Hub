# P08_FINAL_RESEARCH_GATE_REPORT

Session `SMEPLUS-26-09-04-ACC-P08-R2R-REV2-001` · Process P08 — Record-to-Report
Branch `research/account-p08-record-to-report-2026-09-04-001` from `origin/SMEsPlus` `88f52cd`

## 1. Exit criteria — `EC-01` … `EC-08`

Assessed against the Very Deep Research 8-Criteria Universal Exit Constitution. A criterion is not satisfied because the package says so.

| Criterion | Verdict | Reason |
|---|---|---|
| `EC-01` Scope bounded | **NOT SATISFIED** | The 22-root set is bounded, declared and **independently reproduced** — a real advance and the first reproduced cross-session denominator in this lineage. But the 32-object scope matrix rests on an author-chosen collapse of 131 models; the lock-field population has no literal pattern; the product-line column is inferred for 8 of 22 roots; and one published denominator could not be reproduced at all. None was declared `UNBOUNDED / NOT YET ENUMERABLE`; they were published as figures. |
| `EC-02` Enumeration converged | **NOT SATISFIED** | See §2. Four independent passes each returned new material finding classes, and the artefact created to close the programme's standing enumeration defect **contained a defect of the same family**. |
| `EC-03` Unknown exhausted | **NOT SATISFIED** | 17 unknowns now carry the mandated vocabulary, **8 of them `GATING`**. Three of the eight (`P08-U-13`, `-14`, `-17`) are this session's own residuals and are named as such rather than routed forward. But a `GATING` unknown remaining is a `GATING` unknown, and `P08-CONTRA-15` — whether P08 may execute at all before Wave A's Boss gate — is routed to a Boss decision rather than dispositioned. |
| `EC-04` Tolerance-zero closed | **NOT SATISFIED** | **8 raised, 0 closed.** Unconditional criterion. |
| `EC-05` Contradiction resolution complete | **NOT SATISFIED** | 18 contradictions registered with competing claims, evidence and disposition. But an accepted peer correction was published in its superseded form in four places until independent review caught it — a contradiction recorded and not propagated is not closed. Two further contradictions are between **governing instruments** and are raised, not resolved. |
| `EC-06` Negative claim controlled | **NOT SATISFIED** | 3 class-`A` claims legitimately re-run across the root set; **~19 remain scoped to one root of 22 and were not re-run**. Four unqualified negatives carried no class letter until review. One class-`A` claim's published wording exceeded its pattern and was contradicted by this package's own evidence. |
| `EC-07` Two consecutive clean independent passes | **NOT SATISFIED** | **Zero clean passes.** Four passes ran; each returned new material findings; and none ran against a stable baseline, because the package was being written throughout. |
| `EC-08` Final knowledge package complete | **NOT SATISFIED** | Now present: semantic model, coverage register, source-of-truth determination, event model, dependency map, control matrix, failure register, unknown register, contradiction register, negative-claim register, tolerance-zero register, evidence manifest with digests, Boss decision register, clean-room transfer, this report, and full repository lineage. **Absent:** the two clean passes `EC-07` requires, without which the package is complete in form and unproven in fact. |

**`0 of 8` satisfied.**

## 2. Method convergence — `MC-01` … `MC-10`

| Test | Verdict |
|---|---|
| `MC-01` Population boundedness | **PARTIALLY MET** — root set bounded and reproduced; four material denominators are not |
| `MC-02` Systematic enumeration | **PARTIALLY MET** — the root scan, module census, posting sweep and custom census are deterministic and reproduce exactly; the object collapse and the event class list are not |
| `MC-03` Independent delta | **NOT MET** — four passes, four new material finding classes |
| `MC-04` Repeatability | **MET** — every structural figure an independent reviewer re-ran reproduced exactly, including all 22 module counts and all three root-set-wide scans. **The one criterion this package passes outright** |
| `MC-05` Negative-claim compliance | **NOT MET** — ~19 single-root class-`A` claims |
| `MC-06` Unknown classification | **PARTIALLY MET** — the vocabulary is now applied to all 17 |
| `MC-07` Contradiction closure | **NOT MET** — propagation failure, since corrected |
| `MC-08` Tolerance-zero closure | **NOT MET** — 8 open |
| `MC-09` Evidence lineage | **PARTIALLY MET** — Layer 2 sampled by a reviewer at 8 of 8 accurate; but **no prior claim carries a file-and-section citation**, so the correction-citation discipline is asserted and not demonstrated |
| `MC-10` New-finding delta | **NOT MET** — findings arriving during a single review changed the kernel object count, added the invariant the package had omitted, restated the source-of-truth conclusion at a different layer, withdrew a requirement and added an attack |

**`1 met · 4 partially met · 5 not met` — `NOT CONVERGED`.**

## 3. What this package nonetheless establishes

Recorded so the HOLD is not misread as a finding of no value.

1. **The source-of-truth determination**, at the corrected layer: the journal item is both original and derived truth and nothing marks which; the general ledger is uniformly derived; the report layer holds three further independent truth bearers and its definition a fourth.
2. **Four objects the benchmark does not have**, and what each absence causes.
3. **Twenty-three attacks**, of which twenty-two are not stopped and one is stopped on one path only.
4. **Twenty-eight controls with their enforcement layer named** — six at the database layer, of which exactly one protects an accounting relationship.
5. **A reproduced root-set denominator** — the first in this lineage — and three negatives legitimately re-run across it.
6. **The scope splits**: account definition versus number; observation versus policy versus applied measurement; counterparty master versus counterparty as a component of a posted fact.

## 4. Vetoes standing

Four reviewers issued five vetoes. **None is lifted by this report.** Each names editable conditions; the conditions that are closable by editing have been applied, and those requiring re-execution — the ~19 class-`A` re-runs, the two clean passes — have not.

## 5. Recommendation

# `RECOMMEND HOLD`

`CONDITIONAL PASS` is **unavailable by rule**, not by judgement, while tolerance-zero boundaries stand unresolved: conditions that *are* the tolerance-zero set would be a pass with a different label.

All four independent reviewers, the PMO view and this report reached `RECOMMEND HOLD` separately and on different grounds.

## 6. What this session does not declare

Not declared: `PASS` · approval · sign-off · readiness of any kind for any module, wave, state or gate · final freeze · merge · release · implementation authority · Wave A closure · Wave B commencement · any statutory conclusion.

Terminal state: **`HANDOFF PREPARED — CORE ACCOUNTING RECONCILIATION`**. The ledger contract is stated, the evidence is preserved and the open items are named.

**Boss alone converts this recommendation into a decision.**

## 7. The three things most worth doing next, in order

1. **Re-run the ~19 single-root class-`A` claims across the declared root set.** Their patterns are already written. This is mechanical and it closes the unclosed half of the root-set defect.
2. **Sweep the 20 unexamined custom modules that touch the ledger.** The one examined contained a total access bypass. A sample rate of 1 of 1 is not a basis for assuming the rest are clean.
3. **Run one executed call and one database query.** Six findings turn on a single execution each, and every one of them is currently a `SUPPORTED INTERPRETATION` that would become a fact or a retraction in minutes.
