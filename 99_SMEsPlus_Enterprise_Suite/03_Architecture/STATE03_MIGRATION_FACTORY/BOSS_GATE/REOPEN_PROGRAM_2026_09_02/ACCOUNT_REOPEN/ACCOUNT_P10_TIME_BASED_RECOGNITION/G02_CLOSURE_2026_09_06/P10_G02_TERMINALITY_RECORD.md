# P10 — G02 TERMINALITY RECORD

`CQ-P10-13` — evidence integrity and terminality. Round
`SMEPLUS-26-09-06-G02-P10-TBR-BOUNDED-DEEP-CLOSURE-DESIGN-INPUT-001`.

> **This record was ticked before it existed.** The closure-question register marked `CQ-P10-13`
> complete against this filename while the file was absent from disk. Three of four challengers
> found it independently. It is recorded as `G02-R-08` — a disposition column asserting a completion
> the artefact did not support, on **the one question that certifies the other twelve**. The tick was
> false when made. It is not being quietly satisfied.

---

## 1. Closure-Question Dispositions — All Thirteen

| CQ | Subject | Disposition | Basis |
|---|---|---|---|
| `CQ-P10-01` | Recognition event identity | **ANSWERED — DESIGN INPUT** | Five mechanisms graded; the anchor grade is now qualified by the relation-wipe finding (`G02-R-07`). `D-5` is Boss-reserved and P10 withdrew its competing proposal |
| `CQ-P10-02` | Recognition period vs posting date | **ANSWERED — FACT VERIFIED, and re-verified on the deployed generation** | The collapse holds for **2 of 4** mechanisms. What collapses is the **period**, not the identity. Confirmed in the deployed 19.0+e schema (`G02-R-04`) |
| `CQ-P10-03` | Period grid and conventions | **ANSWERED, CORRECTED** | Not three engines: **four in-house conventions across three modules, plus eight named standards.** The deferral convention is `30E/360 ISDA` with its February clause removed (`G02-R-13`) |
| `CQ-P10-04` | Revenue / expense boundary | **ANSWERED, RESTATED** | **One engine, two domains** — not "one domain with a direction switch". The P02 input was corrected (`G02-R-01`, `G02-R-02`) |
| `CQ-P10-05` | Allocation policy and scope | **ANSWERED — DESIGN INPUT** | Normative rows re-classed `DESIGN CANDIDATE` (`G02-R-12`) |
| `CQ-P10-06` | Currency and amount | **ANSWERED, UNDER-SPECIFIED** | The requirement needs four columns and a **monetary/non-monetary classification P10 does not own**. Routed to P08 with the classification question named, not just "FX policy" |
| `CQ-P10-07` | Correction and reversal | **ANSWERED, CORRECTED** | "No in-flight amendment path" is contradicted; a **fourth outcome, `SILENTLY DIVERGENT`,** is added (`G02-R-06`) |
| `CQ-P10-08` | Period close and lock | **PARTIAL** | "Bind" struck and refuted at the ledger owner's package; the lock exception and the entry seal are missing; the tax-lock premise is untested and `OPEN` (`G02-R-10`, `G02-R-11`) |
| `CQ-P10-09` | Shared semantic kernel | **ANSWERED — DESIGN INPUT** | Convention library moves from *build* to *adopt* — **and the route is the upstream MIT package, not the reference module, which is proprietary** (`G02-R-13`) |
| `CQ-P10-10` | Audit and lineage | **ANSWERED — FACT VERIFIED** | Three-population reconciliation with `unexplained` as a visible state. Unchallenged |
| `CQ-P10-11` | Scope ownership | **ANSWERED, RE-CLASSED** | Six of seven scope objects are still uncompared with any peer |
| `CQ-P10-12` | Evidence population | **CORRECTED BY EXECUTION, then PARTIAL** | Denominator 4 → **6 distinct, all six examined** (`G02-R-03`). Version basis closed **at schema level** and `UNRESOLVED` for behaviour (`G02-R-04`) |
| `CQ-P10-13` | Evidence integrity / terminality | **THIS RECORD** | See §2–§5 |

**Nine answered · three partial or corrected-then-partial · one is this record.
Zero closed. No question is dispositioned `PASS`.**

---

## 2. Material Delta Log

The round's earlier statement that **no Material Delta was raised** was true when written and is now
superseded.

| MD | Raised by | CQ | Bounded surface | Ran? | Outcome |
|---|---|---|---|---|---|
| `MD-P10-G02-01` | **P10**, on a peer correction naming P10 | `-04`, `-08`, `-12` | Three deployed archives never opened, inside the declared host path set | **YES** | Denominator 4 → 6; three capability negatives withdrawn; headline finding strengthened |
| `MD-P10-G02-02` | **P10** | `-12` | The deployed 19.0+e DDL carried by an archive already admitted | **YES** | Version basis closed at schema level; the central finding verified on the deployed generation |
| `MD-A3C1-01` | Challenger 1 | `-07`, `-03` | Window-edit guard chain and three field sets | **YES** | `G02-R-06` |
| `MD-C4-01`…`-03` | Challenger 4 | `-03`, `-07`, `-11` | Grid methods, five cited call sites, the UI surface | **YES** | 17 citations re-verified; `G02-R-14` |
| `MD-X3-01`…`-04` | Challenger 3 | `-01`, `-04`, `-06`, `-08`, `-12` | P02, P08 and P11 packages at pinned heads | **YES** | `G02-R-01`, `-02`, `-10`, `-11`, `-15` |
| `MD-C2-01` | Challenger 2 | `-01`, `-07` | Two foreign-key rows | **NO** | Needs a restore. **`G02-R-05` stays `UNRESOLVED`; it is the difference between destructive and catastrophic** |
| `MD-C2-02` | Challenger 2 | `-03` | One convention-distribution query | **NO** | Would widen |
| `MD-A3C1-02` | Challenger 1 | `-04` | Classifying the billed-ahead lines by obligation type | **NO** | **P02's population. Routed, not chased** |
| `MD-A3C1-03` | Challenger 1 | `-07` | Asset pause/resume as a lifecycle comparator | **NO** | Routed to P04/P11 |
| `MD-C4-04` | Challenger 4 | `-12` | A 19-generation source tree | **NO** | Would admit a new root. **Superseded in part by `MD-P10-G02-02`, which reached the same question without one** |

**Every executed delta went deeper on a surface already declared. None admitted a new root, a new
sweep, a new process or a new database beyond the peer-corrected denominator of P10's own population.
Closure became deeper. It did not become wider.**

---

## 3. Evidence Integrity

| Control | Result |
|---|---|
| Every zero carries a positive control | **Yes.** the deferral-entry-to-source link structure exists with a a data-block header and no rows in all seven artefacts — empty, not absent |
| Byte-size used as an absent/empty discriminator | **No.** That control cannot detect its own failure and is not relied on |
| Negatives carry a class letter and a declared boundary | **Yes**, and three were **withdrawn this round** as contradicted by execution (`G02-R-03`) |
| Peer status fields read from the peer's own register, not a summary | **Yes** — verified verbatim in three places. **But every peer object was referred to descriptively; identifiers are now supplied** (`G02-R-15`) |
| Peer supersession checked at claim level | **NO — this control failed twice.** Two peers each issued P10 a written correction to a claim P10 consumed, and the closure round consumed the **pre-correction** version of both (`G02-R-01`, and the peer's withdrawn event-object claim) |
| Arithmetic reproduced rather than asserted | **Yes** — both published figures reproduced to the digit by an independent party |
| Package frozen before adversarial review opened | **NO.** All four challengers read a package still being written (`G02-R-16`) |

---

## 4. Corrections This Round

**Nineteen** (`G02-R-01`…`G02-R-19`), plus one error record (`G02-E-01`, the non-existent baseline SHA,
recorded rather than silently substituted).

**Self-caught: 1. Externally caught: 18.** Cumulative across five rounds: **79 corrections, 5
self-caught.** The ratio has not improved in any round, and the controls that catch these defects are
adversarial and peer-facing, not self-applied.

**Three corrections were produced by the evidence base itself, not by reasoning** — the wrong path
set, the wrong archive format, and an untested capability negative. That is the same triple the
programme has now hit four times with the rule already written down.

---

## 5. Terminal State

> ## `G02-P10 — MAXIMUM AVAILABLE EVIDENCE REACHED FOR THIS BOUNDED SURFACE. RECOMMEND HOLD.`
>
> **Not PASS. Not approved. Not frozen. Not merged. No implementation authorised. No architecture
> decided. No mutation occurred.**

**Named dependencies, all open:**

| # | Dependency | Owner |
|---|---|---|
| 1 | `D-5` — accounting-event identity | **Boss** |
| 2 | `P10-D-02` — the recognition-period ruling | **Boss** |
| 3 | The programme-wide tolerance-zero boundary on silent mutation | **Boss**, raised by `P11` |
| 4 | `BP-03` — revenue on billing or on performance | **Boss** |
| 5 | Segmentation of the cut-off population by product policy | `P02` |
| 6 | The tax-period question | `P08` |
| 7 | Monetary / non-monetary classification of a recognition base | `P08` |
| 8 | Localisation overrides | `P07` |
| 9 | Reproduce-a-prior-statement versus reconcile-a-correction | Boss / PMO |
| 10 | Whether the schedule back-reference is emitted as a cascading constraint | `P04` / runtime |

**Standing vetoes, none discharged:** P10's own implementation veto, and a peer design veto naming
deferred-recognition design.

**Background tasks: 4 launched, 4 complete, 0 outstanding.**

**P06 is not started. This round stops here.**
