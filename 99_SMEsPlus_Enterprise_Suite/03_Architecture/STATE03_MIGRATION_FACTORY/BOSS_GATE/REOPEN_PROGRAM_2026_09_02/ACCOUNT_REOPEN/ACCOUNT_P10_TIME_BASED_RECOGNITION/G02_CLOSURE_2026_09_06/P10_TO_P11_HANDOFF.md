# P10 → P11 HANDOFF  (Central Reconciliation — architecture, scope, Boss decisions)

Published, not executed. **P10 does not decide P11-owned programme architecture.**

---

## 1. Boss Decisions P10 Carries

| # | Decision | Status | Coupling |
|---|---|---|---|
| The peer tolerance-zero boundary | **`HOLD — BOSS DECISION REQUIRED`** — quoted from P11's own register. `UNRESOLVED != ADOPTED` | **First.** Programme-wide; binds more than P10 |
| `D-5` — introduce a layer-3 accounting-event object, or not | Boss | **Second.** Gates one recognition option and P10's kernel |
| `P10-D-02` — may a posting constraint alter an original recognition period? | Boss | **Third.** Six options, **none eliminated** |
| **Revenue on billing versus performance** | **`OPEN — Boss reserved`**, preserved from P02 | Adjacent; P10 supplies the mechanism fact |
| Bill-and-hold scope and trigger | Boss | Adjacent |
| May a tenant **bind** an allocation convention? | Boss | Downstream |
| Fiscal or civil recognition grid? Daily grid? | Boss / P08 | Downstream |
| Is a shared chart of accounts permitted? | Boss / P08 | Decides whether a scope defect fails loudly or silently |

**The first three are coupled and should be presented together, in that order.** The boundary is programme-wide and must not be presented as the second half of a P10 decision.

**And P11's own register records a defect in that boundary's derivation** — it was scoped from the occasion that prompted it and the owed enumeration was never performed. The Boss should see that beside it.

## 2. Architecture Input — `DESIGN CANDIDATE` only

**Shared semantic kernel, three elements:**

| Element | Status |
|---|---|
| **Convention library** | **adopt-and-extend, not build** — a standards-named library of eight conventions already exists in the reference root |
| **Correction algebra** | three outcomes, domain-selected; the *operations* are not shared |
| **Attribution construction** | **the strongest element — four independent mechanism-level implementations, failing three different ways** |

**Relocated:** event identity → `D-5` · period-versus-posting separation → P08 as an obligation.
**Blocked:** period grid, on a ledger concept that does not exist.

**Separate domain engines keep:** object · lifecycle · posting pattern · business rules · **termination condition** · **residue policy**. Deferred revenue and deferred expense are **one** domain, not two.

## 3. Scope

13 determinations, 0 changed this round, **3 carrying expiry triggers**. **Six of seven scope objects have never been compared with any peer's**, and for most a comparison is **currently impossible** because no peer has published one. **Routing a class-`C` item does not discharge it.**

Two exposure mitigations are **data states, not controls**, and each expires on the first change to that data.

## 4. Vetoes

`AASP-VETO-01` **revision 3 — remains and is strengthened.** Grounds cover generation, correction and reversal, reopen, **and the lock-free mutation path**. It lifts only on `D-5` **and** `P10-D-02`. It is **not immune to evidence** — that clause was withdrawn.

**A peer design veto also binds P10** — it prohibits any deferred-recognition design from allocating a balance-sheet row into the management ledger, design adoption only. P10 had asserted its own veto was "the only veto binding P10"; that was wrong and is withdrawn.

## 5. The Invariant

`ONE ECONOMIC FACT → ONE CANONICAL RECOGNITION EVENT → ONE ACCOUNTING EFFECT PATH.`

**Not satisfied by the reference**, and P02 reached the same conclusion independently from the order-to-cash side at the correction/reversal stage. P10 adds four instances: two journal shapes from one setting; two residue destinations; two rounding currencies; total intra-month divergence.

## 6. Population

**P10's estate statements are bounded to four databases out of at least ten inside its own declared population**, at image dates ending 2026-08-03, with a newer snapshot present and unreadable. P02 measured a database P10 has never examined. **P11 owns cross-process population reconciliation; P10 does not close it.**

---

## PEER IDENTIFIERS — `G02-R-15`

P10's substance was verified faithful to P11's own register in three separate places, but **every peer
object was referred to descriptively and none by identifier**, so P11 could not resolve any of it to a
register row. Named:

| P10's description | Identifier | P11's status field, verbatim |
|---|---|---|
| "the peer tolerance-zero boundary" | **`T0-13` / `P11-B-16`** | `HOLD — BOSS DECISION REQUIRED` · tolerance-zero · present defect · found narrow three times |
| "a second re-dating path that fires with no cut-off configured" | **`UAE-05`** | Owned and named by P11. **P10 re-routed it as new and should not have** |
| "the accounting-event identity decision" | **`D-5`** | Class remains `C`; the class `C`→`A` claim is **withdrawn** by P11, and P10 does not claim the withdrawn upgrade |
| "a peer design veto also binds P10" | the analytic-process design veto | Binds **design adoption**, names deferred-recognition design |
| P10's own contribution | already in P11's intake as **"STRENGTHENS `T0-13`"** | cited so P11 can join the two records |

## TWO ITEMS ADDED AFTER CHALLENGE

1. **P02 issued a denominator correction naming P10** — six distinct deployed archives, all readable.
   P10 had recorded four. **P10 has now executed it**; the corrected denominator and its result are in
   `P10_EVIDENCE_POPULATION_BOUNDARY.md` §3. P11 owns population reconciliation and should carry it.
2. **A peer's event-object claim was withdrawn as contradicted**, and that peer required P10 to
   re-state a conclusion resting on it. P10's closure round consumed the pre-correction version.
   The re-statement is made in `P10_RECOGNITION_EVENT_IDENTITY_TRACE.md`: P10's conclusion survives on
   the corrected fact — a per-channel key populated nowhere is still not a platform identity — but it
   **rests on different and weaker ground**, and the correctly-scoped successor claim is
   **untested across the root set**.
