# P10 → P07 HANDOFF

`LAYER 1 — CLEAN ROOM.` Round `SMEPLUS-26-09-06-G02-P10-TBR-BOUNDED-DEEP-CLOSURE-DESIGN-INPUT-001`.

> **Published because it was missing.** P06, P08 and P11 each received a handoff this round. P07
> received a table cell reading *"routed to P07"* with no evidence, no question and no actionable
> content — a declared route with an empty payload (`G02-R-09`). **P10 does not execute P07.**

## 1. P10 Makes No Statutory Claim

Verified by scan over every G02 deliverable: no statutory, tax-treatment, jurisdiction or standards
assertion appears anywhere in P10's closure package. P10 records **mechanism**, not treatment.

## 2. The Evidence P07 Needs, and Why It Is Being Sent

**Nine Thai localisation modules are installed on the deployed estate.** That is `FACT VERIFIED` in
P10's lineage from the deployments' own module manifests.

**And P10's negative about them may not be written.** P10's negative-claim register carries:

> *"Localisation modules do not alter time-based recognition."* — **`C — NOT SEARCHED`. Must not be
> written.** No localisation module was examined for recognition overrides.

P10's own unresolved-evidence register marks this obtainable **"cheaply"** and it was **not obtained**
in any of five rounds. A reader of P10's closure package alone would not learn that a
`C — NOT SEARCHED` localisation surface stands over a nine-module Thai estate. **In a Thai-market
deployment, localisation silence is not a safe default.**

**One discrepancy inside P10's own lineage, unresolved (`G02-R-17`):** two documents record **nine**
localisation modules; a third records **ten**. P10 has not reconciled them. P07 should establish the
count from the manifests directly rather than inherit either figure.

## 3. Exact Questions for P07

| # | Question |
|---|---|
| `Q07-1` | Do any of the installed Thai localisation modules **override, extend or replace** deferred-revenue, deferred-expense or accrual **recognition timing**? |
| `Q07-2` | Do any of them alter the **account derivation** for a recognition entry — the control account, the journal, or the counterpart? |
| `Q07-3` | Do any of them impose a **presentation** requirement on deferred balances that the recognition design must be able to satisfy? |
| `Q07-4` | Does a recognition entry carry the **tax-period field** that exists on the entry and not on the item in the deployed schema — and if so, does the tax cut-off bind it? *(Shared with `P08`; see `G02-R-11`.)* |
| `Q07-5` | Statutory presentation of deferred revenue versus deferred expense: P10 has verified they are **NOT shared** in the reference and makes no claim about which presentation is required |

## 4. What P10 Supplies, and Its Class

| Item | Class |
|---|---|
| Nine (or ten) Thai localisation modules installed | `FACT VERIFIED` — from the deployments' own manifests; count unreconciled |
| "Localisation modules do not alter time-based recognition" | **`C — NOT SEARCHED`. Must not be written by anyone, including P07, without executing the search.** |
| Revenue and expense recognition share one engine and **not** their statutory presentation | `FACT VERIFIED` in the reference; **no statutory claim** |
| A tax-period carrier exists on the entry and is absent from the item, deployed 19.0+e | `FACT VERIFIED` at schema level; behaviour untested |
| The two `HOLD / EVIDENCE REQUIRED` contradictions the G02 contradiction register routes to the tax process | named there; carried here so the route is not empty |

## 5. Bound

Everything above is bounded to **six distinct deployed databases** and to reference generation
**18.0+e**, whose relevant field sets are verified present in the deployed **19.0+e** schema.
**Behavioural equivalence across generations is untested.** Nothing here is a statutory position,
and no statutory position may be inferred from it.
