# P10 — SOURCE LINK REGISTER (G02 CLOSURE ROUND)

**Clean-room boundary.** Layer 1 documents in this closure round carry **no vendor model names, file paths, method names or line numbers**. Raw citations live in the Layer 2 quarantine of the parent package and are referenced by identifier only.

---

## 1. Evidence Consumed by This Round

| Class | Where it lives | Re-derived this round? |
|---|---|---|
| Source citations `E-P10-001` … `E-P10-092` | Layer 2 quarantine, parent package | **No.** Carried forward; the monotonic-scope rule forbids re-running settled searches without a Material Delta |
| Deployed-database observations | Layer 2 §4 and the enumeration outputs | **No** |
| Four prior AAS-03 challenge records | `CHALLENGE/` in the parent package | **No** |
| **P02 authoritative closure** `7cb1c27` | the peer's own branch | **Consumed as controlled input, verified to exist, quoted with its status preserved** |
| **Closure Constitution** `48ee264` | governance directory | **Verified and applied** |

## 2. New Evidence Admitted This Round

| # | Item | Source | Bounded? |
|---|---|---|---|
| `G02-E-A` | The four P02 → P10 routing items with their measurements | P02 package at the pinned commit | Yes — one register, read once |
| `G02-E-B` | P02's Boss Decision 3 (`revenue on billing versus performance — OPEN, Boss reserved`) and unresolved dependency 5 (`owner Boss / P10`) | same | Yes |
| `G02-E-C` | P02's design candidates including **event identity** and a **two-date model**, reached independently | same | Yes |
| `G02-E-D` | P02's invariant finding — *one business fact → one canonical event owner → one accounting effect path* is not satisfied at the correction/reversal stage | same | Yes |
| `G02-E-E` | Verification that the declared baseline SHA does not resolve | this repository | Yes — four `cat-file` checks |

**No source tree was re-searched, no database was re-extracted, and no new evidence population was admitted.** Every source-derived statement in this round rests on evidence already registered in the parent package's Layer 2.

## 3. Material Delta Log

| MD ID | CQ | Bounded surface | Why insufficient | Stop condition | Outcome |
|---|---|---|---|---|---|
| — | — | — | — | — | **None raised by P10 itself.** Challenger-raised deltas, if any, are recorded at `P10_G02_AAS03_CHALLENGE_RECORD.md` §1 with all five fields before execution |

**P10 opened no bounded deepening pass of its own.** The closure questions were answerable from registered evidence plus the P02 controlled input. Recorded explicitly, because *"no Material Delta"* is a finding under the Constitution and not an omission.

## 4. Repository Lineage

| Item | Value |
|---|---|
| Repository | `TH-PATTARAKRIT/AI-Collaboration-Hub` |
| Branch | `research/account-p10-time-based-recognition-2026-09-04-001` |
| Baseline verified at receipt | `284ea6651b7ba2ac712072d2654fc54f83d2deb3` |
| Prompt commit fast-forwarded | `afcfa6456f3ac3d5fb464aeff2b3643c065774d5` |
| P02 input | `7cb1c2783f3f5b9c11bf107d4d7202affe455d7d` |
| Constitution | `48ee264fd74dcb0dee378789e56d028ad8bb6110` |
| Merge status | **Not merged. No merge proposed.** |
| Mutation | **None.** No production source, database, module, migration or deployment was touched |

---

## EVIDENCE ADDED THIS ROUND — MATERIAL DELTA EXECUTIONS

The register's earlier statement that **no source tree was re-searched this round** was true when
written and is superseded. Two Material Deltas raised by P10 and four by challengers were executed.

| Evidence | Method | Result |
|---|---|---|
| Three deployed archives never opened, inside the declared host path set | Read with a newer database toolchain and an ordinary archive extractor; deferral-entry count, window count and the five cut-off columns extracted per archive | Denominator 4 → **6 distinct, all six examined.** Zero deferral entries in every one |
| Positive control for each zero | The deferral-entry-to-source link structure exists as a table with a data-block header and no rows in all seven artefacts | **Empty, not absent.** The byte-size control that cannot make this distinction is not relied on |
| Deployed-generation schema | One archive is a plain-text dump of the deployed generation and carries its full structure definition | The journal item carries the two window-date fields and **no recognition-period column**; the journal entry carries the period-beginning date for depreciation; five cut-off columns; a tax-period carrier on the entry and **not** on the item; a paused-days counter on the schedule record |
| The window-edit guard chain | Four field sets and one write guard read in the reference root | No change tracking, absent from the integrity-hash set, absent from the cut-off-protected sets, guard raises for the account field alone |
| Peer packages at pinned heads | Read directly, status fields verbatim | Two peer corrections addressed to P10 by name, neither previously consumed |

**Every read went deeper on a surface already declared. No new root, no new sweep, no new process
surface, no new database beyond the peer-corrected denominator of P10's own population.**
