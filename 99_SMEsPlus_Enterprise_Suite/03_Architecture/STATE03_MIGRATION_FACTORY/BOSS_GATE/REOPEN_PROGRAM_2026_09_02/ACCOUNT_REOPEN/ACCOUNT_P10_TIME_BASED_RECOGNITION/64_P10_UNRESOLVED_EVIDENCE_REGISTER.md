# P10 — UNRESOLVED EVIDENCE REGISTER

Session: `SMEPLUS-26-09-05-ACC-P10-TBR-DECISION-INTEGRITY-EVIDENCE-REPAIR-001` · Layer 1

Everything classed `UNRESOLVED — EVIDENCE REQUIRED` or `NOT SEARCHED`, in one place, with what would settle it and whether P10 can get it.

---

| # | Item | Class | Obtainable by P10? | What settles it |
|---|------|-------|--------------------|-----------------|
| `UE-01` | The three entries in the populated database whose date year is later than their period-beginning year | **UNRESOLVED — EVIDENCE REQUIRED** | **Yes** | Read those three assets' modification history. A legitimate catch-up stub and a re-date leave the identical signature |
| `UE-02` | The lock-free mutation path's population in P10's own mechanisms | **UNRESOLVED** | Partly | An enumerated sweep of the paths that edit a document date on a non-sale document |
| `UE-03` | Total journal-entry counts for the two 44-company databases | `C — NOT SEARCHED` | **Yes** | One extraction each |
| `UE-04` | The compressed container holding a second snapshot of one database, and an installed-module manifest | `C — NOT SEARCHED` | **Yes** | Extract it |
| `UE-05` | The two available time series — two databases each have an earlier snapshot | `C — NOT SEARCHED` | **Yes** | Difference them |
| `UE-06` | Whether other recognition mechanisms write attributions in the same shape | `C — NOT SEARCHED` | **Yes** | An executed sweep on three pattern forms, not one — the peer's own sweep found its declared pattern missed its headline site |
| `UE-07` | Executing reproduction of the two company-boundary defects | `C — NOT ATTEMPTED` | **No — requires starting a database service** | Operator action |
| `UE-08` | Duplicate-recognition reproduction | `C — NOT ATTEMPTED` | No | As `UE-07` |
| `UE-09` | Asset lifecycle paths — pause, resume, revaluation, disposal | `C — NOT SEARCHED` | Yes | Read five named methods |
| `UE-10` | Localisation overlays | `C — NOT SEARCHED` | **Yes, and now cheaply** | The installed-module manifest in `UE-04` names ten localisation modules |
| `UE-11` | Client-side behaviour | `C — NOT SEARCHED` | Yes | — |
| `UE-12` | A model-declaration scan across every module of the reference root, to settle whether a schedule object exists | `B` | **Yes** | One executed scan |
| `UE-13` | Peer scope determinations for six of P10's seven scope objects | `C` — **currently impossible** | **No** | The peers must publish them |
| `UE-14` | Whether the lock-exception route exists on the older estate line | `C` | Yes | Schema check — the object is absent there, so the answer is probably no |
| `UE-15` | Whether any later peer delta refines the tolerance-zero close condition again | `B` | **Yes** | Re-read at each round |

## Summary

**15 items · 10 obtainable by P10 · 2 requiring operator action · 1 currently impossible · 2 partly.**

**Ten obtainable items were not obtained in this round.** They are listed rather than characterised, because the prior round's habit was to describe outstanding work in a way that implied nobody had to do it.

## The Three That Would Move the Most

1. **`UE-01`** — it is the only candidate evidence anywhere in the estate that the defect has ever fired.
2. **`UE-06`** — it bounds P10's own newest finding, which is currently a floor found by reading rather than a population found by sweeping.
3. **`UE-07`** — the only route to closing two tolerance-zero components either way, and it needs an operator, not a researcher.
