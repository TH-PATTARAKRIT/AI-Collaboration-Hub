# P10 — CORE ACCOUNTING RECONCILIATION HANDOFF PACK — VERSION 2

Session: `SMEPLUS-26-09-04-ACC-P10-TBR-CROSS-PROCESS-RECON-001` · Layer 1
**Supersedes `18` for routing and limitations. `18` is preserved as lineage.**

> **Terminal state: `P10 CROSS-PROCESS RECONCILIATION — MAXIMUM AVAILABLE EVIDENCE REACHED — HOLD FOR SPECIFIC PEER / BOSS DECISION`**

**Read §4 before relying on anything in this package.**

---

## 1. Corrected Routing

The parent package routed the ledger, period close, the fiscal calendar and FX policy to `P04`. **`P04` is the asset process.** The correct counterparties:

| Process | Owns | P10's relationship |
|---------|------|--------------------|
| `P04` Acquire-to-Retire | The asset lifecycle, including depreciation — a competing recognition engine | Four independent agreements; two genuine convergences; no contradiction |
| `P08` Record-to-Report | The ledger, journal posting, period close, currency, reporting | **The binding counterparty.** Five obligations returned; P10's `EC-04` exposures resolve here |
| `P09` Plan-to-Analyze | Management accounting, analytic, budget | One new defect returned; one causal chain adopted from P09 proved wrong |
| `P11` | Cross-process reconciliation and scope | Receives the event-identity forensic, the option classification, and correction `P10-R-09` |

## 2. What P10 Hands Over

| # | Asset | Use |
|---|-------|-----|
| `H-1` | The six-primitive recognition model | The vocabulary peers reconcile against |
| `H-2` | The corrected scope matrix, with **expiry triggers** on three determinations | `P11`'s cross-process scope reconciliation |
| `H-3` | Seven ledger requirements and five obligations on `P08` | The ledger contract P10 needs |
| `H-4` | Eleven open dependencies, three newly opened, three re-routed | The reconciliation agenda |
| `H-5` | Seven Boss decisions, one with an attached acceptance condition | What unblocks design |
| `H-6` | Thirteen dispositioned contradictions, thirty-two controlled negatives, twenty-eight dispositioned unknowns | What must not be re-derived and what must not be asserted |
| `H-7` | The corrected anchor grades for four verified mechanisms | Input to the accounting-event identity decision |

## 3. The Four Things Reconciliation Must Not Get Wrong

1. **A recognition event is not a journal entry.** Every defect follows from that collapse.
2. **The allocation convention is a company binding value**, resolved from the company that owns the financial effect — not from whoever executes.
3. **The service window is a tenant fact; the period grid is a company fact.**
4. **The attribution shape is a mechanism property, not a posting-layer property.** P10 got this wrong in both directions before settling it, and it is the strongest evidence for a shared allocation layer.

## 4. Limitations — Read Before Relying

1. **Twenty claims in this package were withdrawn or materially narrowed by the fresh challenge round, and none was caught by the author.** Cumulative correction count across both rounds: **28, zero self-caught.** Cite `34` and `14`, never the headline tables.
2. **`P10-F-41` is withdrawn as contradicted.** It appeared in the parent round's summary tables.
3. **`P10-F-38` holds on the validation path only**, is **not** a mirror-image relationship (the two records carry different general accounts), and **is** backed by an executed vendor test — P10 under-claimed its own finding.
4. **The exclusion of the status-quo option is conditional on a Boss ruling**, not a consequence. P10 treated an open peer blocker as an adopted rule. The two decisions must be taken together.
5. **The estate is almost entirely un-exposed today.** Three of four deployed databases carry **no lock date on any company**, so the silent re-date cannot fire there. The fourth carries four locks. The parent package's live-exposure argument is withdrawn.
6. **Zero *deferral* entries have ever been generated** in the deployed estate — but **769 asset schedules exist.** The broad "zero recognition entries" phrasing was wrong.
7. **The runtime premise was never tested.** Tooling and initialised data directories are present on the host. "No runtime access" is class `C — NOT ATTEMPTED`.
8. **The deployed-archive population was never enumerated.** At least three further artefacts exist beyond the one directory the script globs.
9. **Three surfaces remain unsearched**: asset lifecycle paths, localisation overlays, client-side behaviour — and a fourth is now known to be cheaply boundable from an installed-module manifest on the host.
10. **A ruling scoped to the lock path does not dispose of the problem.** A peer records a second, **lock-free** re-dating path.
11. **Peer scope matrices were not compared line by line.** Agreement is class `C — NOT COMPARED`, routed to `P11`.
12. **Research gate: `RECOMMEND HOLD`.** `EC-02`, `EC-04`, `EC-07` unsatisfied. `EC-04` is not satisfied **with obtainable work outstanding** — the earlier "cannot close" wording is struck.

## 5. Standing Veto

`AASP-VETO-01` **revision 3** — no implementation of any P10 recognition mechanism until Boss decisions `D-5` and `P10-D-02` are taken, the latter jointly with the peer boundary. Grounds now cover generation, **correction and reversal**, and reopen. The clause claiming the veto is immune to evidence is withdrawn.

**This is the only veto binding P10's implementation.** The parent round believed a peer's veto covered it; that peer's veto explicitly does not bind other processes.

Attached: **`AASP-COND-01`**, P10's acceptance condition on the accounting-event object.

## 6. Terminal Declaration

P10 has reconciled against four peer packages, produced one new defect class the reconciliation itself generated, corrected its own process taxonomy, reopened a deployed archive it had wrongly excluded, and survived four fresh adversarial challenges that withdrew or narrowed twenty of its claims.

It has **not** closed a criterion, and it does not claim to. Genuine unresolved dependencies remain: eleven peer dependencies, ten gating unknowns, and two Boss decisions on which everything else is sequenced.

> **`P10 CROSS-PROCESS RECONCILIATION — MAXIMUM AVAILABLE EVIDENCE REACHED — HOLD FOR SPECIFIC PEER / BOSS DECISION`**
