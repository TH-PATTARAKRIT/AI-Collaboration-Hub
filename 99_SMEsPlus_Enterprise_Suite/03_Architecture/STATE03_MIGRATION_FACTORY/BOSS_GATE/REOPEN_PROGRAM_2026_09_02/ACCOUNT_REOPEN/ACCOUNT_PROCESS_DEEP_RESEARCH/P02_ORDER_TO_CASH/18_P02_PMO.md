# 18 — P02 PMO CONTROL VIEW AND GATE RECOMMENDATION

`LAYER 2 — AUDIT QUARANTINE` · Session `SMEPLUS-26-09-04-ACC-P02-O2C-REV2-001`

**AI/PMO may issue only a recommendation. Boss alone decides Final Exit.**

## 1. Recommendation

> **RECOMMEND HOLD.**
> P02 is **READY FOR CORE ACCOUNTING RECONCILIATION** — which is this session's mandated terminal state
> and is a **handoff**, not an exit. It is **not** ready for a module exit gate, and the eight-criteria
> exit constitution is **not** satisfied.

Two of the eight criteria are **structurally unsatisfiable from this session's position**, for reasons
that no amount of further reading would fix, and they are stated first because they bound everything else:

- **`EC-07` cannot be satisfied.** One independent challenge has run. It produced **twenty
  package-changing findings and six accepted coverage gaps**, including **two outright refutations of
  statements this package had tagged `FACT VERIFIED`**. That is not a clean pass, and two *consecutive*
  clean passes are required.
- **Runtime evidence does not exist for this session.** One very-expert requirement is undischarged for
  that reason, and `C-04` cannot be closed without it. **The database half of that limitation was an
  untested negative claim and has since been discharged** — see `21` and `RE-13`, which is itself a
  finding about this package's method and is reflected in EC-06 and EC-08 below.

## 2. The Finding That Should Govern The Boss Decision

Stripped of everything else:

> **In the reference process the physical event is immutable, the accounting event is reversible, and the
> settlement history is freely destructible — including across a closed period. The durability ordering is
> exactly inverted relative to accounting importance.**

And, from the deployed database, the finding a Boss decision should weigh alongside it:

> **In a live Thai deployment the invoice-side cost mechanism has never run. Cost is recognised at delivery
> and revenue at invoice, with no position connecting them — so any shipment billed in a later period puts
> cost and revenue in different periods permanently, and nothing in the system can detect it.**

A completed delivery cannot be cancelled or reversed. A posted invoice can be reset to draft, which
**destroys its cost lines** and lets them re-derive at a different value. Matching rows — the record of
*what settled what* — can be created and destroyed with **no lock-date check at all**.

Every tolerance-zero candidate in §3 is an instance of that inversion.

## 3. Eight-Criteria Exit Assessment

Assessed against `SMEPLUS-DR-EXIT-8C-001`. Status vocabulary: `SATISFIED` / `NOT SATISFIED` /
`PARTIALLY SATISFIED`. **No criterion is declared satisfied that is not.**

| EC | Criterion | Status | Basis |
|---|---|---|---|
| **EC-01** | Scope bounded | **PARTIALLY SATISFIED** | Nineteen enumerations are closed against literals in source and reproduce independently. But the edge-case set is **author-assembled and declared as such**, eight business situations have **no case at all**, and six process areas are excluded by declaration. The bounded part is genuinely bounded; the unbounded part is declared, not hidden. |
| **EC-02** | Enumeration converged | **NOT SATISFIED** | Convergence requires material-delta stability. The single independent pass produced twenty package-changing findings and **two refutations of verified facts**. That is the opposite of stability. |
| **EC-03** | Unknown exhausted | **PARTIALLY SATISFIED** | Every unknown carries a disposition and none is left unclassified. But `C-04` is a **gating** unknown routed to evidence that this session could not obtain, and the eight new gaps are freshly opened, not closed. |
| **EC-04** | Tolerance-zero closed | **NOT SATISFIED** | **Six** tolerance-zero candidates are open. See §4. |
| **EC-05** | Contradiction resolution complete | **PARTIALLY SATISFIED** | Sixteen contradictions carry a disposition and lineage. Five preserved disagreements (`17` §3) are **deliberately** unresolved and routed. None is left as an unadjudicated difference of opinion. |
| **EC-06** | Negative claim controlled | **NOT SATISFIED** | The discipline failed **three** times. Twice on findings — `RE-06` (a detector that does exist) and `CH-01` (an account wired to nothing). And once, most seriously, on **the package's own statement of what evidence it had** (`RE-13`): six deliverables declared no database access without a search, and five deployed archives were on the host. **A negative-claim control that does not govern the evidence base is not a control.** |
| **EC-07** | Two consecutive clean independent passes | **NOT SATISFIED** | One pass, not clean. And it examined roughly **half** the evidence base. |
| **EC-08** | Final knowledge package complete | **PARTIALLY SATISFIED** | Twenty-two deliverables, 101 evidence identifiers, four track extracts, one deployed-database extract, full lineage. **Missing: Jira lineage** (none raised — lineage is by branch and commit only) and **runtime evidence**. Database evidence, previously listed as missing, is **now present** and was missing only because nobody looked. |

**0 of 8 satisfied. 4 partially. 4 not.** P02 may not be presented for a module exit decision.

EC-06 moved from partial to **not satisfied** after `RE-13`, and that movement is the most important single
line in this assessment: **the package's negative-claim discipline governed its findings and did not govern
its own evidence base.**

## 4. Tolerance-Zero Register — Six Open

Under EC-04 a conditional position may not bypass a tolerance-zero risk. Each of these is an
**unauthorised or invisible movement of financial value**.

| # | Candidate | Why tolerance-zero | Status |
|---|---|---|---|
| **TZ-01** | **A completed outflow with no picked line produces no financial record at all** — reachable by part-picking a multi-line delivery, and **concealed on the field a reviewer would query**. | Goods leave; the ledger is silent; the obvious detection query returns nothing. | OPEN — `03` §2 |
| **TZ-02** | **Posting into a locked period is silently redirected, and the redirect is capped at today so it is not guaranteed to reach an open period.** | Period integrity is not enforced; it is negotiated by the system on the user's behalf, without consent or record. | OPEN — `11` §6 |
| **TZ-03** | **Matching and unmatching are not period-controlled at all.** | The settlement history of a closed period is mutable while its entries are not. | OPEN — `09` §8 |
| **TZ-04** | **A context sentinel disables the lock check entirely, including the irreversible lock** — two use sites, both in a contacts-role operation. | A control disabled by construction rather than by permission is not a control. | OPEN — `11` §6 |
| **TZ-05** | **A missing exchange rate silently substitutes an unrelated rate, or one-to-one** — no log, no warning, no error. | Financial value is converted at a rate with no relationship to the transaction. | OPEN — `09` §4 |
| **TZ-06** | **A customer deposit is recognised as revenue whenever an account property is unset — and no chart in the reference tree sets it.** | Revenue recognised on money that has been received but not earned. | OPEN — `09` §5 |

**None is closed. None may be bypassed by a conditional position.**

Two candidates carried in earlier drafts have been **withdrawn** after verification: the
owner-restricted phantom-cost path (refuted, `03` §5a) and the Thai inbound-interim asymmetry (refuted,
`07` §5).

## 5. Control View — What P02 Actually Depends On

| Control the process needs | Present in the reference? |
|---|---|
| Physical outflow cannot be reversed | **yes** — the strongest control found |
| Inventory cannot be relieved twice | **procedurally**, not by constraint |
| Revenue cannot be recognised twice for one order | **defaulted**, not blocked; and nothing outside the order path is covered |
| A duplicated customer invoice is detected | **detected and then ignored** — no code path consults the result on the sales side |
| Cost of sales cannot be generated twice | **no guard exists** |
| Delivered-not-billed is visible and aged | **no** |
| Billed-not-delivered is visible | **no** |
| The period close holds | **no — it redirects** |
| Settlement history is durable | **no** |
| A missing rate stops the transaction | **no** |
| A customer advance is a liability | **only if configured** |
| The cost of a reversal equals the cost of the original | **only in one of four branches** |

**Twelve controls the process depends on. One is sound. Two are partial. Nine are absent or inoperative.**

## 6. Dependencies And Holds — Nothing Adjudicated Here

| Class | Count | Status |
|---|---|---|
| `DEPENDENCY OPEN` | 4 | Inventory COGS, Inventory invariants, Core Accounting Wave A, the FX ruling |
| `PEER DEPENDENCY OPEN` | 3 | P01 vendor-side mirror, P11 scope reconciliation, and two purchase-side items routed to P01 |
| `HOLD — CROSS-PROCESS RECONCILIATION REQUIRED` | 4 | cost-recognition design, chart roles, FX fallback, the lock bypass |
| `HOLD — SCOPE EVIDENCE REQUIRED` | 3 | rate scope, chart scope, intercompany execution scope |
| `HOLD — STATUTORY EVIDENCE REQUIRED` | 8 | all Thai, each with named sources |
| `BOSS CONTROLLED DECISION` | 2 | cost-recognition timing; revenue on billing versus performance |

**P02 adjudicates none of them.** Where this package names an owner for a fact, that is a **proposal to
the cross-process reconciliation**, not a claim that the named process has accepted it. P01 through P11
were all executing in parallel while this session ran, and P02 had access to none of their conclusions.

## 7. What Would Have To Happen Before P02 Could Be Presented For Exit

In order. Each is a precondition for the next.

1. **Obtain runtime access, and finish the database work already begun.** Four deployed archives remain
   unexamined, and the five named runtime tests remain unrun. `C-04` cannot close from the database
   already read, for a reason that is itself a finding: **the mechanism under suspicion has never executed
   there** (`21` §6).
2. **Run a second independent challenge covering the half of the evidence base the first did not reach** —
   the return/credit/refund and settlement/FX/deposit tracks, which is `08`, `09`, accounting events
   AE-05…AE-13 and edge cases 18–30.
3. **Close or explicitly route the eight open business situations** — drop-shipping and credit control
   first, since both add events to the register rather than cases to a matrix.
4. **Obtain the six Boss and scope decisions**, since several design candidates are unresolvable without
   them.
5. **Obtain the eight Thai statutory answers**, which are the Accounting-Tax track's, not P02's.
6. **Then, and only then, re-run the eight-criteria assessment.**

Steps 1 and 2 are not optional and are not substitutable by further reading.

## 8. What This Package Is Good For Now

Stated plainly, because a HOLD recommendation should not be read as "this work cannot be used".

- It is **sound as evidence.** The independent challenge found **nothing fabricated and nothing misread**
  in the detailed citations, and the deployed database subsequently **confirmed the package's central
  mechanism claim by direct count**. What failed was aggregation — counting, generalising, stating findings
  more broadly than their boundaries licensed — and, once, failing to test a claim about the evidence
  itself. Every such failure is recorded with its correction.
- It is **usable as a requirements input.** The thirty-four requirements in the clean-room handoff
  (`19`) stand independently of the gate assessment, and several — the obligation ledger, the two-date
  rule, close-means-closed, a missing rate is a hard stop — are architecture-shaping and do not depend on
  any open hold.
- It is **directly usable by Core Accounting Reconciliation**, which is what it was produced for.

## 9. PMO Statement

P02 Order-to-Cash reaches its mandated terminal state:

> **READY FOR CORE ACCOUNTING RECONCILIATION**

and no further. No gate is declared satisfied. No module is declared ready. No implementation is
authorised. No merge to the canonical branch is proposed. Boss is the sole final approver.

---

# PART II — PMO TARGETED EXIT REVIEW

Session `SMEPLUS-26-09-04-ACC-P02-O2C-TARGETED-FORENSIC-CLOSURE-001`.

## 10. Recommendation

> **RECOMMEND HOLD — and the terminal state is the second of the two the directive permits:**
>
> **`P02 TARGETED FORENSIC CLOSURE — MAXIMUM AVAILABLE EVIDENCE REACHED — HOLD FOR SPECIFIC EXTERNAL /
> PEER / BOSS DECISION`**
>
> **not** `READY FOR CORE ACCOUNTING RECONCILIATION — P02 TARGETED FORENSIC CLOSURE COMPLETED`.

**Why the weaker of the two terminal states.** The directive permits the completed state, and the closure
did complete every mandated task. But it also uncovered a fact that makes "completed" the wrong word:

> **The generation carrying 99.9% of all deployed transactions has no source tree on this host.**

The package cannot say how the only database with volume produces its accounting. That is not a task left
undone — it is evidence that does not exist here. **Maximum available evidence has been reached; the
subject has not been closed.**

## 11. Eight Criteria — Targeted Exit Assessment

| EC | Round 1 | After closure | Movement |
|---|---|---|---|
| **EC-01** Scope bounded | PARTIAL | **NOT SATISFIED** | **Downgraded.** The denominator was refuted **twice in one round** — the archive count (8 files / 5 databases, not 6) and the generation label (16.0, not 18). A scope corrected twice under challenge in a single round is not bounded. |
| **EC-02** Enumeration converged | NOT | **NOT SATISFIED** | Refutation rate **rose**: 2 → 7. |
| **EC-03** Unknown exhausted | PARTIAL | **PARTIAL** | C-04 now has a precise verdict. **Two new gating unknowns opened**: the 16.0 mechanism, and whether the declared source roots are the code these databases run. |
| **EC-04** Tolerance-zero closed | NOT | **NOT SATISFIED** | 3 of 6 moved from theoretical to **demonstrated**; **2 new candidates raised** (TZ-07, TZ-08). Moving away from satisfaction. |
| **EC-05** Contradiction resolution | PARTIAL | **PARTIAL** | 28 contradictions registered, all dispositioned. |
| **EC-06** Negative claim controlled | NOT | **NOT SATISFIED** | **Three evidence-base failures now**: no search at all (`RE-13`), tooling not exhausted, and a search pattern that never looked inside archives (`RE-16`). |
| **EC-07** Two consecutive clean passes | NOT | **NOT SATISFIED** | Two challenges, **neither clean**, and each found a class the other did not look for. |
| **EC-08** Package complete | PARTIAL | **PARTIAL** | Deployed evidence substantially extended. **Runtime absent. 16.0 source absent. Jira absent.** |

**0 satisfied · 3 partial · 5 not satisfied.** EC-01 moved **down**. **P02 is further from a module exit
than when the closure began**, and that is the correct result of an honest closure.

## 12. The Three Facts A Boss Decision Should Rest On

**1. No deployed database has ever recognised cost of sales through this process.** 447,994 journal lines,
5 distinct databases, 91 company records — **zero cost-of-sales lines**. Verified across three
independently-derived denominators and re-verified against two archives found only under challenge. **This
survived every correction.**

**2. The cost-recognition mechanism is not stable across product generations.** Three generations, three
answers. v19 deletes the interim account, deletes the valuation-layer model, moves inventory relief to the
invoice, and **relabels "Perpetual" to mean "at invoicing"** — which makes the Boss target policy `BP-02`
(*perpetual storable, COGS at delivery*) **not selectable** on the current generation. This is the
strongest possible argument for `BP-01`: **invoice policy and cost-recognition policy must be SMEsPlus
invariants, because the reference cannot hold them still.**

**3. Cost and revenue land in different periods for roughly a third of order lines, and nothing can
detect it.** 31.6% of 3,865 matched order lines, measured on the accounting date. The eight accounts
involved are **not reconcilable**, so no matching is possible even in principle — and those same accounts
carry the manufacturing flow, so the balances cannot be read as cost of sales at all.

## 13. What P02 Now Asks For, In Order

1. **An Odoo 16.0 core source tree**, or an authoritative statement that the 16.0 deployment is out of
   scope. Without one, no source claim can attach to 99.9% of the deployed transactions.
2. **Confirmation of which code these databases actually run.** The challenge states plainly that it has
   **not** established the declared reference roots are that code. P02 has examined **no custom module**;
   two peer processes found their most severe findings in custom modules.
3. **Runtime access** — still the only thing that can close C-04.
4. **The six Boss and scope decisions**, unchanged.
5. **The eight Thai statutory answers**, unchanged, plus the Buddhist-Era date defect (`25` F-01).

## 14. PMO Statement

P02 reaches:

> **`P02 TARGETED FORENSIC CLOSURE — MAXIMUM AVAILABLE EVIDENCE REACHED — HOLD FOR SPECIFIC EXTERNAL /
> PEER / BOSS DECISION`**

No gate is declared satisfied. No module is declared ready. No implementation is authorised. No
architecture is frozen. No merge is proposed. Boss is the sole final approver, and P11 owns the
cross-process reconciliation this package feeds.

---

# PART III — PMO ADDENDUM AFTER PEER CORRECTION

A peer process (P04) reported a deployed database outside P02's declared path set. Testing it, and running
the signature-based sweep it prompted, changed four things.

## 15. What Changed

| # | Change | Direction |
|---|---|---|
| 1 | **A v18 deployment exists** — `idemo18_uat`, 361 modules, 4 companies, 40,353 journal lines, and its source **is** readable here. TC-35 refuted. | **Better.** The package's v18 analysis is re-attached to a real deployment. |
| 2 | **One company has split recognition ON** — the only one in the estate. TC-03 refuted a second time. | Neutral as a count; **decisive as evidence** — see 3. |
| 3 | **That company still has zero COGS lines**, with accounts configured and 47,801 valuation layers, because valuation mode is unset on all 126 categories. | **`P02-F-05` observed in production with the gate open.** The single best piece of evidence in the package. |
| 4 | **A complete, safe, resettable Odoo 18 runtime lab exists on this host, with the exact required test already scripted.** | **Worse for the package's credibility; better for the work.** |

## 16. The Fifth Evidence-Base Failure

`RE-13` (no search), the tooling limit, `RE-16` (pattern missed archives), `RE-20` (path set missed a
folder) — and now **`C-31`: runtime evidence was declared absent without looking for it.**

**Five failures. All the same family. All about the evidence base, none about the analysis.** Three were
found by outside parties. The package's negative-claim discipline governed its findings well and **never
once governed its own statements about what it had**.

**`EC-06` was already NOT SATISFIED. This is now the clearest-cut of the eight**, and no further argument
should be made for it until a review is commissioned whose *only* brief is the evidence base.

## 17. `C-04` — Reclassified

**From `EVIDENCE REQUIRED` to `AUTHORISATION REQUIRED`.**

The blocker was never the absence of runtime. A purpose-built sandbox exists on the right generation, with
`anglo_gross_profit_test.py` already written to execute the outcome-1 scenario end to end — including the
snapshot **between delivery and invoice** — and print the interim balance and the COGS figure.

**P02 did not run it**, and that is deliberate: it commits transactions, this session's constitution
forbids database changes, and the directive asked for read-only verification. **The sandbox being safe is
a reason to ask, not a reason to proceed.**

## 18. Revised Recommendation

**RECOMMEND HOLD — unchanged. Terminal state unchanged.** But the *reason* has narrowed usefully:

> P02 is no longer held open by evidence that does not exist. It is held open by **one authorisation
> decision**, **one unbounded re-run of a discovery pass**, and the standing Boss and statutory decisions.

**The single highest-value next action is now specific, cheap, and safe:** authorise one execution of the
existing test in the existing sandbox and retain the output. It would settle `C-04`, the interim-account
behaviour, the gross-profit computation and the delivery-to-invoice sequence **in one run**.

## 19. Exit Criteria — Net Effect

**0 satisfied · 3 partial · 5 not satisfied — unchanged.** EC-01 remains **NOT SATISFIED**: the population
was reopened for a fourth time, and the sweep written to fix it **contained a bounded-window defect of the
same family** (`26` §9.2). **A denominator corrected four times, by three different parties, is not
bounded.**

**What did improve is the quality of the remaining unknowns.** They are now named, small, and mostly
actionable — which is the honest product of a closure that kept finding it was wrong.

---

# PART IV — PMO FINAL ADDENDUM

A second peer exchange produced two verified traps, one method rule, and one check that **bounds the whole
package**.

## 20. The Bounding Finding

**`27` SC-01: 66 of 361 modules installed in the only deployed v18 database are absent from P02's declared
source root** — including `inherit_sales` and `inherit_inventory`, which by name override the two modules
this process is about, and which **exist nowhere on this host**.

**This does not invalidate the mechanism analysis.** Reading how the standard product works is what
clean-room benchmark learning requires, and it was read correctly. **It does mean P02 must stop making
statements of the form "the deployment behaves as X"** — 18% of its code was never read and 2 modules
cannot be.

**And it has already cost a published claim:** `27` §4 — the Thai withholding-certificate `VERIFIED
ABSENCE` is **withdrawn**. Four such modules are installed and all four are on this host, outside the
declared root.

## 21. Two Controls, Belatedly, On Results P02 Liked

- **The zero-COGS instrument is now positively controlled** (`27` §7): inject one synthetic cost line into
  a real 40,353-row extract and the counter goes 0 → 1. **The headline had run six databases without this.**
- **The symlink trap was verified, not assumed** — real on this host, and P02's sweep is unaffected.

## 22. Sixth Evidence-Base Failure

`26` §9's "16 artefacts / 7 databases" was **a partial read of a still-running job**. The sweep finished at
26 and climbing, including **iCloud Drive** — a location never considered.

**P02 no longer publishes a population count.** **SUPERSEDED BY `28` — the path set is now closed and a
count is publishable with its command.** The position remains the invariant, on a wider base: **17 distinct
deployed databases, FOUR generations (14.0, 16.0, 18.0, 19.0), 2,553,914 journal lines, zero cost-of-sales
journal lines in every one, each zero injection-controlled** — and the
population is **open**.

## 23. Net Effect On The Exit Assessment

**0 satisfied · 3 partial · 5 not satisfied — unchanged in count.** Two criteria are worse in substance:

- **EC-01** — the population has now been corrected **five** times and the source scope is shown not to be
  the deployed code. Not bounded, on either axis.
- **EC-06** — **six** evidence-base failures: no search; tooling; archive pattern; archive path; runtime
  path; source path. Every one about the evidence base, none about the analysis. **Three of the six were
  found by outside parties, and the sixth by re-reading P02's own output.**

**What improved:** the headline is now positively controlled and has survived five successive extensions of
the population, including the two databases most likely to break it — the only v18 deployment, and the
only company with split recognition switched on.

## 24. Recommendation — Unchanged

**RECOMMEND HOLD.** Terminal state unchanged:
**`MAXIMUM AVAILABLE EVIDENCE REACHED — HOLD FOR SPECIFIC EXTERNAL / PEER / BOSS DECISION`.**

The three actions that would move it, in order and all specific:

1. **Authorise one run** of the existing `anglo_gross_profit_test.py` in the existing sandbox — settles
   `C-04`, the interim-account behaviour, gross profit and the delivery-to-invoice sequence together.
2. **Identify the code the deployments actually run**, module by module, and re-scope every source-derived
   negative against it. Two modules needed for this are not on the host and must be obtained.
3. **Re-run the discovery sweep to completion, unbounded**, and publish the command and its output — not a
   count.
