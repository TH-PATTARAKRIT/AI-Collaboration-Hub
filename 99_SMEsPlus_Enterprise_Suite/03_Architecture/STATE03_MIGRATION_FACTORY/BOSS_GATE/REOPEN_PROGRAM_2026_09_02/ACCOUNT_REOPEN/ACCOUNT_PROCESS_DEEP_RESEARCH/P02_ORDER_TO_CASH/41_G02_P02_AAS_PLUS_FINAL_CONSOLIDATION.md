# 41 — G02-P02 AAS+ FINAL CONSOLIDATION

`LAYER 2 — AUDIT QUARANTINE.` Prompt §10. Baseline `ff8be51`.

**No consensus is invented. No disagreement is averaged away.** Where the experts diverge, both
positions are carried with their evidence.

---

## 1. Agreements Across All Four Experts

| # | Agreed position | Held by |
|---|---|---|
| A-1 | **The zero-COGS direction is sound.** No expert challenged the sign; the correction is to the denominator and the scope. | 1, 2, 3, 4 |
| A-2 | **The package conflates source facts with deployed facts in its headline files**, even though it states the bound correctly in `31` §4 and `27` §5. `01`, `07`, `09`, `12` still read generation-neutral and deployment-neutral. | 1, 3, 4 |
| A-3 | **Published arithmetic is reliable.** Two experts recomputed independently; everything reproduced to the digit. | 1, 2 |
| A-4 | **The package's negatives are under-instrumented**, not wrong: single search form, single generation, single route. | 1, 3, 4 |
| A-5 | **A correction recorded in a reviewing file does not reach the owning register.** Found in `06`, and the same shape flagged in `09` and `01`. | 3, 4 |

## 2. Dissents — Carried, Not Resolved

| # | Dissent | Position A | Position B | AAS+ disposition |
|---|---|---|---|---|
| D-1 | **Is `C-04` worth authorising at all?** | Expert 4: `33` §5.9 tests a **determined** outcome — it buys confirmation, not discrimination | P02: the run also yields the **first direct observation of the split path executing anywhere**, which no deployment supplies | **Both stand.** The request was **rewritten** to reproduce `03` §6's mechanism instead. Expert 4's objection to the *old* scope is upheld; the *revised* scope is not yet challenged. |
| D-2 | **What does `P02-F-28d`'s zero establish?** | Expert 2: it is **entailed** by NULL valuation → `manual_periodic`; corroboration, not independence | P02: the spread across five databases still shows the instrument discriminating | **Expert 2 upheld on the framing.** "Measured, not inferred" withdrawn. The figure stands as corroboration. |
| D-3 | **Is the population closed?** | Expert 2: **no** — Docker volumes are structurally invisible to a file sweep; format width untested | P02: the *file path set* is closed and reproduced under a third instrument | **Both true of different sets.** Status split: **file path set CLOSED; population NOT CLOSED.** |
| D-4 | **Does `CA-01` prove a deployment built the control at the right layer?** | Expert 4: only the **line-level `@api.constrains`**; the document-level check hangs on `action_post` and every internal poster bypasses it | P02: the constrains half is genuine data-layer enforcement | **Expert 4 upheld in part.** `P02-F-32a` re-qualified: correct at line level, bypassable at document level. |

## 3. Contradictions Confirmed And Their Standing

**Twelve raised, twelve confirmed against primary evidence: `C-34` … `C-47`.** Two change the headline
(`C-43`, `C-44`). Four are against work published in this same round (`C-37` … `C-40`), plus `C-34`.

**None was withdrawn on re-derivation.** One expert claim was **narrowed** by P02: Expert 1's `CH-5`
(segment by invoice policy) is correct in principle but **not executable as stated**, because
`invoice_policy` is not a column on the sale line. Recorded as an open action, not a finding.

## 4. Evidence Gaps — Consolidated

| Gap | Raised by | Status |
|---|---|---|
| 189 P02-relevant installed modules unreadable | P02 (`31`) | `SOURCE GAP` — bounds everything source-derived |
| Format width: `-Fp` / `-Ft` / gzip dumps untested | 2 | **UNFINISHED — no zero claimed** |
| Docker-volume databases: 9 live, 9 stopped containers | 2 | **Undeclared population boundary** — must be decided in writing |
| Early-payment discount: a 14th accounting event, absent | 1 | Unmeasured; must be added or a bounded absence recorded |
| POS and payment-triggered invoice routes | 1, 4 | Unanalysed, inside the declared path set |
| Service / performance-obligation scenario | 1 | No scenario, no edge case, no spine stage |
| Full v16 reference root exists and was not used | 2 | Inflates the non-standard count on two deployments |
| Six of eight scenario negatives single-instrument | 1, 3 | `EVIDENCE REQUIRED` |

## 5. Boss Decisions — Unchanged By This Round

`BP-01`, `BP-02`, `BP-03` stand exactly as written. **No expert was permitted to re-decide them and none
did.** Expert 3 correctly declined to decide Thai law. `P02-F-38a` records that the target generation
cannot be configured to `BP-02`, as a **build constraint**, not a policy challenge.

## 6. Peer Dependencies And Statutory Holds

Unchanged from `39`. Expert 3 adds one constraint P02 must not treat as its own to close:
**`DC-09-04` would remove the only sale-side WHT posting path in this estate** — routed to P06/P07 as a
constraint on that design candidate.

## 7. Mutation / Authorisation Boundary

**Preserved absolutely.** Every expert operated read-only; Expert 2 and Expert 4 both queried live
containers with `SELECT` only. **No module was installed, no database restored, no transaction created,
no container state altered.** `C-04` remains the single bounded authorisation request, now **narrowed
and re-targeted** (`C-42`).

## 8. AAS+ Position

**The package's findings survive; its denominators, scopes and framings did not.**

Of twelve confirmed contradictions, **none reversed a finding's direction**. Every one attacked how far a
correct observation had been generalised — the denominator it was counted over (`C-43`), the key it was
grouped by (`C-44`), the route it was tested on (`C-40`), the generation it was asserted of (`C-35`), the
branch it was read from (`C-37`), or the counter it was measured with (`C-34`).

**That is the same defect this package has recorded since `RE-07`: aggregation, not citation.** It has
now been recorded thirteen times, in a round that opened by writing the rule down. **A written rule is
not a control; the independent challenge is.**

**AAS+ does not certify P02. It records that P02 is now honestly bounded** — and that the bounding was
done largely by parties other than its author.
