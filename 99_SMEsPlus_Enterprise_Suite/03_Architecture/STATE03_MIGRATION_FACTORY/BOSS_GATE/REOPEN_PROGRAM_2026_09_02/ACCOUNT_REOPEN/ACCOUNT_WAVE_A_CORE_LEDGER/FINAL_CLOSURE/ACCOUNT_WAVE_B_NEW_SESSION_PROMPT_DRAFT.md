# `DRAFT / NOT AUTHORIZED TO EXECUTE`

> # THIS PROMPT MUST NOT BE EXECUTED.
> It becomes executable **only** when Boss has passed the **Wave A Final Research Gate** and has
> explicitly authorised Wave B. Until then it is a **draft artefact** of session
> `SMEPLUS-26-09-04-ACCOUNT-WAVE-A-FC-001`.
>
> **Preconditions `D1`–`D5`, `D7` (`ACCOUNT_WAVE_B_READINESS_PACKAGE.md` §4) are OPEN.**

---

# [SMEPLUS-26-09-XX-ACCOUNT-WAVE-B-001]
# ACCOUNT WAVE B — AR / REVENUE Full-Spectrum Deep Research / VERY DEEP / L99999.99999

Model: `Claude Opus 5 (Extra)`

Parent: `[SMEPLUS-26-09-04-ACCOUNT-WAVE-A-FC-001]` ACCOUNT WAVE A — Final Closure & Wave B Readiness
Programme: `[SMEPLUS-26-09-04-ACCOUNT-FULL-DEEP-001]`

Boss = Sole Final Approver. No Evidence = No Progress. Never Skip Gate.
No silent assumption. No unsupported negative claim.
Do not modify product/source implementation code. Do not merge to canonical SMEsPlus. Do not deploy.

## 0. MANDATORY PRECONDITION CHECK — execute first, before any research

Verify and report each. **If any is open, produce a `WAVE B PRECONDITION HOLD` report and stop.**

| # | Precondition | Required state |
|---|---|---|
| `D1` | **`MCU-21`** — the reference **root set** is declared and the SMEsPlus target root is named | **CLOSED** |
| `D2` | **`GB-08`** — Boss has selected a rate-resolution / build-freeze option | **DECIDED** |
| `D3` | **`T0-12`** — the balance-assertion suppression surface (48 tokens) is opened and assessed | **DISPOSITIONED** |
| `D4` | **`GB-01`** — tenant/company measurement crossing | **DECIDED** |
| `D5` | **`T0-08`** — entry identity | **DISPOSITIONED** |
| `D7` | **`T0-05`** — over-reconciliation | **DISPOSITIONED** |

**Research-only mode is permitted if Boss explicitly authorises it with `D1`–`D5`/`D7` open. In that
mode NO design output may be produced and NO class `A` absence may be claimed.**

## 1. Scope

`AR / REVENUE`. In scope, out of scope, and the prohibition on absorbing Wave A blockers:
**`ACCOUNT_WAVE_B_READINESS_PACKAGE.md` §1. It governs.**

## 2. Denominator discipline — the hard rule carried out of Wave A

Before any enumeration, declare **all four** and prove each:

`POPULATION` + `PATTERN` + **`PATH SET`** + `UNIT` — **none author-chosen.**

- The `PATH SET` must be **proved by enumeration over the declared root set**, not by habit.
- **Wave A's terminal defect was that the ROOT SET itself was never enumerated: 1 root of 22 was
  searched and no artefact said so.** Do not repeat it one level up.
- **No class `A — VERIFIED ABSENCE` may be claimed until `D1` is closed.**

## 3. Evidence classes

`VERIFIED FACT` · `VERIFIED BUSINESS SEMANTIC` · `CONTROL REQUIREMENT` · `INFERENCE` · `DESIGN CHOICE`
· `UNKNOWN / OPEN DECISION`. Every statement carries one.

Negative-claim classes `A`–`E` per `DR-NC-01`. **`NO EVIDENCE FOUND` ≠ `DOES NOT EXIST`.**
**An empty taxonomy class means UNSEARCHED, never ABSENT** (`ER-CORE-4` — 2-of-2 record).

## 4. Mandatory reading rule

**Design and research input comes from a source's ADVERSARIAL and CORRECTION sections, never its
summary.** Named must-reads: `CORR1/C04` (`NC-01`…`NC-24`), `MCC_G §8` (`G-C1`…`G-C8`), `MCC_J`,
`MCC_00` §2 (**and `FC-F1`: `MCC_00` §1 contradicts its own §2**).

## 5. L1–L12

L1 domain map · L2 function coverage · L3 forensic · L4 cross-module · L5 semantic model · L6
failure/edge · L7 control matrix · L8 identity/immutability · L9 SaaS boundary · L10 migration · L11
reconciliation proof · L12 independent review. Chain to trace: readiness §6. Coverage matrix: §7.

## 6. Very Deep / L99999.99999 extension

Denominator-first enumeration over the declared root set · cross-version divergence review · tolerance-
zero boundary discovery · balanced-but-wrong search of **all 9** seed classes (readiness §12) ·
adversarial challenge by non-authors · fixed-point convergence.

## 7. The six known failure shapes — re-test every one on the AR surface

Readiness §8: bare `search` with no company term · root-level guard admitting a tree · elevated read
outside record rules · caller-supplied company id with no access check · model with no company field
and no record rule · report/menu artefact with no company dimension.

## 8. Independent review

≥2 reviewers, **neither an author**. Reviewers verify **claims against source**, not prose.
**Hash the package before review** (`ER-CORE-8`); every verdict names the digest it reviewed.

## 9. Method convergence

Run `MC-01`…`MC-10`. Fixed point = **two consecutive independent passes with no material delta**.
If not converged, name the **exact** remaining enumeration defect. **Do not open a broad new round.**

## 10. Correction propagation — by mechanism, not intention

Maintain a single canonical figures register. **Check it against itself before publishing** — Wave A
shipped one whose §1 contradicted its §2 and no consumer noticed (`FC-F1`, `GB-06`'s 4th instance).

## 11. Publication sequence — in this order, no step skipped

1. verify local branch, commit, manifest, digest
2. **push to `origin`**
3. **re-read from `origin`**
4. verify the direct link resolves
5. verify commit SHA and digest match local
6. **only then** post one Jira evidence comment

**Never post a Jira link to an unpushed branch.** If push is blocked:
`GITHUB EVIDENCE PUBLICATION NOT VERIFIED`, and fabricate nothing.

## 12. Outputs

L1–L12 registers · contradiction register · unknown/gap register · tolerance-zero register ·
balanced-but-wrong register · negative-claim compliance scan · method convergence report · evidence
manifest SHA-256 · Final Research Gate report · Wave C readiness.

## 13. Gate report

Wave B status · convergence status · evidence status · GitHub status · Jira status · remaining
unknowns · gating unknowns · tolerance-zero status · Boss decisions required.
`% Board` / `% STATE` / `% STEP` **only if a verified denominator exists**; otherwise
`NOT CALCULABLE FROM VERIFIED BASELINE`.

Recommendation, exactly one: `RECOMMEND PASS` · `RECOMMEND CONDITIONAL PASS` · `RECOMMEND HOLD` ·
`RECOMMEND FAIL`. **`CONDITIONAL PASS` may not bypass a tolerance-zero risk.**

## 14. Absolute stop rule

Do not start Wave C. Do not claim Boss approval. Do not freeze architecture without a Boss decision.
Do not declare Wave B closed.

---

> # `DRAFT / NOT AUTHORIZED TO EXECUTE`
