# P08_INDEPENDENT_VERIFICATION_TERMINAL_REPORT

Prompt `[SMEPLUS-26-09-06-P08-R2R-INDEPENDENT-EXTERNAL-CORRECTION-VERIFICATION-003]` · prompt commit `f9099c0`
Frozen surface **`00ccd663d55d72830c8e0db46e4cc1aa345d0af1`** · verification branch `audit/p08-independent-verifier-2026-09-06-001`
**The source branch was not modified.**

---

## TERMINAL STATE

# `B` — `P08 INDEPENDENT VERIFICATION FOUND MATERIAL DEFECT — TARGETED REPAIR REQUIRED`

**Not `A`.** The correction surface is not closure-ready: a `VERIFIED ABSENCE` is **false inside the scope it retreated to** and is on two outbound artefacts; a figure with **no referent in the data** is in the P11 handoff; and ten of fourteen handoff rows are superseded, under-qualified, mis-referenced or malformed.

**Not `C`.** Evidence was not unavailable. **It was abundant, it was faithful, and the verification succeeded.** The extracts proved byte-identical to the original dumps; the one unreadable database was explained by a tool-version ceiling and is cheaply liftable. Nothing here is blocked for want of evidence.

## What was verified, and what it cost the package

**The evidence base is sound.** Twenty-one of twenty-one extracted tables are byte-identical to the original dumps — no truncation, no filtering. Across four independent challenges and the verifier's own passes, **the great majority of the package's figures reproduced exactly**, several to five decimal places.

**The reasoning over that evidence is what failed, and it failed in one shape.** Every material defect is an undeclared unit, an undeclared path set, a wrong column, a search token standing in for an existence claim, or a correction recorded and never applied to the text.

## Material defects — 17

| ID | Defect | Class |
|---|---|---|
| `IVR-F-09` | **The period repair is false inside the root set it retreated to** — refuted twice in that set, once from core accounting, and by 89 of 89 deployed companies | 6 |
| `IVR-F-10` | **A previously-applied correction was regressed.** The correct qualified wording was already in the package, installed by an earlier independent review; the round re-published the sentence that review had removed, then narrowed its scope instead of restoring the qualifier | 6 |
| `IVR-F-08` | **A live, wrong number published twice, once as `FACT VERIFIED`** — and the package records that a challenger had it right and that the author preferred its own instrument | 7 |
| `IVR-F-16` | **A figure with no referent shipped to P11** — 0 unbalanced at every tolerance in exact arithmetic; the "3" is the author's float instrument | 7 |
| `IVR-F-15` | **The capability denominator reads the wrong column, and its predicate is falsified by the estate's own data** — a posted entry's company is in no charted set under either instrument | 4 |
| `IVR-F-17` | **A token count published as an existence status** — the newer name searched against the older tree, contradicting the package's own earlier round and its own quarantine | 2 |
| `IVR-F-01` | **The correction register does not enumerate the corrections** — 31 of 74 ids absent, behind a pointer to a table that does not contain them | 7 |
| `IVR-F-02` | **Four incompatible totals for one population**, and **the veto condition inherits the ambiguity** — written as 20 in one artefact and 25 in another; enumerated 32 | 9 |
| `IVR-F-03` … `-06` | **Four standing tables still publish superseded results**, including a `DOMAIN PURITY PRESERVED` disposition its own file says cannot be drawn | 7 |
| `IVR-F-11` | **A malformed outbound row to P11** — 7 cells against an 8-cell header | 8 |
| `IVR-F-12` | **The PMO column-count certification is false for two of four tables** | 8 |
| `IVR-F-13` | **The handoff identifier namespace is collided** across two live artefacts — *verifier's own finding, raised by no challenger* | 8 |
| `IVR-F-14` | **A correction asserting a scope of application it did not perform** — *"wherever it appears"*, applied nowhere, naming a file that never carried the defect | 7 |
| `IVR-F-07` | **"Not one arithmetic error was found" is contradicted** by the package's own record and by `IVR-F-08` | 7 |

## Veto status — nothing discharged

| Veto | Conditions | Satisfied |
|---|---|---|
| `AAS+-VETO-01` (inherited) | 2 | **0** |
| `AAS+-PS-VETO-01` | 6 | **0** |
| 4 vetoes from the audited round | 25 | **0 evidenced** |
| **4 new vetoes from this verification** | **41** | — |

**`AAS+-PS-VETO-01` C-6 is not discharged by this track**, on two independent grounds:

1. **`IVR-IND-01`** — procedural independence was met; **structural independence was not.** The verifying agent is the same model that authored the repairs. **Whether that satisfies C-6 is a Boss decision.**
2. **`IVR-F-02`** — the condition's own population is indeterminate. A completeness verification cannot be certified against a denominator written two ways and enumerating to a third.

**Three conditions cannot be discharged by P08 either** — the purity re-run, the 19.0 root naming, and the correction-count resolution are audits of the party that made the errors.

## Bounded repair requirements — routed, not performed

Per mandate §7, this track **did not repair the source package**. Requirements, in priority order:

| # | Requirement | Owner |
|---|---|---|
| 1 | **Withdraw the re-scoped period absence** and restore the earlier round's qualified wording, correcting **by identifier** across all seven carriers including the two outbound artefacts | P08 |
| 2 | **Delete the "3 at 1e-7"** from the P11 handoff and re-run every balance measurement in exact arithmetic | P08 |
| 3 | **Correct the all-zero descriptor** — 36 in both frames, 38 in one | P08 |
| 4 | **Re-derive the capability denominator from the company-account relation**, and run `history ⊆ capability` as a standing control on every eligibility predicate | P08 |
| 5 | **Re-issue the retention row** with each control searched by **its own name in each line** | P08 |
| 6 | **Audit every correction against the text, not its disposition line** — five stale families survive, one under a claim of *"wherever it appears"* | P08 |
| 7 | **Re-issue all 14 outbound rows** with a version marker and a reachability qualifier; repair the malformed row; retire or re-number the collided identifier family | P08 |
| 8 | **Resolve the correction count** and re-express C-6's scope over the resolved set | **not P08** |
| 9 | **Re-run the purity audit** over the true artefact population with a pattern that reproduces its own published membership | **not P08** |
| 10 | **Obtain a newer restore tool and read the fourth database** — a specific, cheap, liftable blocker | P08 |

## What this verification affirms

Stated because a verification that reports only defects is as misleading as one that reports none.

- **The evidence base is faithful.** 21 of 21 tables byte-identical to source.
- **The arithmetic is overwhelmingly sound**, across four independent re-derivations, several from the original dumps rather than the intermediate extracts.
- **The clean-room separation holds** — zero vendor tokens across the Layer-1 artefacts, with controls that behave.
- **The identifier families are contiguous** with no gaps or orphans.
- **The package's self-reported failures are real and were not softened.** Where the round found itself wrong, it said so in stronger terms than a defensive author would.
- **A challenger withdrew a finding on its own control**, and the verifier recorded five challenger errors rather than adopting them.

## Standing

| | |
|---|---|
| **Terminal state** | **`B` — MATERIAL DEFECT — TARGETED REPAIR REQUIRED** |
| Material defects | **17** |
| Vetoes standing | **10** — 2 inherited, 4 from the audited round, 4 new |
| Lifting conditions outstanding | **74** |
| Boss decisions answered | **0 of 19** |
| Source-package repairs performed here | **0** |
| Source branch modified | **NO** |
