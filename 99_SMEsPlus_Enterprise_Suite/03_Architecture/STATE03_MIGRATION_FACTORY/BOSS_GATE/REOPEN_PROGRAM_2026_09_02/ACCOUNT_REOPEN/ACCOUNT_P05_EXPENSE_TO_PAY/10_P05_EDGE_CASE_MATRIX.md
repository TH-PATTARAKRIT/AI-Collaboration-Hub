# 10 — P05 EDGE CASE / FAILURE MATRIX

`LAYER 2 — AUDIT QUARANTINE`
Every row the P05 directive names, plus failure classes found during the trace.
`Behaviour` is what the source does; `Class` follows the finding taxonomy.

## 1. Directive-Named Edge Cases

| ID | Edge case | Behaviour in the reference | Evidence | Class |
|---|---|---|---|---|
| `EC-01` | **Partial reimbursement** | Supported only as a partial *payment* against the bill. The claim's `payment_state` collapses a mixed set of states to `partial` on the company branch, but on the employee branch it reads only `account_move_ids[:1]`. There is no notion of partially *approving* a claim — approval is whole-sheet. | `hr_expense_sheet.py:221-234, 711-714` | FACT VERIFIED |
| `EC-02` | **Multi-currency** | The claim currency collapses to company currency whenever lines differ or the mode is `own_account` (`_compute_currency_id`). The per-line rate is either the platform rate at `expense.date`, **or**, when nothing changed, `total_amount / total_amount_currency` — a rate implied by two user-entered numbers. | `hr_expense_sheet.py:322-328`; `hr_expense.py:250-281` | FACT VERIFIED |
| `EC-03` | **Missing receipt** | **No control.** No required-attachment constraint was found. Declared boundary: `ENT18/hr_expense` all files; patterns `grep -rnE "attachment_ids"` and `grep -rniE "required.*receipt\|no.*receipt\|missing.*receipt"`. The only receipt logic is duplicate detection and main-attachment selection. Class **B**. | `21 NC-12` | NOT FOUND IN SEARCHED SCOPE |
| `EC-04` | **Tax non-deductible** | `account_disallowed_expenses` exists in `ENT18`. Its mechanism and any link to `hr_expense` are assigned to Expert 4; unresolved at time of writing. | `20 U-04` | UNRESOLVED |
| `EC-05` | **Tax recoverable** | Purchase taxes on an expense line are always forced **price-included**, in both the tax-detail path and `_compute_totals`. A price-excluded VAT configuration is silently reinterpreted. | `hr_expense/models/account_move.py:78-83`; `account_move_line.py:24-27`; `hr_expense.py:198` | FACT VERIFIED |
| `EC-06` | **WHT** | Applied at payment registration as a write-off line. Not applied to advances (`tax_ids: []`). See `07`. | `advance_expense_request.py:255` | FACT VERIFIED |
| `EC-07` | **Advance liquidation** | Two paths, both crediting `line_ids[0].account_id`, neither carrying currency, neither creating a payment object on the cash-return path. | `05 §4`; `GL-05`, `GL-06`, `SR-04` | FACT VERIFIED |
| `EC-08` | **Over / under advance** | *Under*-advance (employee owed more) has no mechanism — the shortfall is simply a separate expense claim. *Over*-advance (employee owes money back) is handled by the cash-return wizard, which **credits the expense account**, reducing recognised expense rather than creating a receivable. | `advance_request_reconcile.py:62-92` | FACT VERIFIED |
| `EC-09` | **Project / department allocation** | See `06`. Debit line only; absent from the whole advance and float chain. | `06 §2` | FACT VERIFIED |
| `EC-10` | **Reversal** | `_do_reverse_moves` reverses non-draft moves with `cancel=True` and **unlinks draft moves outright**. On the employee branch `_reverse_moves` first nulls `expense_sheet_id` and `ref` on the original, explicitly to avoid a duplicate-vendor-bill error. | `hr_expense_sheet.py:787-796`; `hr_expense/models/account_move.py:85-90` | FACT VERIFIED |
| `EC-11` | **Cancel** | Three different cancels with three different semantics: claim refusal (`_do_refuse`, deletes draft entries), move cancellation (`button_cancel`, severs the link), and the advance path's direct `write({'state':'cancel'})`. | `hr_expense_sheet.py:727-744`; `hr_expense/models/account_move.py:97-103`; `advance_expense_request.py:214-215` | FACT VERIFIED |
| `EC-12` | **Backdate** | Employee branch: the accounting date is *computed away* from a back-dated cost — a cost older than the current month books to end-of-month or to the first period after the lock, not to the cost date. Company branch: the entry takes the raw expense date and will simply fail at posting if that period is locked. | `hr_expense_sheet.py:798-822`; `hr_expense.py:959` | FACT VERIFIED |
| `EC-13` | **Locked period** | The *default date derivation* is lock-aware on the employee branch only; *enforcement* is symmetric and happens at post on both (see `03 §3.2` correction). The advance's direct `state='cancel'` write **does** reach `_check_fiscal_lock_dates` (`ENT18/account/models/account_move.py:3239-3241`), so the fiscal lock is **not** bypassed. **CORRECTED by AAS-03 Expert 3:** the brief's note about the `unmodifiable_fields` guard being skipped at `:3244-3250` is true but **inert** — `'state'` is not a member of `unmodifiable_fields` (`:3245-3247`) and the vals contain nothing else, so no protection is actually lost on that call. That limb is **withdrawn**. What *is* bypassed is enumerated at `EC-13a`. | `11 C-03` | FACT VERIFIED |
| `EC-13a` | **What the raw `state='cancel'` write actually bypasses** (Expert 3, exhaustive against `button_cancel`/`button_draft`) | **Bypassed:** the inalterable-hash lock (`_check_draftable`, `account_move.py:5351-5352`) — because `'state'` is not in `_get_integrity_hash_fields()` = `['name','date','journal_id','company_id']` (`:3836`), so **a hashed, inalterable journal entry can be forced to `cancel`**; reconciliation unwinding (`:5283` `remove_move_reconcile()`) — partial reconciliations survive against a cancelled move; analytic-line removal (`:5282`); exchange-difference and tax-cash-basis blocks (`:5342-5350`); e-invoicing cancellation request (`:5276-5277`); `auto_post` reset (`:5374`); attachment detach (`:5286`). **Not bypassed:** fiscal/tax lock dates; sequence-gap integrity (`_set_next_made_sequence_gap` keys on `sequence_prefix`/`sequence_number`/`journal_id`/`name`, `:3258` — none present); payment state (`button_cancel:5373` writes `payment_ids`, a One2many on the payment's *own* move, empty on a vendor bill). | `ENT18/account/models/account_move.py` as cited | FACT VERIFIED |

## 2. Failure Classes Found During the Trace

| ID | Failure | Behaviour | Evidence | Class |
|---|---|---|---|---|
| `EF-01` | Expense with no resolvable account, company-paid branch | `_get_base_account` returns an empty recordset and `account_id: False` is written into the move line vals. The 4th documented fallback cannot fire on this branch. | `hr_expense.py:970-1002, 1006-1010` | FACT VERIFIED |
| `EF-02` | Company-paid expense with no `vendor_id` | A `partner_type='supplier'` payment with no partner. | `hr_expense.py:180, 950-951` | FACT VERIFIED |
| `EF-03` | Employee with no work contact | `UserError` at approval — a hard, clear stop. | `hr_expense_sheet.py:896-897` | FACT VERIFIED (correct behaviour) |
| `EF-04` | Employee with no work e-mail | `RedirectWarning` at approval. A *notification* prerequisite blocks an *accounting* event. | `hr_expense_sheet.py:701-705` | FACT VERIFIED |
| `EF-05` | No outstanding account configured, company-paid | `RedirectWarning` to settings, or bare `UserError` if the user cannot write settings. | `hr_expense_sheet.py:884-894` | FACT VERIFIED (correct behaviour) |
| `EF-06` | Expense edited after its entry is posted | Permitted for amount, currency, date, product and quantity; nothing propagates. Claim total and ledger total diverge silently. | `hr_expense.py:613-643` | FACT VERIFIED |
| `EF-07` | Petty cash overdrawn by concurrent claims | The balance check reads a **non-stored** compute over posted lines, with no reservation and no lock. Entries are created **draft** at approval, so N claims approved before any posts all measure the same balance. | `hr_expense_petty_cash/models/hr_expense_sheet.py:55-83`; `petty_cash.py:38-49` | SUPPORTED INTERPRETATION |
| `EF-08` | Petty cash sheet spanning two holders | `_compute_petty_cash` raises `ValidationError` **from inside a compute method**, so the failure surfaces on read — including in list views — not only on write. The same compute also side-effects a different field (`payment_method_line_id`). | `hr_expense_petty_cash/models/hr_expense_sheet.py:35-52` | FACT VERIFIED |
| `EF-09` | Advance with no lines, cleared | `line_ids[0]` raises `IndexError` — an unhandled traceback, not a business error. | `advance_request_reconcile.py:73`; `scgl_advance_expense_request/models/account_move.py:90` | FACT VERIFIED |
| `EF-10` | Advance created in batch (import, duplicate) | Fact confirmed (`advance_expense_request.py:183-191`), **severity CONTRADICTED by AAS-03 Expert 3.** `ENT18/odoo/api.py:419-429` intercepts a method named `create` under `@api.model` and wraps it with `model_create_single`, whose implementation (`:466-472`) accepts a list and loops. Batch creation therefore **works correctly**. The real consequences are a `DeprecationWarning` at registry build (`:481-484`) and loss of batching. **Re-classified: maintenance / performance, NOT data integrity.** | `advance_expense_request.py:183-191`; `ENT18/odoo/api.py:419-429, 466-472, 481-484` | FACT VERIFIED, severity corrected |
| `EF-11` | Accountant reconciling another employee's advance | The candidate set is filtered on `self.env.user`, not on the bill's employee, so the operator's **own** advances are offered against a third party's bill. | `advance_request_reconcile.py:12-14`; `scgl_advance_expense_request/models/account_move.py:16, 24` | FACT VERIFIED |
| `EF-12` | Petty-cash module uninstalled | `payment_mode` `selection_add` carries `ondelete={'petty_cash': 'set default'}`; every petty-cash expense silently becomes `own_account` — an employee reimbursement. | `hr_expense_petty_cash/models/hr_expense.py:11-14` | FACT VERIFIED |
| `EF-13` | Batch of mixed own-account and company-account sheets, petty-cash module installed | The forked `_do_create_moves` **reassigns** `moves_sudo` instead of accumulating it, discarding the employee-bill recordset from the return value. Upstream uses `|=`. | `hr_expense_petty_cash/models/hr_expense_sheet.py:103-119` vs `hr_expense_sheet.py:760-782` | FACT VERIFIED |
| `EF-14` | Multi-record WHT recompute | `_compute_wht_amount` assigns to `self.wht_amount` inside a `for rec in self` loop; the last record's value is written to every record in the set. | `l10n_th_withholding_tax/models/account_move.py:50-58` | FACT VERIFIED |
| `EF-15` | Payment register re-edited after WHT computed | `amount_wt_computed` latches; changing the payment date or amount does not recompute the withholding. | `l10n_th_withholding_tax/wizard/account_payment_register.py:28, 39-41, 70` | FACT VERIFIED |
| `EF-16` | Expense-linked payment's journal or reference edited | The immutability guard's member set contains `'journal_idref'` instead of `'journal_id'` and `'ref'`. Neither field is protected. | `hr_expense/models/account_payment.py:20-28`; AST evaluation in `14 §3` | FACT VERIFIED |
| `EF-17` | Sheet flipped to `done` by the payment wizard | `_reconcile_payments` writes the **stored computed** `state` field directly; the next recompute overwrites it. | `hr_expense/wizard/account_payment_register.py:39-40` | FACT VERIFIED |
| `EF-18` | Advance request approved by a user with several employee records | The approver is resolved through a One2many and the first record wins, with no assertion that it belongs to the company owning the financial effect. See `22 §3 R-04`. | `advance_expense_request.py:63` | FACT VERIFIED |

## 3. Tolerance-Zero Register (`EC-04` of the Exit Constitution)

| ID | Boundary | Status |
|---|---|---|
| `TZ-01` | Financial integrity — petty-cash disbursement may be booked as employee payable, leaving the float permanently overstated | **OPEN**, pending Expert 2 |
| `TZ-02` | Company isolation — `petty.cash` is COMPANY-scoped by its own semantics but carries no company, and its uniqueness constraint is global | **OPEN**, re-derived at `22 §3 R-02` |
| `TZ-03` | Immutable posted facts — expense amount/currency/date remain writable after posting with no propagation (`EF-06`) | **OPEN** |
| `TZ-04` | Immutable posted facts — declared payment immutability guard does not protect `journal_id` or `ref` (`EF-16`) | **OPEN** |
| `TZ-05` | Immutable posted facts — posted entries are force-cancelled by a raw `state` write from a non-accounting document (`EC-13`) | **OPEN**, scope pending Expert 3 |
| `TZ-06` | Unauthorised / duplicate posting — three cross-document duplicate classes undetected (`OW-01`) | **OPEN** |
| `TZ-07` | Financial integrity — advance clearing credits a single arbitrary account, carries no currency, and can collapse to a self-cancelling entry on the same bank account while reporting the advance cleared (`GL-05`, `GL-06`) | **OPEN** |
| `TZ-08` | Immutable posted facts — **a hashed, inalterable journal entry can be forced to `cancel`**, because `'state'` is not a member of `_get_integrity_hash_fields()`; reconciliation partials survive against the cancelled move (`EC-13a`) | **OPEN** — raised by Expert 3 |
| `TZ-09` | Unauthorised posting — approval is enforced in the action, not on the field; an employee can write `approval_state='approve'` on their own sheet (`E1-01`) | **OPEN** — raised by Expert 1 |
| `TZ-10` | Financial integrity — an expense report can reach state `done` / payment "Paid" with **no journal entry and no payment at all**, and remain deletable (`E1-02`) | **OPEN** — raised by Expert 1 |
| `TZ-11` | Duplicate payment — a self-documented double-payment path in payroll reimbursement (`E1-03`), and a vendor down payment that is never deducted from the final bill (`E3-12`) | **OPEN** — raised by Experts 1 and 3 |
| `TZ-12` | Unauthorised posting — any internal user can create a vendor bill through a `sudo()` wizard (`E3-13`) | **OPEN** — raised by Expert 3 |
| `TZ-13` | Financial integrity — the only universally-available advance clearing route books a bank receipt that never occurred (`E3-05`) | **OPEN** — raised by Expert 3 |

**No tolerance-zero boundary is closed.** Per `EC-04`, a conditional advance may not bypass any of them.
Seven of the thirteen were raised by independent review, not by the primary research.

## 4. Reviewer-Originated Failure Classes

Found by independent AAS-03 review, **not** by the primary research. Listed separately because the
ratio of reviewer-found to author-found material findings is itself a governance metric (`18 §3`).

### 4.1 Expert 1 — Core Expense Engine

| ID | Severity | Failure | Evidence |
|---|---|---|---|
| `E1-01` | **HIGH** | **Self-approval is reachable by a direct field write.** `approval_state` (`hr_expense_sheet.py:79-86`) is a plain `Selection` — no `readonly`, no `compute`, no `groups`, no `@api.constrains`. Every approval control lives in `action_approve_expense_sheets` / `_do_approve`, not on the field. ACL `security/ir.model.access.csv:3` gives `base.group_user` write and `ir_rule.xml:49-54` permits write on own **draft** sheets, so an ordinary employee can `write({'approval_state':'approve'})` on their own sheet: it then reports **Approved** with no approver check and **no journal entry created**. The existing test asserts only that the *action* raises (`tests/test_expenses_access_rights.py:72-74`). | Enumeration: whole `ENT18/addons`, pattern `approval_state` over `*.py`/`*.xml`, i18n excluded — 13 hits, none a guard. Class **A** in that scope. |
| `E1-02` | **HIGH** | **An expense report can reach "Paid" with zero accounting entries.** `hr_expense_extract/models/hr_expense.py:23` declares `sample = fields.Boolean(...)` — no `groups`, no `readonly`, no constraint, absent from every view. When set, `_do_approve` (`:200-214`) skips both `_check_can_create_move()` and `_do_create_moves()`, and `action_sheet_move_post` (`:216-223`) calls `set_to_posted()` and `set_to_paid()`. Result: sheet `done`, payment "Paid", **no `account.move`, no `account.payment`** — and `_unlink_except_posted_or_paid` is bypassed (`:186-188`), so the "paid" sheet is deletable. `_is_expense_sample` raises only on a *mixed* sheet; a wholly-sample sheet passes silently. | Conditional on `hr_expense_extract` being installed (`U-01`). |
| `E1-03` | **HIGH** | **Payroll reimbursement contains a self-documented double-payment path.** `hr_payroll_expense/models/account_move.py:62-66` excludes already-paid expense sheets from the reconciliation map while leaving them on the payslip's expense input lines. The source comment states the employee "will get paid twice" and assigns responsibility to the user. No guard, no warning. | Conditional on `hr_payroll_expense` (`U-01`). |
| `E1-04` | MED-HIGH | Posting a payslip posts arbitrary expense bills under `sudo()` (`hr_payroll_expense/models/account_move.py:89-91`), gated only by `account.group_account_invoice`. A synthesised `matching_number` is written at `:85` before `:94-95` can return empty — the fabricated number persists while nothing reconciles. | as cited |
| `E1-05` | MED-HIGH | **Splitting an expense in state `approve` desynchronises the sheet from an existing draft entry.** `wizard/hr_expense_split_wizard.py:42-61` has no state guard; `hr_expense_sheet.py:395` treats `approve` as editable, and entries already exist at `approve` (`EX-02`). The split rewrites the original's `total_amount_currency` down (`:47`) and `copy()`s the remainder into expenses carrying **no sheet** (`sheet_id` is `copy=False`). The draft entry keeps the full original amount. If a split line changes `employee_id`, `hr_expense.py:645-654` silently sets `sheet_id = False` on a multi-line sheet — removing an expense whose line is already in the entry. | as cited |
| `E1-06` | MEDIUM | `split_possible` is computed (`hr_expense_split_wizard.py:36-40`) but never checked by `action_split_expense` (`:42`). Enforcement is exclusively view-level `invisible`/`disabled`. An RPC caller can split into amounts that do not sum to the original. | as cited |
| `E1-07` | MEDIUM | Split rounds a **company-currency** amount with the **foreign** currency's rounding: `hr_expense_split.py:95` uses `self.expense_id.currency_id.round(...)` on `total_amount`, which is declared `currency_field='company_currency_id'` (`hr_expense.py:137-142`). Wrong precision whenever the two currencies differ in `decimal_places`. | as cited |
| `E1-08` | MED-HIGH | **Mail-in ingestion identifies the employee by an unanchored substring match, in two divergent implementations.** The alias guard does exist — `alias_contact='employees'` (`hr_expense/data/mail_alias_data.xml:8`) handled by `hr/models/models.py:11-21` — but it matches `('work_email','ilike',email_address)`, and Odoo `ilike` wraps the value in `%…%`. A sender at a look-alike shorter domain passes the gate, and `message_new` (`hr_expense.py:1064-1106`) then creates an expense **in that employee's name** with attacker-chosen description, amount and currency. Worse, the gate and the attribution use **different predicates**: `hr/models/models.py:15-17` runs two `limit=1` searches with no `user_id` filter, while `hr_expense.py:1110-1127` runs one OR-search filtered on `user_id != False` with **no limit** — so the employee that passes the gate need not be the employee charged, and the unlimited search reaches `employee.id` at `:1089`, raising `ValueError` on a multi-record set. Neither search is company-scoped. | as cited |
| `E1-09` | MEDIUM | Mail-in can stamp an expense with a currency belonging to a **different company**: `company` comes from `employee.user_id.company_id` but `currencies` is the union over all of the user's `company_ids` (`hr_expense.py:1073-1078`), and `currency_id` (`:149-155`) has no `check_company`. | as cited |
| `E1-10` | MEDIUM | `_parse_price` discards the sign and misreads thousands separators: `hr_expense.py:1148` puts `[+-]?` **outside** the capture group while `:1161` reads the inner group. A negative amount is ingested as positive; `"1,205.91"` → **1.205**; `"1.205,91"` → **1.205**. Silent three-orders-of-magnitude understatement. | as cited |
| `E1-11` | MEDIUM | The expense mail gateway is **enabled by default at install** — `data/hr_expense_data.xml:80` sets `hr_expense.use_mailgateway` to `True` and `data/mail_alias_data.xml:5-9` creates a live alias, while the settings checkbox reads as opt-in. | as cited |
| `E1-12` | MEDIUM | **`hr_expense` widens Team Approver visibility over the accounting projection.** `security/ir_rule.xml:78-83, 84-89` grant `group_hr_expense_team_approver` a rule of `[('line_ids.expense_id','!=',False)]` on `account.move`/`account.move.line` with **no employee predicate**, while the same group's `hr.expense.sheet` rule (`:38-48`) is narrowly scoped to their own reports. Correctly bounded: the ACL grants read only, and the core company rule still applies — so the net effect is that a team approver can **read every other employee's expense entry amounts, accounts, partners and analytic distribution in the company**, which the operational object hides from them. `ir.model.access.csv:15` also grants that group `1,1,1,1` on `account.analytic.line`. | as cited |
| `E1-13` | MEDIUM | Sheet journal and payment method are **view-only protected**: `hr_expense_views.xml:843, 848-849` apply `readonly="not is_editable"`, but model-side `employee_journal_id` is a plain Many2one and `payment_method_line_id` is `store=True, readonly=False`, and `write()` guards neither. `journal_id` is a stored compute over both, so the sheet's journal can change after posting while the entry keeps the original. **Composes with `TZ-04`:** journal identity is unprotected at the sheet layer *and* the payment layer. | `hr_expense_sheet.py:180-187, 192-198, 212-217, 273-279, 491-509` |
| `E1-14` | LOW-MED | `_check_journal_move_type` is fully disabled for expense moves (`hr_expense/models/account_move.py:48-49`) — the enabling condition behind `EF-01`. | as cited |
| `E1-15` | LOW-MED | `sale_expense/models/hr_expense.py:10-14`: `sale_order_id`'s domain carries an adjacent comment stating the domain "is not the one applied"; no `check_company`, and the module contains **zero** constraints (pattern `check_company\|@api\.constrains\|def _check_` over `*.py` → 0 hits). An expense can be flagged for reinvoicing against a draft or cancelled order. | as cited |
| `E1-16` | LOW-MED | Unauthenticated OCR webhook: `hr_expense_extract/controllers/main.py:8-17` is `auth='public'`, `csrf=False`, performing a `sudo()` search on a caller-supplied UUID. Impact bounded to triggering an outbound poll and acting as a UUID oracle. | as cited |
| `E1-17` | LOW | `_compute_same_receipt_expense_ids` has a `KeyError`/`set.remove` path for attachments with a NULL checksum (`hr_expense.py:477, 485-486`). | as cited |

### 4.2 Expert 3 — Advance & Liquidation

| ID | Severity | Failure | Evidence |
|---|---|---|---|
| `E3-01` | **HIGH** | **Demo data ships as production data.** `scgl_advance_expense_request/__manifest__.py:19` lists `demo/advance_request_demo.xml` under the **`data`** key; the `demo` key is commented out at `:21`. See `05 §3` — the advance-to-expense misclassification is the shipped default. | `demo/advance_request_demo.xml:3-10, 21` |
| `E3-02` | **HIGH** | **There is no live integration between the advance module and `hr_expense`.** Declared search: whole module, pattern `grep -rn --include='*.py' --include='*.xml' --include='*.csv' -E "hr\.expense\|hr_expense"` → only the manifest dependency, two **commented-out** imports in `models/__init__.py:3-4`, and one commented method. `models/__pycache__/` retains `hr_expense.cpython-310.pyc` and `hr_expense_sheet.cpython-310.pyc` with **no corresponding `.py`** — the module once patched `hr.expense` and was stripped without re-basing. Class **A** within the module. **This makes `DUP-03` structural, not incidental:** the advance system and the claim system cannot see each other at all. | as cited |
| `E3-03` | **HIGH** | **`button_post_bill` has no server-side state assertion** (`advance_expense_request.py:239-289`) — gated solely by view `invisible` and a view-level `groups=`, both display-only. By RPC it posts a vendor bill from any state. It also lacks `ensure_one()` while reassigning `res_ids` each iteration (`:271`), so the multi-record return action can only describe the last record. | as cited |
| `E3-04` | MED-HIGH | **Approved amount and billed amount are decoupled.** `_compute_requested_amount` sums `requested_amount` ignoring quantity (`:151-154`), while the bill line is `price_unit = requested_amount`, `quantity = product_qty` (`:250-251`). `product_qty` has no default and is `column_invisible="1"` in the only editable list, so any non-UI creation with `product_qty = N` bills **N ×** the approved amount. | as cited |
| `E3-05` | **HIGH** | **The only universally-available clearing route fabricates a bank movement.** `apply_payment` debits `journal_id.default_account_id` — a bank/cash account — for `amount_toclear`, booking a cash receipt the employee never made. | `wizard/advance_request_reconcile.py:62-92` |
| `E3-06` | **HIGH** | **A posted-but-unpaid advance can never be cleared.** `amount_toclear` derives from the amount **paid** (`compute_amount:125-129` + `get_amount_toclear:140-149`), while the expense debit was the amount **posted**. An advance bill that is posted and unpaid leaves the full amount in expense with `amount_toclear == 0`, so `is_clear` never turns True and the CLEAR ADVANCE button (`views/…:18`, requires `bill_state_dump == 'paid'`) never appears. There is no clearing path for that residue. | as cited |
| `E3-07` | MEDIUM | The reconcile wizard's `advance_ids` (`:23-27`) is a plain Many2many with **no server-side domain**; the user-scoping (`P05-F-09`) is a client-side domain only (`advance_request_reconcile_view.xml:10`). `apply()` (`:35-49`) validates nothing — not ownership, not company, not `bill_state`, not `move_id.state` — and the wizard's ACL grants CRUD to **all users** (blank `group_id`, `security/ir.model.access.csv:5`). | as cited |
| `E3-08` | MEDIUM | Zero `@api.constrains`, zero `_sql_constraints`, zero `check_company` in the whole module. `journal_id` is `required` in the view only; `company_id` is never cross-checked against `journal_id.company_id`, the account's companies, or the bill's company. | pattern as in `21` |
| `E3-09` | MEDIUM | The clearing journal is selected by `search([company, type='general'], limit=1)` with **no `order`** — an arbitrary general journal. | `models/account_move.py:62-68` |
| `E3-10` | MEDIUM | `apply()` filters the line to reconcile by **partner**, not account (`advance_request_reconcile.py:43`). Where the bill partner equals the requester's partner — exactly what `button_post_bill:261` produces — that set includes the advance credit lines as well as the intended debit line. | as cited |
| `E3-11` | LOW-MED | Permanently-false guards: `'clear'` is referenced in `_can_be_deleted` (`:203`) and in two view expressions but is not a member of `_STATES` (`:6-12`); `bill_state_dump in (…,'in_payment')` is likewise dead against `bill_state`'s selection (`:312`). | as cited |

### 4.3 Expert 3 — `scgl_purchase_advance_payment` (out of the primary research's declared scope)

| ID | Severity | Failure | Evidence |
|---|---|---|---|
| `E3-12` | **HIGH** | **Vendor down payments are never deducted.** Declared search: whole custom tree, pattern `deduct_down_payments` → 4 hits: the field (`wizard/purchase_advance.py:51`), three view references, and **one commented-out consumer** (`:178`). The live `'delivered'` branch calls `purchase_order_ids.action_create_invoice()` (`:179`), which knows nothing of the flag. **The vendor is billed the full order value in addition to the down-payment bill.** Class **A** within the custom tree. | as cited |
| `E3-13` | **HIGH** | **Any internal user can create a vendor bill.** `security/ir.model.access.csv:2` grants full CRUD on `purchase.advance.payment.bill` to `base.group_user`, and `_create_bill` creates the `account.move` with `.sudo()` (`wizard/purchase_advance.py:203`), bypassing `account.group_account_invoice`. | as cited |
| `E3-14` | MED-HIGH | Percentage down payment computed on the wrong base with the wrong tax set: `_get_down_payment_amount:116` tests `all(self.product_id.taxes_id.mapped('price_include'))` — `taxes_id` is the **customer** tax field (the module itself uses `supplier_taxes_id` at `:26` and `:94`), and `all([])` is `True`. Whenever the deposit product carries no customer taxes — the normal case — the percentage is applied to **tax-inclusive `amount_total`** instead of `amount_untaxed`. | as cited |
| `E3-15` | MEDIUM | Redundant fork of a native Odoo 18 feature: `purchase` already ships `is_downpayment` on both `purchase.order.line` (`:79`, with a table CHECK at `:83`) and `account.move.line`, plus `_create_downpayments` and `_prepare_down_payment_section_values` (`purchase/models/purchase_order.py:611-645`). The custom module redefines the field and reimplements the section logic in parallel. | as cited |
| `E3-16` | MEDIUM | The button replacement is incomplete: `view/purchase_view.xml:8-17` replaces `action_create_invoice` twice, but the core view defines **four** such buttons (`:140, :146, :555, :597`). The two list-header "Create Bills" buttons still call the native action, bypassing the down-payment wizard. | as cited |
