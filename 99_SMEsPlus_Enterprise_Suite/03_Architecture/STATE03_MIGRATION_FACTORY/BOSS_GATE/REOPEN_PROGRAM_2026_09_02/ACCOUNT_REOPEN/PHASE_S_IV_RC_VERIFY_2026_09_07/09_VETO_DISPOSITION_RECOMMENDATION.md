# 09 — VETO DISPOSITION — **RECOMMENDATIONS ONLY**

> **This session discharges nothing.** §7 permits evidence-backed recommendations only, and
> `Q-BOSS-02` §1 control 8 forbids self-discharge. **0 of 17 discharged, and none could be** — every
> lifting condition that requires independent proof is blocked behind an RC no eligible party has run.

## 1. `IV-R-05` — **the veto population total is not reproducible from its own register. MATERIAL.**

Before recommending a disposition per veto, this session tried to reproduce the population. **It does
not reproduce.**

**Authoritative source:** `06_VETO_AND_BOSS_DECISION_DEPENDENCY_REGISTER.md` @
`audit/account-xrecon-2026-09-06-001` `3291210`, §1 *"Standing vetoes — 4 tracks"*.

**Asserted, line 33:** *"Total vetoes standing across the four tracks: **17 named**."*
The string `17` appears **once** in the 95-line file — in that sentence. **There is no enumeration
behind it**, and no `POPULATION` / `PATTERN` / `UNIT` declaration for the count.

**Enumerated from the register's own rows:**

| Track | Rows | Vetoes |
|---|---|---|
| P06 | `AASP-VETO-07` · `AASP-VETO-06` · `AASP-VETO-04` | **3** |
| P08 | `AAS+-VETO-01` (inherited) · `AAS+-PS-VETO-01` · *"4 vetoes from the audited round"* · *"4 new vetoes from the verification"* | **10**, or **8** if the two named rows are members of the two 4-rows |
| P09 | `AAS+-VETO-04` · `AAS+-VETO-01` · `AAS+-VETO-02` · `AAS+-VETO-03` | **4** |
| P11 | `AASP-P11-C3-VETO-01` … `-04` | **4** |
| *(P10 — outside "the four tracks")* | `P10 AASP-VETO-01` r3 | *1* |

**Four-track total: 19 (lower reading) to 21 (upper reading). Adding P10: 20 to 22.**
**No reading of the register's own rows yields 17.** To reach 17, P08 would have to enumerate 6, and
no row structure gives 6.

**The passing control is in the same file, 15 lines later.** Line 48 states the Boss-decision total as
*"**51 named items** (19 + 19 + 10 + 3)"* — **arithmetic shown, per-track enumerated, and it checks:
19+19+10+3 = 51.** The same register enumerates one total and asserts the other. **That is what makes
this a defect in the veto count and not a complaint about registers in general.**

**What is NOT contradicted:** *"Zero discharged"*. That is independently robust — every per-row state
in the register reads `standing` / `UNDISCHARGED` / `NOT DISCHARGED` / `PRESERVED` / `UPHELD`, and
**0 is 0 under every reading of the denominator**.

**What IS contradicted:** the figure **17**, which `12_VETO_DISPOSITION_REGISTER.md` @ `0941161`,
Phase S closure criterion 6, and this session's own §2 draft all carry forward as
*"17 standing, 0 discharged."*

**Disposition: `CONTRADICTED`. Routed to the XRECON register owner.** Not edited here.
**Consequence for this package: this session does not publish a veto count.** It publishes a
disposition per named row, and marks the denominator unresolved — a total this session cannot
reproduce is not one it will restate.

## 2. Recommendation — **0 DISCHARGEABLE, per named row**

**0 of the register's rows are `DISCHARGEABLE BY VERIFIED EVIDENCE`, and none could be** — every
lifting condition requiring independent proof is blocked behind an RC no eligible party has run.
Dispositions are given **per row as the register names them**; the two P08 group-rows are
dispositioned as groups, exactly as the source does, **and are not expanded into a count**.

| Register row | Recorded disposition (`12_` @ `0941161`) | This session's recommendation | Why |
|---|---|---|---|
| `AASP-VETO-07` (P06) | UPHELD | **UPHELD** | lifting requires an independent verifier that is not the P06 IEV actor. `RC-03`/`RC-04` on HOLD. Their *paths* are clear — surfaces exist, inputs repository-only — but **clearing an obstacle to a challenge is not the challenge** |
| `AASP-VETO-06` (P06) | PARTIALLY SATISFIED — REMAINS OPEN | **PARTIALLY SATISFIED — REMAINS OPEN** | unchanged |
| `AASP-VETO-04` (P06) | UPHELD | **UPHELD** | unchanged |
| `AAS+-VETO-01` inherited (P08) | UPHELD | **UPHELD** | 0 of 2 conditions met |
| `AAS+-PS-VETO-01` (P08) | UPHELD; `C-6` BOSS DECISION REQUIRED | **UPHELD; `C-6` BOSS DECISION REQUIRED** | §3 |
| *"4 vetoes from the audited round"* (P08) | UPHELD | **UPHELD** | 0 of 25 evidenced |
| *"4 new vetoes from the verification"* (P08) | UPHELD | **UPHELD** | 41 conditions, none evidenced |
| `AAS+-VETO-04` (P09) | PARTIALLY SATISFIED — REMAINS OPEN | **PARTIALLY SATISFIED — REMAINS OPEN** | `M-1` resolved at `2079a25`; **`M-2` open**. The condition is conjunctive — **not a partial discharge** |
| `AAS+-VETO-01/-02/-03` (P09) | UPHELD, unchanged | **UPHELD** | unchanged |
| `AASP-P11-C3-VETO-01` | UPHELD | **UPHELD** | 6 lift conditions, none evidenced |
| `AASP-P11-C3-VETO-02` | UPHELD | **UPHELD** | lifts on implementation — **not reachable in Phase S** |
| `AASP-P11-C3-VETO-03` — *no count without `E6`* | UPHELD, NEWLY REINFORCED | **UPHELD, AND REINFORCED AGAIN** | §3 |
| `AASP-P11-C3-VETO-04` — *no control set drawn by the party it controls* | UPHELD | **UPHELD — and it binds this session too** | §3 |
| `P10 AASP-VETO-01` r3 | UPHELD | **UPHELD** | P10 not opened; outside Phase S scope |
| `AAS+-PS-VETO-01 C-6` *(tracked separately by `12_`; **a condition of a row above, not a separate row in the source register** — noted as a second unit inflation, see `IV-R-05`)* | BOSS DECISION REQUIRED | **BOSS DECISION REQUIRED** | `XRD-009` = NOT SATISFIED. **Boss only** |

**Disposition tally by kind — deliberately not summed to a veto count:**
`DISCHARGEABLE BY VERIFIED EVIDENCE` **0 rows** · `PARTIALLY SATISFIED — REMAINS OPEN` **2 rows** ·
`UPHELD` **11 rows** · `BOSS DECISION REQUIRED` **1 row** · `SUPERSEDED WITH LINEAGE` **0** ·
`ROUTED` **0**. **14 rows dispositioned; the number of vetoes those rows represent is `IV-R-05`.**

## 3. Three vetoes on which this session produced new material

**`AAS+-PS-VETO-01` (P08).** `RC-05` is **evidence-complete** — 4 of 4 inputs hash-verified, both
`pg_restore` versions present, 7 of 7 `Q-BOSS-03` §2 requirements met (`02_` §3). **The disposition
does not move.** The veto lifts on independent *verification*, and evidence-readiness is a
precondition to that, not a part of it. **Recording this so a later round does not read "RC-05
unblocked" as "RC-05 satisfied."**

**`AASP-P11-C3-VETO-03` — *no count without `E6`*.** Reinforced a second time, on new grounds.
`CO-F-01` reinforced it via a non-deterministic intake denominator. `IV-R-04` (`07_` §4) now shows a
**published coverage assertion that is false against its own declared command** in two live manifests
— including the one repaired for exactly this defect in the same commit. **A count whose stated
derivation does not reproduce is what this veto exists to prevent.** No discharge; the standing case
is stronger again.

**`AASP-P11-C3-VETO-04` — *no control set drawn by the party it controls*.** This veto **binds this
session's own reading of the handoff matrix.** The matrix's "controls published" column is
**owner-published and owner-selected**; this session did not re-derive a single control set, and says
so in every RC file. An eligible verifier must **draw its own control set** for `RC-02` in particular,
where all 8 controls are the owner's.

## 4. Boss-only dependencies — unchanged and enumerated

| Item | State |
|---|---|
| `AAS+-PS-VETO-01 C-6` | **BOSS DECISION REQUIRED** — reserved by `XRD-009` |
| The 51 open domain Boss decisions **(19+19+10+3 — enumerated and reproduced)** | **51 open, 0 answered, 0 narrowed, 0 eliminated** by this session |
| `PHASE-S/Q-BOSS-02` | answered `2026-09-07`; **this session is bound by it, not entitled to interpret around it** |
| Whether an executor other than ChatGPT GPT-5.6 Sol may run `RC-01`…`RC-06` | **NOT ANSWERED — and this session may not answer it.** See `11_` §2 |

**The last row is raised by this session and is the only new Boss item in this package.**
