# P04 — G01 HANDOFF INTAKE (P03, P01) AND BASELINE VERIFICATION

**LAYER 2 — AUDIT QUARANTINE.** `CP-01`. Consumed, not executed.

---

## 1. The four mandated SHAs — verified individually

| Prompt names | Resolves? | What is actually there |
|---|---|---|
| P04 baseline `fc3620be8d4ea566…` | **NO** | Not an object in this repository. The real P04 head at intake is **`fc362fcf533bdb546a6d8f9c67e2b1902ad542a3`**. Both abbreviate to `fc362f`/`fc3620` — a transcription slip of the same short hash |
| P03 closure `bc7676733a1bcfd8…` | **NO** | Remote refuses: *"upload-pack: not our ref"*. The real P03 bounded-deep closure head is **`bc767a8`**, *"the source basis did not match the deployment"*, on `research/account-p03-manufacture-to-cost-2026-09-04-001` |
| P01 closure `b820b29b13f351ec…` | **YES** | `b820b29` *"background-task disposition — the terminal count drifted after publication"* |
| Constitution `48ee264fd74dcb0d…` | **YES** | `48ee264` *"establish bounded-deep Closure Execution Constitution v1"* |

> **Two of four mandated inputs do not resolve as written, and both are near-miss
> transcriptions of real short hashes.** They are **declared, not silently repaired**.
> This run proceeds on `fc362fcf533b` (own baseline, content-verified as this package's
> own head) and on `bc767a8` (P03, content-verified: it carries `CLOSURE_G01/` and the
> `CQ-P03-01..10` register the prompt describes).
>
> **Substitution basis is content, not prefix similarity.** A prefix match alone would
> not be evidence; the P03 head was accepted because its closure register and its
> `CQ-P03-06` file are the artefacts §5 and `CQ-P04-07` name.

## 2. P03 → P04, consumed

**`CQ-P03-06`, `FACT VERIFIED — CLOSED`** (`P03_EQUIPMENT_OPERATION_COST_CAUSALITY.md`):

| Claim | P03 | P04 independent status |
|---|---|---|
| Operation → Work Centre | **proven present** | not re-derived; accepted with attribution |
| Operation → specific Equipment | **proven absent** | **independently verified here** (`CQ-P04-07`) |
| Equipment → Asset | **no reference either direction** | **independently verified here in the reference tree** (`CQ-P04-03`) — and P04 adds the custom-module half P03 did not examine |
| Time log → machine | absent; every measurement resolves to a work centre | accepted, P03-owned |
| Equipment cost into MO/WIP/FG | **no path, 0 of 4 deployments** | accepted, P03-owned |

P03 routes the design question to **the Asset register as `BOSS DECISION REQUIRED`**. P04 accepts
ownership: it is `BLK-07` and `P04-BD-05`, and it is **not** closed by this run.

## 3. The intake finding that matters more than the handoff

P03's `MD-01`: **it read series-18 source for four rounds and reported it as the behaviour of a
series-16 deployment**, and no series-16 source exists in a fully enumerated path set with a
28×-firing positive control.

**P04 tested itself for the same defect and does not reproduce it** — see
`P04_DEPLOYED_CODE_IDENTITY_DELTA.md` (`MD-P04-01`). P04 published the generation gap itself at
`P04-F-85` on 2026-09-05, one day before P03 reached it. **What P04 never did was ask whether the
gap is closable.** P03's enumeration answers that, and P04 consumes the answer with reliance
declared.

## 4. P01

**Not consumed as evidence in this run.** §5 admits P01 only when an acquisition / vendor-bill
lineage question directly needs it. `CQ-P04-01` and `CQ-P04-09` were both answerable from the
`account_asset` module itself. **Verified present, deliberately unread** — recorded so that a
later reader does not mistake silence for oversight.
