# P11 — TOLERANCE-ZERO REGISTER — CORR2

`[SMEPLUS-26-09-06-…-CORR2-PHASES-001]` · `CP-P11C2-06` · **PHASE S**

> `CONDITIONAL PASS` may not bypass a tolerance-zero boundary. **This is a rule, not a judgement.**

| id | Boundary | CORR2 movement |
|---|---|---|
| `T0-01` … `T0-07`, `T0-09` … `T0-12` | as inherited | **unchanged.** `T0-11`/`T0-12` retain the `KRN-INV-00` inheritance embargo (`CONTESTED`, must not be inherited downstream) |
| `T0-08` | posted detail mutability | **re-scoped, not resolved.** Split into `S3-SRC` (18.0 source line) and `S3-DEP` (deployed estate). **Both fail.** The boundary now rests on deployed evidence from two peers |
| `T0-13` | the peer boundary a peer over-adopted | **unchanged — and its misuse is corrected.** `HOLD — BOSS DECISION REQUIRED`. `P10` withdrew the adoption; `OPT-A` restored; option set **six** |
| `T0-14` | financial history and audit trail deletable with **no server-side authorisation** | **strengthened.** `P01` `S16-B-05` supplies the schema mechanism (`ON DELETE SET NULL`) and confirms the module is **installed**, raw `DELETE FROM` + `commit()`, *"no ORM, no lock-date check, no company filter, no log"*, **10 of 20 destructive buttons carry no confirmation** |

## New

| id | Boundary | Evidence |
|---|---|---|
| **`T0-15`** | **There is no accounting period to reconcile within, and no company closes one.** A period is a **date range on a company record**, not an object; **0 of 6** transacting companies set a close; **no lock date on any of three locking surfaces** over **169,143 posted entries** | `P08` `58_` (`A VERIFIED ABSENCE`, 7-observation limit) · `P01` @ `b820b29` (enumerated **after** challenge; the earlier phrasing had been tested against **one** surface) |

> **`T0-15` is not a new discovery — it is a boundary the package has been describing in pieces for
> three rounds without registering as tolerance-zero.** Period, close and lock were carried as three
> separate observations. They are one boundary: **the ledger has no period.**

## Position

**`15` boundaries · `0` resolved · `0` closable by more source reading.**
**`CONDITIONAL PASS` unavailable by rule.**

CORR2 **re-scoped one, strengthened one, added one, resolved none.**

**`CP-P11C2-06` (tolerance-zero) — COMPLETE — EVIDENCE VERIFIED.**
