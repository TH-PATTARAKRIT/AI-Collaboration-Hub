# P11 — BOSS DECISION MATRIX — CORR2

`[SMEPLUS-26-09-06-…-CORR2-PHASES-001]` · `CP-P11C2-06` · **PHASE S**

> **`0` of these are decided by P11. `0` were decided by P11 in CORR1. P11 decides none.**
> **This is a declared FLOOR, not a total.**

---

## 1. The three coupled decisions — present together, in this order

`P10` states the coupling and the sequence, and P11 does not re-order it.

| # | Decision | Why it is first |
|---|---|---|
| **1** | **`T0-13` / `B-16` — the peer tolerance-zero boundary** | **Programme-wide; binds more than P10.** And **P11's own register records a defect in its derivation** — it was scoped from the occasion that prompted it and the owed enumeration was never performed. **The Boss should see that beside it.** `P10` over-adopted it and eliminated `OPT-A`; that is now withdrawn |
| **2** | **`D-5` — introduce a layer-3 accounting-event object, or not** | Gates one recognition option and P10's kernel. **Eight** dependent processes. Evidence class **`C`** — the CORR1 upgrade to `A` is withdrawn and stays withdrawn |
| **3** | **`P10-D-02` — may a posting constraint alter an original recognition period?** | **Six options, none eliminated** |

## 2. Full population — `18`, a declared floor

| id | Decision | Owner | CORR2 movement |
|---|---|---|---|
| `D-1` | **Declare which reference root / generation SMEsPlus targets** | Boss | **Narrowed, not decided.** A **series-18 deployment exists** (`ERR-P01-23`); **31 core trees across five series** exist (`ERR-P01-41`). Bounds `B-20`, `B-21`, `B-23`. **Still one sentence only the Boss can write** |
| `D-2` | *(as inherited)* | Boss | unchanged |
| `D-3` | *(as inherited)* | Boss | unchanged |
| `D-3b` | Evidence-selection authorisation | Boss | **v5 specified** — `E0` population + `E6` independent denominator challenge added. `AUTHORIZATION REQUIRED — EXACT EVIDENCE ACTION NAMED` |
| `D-4` | *(as inherited)* | Boss | unchanged. **Note the namespace hazard at `OC-07`** |
| **`D-5`** | **Accounting-event identity** | Boss | **Coupled second.** Dependent processes **five → eight** (`P05` `HE-09` joins via the settlement route) |
| `D-6` … `D-9` | *(as inherited)* | Boss | unchanged |
| `D-10` | Authorise CORR1 | Boss | **discharged** at CORR1 |
| `D-11` | Exception to *unrelated independent companies = separate tenants* | Boss | unchanged |
| `D-12` | May a company hierarchy span a tenant boundary? | Boss | unchanged. P11 recommends **NO** |
| `D-13` | May a scope narrowing for one object read across to a related object? | Boss | **`P03` dissent preserved** — the two objects are *"unlinked in both directions"* |
| `D-14` | What closes a defect whose specified evidence returns an **empty population**? | Boss | **Partly answered by `P03`** — the population was found in a **fourth database** and returned **`0 of 60`**. The question survives: *is `0 of 60` a closure or a smaller empty set?* |
| `D-15` | Is `MA-11` binding on `P01`–`P10`? | Boss | unchanged |
| **`D-16`** | **Which ledger is authoritative** where the inventory subsidiary ledger and the GL disagree, and how is divergence detected? | Boss | **NEW** — routed by `P03` as `P11-D-4`. *"Neither P03 nor Inventory may set it."* Compounded by `P11-C-12`: for the party dimension there is **no independent record at all** |
| **`D-17`** | **Remediation sequencing** for a corrupt position that currently **self-cancels** | Boss | **NEW** — routed by `P03` as `P11-D-5`. *"Any single-process fix releases the full gross exposure"* (**−48.7 %** of inventory valuation). **Above every individual process** |
| **`D-18`** | May a defect with **zero measured incidence** but a permitted mechanism be closed? | Boss | **NEW** — routed by `P03` as `P11-D-6`. *"Recurs for every latent defect in the programme."* **Now sharpened by `B-26`: zero incidence may be a deletion artefact** |

## 3. Decisions carried by peers that P11 records but does not adopt

`P04`: `BLK-07` allocation denominator · `P04-BD-05` units-of-production as TAS 16 with normal capacity
as TAS 2 denominator · `BD-06` must derecognition post automatically · `BD-07` is scrap a distinct
retire event · `BD-08` capitalise-vs-expense threshold — *"no decision point exists in the estate at
all"* · `BD-09` asset model as tenant template or company accounting truth.
`P10`: revenue on billing vs performance *(preserved from `P02`, Boss-reserved)* · bill-and-hold scope ·
may a tenant **bind** an allocation convention · fiscal or civil recognition grid · **is a shared chart
of accounts permitted** — *"decides whether a scope defect fails loudly or silently"*.

> **≈ 11 further decisions are routed inside peer packages without P11 `D-nn` ids.** That is why **18
> is a floor.** `UNRESOLVED ≠ ADOPTED` — none is adopted by recording it here.

## 4. Position

**`18` decisions · `1` discharged (`D-10`) · `3` new · `4` materially better informed · `0` decided by
P11 · a declared FLOOR.**

> **`D-1` remains the single highest-leverage act available to anyone**: one sentence, Boss-only,
> bounding `B-20`, `B-21` and `B-23`.

**`CP-P11C2-06` (decisions) — COMPLETE — EVIDENCE VERIFIED.**
