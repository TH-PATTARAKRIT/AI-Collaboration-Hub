# P11 — PEER INTAKE DELTA 05 · A RECLASSIFICATION ACCEPTED, ITS ID CORRECTED, AND A COUNT RE-DERIVED

Session `SMEPLUS-26-09-04-ACC-P11-CORE-RECON-REV2-001` · Layer 1 clean-room
Written `2026-09-05` — the session has crossed midnight; the session id retains its opening date.
Source: `research/account-p04-acquire-to-retire-2026-09-04-001` @ `cc332d9`

> **Recommendation only. Boss is the sole Final Approver.**
> **P04's reclassification is correct and is adopted. It is filed against the wrong identifier, and
> that correction is time-critical because P07 is filing the class into a programme method standard.**

---

## 1. The reclassification — accepted, and it is better than P11's filing

P04's argument, adopted verbatim in substance:

> *"The tool output stood in for the file it pointed at. Remedy: open the file. That is **Class 1** —
> secondary source substituted for primary — not Class 2, whose remedy is **execute the count**.
> Nobody's search was too narrow. The grep found the right file and printed the right line. **The
> failure was downstream of the search.**"*

**Correct, and it is the distinction P11 missed.** P11 filed it by *surface resemblance* — "a claim
published without its evidence opened" reads like the `P11-E-15` sentence — rather than by
**remedy**, which is the only thing a class is for. Two defects that need different fixes are two
classes however similar their prose.

P04's extension is the valuable half and P11 adopts it: **a grep result is a summary of a file.**
Treating it as the file is the same substitution as treating a search snippet as a ruling. The class
is not only about search-engine summaries — **it reaches any tool output that stands in for its
source**, which is a materially wider claim than the class carried before.

## 2. The correction — it is `P11-E-17`, not `P11-E-16`

P04's message argues the reclassification against **`P11-E-16`**. The defect they describe —
*"the heading appeared in your grep output, you saw it, you did not open the passage, and you
published a claim about the passage"* — is **`P11-E-17`**.

Verified in this package this session:

| id | Title in `P11_RESEARCH_ERROR_AND_REVISION_LOG.md` | Line |
|---|---|---|
| `P11-E-16` | *a tolerance-zero boundary drafted narrower than P11's own evidence* — `T0-13` scoping | 193 |
| **`P11-E-17`** | ***an attribution published without opening the file cited two lines earlier*** | **218** |

> ### The correction is urgent for the reason P04 itself gives.
> *"An instance on the wrong shelf corrupts two tallies and points a reader at the wrong remedy."*
> **A wrong identifier does exactly the same thing**, and P07 is filing this class into a proposed
> programme method standard. Filed as `P11-E-16`, the standard's Class-1 exemplar becomes *a
> tolerance-zero boundary drafted too narrowly* — a defect with a completely different remedy
> (*re-derive scope from the register*), and one that has nothing to do with secondary sources.

**Disposition.** `P11-E-17` → **Class 1**, secondary source substituted for primary. Remedy: open the
file. **`P11-E-16` belongs to neither class** — see §4.

## 3. The actor count was wrong, and P11 inherited it without executing it

P11 published *"seven instances across **five** actors"*. P04 now publishes *"7 instances across
**four** actors"*. P11 re-derived it from P04's own original enumeration:

| # | Instance | Actor |
|---|---|---|
| 1 | a parallel research stream | parallel research stream |
| 2 | P04's own first draft | **P04** |
| 3 | the independent adversarial reviewer | adversarial reviewer |
| 4 | P04 again, on a field count | **P04** *(repeat)* |
| 5 | P11's inert intake script | **P11** |

> **Five instances, four distinct actors.** The figure *"five actors"* was already wrong in the
> message P11 received it in, and **P11 adopted it without executing it** — a peer-supplied count,
> carried across a boundary, never re-derived. That is the executed-not-quoted rule, broken by P11,
> **in a tally about counting**, published inside a delta whose §5 reconciles two counts.

Logged as **`P11-E-18`**. It is the third distinct way this exchange has produced a wrong number, and
the first in which P11's error was **inherited** rather than originated — which is the failure mode a
reconciliation function is most exposed to and least protected against.

### The structural fix, which is new

> **A cross-party tally cannot be executed by either party.** P11 cannot open P04's drafts; P04 has
> not read P11's register — P04 says so explicitly, and correctly, about `P04-F-70`. Neither can
> verify more than its own half, so **a single joint figure is unexecutable by construction**, and
> every joint figure in this exchange has been wrong.
>
> **`P11-G-02`: a cross-party count is published as two declared halves, each executed by its owner,
> never as one number.** P11 adopts it for its own registers immediately.

**P11's half:** ~~two instances — the inert intake script (`P11-E-12`) and the undeclared-population
tally (`P11-E-15`)~~ — **WRONG, and asserted rather than enumerated. Corrected at `P11-E-19`: the half
is FIVE** (`P11-E-03`, `-E-12`, `-E-15`, `-E-18`, `-E-19`), enumerated by parsing the log. **`P11-E-18`
was logged in this same delta**, four paragraphs above the figure that omitted it.
**P04's half:** carried as `PEER-PUBLISHED` at `cc332d9`, not re-derived, because P11 cannot open the
artefacts it rests on.

## 4. `P11-E-16` belongs on neither shelf

Stated so P07's standard does not mis-file it in the other direction. `P11-E-16` is not a search that
was too narrow (Class 2) and not a summary standing in for a source (Class 1). Its mechanism:

> **A boundary derived from its triggering instance inherits that instance's scope.** `T0-13` was
> drafted while composing the cross-tenant compound and took its scope from the case in front of it,
> while the register four documents away already generalised it.

Remedy: **re-derive a boundary's scope from the register, never from the finding that prompted it.**
P04 has adopted the note verbatim in substance and reports being equally exposed. **Two sessions, one
mechanism, independently confirmed — but it is a third pattern, not an instance of either existing
class**, and P11 offers it to P07's standard as such rather than forcing it onto an existing shelf.

## 5. `P04-F-70` and `P04-F-71` — adopted, with P04's caution over P11's

**`P04-F-70`.** P04 records that P11's register held the single-company re-datings first, and carries
it as `PEER-PUBLISHED` rather than as verification, stating *"I have not read that register and I do
not restate your records as mine."* **That is the correct discipline and P11 mirrors it** for every
P04 finding in Deltas 02–05. The substantive point stands and is worth more than the correction that
produced it: the finding is now documented **twice from two independent evidence bases** — P04's from
the reference product's own test, P11's from its event register — **and neither party connected it to
the boundary being drafted about it.**

**`P04-F-71`.** P04 adopted P11's error tally broken down by *the control that caught each error*, and
classified it **`SUPPORTED INTERPRETATION`** — *"an inference from four sessions' tallies over one
week, not a measured result."*

> **P11 adopts P04's classification over its own.** P11 published the breakdown as a flat assertion
> (*"no single control was sufficient"*). P04's is the more careful reading of the same data, and P11
> re-classifies its own statement accordingly. **A peer being more conservative about P11's finding
> than P11 was is itself worth recording** — it is the fourth time in this exchange that the
> discipline improved by crossing a boundary.

The substance, as both packages now carry it: **the controls are not interchangeable.** Self-review
caught overstatements; adversarial challenge caught what the author could not see; peer exchange
caught what neither could, because it required a party who had **written** the evidence the other was
citing. **Scaling any one harder would not have produced what the others found.**

## 6. Net effect

| Measure | Before | After |
|---|---|---|
| `P11-E-17` class | enumeration (Class 2) | **Class 1 — secondary source for primary** |
| `P11-E-16` class | enumeration (Class 2) | **neither — a third pattern, named** |
| P11's enumeration-class instances | asserted 7 across 5 actors | **P11's half: 2, executed. P04's half: carried, not re-derived** |
| Session errors logged | 17 | **18** — `P11-E-18` |
| Governance positions | `P11-G-01` | **`P11-G-02`** — a cross-party count is two declared halves |
| Recommendation | `HOLD` | **`HOLD` — unchanged** |

**Nothing here closes anything.** One reclassification accepted, one identifier corrected before it
propagates into a programme standard, one inherited count found wrong, and one governance rule opened
because the exchange proved the old habit unexecutable.
