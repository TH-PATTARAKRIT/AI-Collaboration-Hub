# P11 — AAS+ CORR3 CONSOLIDATION

`[SMEPLUS-26-09-06-…-CORR3-ACCOUNTING-INTAKE-INTEGRITY-001]` · `CP-P11C3-10` · **PHASE S**

> **Recommendation only. Boss is the sole Final Approver. AAS+ may not declare Boss approval.**
> Four experts, frozen surface `9356557`, no contact between them, **freeze held throughout**.

---

## 1. Agreements

1. **The peer snapshot is exact.** Three experts independently reproduced all ten `.md` counts and
   confirmed every SHA is its branch's terminal head.
2. **`D1`, `D2` and the union/intersection test are exact.** 55 · 48 · 19/36/29, reproduced separately
   by three experts. **This is the control CORR2 lacked and it holds.**
3. **The `D26` diagnosis and `P11-G-07` are sound** (E4-S5). Terminal-member-only genuinely loses `D26`.
4. **The five P08 corrections are carried faithfully and against P11's own interest** (E2-S7, E3-S3).
5. **No statutory determination anywhere** — E3 checked the Thai/localisation boundary explicitly and
   found *"no smuggling"*.
6. **Domain purity held.** No peer internal, database, module or source tree opened.

## 2. Disagreements — preserved

| Item | Panel | P11 | Status |
|---|---|---|---|
| `D3=155` / `UNION=212` | E2/E4: **wrong**, executed 212/268 | **Disputed and settled by publication** — the code reproduces 155/212/19/36/29 exactly. The defect is **unreproducibility**, which was P11's to create | resolved by evidence |
| `IC-01` `฿29.0m` | E1: P11 now **under-claims** | **Accepted in substance.** `B-28` re-instated at `฿27,490,865.80` / 1,411 receipted lines | accepted |
| `F-01` settlement graph | E3: P08 did **not** withdraw it | **Accepted.** A consequence, not a withdrawal | accepted |
| `B-31` scope | E1/E3/E4: falsified | **Accepted, re-worded, downgraded to MEDIUM.** The narrow leg survives | accepted |
| `OC-01`/`OC-02` split | *(carried from CORR2)* | unchanged | preserved |

## 3. The consolidated finding

> ### `AASP-C3-F-01` — **CORR3 was ordered to repair intake integrity first. It built a better instrument, certified it on controls that could not fail, and did not publish it.**
>
> The instrument is genuinely better than CORR2's — the union/intersection test is real, the blind spot
> is real at 29, `P11-G-07` is a real gain. **And every one of its certifying controls is defective:**
> the failure control is vacuous (the identical defect CORR2 condemned), the blind-spot table is a
> tautology, `TAIL` is fitted, and **the positive control set was drawn from P11's own writing and
> silently shortened from twelve to ten — dropping `S06`, which fails, and which carries `NC-8`, the
> rule forbidding a `tail` to bound a population.**

**The second-order finding is the one that repeats.** `P11-C-09` was withdrawn at CORR2 for relaying a
peer's word *"converge"* untested. **`P11-C-15` was published at CORR3 relaying a peer's word
*"independently"* untested, against a peer P11 froze and did not open.** The correction round
reproduced the defect it corrected, one artefact to the left.

## 4. Risks

| id | Risk |
|---|---|
| `AASP-C3-R-01` | **Every CORR3 disposition inherits an uncertified denominator.** `B-27` is **not** discharged |
| `AASP-C3-R-02` | **`CURRENT-CRITICAL` is unestablished for every un-numbered series** — a lexical tail cannot establish currentness, and 155 rows carry that label |
| `AASP-C3-R-03` | **The generation-discarding key** silently selects one of five same-named registers per peer directory |
| `AASP-C3-R-04` | **P07 is unopened across three rounds**, while a P07 handoff pack titled for core accounting reconciliation sits inside P11's own `ADDRESSED` set |
| `AASP-C3-R-05` | **`P11_AUTO_RESUME_STATE` instructs the successor to use CORR2's superseded heads** |
| `AASP-C3-R-06` | **The registers still publish claims the falsification pass withdrew** — 7 files changed, **0 deletions** |
| `AASP-C3-R-07` | **`P09` declares its own package internally contradictory** at the head P11 dispositioned `CONSUMED` |

## 5. Vetoes

> ### `AASP-P11-C3-VETO-01` — **UPHELD and RE-GROUNDED.** No part of this package may be relied on as a
> cross-process reconciliation. Previously grounded in claim currency, then in population validity;
> **now in instrument certifiability.** Lift conditions: **(1)** re-derive `D3` on commit timestamp, not
> lexical order; **(2)** key on **path**, not basename; **(3)** word-boundary membership; **(4)** justify or
> replace `TAIL` with a sensitivity analysis; **(5)** certify on a control set **P11 did not choose**,
> including `S06`; **(6)** re-challenge.

> ### `AASP-P11-C3-VETO-02` — **UPHELD.** No design position may seed implementation.

> ### `AASP-P11-C3-VETO-03` — **UPHELD** (no count to the Boss without an `E6` record).

> ### `AASP-P11-C3-VETO-04` — **NEW.** **No control set may be drawn by the party it controls.** CORR3's
> positive control was selected from P11's own error log and shortened by P11 without notice. A control
> set must be taken **in full** from a published list, or supplied by another party.

> **Inherited and now recorded: `P06 AASP-VETO-06`** — *"a handoff is not delivered by being written"* —
> which states `B-31` as a standing peer veto and was absent from P11's tree. Plus `P08 AAS+-VETO-01`
> (undischarged), `P06 AASP-VETO-04`, `P09 AAS+-VETO-04` (itself **not discharged** at P09's head).

## 6. Verdict

> ## `NOT CONVERGED — INTAKE INTEGRITY NOT ESTABLISHED — CORR4 REQUIRED`
>
> **CORR3 did not repair intake integrity. It measured how far from repaired it is** — which is a
> smaller, later and more honest result than the one it published, and it is the only kind of result
> this package has produced that has survived a challenge intact.
