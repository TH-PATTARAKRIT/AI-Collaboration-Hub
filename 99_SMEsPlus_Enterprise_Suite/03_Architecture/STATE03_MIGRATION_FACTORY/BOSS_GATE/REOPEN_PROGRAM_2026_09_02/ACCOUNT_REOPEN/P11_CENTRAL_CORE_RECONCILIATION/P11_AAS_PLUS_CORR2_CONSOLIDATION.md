# P11 — AAS+ CORR2 CONSOLIDATION

`[SMEPLUS-26-09-06-…-CORR2-PHASES-001]` · `CP-P11C2-13` · **PHASE S** · Layer 1 clean-room

> **Recommendation only. Boss is the sole Final Approver.**
> Four experts, bounded to `14de462` and the frozen peer artefacts, unable to see one another.
> **The package did not move while they read** (`P11-M-06`), so — unlike CORR1 — their coverage is
> complete for the surface they were given.

---

## 1. Agreements across the four panels

1. **The method is the best thing in the package, and better than any peer artefact reviewed.**
   E2: *"the package's **method** is stronger than any peer artefact I read."* E1: the
   `S3-SRC`/`S3-DEP` split is *"the strongest single piece of reasoning in the package."*
2. **P11's self-caught errors are genuine.** `P11-E-36` and `P11-E-37` were independently verified as
   correctly reasoned by two panels. E4 called `P11-E-37` *"the strongest single piece of
   self-discipline in the package."*
3. **The peer facts P11 quotes, it quotes accurately** — verified figure-by-figure against P09, P03,
   P05, P04, P01, P08.
4. **`OC-08` / `P11-F-15`** — gross-not-net, 43× — is *"the strongest genuinely P11-authored accounting
   statement in the package"* (E3).
5. **10 of 10 peer heads moved**, fully re-derived, commit distances 2–45.

## 2. Disagreements — preserved, not harmonised

| Item | Panel position | P11 position | Status |
|---|---|---|---|
| `P11-C-12` party-dimension identity | E1: **falsified** by 10 of 1,904 mis-typed payables | **Re-scoped, not withdrawn** — P08's structural point survives inside P08's stated scope | **preserved as partial disagreement** |
| `OC-01`/`OC-02` | E1: one peer finding split in two | **Disputed** — one kernel requirement over two stores with different owners, counterparties and remedies | **preserved** |
| The FK strengthens `S3` | E2: it is an **`S4`** property | **Accepted as a criterion correction** | resolved |
| `B-17`'s scope split | all four: correct method | — | agreed |

## 3. The single finding that subsumes the others

> ### `AASP-C2-F-01` — **P11 built an instrument that selects on filename to enforce a rule that says filenames do not determine currency. The instrument won.**
>
> Every CORR2 conclusion inherits a population chosen by that instrument: a conjunction whose blind
> spot was declared as an intersection, with a positive control that could only exercise the half
> that works and a negative control that could not fail at all.
>
> **This is not one bad count. It is the evidence base.** `53` enumerated against `47` published,
> `18` addressed against `12`, `≥15` read against a declared `12`, and six addressed artefacts
> enumerated and never opened — one of them from the peer recorded as having addressed nothing to P11.

**The second-order finding is worse than the first.** P11 wrote `P11-E-36` this round — *a correction
is not current either* — and then **stopped at generation four of a six-generation chain inside the
document containing that rule**. The defect the round diagnosed recurred in the round's own diagnosis.

## 4. Risks

| id | Risk |
|---|---|
| `AASP-C2-R-01` | **Every CORR2 disposition is provisional**, because the population is not the declared one and has not been re-enumerated |
| `AASP-C2-R-02` | **`B-17` has now been closed and withdrawn twice in two rounds**, each time by P11's own commissioned control. The pattern is closure-before-challenge, not error |
| `AASP-C2-R-03` | **P11 published a convergence it never tested.** `P11-C-09` reached the Boss pack, the decision matrix and the auto-resume state as *"one genuine convergence"*, on a peer's own word *"independently"* |
| `AASP-C2-R-04` | **The largest unrecognised position in the frozen evidence — `฿29,029,467.66`, headed by its owner *"the number P11 and P08 both need"* — is absent from the package** |
| `AASP-C2-R-05` | **`E6` was specified and skipped in the same commit.** The control that would have caught four of the six count errors was written and not run |
| `AASP-C2-R-06` | **P08's `AAS+-VETO-01` over P11's largest evidence source was unrecorded** while P06's and P09's vetoes were recorded — a **selective** omission, landing on the artefact carrying the most `FACT VERIFIED` rows |

## 5. Vetoes

> ### `AASP-P11-C2-VETO-01` — **UPHELD, WIDENED, and now grounded in the evidence base itself.**
> **No part of this package may be relied on as a cross-process reconciliation.** Previously grounded
> in claim currency; now grounded in **population validity**. Lift conditions: **(1)** re-enumerate
> with the disjunction and publish `returned` vs `processed` per `NC-12`; **(2)** consume the six
> enumerated-but-unread addressed artefacts, plus `43_G02_P02_…`; **(3)** walk every peer chain to its
> terminal statement, not its first correction; **(4)** execute `E6` against every published count;
> **(5)** **re-challenge**, because a corrected package that has not been re-attacked is not a
> reviewed package.

> ### `AASP-P11-C2-VETO-02` — **UPHELD.** No design position may seed implementation.

> ### `AASP-P11-C2-VETO-03` — **NEW.** **No count in this package may be quoted to the Boss until it
> has an `E6` record.** Six of the counts published at `14de462` were wrong, and **five of the six ran
> in the direction that flattered the round.** That is the asymmetry P11's own error register measures
> at **11 : 2**, now reproduced inside a single commit.

> ### **Inherited and now recorded:** `P08 AAS+-VETO-01` binds every P08-sourced row (`P11-B-29`);
> `P06 AASP-VETO-04` and `P09 AAS+-VETO-04` continue to bind P11's method.

## 6. Verdict

> ## `NOT CONVERGED — EVIDENCE BASE INVALID — P11 CORR3 REQUIRED`
>
> CORR2 **repaired real method integrity**: it froze before review, it split source from deployed
> scope, it caught two of its own errors before publication, and it refused a peer's claim about
> P11's own register. Three of four panels called its method the strongest thing they read.
>
> **And it selected its evidence with a broken instrument, published six wrong counts, withdrew a
> convergence it had never tested, closed a `CRITICAL` that did not hold, and asserted a blocker
> against a position its owner had already withdrawn.**
>
> **A round whose method is sound and whose evidence base is invalid has not converged. It has
> produced a well-governed register of the wrong population.**
