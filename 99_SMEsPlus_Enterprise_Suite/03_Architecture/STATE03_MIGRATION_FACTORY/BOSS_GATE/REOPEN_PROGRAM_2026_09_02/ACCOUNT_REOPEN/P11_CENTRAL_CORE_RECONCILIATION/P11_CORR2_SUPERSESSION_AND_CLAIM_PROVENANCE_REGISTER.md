# P11 — CORR2 SUPERSESSION AND CLAIM PROVENANCE REGISTER

`[SMEPLUS-26-09-06-…-CORR2-PHASES-001]` · `CP-P11C2-04` (`P11-G-04` v3) · Layer 1 clean-room

> **The rule this register enforces:** *a claim is current only at its **last** statement by its
> **owner**. Neither the branch, nor the SHA, nor the filename, nor recency of a correction makes a
> claim current.* CORR1 learned the first three. **CORR2 learned the fourth, at its own expense.**

---

## 1. `SPR-01` — the P09 chain runs ~~**four deep**~~ **SIX deep**, and P11 consumed generation two, then stopped at four

> ### ⚠ CORRECTED `2026-09-06` by `X4-C1`. **This section was wrong about the very thing it exists to police.**
> `D24`, `D26` and `D27` all exist at P09's frozen head `5441f8d`. **`D26` (`434e97c`, 10:42) and `D27`
> (`0b61c92`, 14:18) both post-date `D25` (`4eb853a`, 09:01).** P11 declared the chain four deep and took
> generation 4 — **inside the document stating "a claim is current only at its last statement by its
> owner".** The rule was written and violated in the same file. `P11-E-39`.
>
> **Generation 5 (`D26`):** *"`B7` is **PARTIALLY WITHDRAWN** … **Every mechanism claim in this
> programme is version-matched to at least two real deployments**"* — two version-18 deployments exist.
> **This directly falsifies P11's `B-23`.**
> **Generation 6 (`D27`) — terminal:** the `99.6507 % / 17,465` headline is *"**unchanged in value,
> narrowed in scope**: one artefact out of **17** in scope, not one out of five"*; `B5` is **NARROWED,
> NOT CLOSED** with **8 in-scope artefacts unread**; and it publishes two rules P11 needed —
> **`NC-12` coverage assertion** (*"count what it **processed** and compare against what its search
> **returned**, and publish both"*) and **`NC-13` per-artefact positive control**.
>
> **`NC-12` is the rule P11's own `S8` scan broke, published in an artefact P11's pattern excluded.**

| Gen | Artefact | Claim about the analytic depreciation net |
|---|---|---|
| 1 | `S18_P09_P11_SUPPLEMENTAL_CRITICAL_EVIDENCE_HANDOFF.md` | 17,716 / 18,483 records · net **−2,961,221.81** · 98.57 % |
| 2 | `S23_P09_POST_PUBLICATION_CORRECTION.md` | 17,444 / 17,488 · net **+3,595,851.11** · 98.24 % · *"a sign-inverted CREDIT … depreciation makes the cost centre look more profitable"* |
| 3 | `D23_P09_P11_DENOMINATOR_SIGN_PLATFORM_SUPPLEMENT.md` | **17,404 of 17,405 entries net exactly 0.00.** Both earlier figures *"counted **accounts**; the claim is about **entries**"*. `+3,595,851.11` re-identified as a **migration artifact**, reconciled exactly. Profitability distortion **`DISPROVED`** |
| 4 | `D25_P09_CHALLENGE_CORRECTION.md` | **`DISPROVED` is WITHDRAWN → `CONFIRMED`.** Population was **17,465**, not 17,405 — silently pre-filtered to both-legged entries; **61** non-zero, not 5; rate **99.6507 %**. *"A cost centre that should bear depreciation and bears net zero has an **overstated margin, by exactly the amount erased**."* |

### 1.1 What P11 did with it — and it was wrong twice

**CORR1 (`P11-B-22`)** withdrew the inherited *"nets to zero"* and published generation **2** as the
correction: *"the net is `+3,595,851.11`, a sign-inverted CREDIT."*

> **`P11-E-36`. Both P11 positions were wrong, and in opposite directions.**
> The original *"nets to zero"* was **substantially right** and P11 withdrew it; the replacement was a
> **migration residue** P11 published as a depreciation effect. **P11 corrected a peer figure to a
> value the peer withdrew one generation later, and did so while writing the rule about supersession.**

| 5 | `D26_P09_V18_DEPLOYMENT_CORROBORATION.md` | **`B7` PARTIALLY WITHDRAWN** — two v18 deployments exist; every mechanism claim is version-matched to **at least two** real deployments |
| **6 — CURRENT** | **`D27_P09_EVIDENCE_BASE_CENSUS_B5_DISCHARGE.md`** | the headline is **narrowed in scope — 1 of 17 artefacts, not 1 of 5**; `B5` **narrowed, not closed**, 8 unread; issues `NC-12` and `NC-13` |

**Corrected P11 position, taken from generation 6 and from nothing earlier:**

| Statement | Status |
|---|---|
| Depreciation-expense analytic leg totals **−100,400,792.57**, offset by an accumulated-depreciation credit **on the same cost centre**; **12 of 23** cost centres net **exactly 0.00** | `FACT VERIFIED — P11 ACCOUNTING BOUNDARY` (received from P09 @ `5441f8d`) |
| **Cost centres bear approximately zero net depreciation; their margin is overstated by the depreciation they should have borne** | **`CONFIRMED`** by the owner |
| `+3,595,851.11` is a **migration artifact** — one opening-balance adjustment, one carry-forward, three others, 56 unpaired expense entries — **not** a depreciation effect | `FACT VERIFIED` |

### 1.2 The finding that outranks the figure

> **`P11-F-15` — every headline this programme has published on this question, P11's included, was a
> SCALAR NET whose GROSS distribution is ≈ 43× larger.**
> Net across cost centres **+3,595,851.11**; **gross absolute movement 154,922,194.55**. And inside the
> entries counted as *"exactly 0.00"* there is cross-cost-centre displacement of **2,019,008.49 in each
> direction** — cost moved off one operating centre onto another, **entry-balanced and invisible to
> every aggregate P11 holds**.

**Accounting consequence, and it is P11's to state:** *a net of zero is not evidence that nothing
happened.* An attribution question over 23 cost centres was answered four times with a single scalar,
by four rounds, and the scalar was never capable of answering it. **`CANDIDATE ACCOUNTING TRUTH`:
management-accounting attribution must be reconciled on **gross movement per centre**, never on a net.

## 2. `SPR-02` — P01 supersedes two of its own negative claims, and one of them frees a peer

| Superseded | Current | Record |
|---|---|---|
| *"No deployed series-18 database exists"*; P01 source and deployment evidence do not overlap on any series | **False.** A series-18 deployment exists on this host — **4 companies, 15,522 journal entries, 47,801 valuation layers, goods-received clearing account configured** | `ERR-P01-23` |
| *"The series-16 core source does not exist"* — attached to the word **anywhere** while the enumeration covered `/Volumes/iMacSys` only | **False. 31 core trees across five series** — 14.0 ×1, **16.0 ×3**, 17.0 ×2, 18.0 ×15, 19.0 ×10 — and **every series-14/-16/-17 tree is under `/Users/admin`**. Verified by reading: `version_info = (16, 0, 0, FINAL, 0, '')`, 955 addons | `ERR-P01-41` — *"a search gap, not a source gap"* |

> ### `P11-C-10` — a cross-peer contradiction that P11 can close by routing, not by research
> **`P04-B-51` states *"no series-16 source exists on this host"*** and bounds every P04 source claim to
> series 18 on that ground. **`P01` `ERR-P01-41` contradicts it with a read-verified enumeration.**
> **P03 carries the same hold** — *"source matching the deployment's own generation: owner
> environment / P01; a source tree of that generation. **Nothing else**"*.
>
> **Disposition: `EXTERNAL / PEER OWNER — HANDOFF PUBLISHED`.** P11 does not adjudicate whose
> enumeration is right and does not open either tree. **P11's contribution is that the two statements
> cannot both be true, and that P04's and P03's named hold may already be discharged by a peer neither
> of them has read.** Routed to P01, P03, P04.

## 3. `SPR-03` — P10 withdraws an adoption it made of **P11's own** unresolved boundary

`41_P10_P11_DECISION_INTEGRITY_CORRECTION.md`: P10 had treated `T0-13` (= `P11-B-16`,
`HOLD — BOSS DECISION REQUIRED`) as an **adopted programme boundary** in three documents and
eliminated option `OPT-A` on that ground.

> *"P10 converted an unresolved peer decision into a programme boundary and reasoned from it."*
> **`OPT-A` is restored unconditionally. The option set is six, not three.**

**P11's stake in this is direct and uncomfortable:** the boundary P10 over-adopted is **P11's**, and
P11's own register already recorded that this boundary *"was scoped from the occasion that prompted it
and the owed enumeration was never performed"*. **A defective P11 boundary was adopted by a peer and
eliminated a Boss option.** Both halves are now corrected at both ends. `P11-C-11`.

## 4. `SPR-04` — P05 invalidates a whole **class** of prior peer negatives, including P11's use of them

`65_` §1: the prior P05 handoff *"rested on a population that **excluded the target platform**"* —
6 registries became **9 identities**, and `idemo18_uat` (Odoo 18, 44 MB) *"on the host throughout"* was
never opened. Three modules recorded as *absent/uninstalled* are **INSTALLED**.

> **`RE-20`, quoted as the owner wrote it:** *"P11 should treat any prior P05 statement of the form
> **'not found in the deployed population'** as **class B at best** unless it post-dates `41`."*

**Accepted, and P11 widens it against its own package rather than only P05's.** Every P11 register row
whose evidence is a peer negative of that form, dated before that peer's own evidence-base correction,
is **downgraded to class B** pending re-derivation. **`P11-B-25`.**

## 5. `SPR-05` — P05's inversion finding, restated as a P11 method rule

> *"P05's depth of analysis was inverted relative to real usage."* The reimbursement path traced most
> thoroughly is used **twice**; petty cash — traced as a defect story — carries **634 of 993 (63.8 %)**.

**`P11-G-05`:** where a peer supplies both a mechanism and its incidence, **P11 ranks by incidence
before it ranks by mechanism.** P11 has repeatedly inherited a peer's depth ordering as if it were a
materiality ordering. It is not; it is the peer's research history.

## 6. Claims P11 is now **prohibited** from quoting

| Claim | Owner's current position |
|---|---|
| *"There is no accounting-event identity — a verified absence across all 22 declared roots"* | **narrowed** in `52_`/`58_`: identity exists on **one** inbound channel, nullable, **unpopulated on all 13,814 rows**. Absent as a **platform property**, not absent as a fact |
| *"Depreciation makes the cost centre look more profitable"* (as an inflation claim about `+3,595,851.11`) | that framing is a **strawman the owner disproved**; the substantive claim is `CONFIRMED` on different grounds (§1) |
| *"The net is +3,595,851.11, a sign-inverted CREDIT"* as a **depreciation** effect | **migration artifact**; withdrawn |
| *"No series-16 source exists on this host"* | **contradicted** by `ERR-P01-41` |
| *"Nets to zero"* stated as a **scalar** conclusion | true as far as it goes and **not the answerable measure** — gross is 43× (§1.2) |
| P01's *"capitalised into inventory / no P&L variance"* | ***"FALSE IN BOTH HALVES"*** — `ERR-P01-49` |

**`CP-P11C2-04` — COMPLETE — EVIDENCE VERIFIED.**
