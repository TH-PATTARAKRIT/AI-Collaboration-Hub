# 66 — P04 RECONCILIATION REFRESH

**LAYER 2 — AUDIT QUARANTINE.**
**P04 LAST CONSUMED SHA: `5f728cb`.** Round 3 consumed the same commit — **no material
delta on P04's side**, so per §5.5 P04's evidence is **not reprocessed**. Only items where
*P03's* new evidence changes the joint position are refreshed.

---

## 1. Items refreshed by P03's new evidence

| Item | Joint position after round 4 |
|---|---|
| **`DEP-13` / `P04-B-35`** | **Now answerable.** P04's specified count returns **0 of 60** in `iTEST02`, the only deployment with work centres. Round 3 reported it *executed and vacuous* (0 of 0); it is now **executed and substantive**. Blocker **not closed** — mechanism stands, severity **High → Medium** on measured incidence |
| **`DEP-04`** | **Closed** on four databases (`61`). P04's `Q-04` remains Asset-owned and is **not** closed by P03 |
| **Depreciation cost path** | P04's netting finding **re-confirmed with the maintenance bridge installed** (`64` §1). Strengthened, not changed |
| **Operation ↔ equipment gap** | **Re-verified in a deployment with `mrp_maintenance` installed** — the link is still equipment → work centre only. This removes the "module absent" objection to P04's finding |
| **Cost-mechanism population** | P04's **7** under its declared unit stands. P03's `15` is a different unit and population (`59` §1). No conflict |
| **Asset scope** | Unchanged. P03's dissent against extending the tenant reading to Asset is **reinforced** (`64` §4) |

## 2. What P03 owes P04

| # | Item |
|---|---|
| 1 | **`P04-B-35`'s closing criterion is satisfiable by a vacuous zero** and now by a substantive one. P03 proposed an amendment in `32` §4; with `iTEST02` open the amendment is **satisfiable** — a deployment with a non-empty work-centre population exists |
| 2 | The **one rated work centre** answers Asset query `Q-07` (*whether any machine cost is in finished goods today*): **yes, for 1 of 60 work centres, ~20 hours, ~2,000** |
| 3 | `DC-07`'s measured exposure — **60 of 60 work centres have no expense account** — is new and bears on any asset-cost absorption design |

Delivered by publication on P03's branch. P03 **does not amend P04's register**.

## 3. `BLK-07` — unchanged, with one added observation

P04 opened a **third option** (usage-based charge with normal-capacity absorption). **P03
does not evaluate it.**

P03 adds one measured observation and no recommendation: across four deployments the
**numerator** of any absorption rate has no data source — 0 work centres in three
databases, 1 rated of 60 in the fourth. Whichever denominator Boss selects, it currently
divides a numerator that is not being captured.

`DEP-01` unchanged: **HOLD — DESIGN DECISION REQUIRED. Owner: Boss.**
