# S18 — P09_P11_SUPPLEMENTAL_CRITICAL_EVIDENCE_HANDOFF

**Checkpoint:** `CP-P09S18` · **Layer:** 1 — clean-room.
**Material delta only. Prior P09 handoffs are NOT replaced.** P11 has no published branch at time of writing — **`HOLD — PEER PROCESS RECONCILIATION REQUIRED`**.

---

> ## ⚠ CORRECTED AFTER PUBLICATION — see `S23`
> The headline figures in this document were re-measured and are wrong: the correct counts are **17,444 / 17,488**, annihilation **98.24 %**, and **the net is `+3,595,851.11` — a sign-inverted CREDIT, not zero.** `TH-F-02` is far larger than stated here (**169,954 records, a 277 M swing**). Read `S23` before quoting any number from this file.


## 1. READ THIS FIRST

> **P09's mechanism claims are read from a platform version that no deployment runs.** One deployment runs version 16; three run version 19; the source is version 18. **Until this is resolved, no P09 mechanism claim may be treated as describing a running system.** The *measurements* are unaffected.

## 2. THE MATERIAL DELTA — EIGHT ITEMS

| # | Item | Class | What P11 must do with it |
|---|---|---|---|
| **1** | **version basis defect** (`B7`) | **CRITICAL** | treat every P09 mechanism claim as version-18-source-only until re-verified |
| **2** | **`TH-F-02`** — the **v19 budget gate explicitly admits fixed, current and non-current asset types**, so the balance-sheet leg enters budget consumption on the shipping platform. Not Thai, not template-specific | **CRITICAL, open** | route the account-type semantics to P08; the highest-value open item is **which addons path each v19 server runs** — two candidate builds carry opposite filters |
| **3** | **TH-F-01 SUPERSEDED** — a v18-template defect **already fixed in the v19 template**. My prior claim that budget consumption zeroes on a Thai-chart install is **withdrawn** | **closed as a live risk** | do not carry the prior claim forward |
| **4** | **database denominator corrected** — 6 distinct database artefacts, 5 restored, **781 assets, 670 allocated**, all in one deployment. Prior rounds said "two", then "four" | FACT VERIFIED | use this population, not any earlier one |
| **5** | **negative-claim control failure, third instance** — a truncated listing, a template promoted to a deployment, a version mismatch. Rules `NC-8`, `NC-9`, `NC-10` issued | standard | adopt the protocol programme-wide |
| **6** | **event-type denominator** — 45 / 82 / 83 / 328 in the unit *file:line match*, four residue classes, **all lexical proxies** | corrected | never quote a count without its unit |
| **7** | **zeroing confirmed and measured**; **corrected bridge double-count path** confirmed source-only and unexercised; the two **can conceal each other** in one cost-centre figure | FACT VERIFIED / source-only | `AI-X-01`: every management record must carry the mechanism that produced it |
| **8** | **scope row re-opened** — cross-company mirroring keeps company-less shared axis values, so two companies' records partly cancel on one tenant-level object | **`P11 RECONCILIATION REQUIRED`** | P11 owns "who owns a tenant-level aggregate over company-scoped records" |

## 3. BLOCKERS AND VETOES

**7 open blockers** — 3 CRITICAL, 2 HIGH, 2 MEDIUM; **1 closed by evidence** this supplement. Full register in `S16`.

**Four vetoes**, all limited in scope: the implementation veto and the report-shadowing veto **upheld**; the balance-sheet-allocation veto **STRENGTHENED**; and **`AAS+-VETO-04` raised** — no P09 mechanism claim may be relied on as describing a running system until version-matched.

## 4. PRESERVED, UNADJUDICATED

`HOLD-AS-01` — **CONTRADICTION STRENGTHENED**, not settled. `DIS-09` — unchanged. Five AAS+ dissents preserved. **P09 does not adjudicate between parallel evidence tracks; that is Boss-level.**

## 5. WHAT P09 ASSERTS AND WHAT IT DOES NOT

**Asserts:** the measurements, the algebra, the version mismatch, the v19 gate text, the eligibility structure as read from v18 source.
**Does not assert:** any statutory position; any canonical account-type semantics; any Core Ledger truth; that any mechanism claim describes a running system; that any class B, C or D item is closed.

## CHECKPOINT
**`CP-P09S18` — COMPLETE — EVIDENCE VERIFIED.** Auto-continue.
