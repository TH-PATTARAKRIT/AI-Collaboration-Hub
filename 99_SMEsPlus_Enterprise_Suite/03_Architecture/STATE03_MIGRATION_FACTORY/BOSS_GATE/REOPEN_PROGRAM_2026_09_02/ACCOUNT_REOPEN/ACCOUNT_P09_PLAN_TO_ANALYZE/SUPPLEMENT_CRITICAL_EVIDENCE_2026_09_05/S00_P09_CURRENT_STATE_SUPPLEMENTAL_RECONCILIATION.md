# S00 — P09_CURRENT_STATE_SUPPLEMENTAL_RECONCILIATION

**Checkpoint:** `CP-P09S00` · **Layer:** 1 — clean-room.

---

## 1. BASELINE VERIFIED BEFORE USE

| Item | Verified |
|---|---|
| baseline commit `70f8d20253fe3095152336fc76a3293e72b5d5b9` | **exists**, is a commit object, message matches |
| working head | identical to baseline |
| remote head | identical to baseline — **published** |
| working tree | clean |
| branch | `research/account-p09-plan-to-analyze-2026-09-04-001` — continued, **not** re-created |

## 2. THE PROMPT'S OWN SUMMARY — RE-DERIVED, NOT TRUSTED

The directive instructs that its summary must not be relied on. Each material claim was re-checked against the authoritative artefacts.

| Prompt claim | Re-derived result |
|---|---|
| A — line eligibility by assignment alone | matches the package; **re-tested independently this round**, see `S04` |
| B — prior database conclusion wrong, defeated by an output limit | **confirmed**, and the population is rebuilt exhaustively in `S05` — it is **larger than the prior round recorded** |
| C — runtime proof holds, no write authority | **confirmed**, and the hold is **narrowed** in `S14` |
| D — event-type denominator materially corrected | **confirmed**; exact figures in `S07`, not taken from the prompt |
| E — one cross-company scope row reopened | **confirmed**, see `S12` |
| F/G — zeroing and double-counting confirmed | **confirmed**, classified in `S09`/`S10` |
| H/I — `HOLD-AS-01`, `DIS-09` preserved | **confirmed**; refreshed in `S13` |
| L — six author errors, zero self-caught | **confirmed** against the revision log |
| Q — TH-F-01 | **PARTLY CONTRADICTED — the largest single correction of this supplement**, see `S01`/`S02` |

## 3. THE SUPPLEMENT'S HEADLINE

**My own prior TH-F-01 claim is retracted, and a larger finding replaces it.**

| | |
|---|---|
| **retracted** | *"budget consumption nets to zero on a Thai-chart install."* The deployment measured holds **zero** accounts on the template's code range, types all ten of its accumulated-depreciation accounts correctly as balance-sheet, and holds **zero budgets** — so the claim is both unobserved and untestable there |
| **retained** | the template mistyping is real and is a **configuration trap**: a configurer following it has no correctly-typed fixed-asset account available |
| **replacing it** | **226,612 of 339,382 management records — 66.8 % — sit on balance-sheet general accounts** in that deployment. This needs no localization, no template, and no configuration to hold |

## 4. WHAT IS PRESERVED UNCHANGED

Every prior P09 round, the scope correction, the prior database evidence, the prior event sweep, `HOLD-AS-01`, `DIS-09`, all four prior AAS-03 challenges, all AAS+ dissents, `AAS+-VETO-03`, the PMO hold, and the full revision history. **Nothing is discarded. No completed work is repeated without material delta.**

## CHECKPOINT

**`CP-P09S00` — COMPLETE — EVIDENCE VERIFIED.** Baseline verified, prompt summary re-derived, headline correction identified. Auto-continue.
