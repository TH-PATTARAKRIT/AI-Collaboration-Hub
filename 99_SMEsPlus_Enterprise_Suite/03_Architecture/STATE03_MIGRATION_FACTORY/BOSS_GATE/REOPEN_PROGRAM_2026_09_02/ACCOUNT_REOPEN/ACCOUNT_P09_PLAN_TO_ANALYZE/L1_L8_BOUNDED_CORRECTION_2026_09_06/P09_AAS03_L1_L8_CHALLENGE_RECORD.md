# P09_AAS03_L1_L8_CHALLENGE_RECORD

**Prompt:** `SMEPLUS-26-09-06-P09-P2A-L1-L8-FINAL-BOUNDED-CORRECTION-005` · **PHASE S** · **AI EOS = OFF**
**Layer:** 1 — clean-room.

Four independent challenges on **changed surfaces only**, against a frozen surface. **Every material finding re-verified against source or package evidence before adoption.** Dissent preserved.

---

## 1. RESULT

| | |
|---|---|
| Findings returned | **35** — 10 / 9 / 7 / 9 |
| Re-verified before adoption | **35** |
| **Adopted** | **24** |
| Supported without correction | **11** |
| **Convergence** | **three of four** found the classifier/instrument mismatch independently; **two of four** found the exclusion-authority falsification |

---

## 2. THE THREE THAT CHANGED THE ROUND

### 2.1 The classifier published as ACCEPTED was not the classifier that produced the numbers

The re-specified K-3 control lived in a **second script that computes no residue**, while the instrument that produced every K-3 figure still carried the superseded control, still gated on it, and shipped `"k3_controls_ok": false`. Run as packaged it printed **`REJECTED`** against a register publishing **`ACCEPTED`**.

**By this round's own printed rule — *"REJECTED — result is not evidence"* — every L-8 figure and the whole L-4 denominator were emitted under a failing gate.**

**Fixed by merging the control into the producer and re-running.** Not asserted — executed.

### 2.2 The corrections were asserted in a new register and never applied to the text carrying the errors

**Nothing outside the new directory was edited.** So `ZERO touch a planning model` was still live in the prior round's Layer 2 evidence, and the consumer-facing handoff pack still carried `CO-02b` at the superseded status — *three* rows in three files, not the one row `L-6` demanded.

**This is the programme's own recorded defect class: a revision log is not a correction.** Both are now **struck in place**, with superseded wording retained beside them.

### 2.3 `L-4`'s exclusion authority is falsified by the package's own register

The stated authority was *"no current P09 claim is stated over the references-a-P09-model relation."* The prior round publishes, un-withdrawn, **193** and **170 / 52** over exactly that relation — and this round re-publishes it. **The reversal condition had already fired.**

**`L-4`'s assessment limb closes; its authority limb does not.** The 159 are assessed and disturb no ownership or extension count — and they are **not excludable**, because P09 itself publishes measurements over the relation they belong to.

---

## 3. ALSO ADOPTED

| # | Finding | Disposition |
|---|---|---|
| 4 | **P11 has published a branch** — at CORR3 — and its package **already consumes P09 artefacts by name**, pinning P09 at `5441f8d`. *"P11 has published no branch"* was a peer-state assertion never executed | **CONTRADICTED — withdrawn in all four places it appeared.** What survives on measurement: **P11 cites zero `CH-0x`/`CO-0x` identifiers**, so no peer closed on the withdrawn terminality |
| 5 | The `CH-09` enumeration is **21 occurrences across 11 files**, not 17/8; four lie **outside** the package directory; two counted rows contain no identifier at all; the token collides with three unrelated prefixed families | **CONTRADICTED — count and boundary corrected; the exposure verdict survives** |
| 6 | *"the distinctive phrases … absent"* is a **false negative**. The Phase S challenge record carries *"three outputs are terminal, cross no boundary, need no contract"* — **nearer verbatim than the paraphrase named as nearest** | **CONTRADICTED — and it strengthens the reconstruction's basis** |
| 7 | One of the 160 entered on `account.analytic.account.id` — **a dotted field path, not a model** | **CORRECTED — inclusion rule now requires a declared model; residue 160 → 159, outside 170 → 169** |
| 8 | The planning-table set omitted the split-wizard table | **CORRECTED — added.** Re-run with the corrected set and a widened SQL regex: **denominator 12, planning 1, delta 0** |
| 9 | The v2 negative control **drew its witness from the set defined by the property it tested** — it could not fail | **CORRECTED — v3 selects the witness independently** |
| 10 | The failure-control row was **narrated, not asserted** — the script computed no predicate for it | **CORRECTED — now asserted** |
| 11 | The line-based column of the L-8 comparison was a **hard-coded literal**; *"Identical"* compared against a remembered number | **CORRECTED — both columns now executed** |
| 12 | *"inside the package and checksummed"* — no checksum existed, and the producer read its denominator from the **scratchpad** | **CORRECTED — population packaged, paths made package-relative, checksum manifest added** |
| 13 | *"by construction"* over-claims the AST strip: annotated / augmented / tuple- / attribute-target declarations are unhandled | **NARROWED — enumerated and measured at zero on P09 models; safe by a measured property of this root, not by construction** |
| 14 | §0 recorded the pre-merge HEAD as the frozen value | **CORRECTED** |
| 15 | `CH-09` carried two live dispositions — *"deleted"* and *"tombstoned"* | **CORRECTED — the tombstone is authoritative; the other is marked superseded in place** |
| 16 | The `CO-02b` mechanism field still asserted present-tense consumption | **CORRECTED — *"present in the source of the declared root"*** |
| 17 | §10's *"no adjacent-domain internal researched … at the P09-owned files"* — the report-engine file is **outside** the K-1 population by this round's own measurement | **ADOPTED — the sentence is corrected below** |

**On finding 17.** The report-engine file is not P09-owned; P09 read it to classify a hit and to describe a mechanism. **The correct statement:** *P09 read one adjacent-domain file to the minimum depth needed to classify a K-2 hit and to name the mechanism it carries; it researched no adjacent-domain lifecycle, state machine or internal.* The earlier wording claimed more purity than the work supports.

---

## 4. SUPPORTED WITHOUT CORRECTION

| Item | Verification |
|---|---|
| **L-1's headline, 1 of 12** | source-verified at two raw-SQL sites; reproduced by three challengers; robust to a corrected table set **and** a widened SQL regex |
| **L-7's git claim** | independently verified: exactly one commit for that file; the row is already corrected at its first commit. **No committed original exists** — the claim is exact |
| **L-3** | the restated set-difference rule matches the instrument exactly, per class body; witness confirmed in source |
| **L-5 method counts** | 0 / 2 / 10 / 2 / 9 / 2 — reproduced exactly |
| **L-7 successors** | `CO-01` / `CO-02a` / `CO-03` match the prior pack word for word; naming them resolves an orphan rather than creating a position |
| **L-6 provenance** | *"first published in the targeted-correction round, status changed in the final-completion round"* — corroborated |
| instrument identity | all packaged artefacts byte-identical to the copies that produced them; re-running regenerates the results file byte-for-byte |
| **the eight items** | byte-identical to the package's authoritative list; **nothing invented or broadened**; `BD-01` confirmed untouched |
| localization | full sweep: **no statutory claim, no Thai matter, nothing to hold** |

---

## 5. DISSENT PRESERVED

| ID | Disagreement | Position |
|---|---|---|
| **DIS-L1** | Does the exposure verdict survive the false P11 premise? Two challengers say yes on measurement (no peer cites the identifier); one says the conclusion must be restated as **open**. | **Both recorded. The verdict is narrowed to what was measured** — *no peer package cites the `CH-09` identifier* — and the broader *"no external exposure"* is withdrawn. |
| **DIS-L2** | Is `L-4` closed? One holds the assessment sufficient; two hold the authority limb falsified. | **The challengers prevail. `L-4` is split: assessment closed, authority not.** |
| **DIS-L3** | Is the K-2 positive control adequate? One notes the witness **is** the single result file, so it cannot discriminate. | **ADOPTED as a limitation, not repaired: there is exactly one positive instance in this root, so an independent witness does not exist here.** Recorded rather than papered over. |
| **DIS-L4** | Is `LC-01`'s corroboration clause measured? | **UNRESOLVED — it names two rows but not which sentence of either is corroborated.** |

---

## 6. AUTHOR-ERROR LINEAGE

| Round | Errors | Self-caught |
|---|---|---|
| lineage before | 67 | 14 |
| **this round** | **14** | **2** |
| **total** | **81** | **16** |

**The shape of this round's fourteen:** three were *asserting a correction without applying it*; three were *controls that could not fail or were never asserted*; two were *comparisons against remembered numbers*; two were *peer-state claims never executed*; two were *unit/eligibility errors*; two were over-claims of construction or purity.

**The lesson this round adds:** *a correction round's own controls are the least-controlled thing in it.* K-1 was armoured, K-2 and K-3 were not; then the control written to fix that was itself written three times before it could fail honestly.
