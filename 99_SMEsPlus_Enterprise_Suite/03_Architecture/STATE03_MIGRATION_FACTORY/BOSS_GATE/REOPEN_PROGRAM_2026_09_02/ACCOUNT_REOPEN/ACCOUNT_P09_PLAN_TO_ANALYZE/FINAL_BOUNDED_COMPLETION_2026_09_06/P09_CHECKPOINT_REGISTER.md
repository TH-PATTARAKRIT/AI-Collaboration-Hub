# P09_CHECKPOINT_REGISTER — FINAL BOUNDED COMPLETION

**Prompt:** `SMEPLUS-26-09-06-P09-P2A-FINAL-BOUNDED-COMPLETION-004` · **PHASE S** · **AI EOS = OFF**
**Baseline:** `b7bf546` → frozen at **`a10c5ad`** · prior correction commit `cfe9e10`

---

## 1. CHECKPOINTS

| ID | Step | Status |
|---|---|---|
| `CP-P09F01` | record local HEAD; read authoritative remote; **classify the delta before proceeding** | **COMPLETE** — remote ahead by one prompt-only commit; no artefact touched |
| `CP-P09F02` | freeze the package; verify manifest integrity at freeze | **COMPLETE** — 121 files, 120 rows, **0 mismatches** |
| `CP-P09F03` | **K-1** rebuild the instrument; controls before evidence | **COMPLETE — INSTRUMENT ACCEPTED** (9 controls) |
| `CP-P09F04` | **K-2** raw-SQL bounded pass | **EXECUTED; ITS MATERIALITY RESULT WITHDRAWN** |
| `CP-P09F05` | **K-3** relational-reference bounded pass | **COMPLETE** — floors corrected; residue enumerated |
| `CP-P09F06` | **K-4** restate `CI-05b` at the report-cell unit | **COMPLETE**, one mechanism sentence narrowed |
| `CP-P09F07` | **K-5** `CO-02b` + `CH-09` tombstone | **COMPLETE WITH THREE DEFECTS RECORDED** |
| `CP-P09F08` | **K-6** `CH-06` per component | **COMPLETE; component (d) regressed and is corrected** |
| `CP-P09F09` | **K-7** generation basis | **COMPLETE — ESTABLISHED 18.0 Enterprise** after the author's hold was contradicted |
| `CP-P09F10` | **K-8** field-count reconciliation | **COMPLETE** — resolved against the author; two further rows corrected |
| `CP-P09F11` | freeze corrected surface; four changed-surface challenges | **COMPLETE — package frozen throughout; no edit while challenges ran** |
| `CP-P09F12` | re-verify every finding before adoption | **COMPLETE — 32 of 32 re-verified** |
| `CP-P09F13` | apply corrections; retain superseded wording | **COMPLETE** |
| `CP-P09F14` | **store instruments durably in Layer 2** | **COMPLETE** — closes the ephemeral-locator defect rather than only recording it |
| `CP-P09FFINAL` | commit, push, verify remote, update state | **COMPLETE — PUBLISHED `acf58d2`**; remote SHA read back identical; tree clean; both mandatory scans ZERO |

---

## 2. THE EIGHT ACTIONS — FINAL DISPOSITION

| # | Disposition |
|---|---|
| **K-1** | **CLOSED** — instrument accepted on 9 controls; population **52/29**, owning **10/3** reproduced independently by two experts, one from a scratch build |
| **K-2** | **DEFECTIVE** — pass executed and its counts stand; **its materiality result is withdrawn**, the predicate could not fire |
| **K-3** | **CLOSED** — 170/52 outside; residue **10 planning / 6 tests / 4 non-test**; survives an AST re-run with 0 false positives |
| **K-4** | **CLOSED** — unit correction sound; one mechanism sentence corrected |
| **K-5** | **CLOSED WITH DEFECTS** — three recorded: unacknowledged reversal, missing locator, no successor identifiers |
| **K-6** | **CLOSED WITH ONE REGRESSION CORRECTED** — component (d) contradicted this package's own `N-1` |
| **K-7** | **CLOSED IN P09'S FAVOUR** — generation **established: series 18.0, Enterprise**; deployment rungs remain held |
| **K-8** | **CLOSED** — plan line 16; two further rows corrected from file unit to model unit |

**Six closed, one closed in P09's favour after being wrongly held, one materially defective.**

---

## 3. TERMINAL STATE

> ### **TERMINAL B — P09 MATERIAL EVIDENCE-INTEGRITY DEFECT REMAINS — TARGETED CORRECTION REQUIRED**

**Why not A:** `K-2`'s materiality result was produced by a predicate that cannot fire, and `FC-01` is withdrawn. A material internal defect remains inside the declared root.

**Why not C:** the evidence is **not** unavailable. `K-7` proved the opposite — the marker was in-root all along. Every remaining defect is correctable inside the declared root.

**Why not D:** no domain-purity contradiction was found; purity remains **PRESERVED at file granularity**, with §10's blanket wording narrowed.

### Named remaining corrections — all bounded, all inside the declared root

| # | Correction |
|---|---|
| `L-1` | re-run K-2's materiality with a **table-name** predicate **and its own positive control**; republish the corrected classification |
| `L-2` | give **K-2 and K-3 control tables** of their own — every filter carries its own controls, not only the headline instrument |
| `L-3` | restate the `K-1` control row *"declare and inherit"* so it expresses the set-difference rule it actually implements |
| `L-4` | assess the **160 unassessed K-3 files** against non-planning completeness claims, or state the exclusion's authority |
| `L-5` | state what the **6 test files** assert before excluding them |
| `L-6` | reconcile the **two `CO-02b` rows** across two files to one row, one status |
| `L-7` | give `CH-09`'s withdrawn text a **locator**, name the **successor identifiers** for `CO-01` / `CO-02a`, and **enumerate inbound citations** |
| `L-8` | re-run K-3's declaration-stripping on an **AST** basis in the published artefact (already verified clean by challenge; the published instrument is still line-based) |

**No prior-round P09 finding is withdrawn by this round.** `AAS+-VETO-01/02/03` upheld unchanged; **`AAS+-VETO-04` NOT DISCHARGED** — see its disposition.
