# P09_AAS03_FINAL_CHALLENGE_RECORD

**Prompt:** `SMEPLUS-26-09-06-P09-P2A-FINAL-BOUNDED-COMPLETION-004` · **PHASE S** · **AI EOS = OFF**
**Layer:** 1 — clean-room.

Four bounded challenges against **changed surfaces only**, run on a **frozen** package. **Every finding re-verified against source before adoption.** Dissent preserved.

---

## 1. RESULT

| | |
|---|---|
| Findings returned | **32** — 11 / 6 / 8 / 7 |
| Re-verified against source | **32** |
| Adopted | **26** |
| Adopted **in P09's favour** | **1 — and it is the round's most consequential** |
| Supported without correction | **5** |
| Reporting formats obeyed (`SUPPORTED / CONTRADICTED / NARROWED / MISSING EVIDENCE` + file + material?) | **4 of 4** |

**The convergence:** three of four experts independently found the **same** defect — the K-2 materiality predicate — and each traced it to the same root cause: **K-1 carried nine controls; K-2 and K-3 carried none.**

---

## 2. THE TWO CONTRADICTIONS THAT CHANGED CONCLUSIONS

### 2.1 Against P09 — `K-2`'s headline is an artefact

The classifier compared **dotted model prefixes** with **underscored table names**. It returns zero on every possible input.

- Published: *"Of the 12 files outside the population, **ZERO** touch a planning model."*
- Corrected: **1 of 12** — and it is the file this round names elsewhere as a K-3 residue and documents as `CO-02b`'s mechanism.

> **The round published a fact and its negation in three places and closed `K-2` on the half that was never measured.** `FC-01` **WITHDRAWN**.

### 2.2 **In P09's favour — `K-7` was wrongly held**

Two markers **inside the declared root** that my probe set never tested:

| Marker | Declared root | Comparison root |
|---|---|---|
| translation-catalogue series header, per module | **18.0 / 18.0+e** | 19.0 / 19.0+e |
| in-root edition patch | **Enterprise** | — |

**The marker discriminates the two roots cleanly.** My claim that *"the only release marker found governs a sibling tree"* was **false**.

> ### **`K-7` CORRECTED: the source generation IS established — series 18.0, Enterprise edition.**
> **A challenger closed a hold the author had wrongly opened.** The deployment rungs — installed, configured, exercised, economically correct — remain held, because a translation header establishes a *source generation*, never a deployment.

---

## 3. ADOPTED FINDINGS

| # | Finding | Disposition |
|---|---|---|
| 1 | `K-2` materiality predicate cannot fire; true value 1, not 0 | **CONTRADICTED** — `FC-01` withdrawn |
| 2 | `K-7` generation **is** establishable in-root: 18.0 Enterprise | **CONTRADICTED — in P09's favour** |
| 3 | `CH-06.d` says the consumption carrier has *"no currency field"*; it **declares `currency_id`, drops its NOT-NULL, and writes NULL** — **a regression against `N-1`, this package's own evidence** | **CONTRADICTED** |
| 4 | `CH-06.b` — company **is** recoverable via a required parent link; and the real exposure is larger: **no record rule at all**, no company check on the account reference | **NARROWED — corrected to "unconstrained"** |
| 5 | `K-8` fixed one unit error and left two rows at the **file** unit under **model** labels — fact table 13 → **11 + 2**; second family 8 → **4 + 4** | **NARROWED — three rows were wrong, not one** |
| 6 | `FC-02` over-closes: **160 of the 170** were assessed against nothing | **NARROWED** |
| 7 | *"6 are test files"* used as a materiality criterion with **no stated basis**; two concern behaviour P09's own handoffs turn on | **NARROWED — exclusion needs authority** |
| 8 | `CO-02b` presented as *"(added)"*; it was **already published**, and its status changed without editing the earlier row — **two rows, two statuses, two files** | **CONTRADICTED** |
| 9 | `CH-09` tombstone **reverses** the prior *"deleted, not renumbered"* unacknowledged; no locator for the "verbatim" text; **no successor identifier** for the two crossings it creates | **CONTRADICTED** |
| 10 | The withdrawal creates outbound obligations with **no enumeration of who cited `CH-09`** | **MISSING EVIDENCE** |
| 11 | `K-1` control row *"declare and inherit → must classify EXTENDS"* is **mis-stated**; under it the plan line and header would leave the owning set. The rule is right; the control wording is not | **CONTRADICTED** |
| 12 | **`K-2` and `K-3` carry no controls at all**; K-3's declaration-stripping is line-based — the construct the AST rebuild existed to remove | **MISSING EVIDENCE** |
| 13 | `FC-05`'s universal *"no deployment status inferred anywhere"* is **violated twice in its own document** | **NARROWED** |
| 14 | Reproducibility locators pointed into a **session-scoped** directory, not the frozen package | **CONTRADICTED — now fixed; instruments stored in Layer 2** |
| 15 | `K-7`'s *"94 of first 400"* does not reproduce (**83**); full population **216 of 1,273** was available in one pass | **NARROWED** |
| 16 | `CI-05b`: the stored row's amount is a **running accumulation**, not a delta | **NARROWED — conclusion strengthened** |
| 17 | §10's blanket *"no adjacent-domain internal researched"* is too broad for a package that annexes a reporting-module file | **NARROWED** |
| 18 | Freeze note: 121 files / 120 rows, exclusion unnamed | **NARROWED — non-material; the manifest cannot checksum itself** |

---

## 4. SUPPORTED WITHOUT CORRECTION — INDEPENDENTLY REPRODUCED

| Item | Verification |
|---|---|
| **`K-1` population 52 / 29; owning 10 / 3; extending 42 / 27** | **reproduced exactly by two experts**, one from a **scratch-built** AST instrument |
| per-model extension counts 17 / 11 / 8 / 7 / 4 | reproduced exactly, **including both corrected mixin counts** |
| 13,515 files parsed, 0 unparseable | reproduced |
| **the seed set** | tested against **all 2,050** model strings in the root — 13 in-seed cover every declared analytic/budget model; **no gap**. The pre-18 budget model names do **not** exist in this root, consistent with the 18.0 finding |
| the ownership rule | holds in every case |
| `K-3` residue: 10 planning, 6 tests, 4 non-test | reproduced; **and K-3 re-run with an AST strip returns 0 false positives — it survives its own worst construction** |
| **`K-8` plan line = 16**, 17th a method-local | reproduced by AST |
| `K-4` / `FC-03` | *"the strongest item in the package"*; no smuggled model-level claim found |
| residual AST blind spots | enumerated and **measured at zero on P09 models** — real immunity, but **untested by the controls** |
| **localization** | full grep: **no statutory claim, no Thai claim, nothing requiring HOLD** |
| domain purity | no adjacent-domain internal in either changed file |

---

## 5. DISSENT PRESERVED

| ID | Disagreement | Position |
|---|---|---|
| **DIS-F1** | Does `FC-01`'s *conclusion* survive its broken evidence? Two experts say the exposure is already published under `CO-02b`, so the conclusion may stand; one says the sentence and its lesson are both unsupported. | **Both recorded. The finding is withdrawn either way** — a conclusion that happens to be right is not evidence. |
| **DIS-F2** | Is P09 entitled to issue *"RECOMMENDS AGAINST"* on a mechanism inside a module it does not own? One expert calls it a scope export; another verifies the classification as correct. | **UNRESOLVED.** The observation is P09's to record; the recommendation may not be P09's to set. Recorded, not settled. |
| **DIS-F3** | `K-3` count: 170 (author) vs 169 (expert), from stricter multi-line stripping. | **Both are floors; the residue is identical. No adjudication needed.** |
| **DIS-F4** | Is the second family a P09 planning object or a reporting artefact? | **Still UNRESOLVED**, carried from the prior round. |

---

## 6. AUTHOR-ERROR LINEAGE

| Round | Errors | Self-caught |
|---|---|---|
| lineage before this round | 54 | 12 |
| **this round** | **13** | **2** |
| **total** | **67** | **14** |

**Twelve of the sixty-seven are denominator or unit errors.** This round added two more — a filter that could not fire, and two field rows still at the file unit **inside the section written to eliminate unit conflation**.

**The pattern this round adds, stated plainly:** *the controls went where the last defect was, not where the next one would be.* K-1 was armoured with nine controls because K-1 was where the previous round failed. K-2 and K-3 were built the same afternoon, carried no controls at all, and that is exactly where the defect landed.
