# P08_RC05_OWNER_BOUNDED_CORRECTION_PROMPT

Status: **GENERATED — AWAIT BOSS AUTHORIZATION**
Owner: P08
Baseline: `e368d11da6f7e4973469ff5608d676ec2d13811c`
Parent verifier result: `05_RC05_P08_CHALLENGE.md`

## Objective
Preserve the independently reproduced zero-balance result while repairing population, namespace, and control-provenance carriers. No whole-P08 reset.

## Bounded items
### P08-RC05-C1 — four-DB balance propagation
Update `58_P08_P11_RECONCILIATION_BOUNDARY_HANDOFF.md` §1 item 1 / §8 from three frozen databases to the four RC-05 inputs. Add DB-T2 = **5 posted moves with lines / 14 posted lines**, zero at exact / 1e-7 / 1e-4 / 0.005 on computed and stored balance.

Do not call the four dumps the complete deployed estate unless completeness is independently established. Use `four frozen RC-05 database extracts` if deployment completeness remains unproven.

### P08-RC05-C2 — retire the old HO family at its carrier
Edit `25_P08_CORE_RECON_HANDOFF_PACK.md` itself so its `HO-01…HO-06` family is visibly **RETIRED / SUPERSEDED BY PRODUCER-QUALIFIED P08-HO IDs**. Preserve the historical rows as lineage; do not renumber P06.

Re-test every live P08 `HO` identifier for one producer/one row.

### P08-RC05-C3 — create real pre-execution control lineage
Do not backfill a claim that an earlier prediction was immutable. Instead:
1. publish a **new prediction/control file in its own commit**;
2. record its immutable Git SHA and content digest;
3. only after that commit exists, run the unchanged instrument;
4. publish the run in a later commit;
5. demonstrate the prediction commit predates the result commit.

If instrument code changes, freeze the code in the same pre-run commit and fresh-challenge the changed instrument.

### P08-RC05-C4 — bounded four-DB propagation sweep
Sweep only current P08 claims whose population premise is “all three deployed databases” and whose truth can change because DB-T2 is now in the RC-05 frozen population.

At minimum re-check:
- balance population in `58_`;
- `P08-HO-13` / destructive module install-state claims. DB-T2 independently shows `om_data_remove` installed; publish the four-input result or explicitly distinguish deployment sets.

Do not reopen unrelated 3-DB historical measurements unless they are currently presented as exhaustive/current.

### P08-RC05-C5 — manifest wording
Make the RC05 manifest self-exclusion explicit: substantive population excludes `MANIFEST.md` itself. No substantive hash omission was found.

## Mandatory fresh challenge
Fresh RC-05 must challenge only changed population/namespace/provenance surfaces plus any changed instrument. The already reproduced four-DB arithmetic need not be re-researched, but any newly published run must be independently re-executed.

## Prohibited
No P11 mutation · no adjacent-domain research · no Veto discharge · no Phase SA/A/B/C · no merge/release.

## Exit
`P08 RC05 BOUNDED CORRECTION PUBLISHED — READY FOR FRESH RC-05 DELTA CHALLENGE`.

No Evidence = No Progress. Never Skip Gate. Boss is sole Final Approver.