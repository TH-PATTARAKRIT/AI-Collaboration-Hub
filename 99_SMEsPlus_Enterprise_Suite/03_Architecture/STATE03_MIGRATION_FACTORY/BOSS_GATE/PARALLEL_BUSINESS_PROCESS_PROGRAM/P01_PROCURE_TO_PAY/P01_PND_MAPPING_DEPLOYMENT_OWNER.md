# P01 — PND MAPPING: DEPLOYED OWNER

Session: `SMEPLUS-26-09-05-…-EVIDENCE-VERSION-DEPLOYMENT-INTEGRITY-001`
Layer: **1.**
**P01 determines no statutory truth. Every question of which form is correct is `HOLD —
STATUTORY EVIDENCE REQUIRED` and belongs to P07.**

---

## 1. VERDICT

> ### `REFINED` — deployed owner **survives**; one limb of the prior position is **BROKEN**

---

## 2. WHAT SURVIVES

**The deployed owner is IMPLEMENTATION B** — the later-series copy, in which a corporate
counterparty maps to one specific form. Confirmed.

**The conflict between the two shipped copies is real** — same file, same two lines, opposite
mapping, no configuration switch.

---

## 3. WHAT IS BROKEN — AND IT REVERSES THE PRIOR CONCLUSION

The prior position held that **neither mapping governs**, because *"the operator picks the form"*
in the dominant creation route.

> **That limb is BROKEN for the later series.** The deployed wizard **auto-assigns the form** from
> the withholding account's tax tag. And a **third** coupling of counterparty type to form exists
> at the tax-domain layer.

**Three consistent decision points, all mapping a corporate counterparty to the same form.**

So in the later series the mapping is **not** operator-chosen — it is applied automatically, from
three places that agree with each other.

The operator-choice observation stands for the **series-16** deployment, where the distribution
across both forms was measured.

---

## 4. THE FINDING THAT MATTERS MOST — AND IT IS NEW

> **The statutory export reads neither mapping.**

It uses the tax-tag route only, and that route exists in **only one** of the shipped copies.

| Artefact | Which mapping decides it |
|---|---|
| The printed certificate | the conflicting mapping |
| An internal listing | the conflicting mapping |
| **The statutory export** | **neither — a separate tag-based route** |

So the conflict P01 has tracked for two rounds **governs the document handed to the vendor and an
internal report — and does not govern the statutory filing at all.** The filing is decided
somewhere else entirely.

That materially re-ranks the finding: it is a **vendor-facing and internal-consistency** problem
first, and a filing problem only if the tag route is itself wrong — which is **P07's** question.

---

## 5. THE COUNTERPARTY-TYPE INPUT REMAINS CONTESTED

All three coupling points key off the **same boolean company flag**. Peer **P07** records that
legal personality must be a **typed attribute**, and that the boolean is the wrong instrument —
`BLOCKING` for P07.

**P01 accepts P07's position and does not contest it.** Three consistent mappings from a wrong
input are consistent, not correct.

---

## 6. DISPOSITION

| Axis | Status |
|---|---|
| Two copies conflict | **FACT VERIFIED** |
| Deployed owner | **IMPLEMENTATION B** |
| *"Neither mapping governs"* | **BROKEN for the later series; stands for series 16** |
| Statutory export | **reads neither mapping** — expert-reported, **not re-derived** |
| Which form is correct | **`HOLD — STATUTORY EVIDENCE REQUIRED` — P07** |
| Who should decide the form | **Boss / target design** |

## 7. WHAT P01 HANDS P07

1. The two conflicting automatic mappings, with the deployed one identified.
2. The finding that the later series applies the mapping automatically from **three** consistent
   points, not by operator choice.
3. The finding that the **statutory export reads none of them**.
4. P07's own position on the counterparty-type input, accepted and not contested.
5. **No statutory opinion whatsoever.**
