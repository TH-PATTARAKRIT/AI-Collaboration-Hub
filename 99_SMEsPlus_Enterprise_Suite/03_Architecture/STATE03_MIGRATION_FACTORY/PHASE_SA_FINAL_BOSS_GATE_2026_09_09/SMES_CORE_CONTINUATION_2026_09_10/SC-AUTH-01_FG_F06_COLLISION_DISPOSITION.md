# `SC-AUTH-01` — `FG-F-06` COLLISION DISPOSITION

# ONE BOSS AUTHORITY CARD. ONE QUESTION.

Session: `[SMEPLUS-26-09-10-ACC-PHASE-SA-SMECORE-CONT-001]` · branch `architecture/phase-sa-smes-core-final-scrub-2026-09-10-001` · head `2cfb57eb`
Boss: **SOLE FINAL APPROVER**

> **This card does NOT re-ask the Phase SA design question.** The Reading A / Reading B merits are settled
> as evidence at `SC-09`, `SC-10` §9 and `SC-04` §6 and are not re-litigated. **This asks only which
> existing Boss record controls.**

---

## THE QUESTION

> ## Which existing Boss record controls prospectively — and what is the status of the other?

```
FG-F-06 CONTROLLING RECORD = SC-BD-01 (Reading B)   |   SC-CONTRA-01 (Reading A)

STATUS OF THE OTHER        = SUPERSEDED
                           | HISTORICAL LINEAGE
                           | VOID FOR COLLISION
                           | OTHER EXPLICIT BOSS DISPOSITION
```

---

## THE TWO RECORDS

| | **`SC-BD-01`** | **`SC-CONTRA-01`** |
|---|---|---|
| Ruling | **`READING B` — `8C-001` does not bind this exit** | **`READING A` — it binds; proceed under `EC-07`** |
| Issued against head | `6d08bcc5` (02:18) | `7eeb5d8e` (02:24:53) |
| **Could it see the other?** | **No — proven ABSENT** | **No — proven ABSENT** |
| Explicitly supersedes the other? | **No** | **No — and it expressly disclaims priority** |
| **Downstream reliance** | **16 Boss rulings** (`SC-BD-02`…`-10`), `SC-11`, `SC-12`, the `04_` prompt, `SC-13`…`SC-21` | **none** — preparation only |

**Both were issued in good faith into contexts that did not contain the other. Neither is a correction of
the other. This is a concurrency artefact, not a change of mind** — and Boss's `05_` §0 single-session lock
has already closed the cause.

---

## WHAT EACH DISPOSITION COSTS

| If Boss selects | Immediately true | Immediately at risk |
|---|---|---|
| **`SC-BD-01` (Reading B) controls** | The 16 rulings stand on an open gate. Work resumes at that path's own `TERMINAL D`. `EC-04`/`EC-05`/`EC-07` are **not** exit criteria for Phase SA → Pre-Test | **Nothing is invalidated.** `SC-EC07-01`/`-02` become reserve preparation. **`RC-V-01` still bars implementation start** — independent review does not leave the programme |
| **`SC-CONTRA-01` (Reading A) controls** | `EC-01`…`EC-08` become live exit criteria. `EC-05` must close first; then `B-7` is appointed and `EC-07` opens against a re-frozen baseline | **The 16 rulings' gate precondition fails.** Boss must also state what becomes of them — **SMEs Core does not presume they are void, and does not presume they are safe.** And **`EC-04` still does not pass** (`SC-26`) |

---

## THE ONE THING THAT DOES NOT CHANGE EITHER WAY

> **`EC-04` is unresolved on the evidence, and `SC-26` finds the constitution internally inconsistent about
> whether it even applies here.** The word **`phase`** occurs **exactly once** in the 257-line constitution
> — in §4 — while §11, the clause the constitution itself labels *"Project-Wide Constitutional Rule …
> Effective immediately"*, restates that same Module rule using **`STATE`** and **drops `phase`**.
>
> **That finding weakens Reading A's §4 ground**, and it is published because it is true, not because of
> which way it points. **If Boss selects Reading A, `SC-26`'s constitutional clarification card should be
> answered in the same act** — otherwise `EC-04` blocks the gate on a clause whose reach is disputed.

---

## WHAT SMEs CORE RECOMMENDS

> ### On the collision itself: **NO RECOMMENDATION. `NO AUTHORITY TO SELF-SELECT.`**

**Why none is offered.** Every available tiebreaker is one the `05_` prompt forbids: recency (§0.4, §3),
strictness (§3), gate-opening convenience (§3), and downstream weight — which measures cost, not authority.
**`CF-D-01` reserves the question: *"only Boss may state what a Boss ruling covers."***

**Two procedural observations, offered because they cost nothing on either branch:**

1. **`B-7` is worth appointing under either disposition.** Gate-blocking under Reading A; valuable and
   non-blocking under Reading B; and `RC-V-01` requires an independent check before implementation start
   **regardless of `FG-F-06`**. `Q-BOSS-02` §2 already names an eligibility class (`SC-27` §3).
2. **Whichever record Boss selects, naming the other's status explicitly matters more than which is
   chosen** — an unlabelled contradiction keeps `EC-05` open, and `EC-05` is what stops any `EC-07`
   sequence from starting cleanly.

---

## EVIDENCE

`SC-24` — the nine-point reconciliation, the supersession search with its firing control, and the intent
evidence found on both sides · `SC-23` §3 — the symmetric-blindness proof · `SC-22` §2 — the collision
timeline · `SC-25` — why `EC-05` cannot close without this answer · `SC-26` — the `EC-04` finding above ·
`SC-BD-01` and `SC-CONTRA-01`, both **byte-identical and unmodified**.

---

**No `PASS` is declared. Phase SA is not closed. No veto is discharged. Structurally independent passes: `0`.**

No Evidence = No Progress. Never Skip Gate. Boss is the sole Final Approver.
