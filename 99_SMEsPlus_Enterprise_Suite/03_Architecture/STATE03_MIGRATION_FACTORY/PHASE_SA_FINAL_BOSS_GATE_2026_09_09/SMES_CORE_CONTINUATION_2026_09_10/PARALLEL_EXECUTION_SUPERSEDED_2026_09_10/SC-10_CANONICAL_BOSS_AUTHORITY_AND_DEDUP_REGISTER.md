# SC-10 — CANONICAL BOSS AUTHORITY AND DE-DUPLICATION REGISTER

## CP-SA-SC-100 — CANONICAL DECISION POPULATION LOCKED

Session: `[SMEPLUS-26-09-10-ACC-PHASE-SA-SMECORE-CONT-001]`
Execution host: `architecture/phase-sa-smes-core-final-scrub-2026-09-10-001`
Executing body: **SMEs CORE**
Boss: **SOLE FINAL APPROVER**

> **IDENTIFIER NOTE.** `SC-SMT-01`…`SC-SMT-11` are **historical finding identifiers** minted in `SC-03`
> (published at `2139088b`, before the nomenclature control). They are retained verbatim so citation
> lineage is not severed. **`LEGACY NAME — CURRENT BODY = SMEs CORE`.** They name findings, never the
> active body.

---

## 1. The count unit, declared before any number

> **UNIT: one atomic Boss *decision* — an item with alternatives, on which Boss's answer changes what is
> built or tested. NOT one identifier.**

**Why this matters more than once.** The parent Final Gate pack's `26` sliced `F5`'s single governance act
(`POH-D-06`) into subjects and counted the slices; this session's own first publication repeated it to keep
the delta comparable. **Both are corrected here on primary source** — `SA_CORR3_03` `POH-F-16`: *"The Boss
residue is **six items**, five of them small."*

**Boss acts are NOT decisions and are not in the decision count** (§5). **`FG-F-06` is NOT an `F1`–`F8`
atomic decision** — no evidence defines it as one; it is classified separately (§6).

---

## 2. Result

| Classification | Count |
|---|---:|
| `ALREADY RULED — REMOVE` | **0** |
| `SMEs CORE AUTHORITY — CLOSE BEFORE BOSS` | **1** |
| `PMO / DOCUMENT OWNER — CLOSE BEFORE BOSS` | **0** |
| `OTHER PROGRAMME — RELOCATE WITH POINTER` | **1** |
| `BOSS ACT — NOT A DECISION` | **5** |
| `BOSS SCOPE CLARIFICATION` | **1** |
| **`GENUINE BOSS DECISION`** | **23** |
| **Declared population** | **31** ✓ |

`23 + 1 + 0 + 1 + 5 + 1 + 0 = 31`.

**Derivation chain, each step sourced:**

`32` parent candidates → **`30`** re-derived (`AR-F-01`: `F5`'s governance act is one decision with two
subjects, not three; the register's own residue is six) → **`24`** decisions (`− 1` relocated, `− 5` acts)
→ **`23`** (`− 1` closed by SMEs Core authority, §4).

**Population declared:** the `30` re-derived atomic candidates **plus `FG-F-06`**, which no prior round
placed in a classified population at all. **Adding it is not an escalation** — it is classified as a scope
clarification and kept out of the decision count.

---

## 3. The canonical 23, by family

| Family | Atomic decision IDs | n |
|---|---|---:|
| **`F1`** COGS recognition and reversal basis | `JT-04`, `JT-05` | **2** |
| **`F2`** Commercial control-default policy | `XD1-P1`, `TV6-BOSS-01`, over-receipt tolerance default | **3** |
| **`F3`** Direct shipment — valuation | `XMC-D-01` *(conditional — arises only on the non-recommended branch)* | **1** |
| **`F4`** Cross-module contract scope and supply binding | `XMC-D-02`, `C2-D-01` | **2** |
| **`F5`** Manufacturing overhead governance | `POH-D-01`, `POH-D-02`, `POH-D-03`, `POH-D-04`, `POH-D-05`, `POH-D-06` | **6** |
| **`F6`** Cross-company visibility and commercial scope | `MTI-D-04`, `RC-D-03`, `RC-D-04`, `TV6-BOSS-02` | **4** |
| **`F7`** Authorization axis and configurable-record scope | `RC-D-01`, `RC-D-02`, `CF-D-01`, `CF-D-02` | **4** |
| **`F8`** Idempotency severity | `C-02` | **1** |
| **Total** | | **23** ✓ |

`2+3+1+2+6+4+4+1 = 23`, summed from the rows.

---

## 4. The one `SMEs CORE AUTHORITY — CLOSE BEFORE BOSS`, and the lever Boss keeps

### `C2-D-02` — where dropship cost lands, and by what identity it binds to the sale

| | |
|---|---|
| **Escalation ground (parent, and AR `RETAIN 2`)** | `SA_CORR2_02` §6: *"Both reference generations are `FACT VERIFIED` and they disagree. **Choosing between them is a determination.**"* |
| **Why it closes** | **SMEsPlus is not obliged to choose between two vendor generations, and doing so would be the clean-room defect itself.** Two instruments already determine the answer, neither of them a vendor generation: **`XMC-C-C6`** (adopted SMEsPlus clause) — *"Every recognition of revenue produces **either** a cost recognition bound to the same identity, **or** an explicit, recorded determination that no cost arises"*, whose own closing sentence names and forbids the reference's shape; and **`BD-ACC-01`** (`CLOSED / BOSS APPROVED`) — *"Accounting Core owns the canonical Accounting Event Identity… Every accounting event is bounded by Tenant + Company context."* |
| **Determination** | The cost recognition **binds to the same canonical Accounting Event Identity as the revenue recognition**, owned by Accounting Core, Tenant + Company bounded |
| **Why the answer does not depend on `XMC-D-01`** | Under option (a) the cost binds through the movement chain's valuation fact; under option (b) directly. **`BD-ACC-01` makes the identity the binder either way.** Challenged and upheld at `SC-03` `SC-SMT-10` |
| **Why AR retained it** | **Not a disagreement on the merits — a time difference.** AR's `RETAIN 2` predates the `F3` bounded re-read; AR's own R2 records that re-read as owed to this track. It has since been performed (`SC-02`) |
| **What is NOT closed** | **The date.** Dropship cost timing is a special case of `JT-04` and stays in **`F1`**. It is not asked twice |

> ### `SC-BOSS-LEVER-01` — the reversal lever, handed to Boss explicitly
>
> **The master prompt forbids SMEs Core deciding a genuinely Boss-reserved policy election merely to
> shorten the list. This closure is offered against that test, not around it.**
>
> **If Boss judges that the binding identity is a policy election rather than a consequence of
> `BD-ACC-01` + `XMC-C-C6`, then `C2-D-02` is reinstated and the canonical count is `24`, not `23`.**
> Reinstating it requires one word from Boss and no further work from anyone. **The `F3` card at `SC-11`
> carries this lever on its face.**
>
> **This is the only item in the register whose classification a reasonable Boss could reverse**, and it
> is the only one where a single closure moves the headline count.

---

## 5. `BOSS ACT — NOT A DECISION` — five, kept out of the count

`B-7` · `C4-D-01` · `C4-D-02` · `AAS-V-02` discharge ratification · Thai user/domain panel commissioning.

**None has alternatives, a dissent, or a scenario-level effect that changes with the answer.** Each is a
single authorised act with one correct form. **Full register at `SC-12`.**

---

## 6. `BOSS SCOPE CLARIFICATION` — one, kept out of the count

**`FG-F-06` — does `SMEPLUS-DR-EXIT-8C-001` bind the Phase SA exit?**

**Not an `F1`–`F8` atomic decision**, and no evidence defines it as one. It is a question about **what a
Boss ruling covers**, governed by `CF-D-01`'s ground: *"only Boss may state what a Boss ruling covers."*
**Card at `SC-13`.**

---

## 7. The four `REMOVE` tests that returned zero — and why that is the honest result

| Test | Removed | Basis |
|---|---:|---|
| **`ALREADY RULED`** | **0 of 30** | Two independent executions agree. AR re-ran it **one ref per iteration over 189 of 189 refs** after a single multi-ref `git grep` returned a false zero *including for known-ruled identifiers* (`AR-I-01`). Candidate hits landed in **7 files, all read, all `READY FOR BOSS REVIEW` / `READY FOR BOSS DECISION` escalation packages** — documents that *ask*, not Boss acts. **A name-based instrument cannot tell a decision package from a decision; only the status field can** |
| **`PMO / DOCUMENT OWNER`** | **0** | No surviving item is owned by PMO or a document owner. The one PMO item in the programme — the mainline compliance act — **closed** (`SC-00`). `GAP-KC-01` (folder disposition) remains PMO-owned, is **not** a Boss decision, and is **not** escalated |
| **`OTHER PROGRAMME`** | **1** | Prepaid-wallet balance-sheet character and tax treatment → `ERPPLUS-152` `WS-09` / gate `G6`. **Re-checked this round: that session has published through `G3`; `G6` is not reached and the question is still undecided there.** The relocation holds and the item is **not** re-imported |
| **`SMEs CORE AUTHORITY`** | **1** | §4 |

> **`0 of 30` were wrongly escalated on authority — the third consecutive round to find this, on three
> differently-derived populations.** The de-duplication achieved across the two tracks is **structural**
> — a corrected count, one closure, one relocation, five items re-shaped as acts, one as a scope
> clarification — **not a discovery that Boss was being asked to decide things others could decide.**
>
> **Recorded because the opposite finding would have been the more flattering one**, and three
> independent derivations do not support it.

---

## 8. What SMEs Core deliberately did NOT close

Stated so the scrub can be audited for over-reach in the direction that shortens the list.

| Item | Could SMEs Core have argued it closed? | Why it was not |
|---|---|---|
| **`XMC-D-01`** (`F3`) | Yes — `XMC-F-03` and `BD-ACC-03A` both point to option (a) | Option (b) requires **a scope statement about `BD-ACC-03A`**, and *"only Boss may state what a Boss ruling covers."* **The argument that a route may not override a Product-Category policy was available and deliberately not used** |
| **`JT-05`** (`F1`) | Yes — `BD-ACC-01` says a reversal *references* the original event | The reference relationship is ruled; **the measurement is not**, and `09_JT05` §5 assigns the return-cost-basis rule **and the Average-costing reconciliation design** to **Boss** at primary text |
| **`C-02`** (`F8`) | Yes — CORR5 tried exactly this | **Two challengers independently reversed it.** Five instruments name the owner *"Boss directly"* |
| **`C2-D-01`** (`F4`) | Partly — its shortage-exit half **is** closed as a defect | The **binding strength** remains *"SMEs Core design position → Boss confirm"*. A confirmation is still Boss's |
| **`FG-F-06`** | Yes — Reading B is now materially better evidenced than either track had it | **Reading B is the reading that opens this gate.** Two tracks have now each narrowed Reading A and **neither adopted the conclusion the narrowings point toward** |

---

## 9. Checkpoint

> ## `CP-SA-SC-100 — CANONICAL DECISION POPULATION LOCKED`
> **Unit declared before the number · population **31**, closing on the disposition column ·
> **`23` genuine Boss decisions** · `1` closed by SMEs Core authority **with a reversal lever handed to
> Boss** (`SC-BOSS-LEVER-01`) · `1` relocated, re-checked at its own gate · `5` acts and `1` scope
> clarification held out of the count · `0` already ruled on two independently-executed instruments ·
> `0` new decisions originated · `5` items SMEs Core could have argued closed and deliberately did not.**

No Evidence = No Progress. Never Skip Gate. Exhaust Team Authority Before Boss Escalation —
and do not decide a Boss-reserved election to shorten the list.
Boss remains the sole Final Approver.
