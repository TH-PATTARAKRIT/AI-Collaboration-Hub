# SC-31 — SMEs CORE FINAL FALSIFICATION REGISTER

## CP-SA-SC-250 — SMEs CORE FINAL FALSIFICATION COMPLETE

Session: `[SMEPLUS-26-09-10-ACC-PHASE-SA-SMECORE-CONT-001]` · head consumed `2cfb57eb`
Executing body: **SMEs CORE** · Boss: **SOLE FINAL APPROVER**

> **`INTERNAL ADVERSARIAL SELF-CHALLENGE`. Not independent assurance, and not offered as any part of one.**
> Doctrine applied: *Falsify before Accept · Correct before Escalate · Truth over Pass.*

---

## 1. Result

| | |
|---|---|
| Classes run | **20 of 20** |
| Classes returning a finding | **4** |
| Findings, all corrected before publication | **4** |
| **Findings that WEAKEN this executor's own governing instruction** | **1** — `SC-F-15`, and it is the round's most consequential |
| **Instrument failures caught by their own controls** | **1 this round** (`SC-I-06`), **8 across the session** |
| Peer artefacts modified | **`0` of 21**, byte-verified |

---

## 2. The twenty classes

| # | Class | Method | Result |
|---:|---|---|---|
| **1** | Authority collision / supersession error | 9-point reconciliation per record; supersession search with a firing control | **CLEAN** — `0` supersessions found, `0` readings chosen. `SC-24` |
| **2** | Wrong denominator | population re-derived on **three** agreeing shapes | **CLEAN** — `23`; `F5 = 6` re-confirmed at `POH-F-16` |
| **3** | Secondary-source inheritance | every load-bearing claim re-read at primary source, not through a package | **1 FINDING** — §3.1 |
| **4** | Suppressed clause | checked every quotation for an omitted clause that changes meaning | **CLEAN** — and the round **adds** two previously-suppressed clauses (`POH-F-06`'s *"issuer and Boss"* and *"would not lift the veto"*) |
| **5** | False zero from dead instrumentation | positive control on every negative | **1 FINDING** — §3.2 (`SC-I-06`) |
| **6** | Overstated closure consequence | tested each "closed" against what it actually closes | **CLEAN** — `EC-05` explicitly **not** marked ready; `SC-29` marked **conditionally** canonical |
| **7** | Phase-boundary circularity | the whole subject of `SC-26` | **1 FINDING** — `SC-F-15`, §3.3 |
| **8** | Tolerance-zero bypass | swept for closure-shaped language | **CLEAN** — 6 hits, all negations (*"not evidence-closed"*, *"`0 of 3`"*) |
| **9** | Same-model independence as assurance | swept for independence claims | **CLEAN — `0`.** Peer exchange labelled `PEER CROSS-EXAMINATION`; **`0` subagents used** |
| **10** | Vendor implementation becoming SMEsPlus policy | re-checked the `F3` ruling's basis | **CLEAN** — `SC-BD-08` rests on `XMC-F-03` and `BD-ACC-03A`; the reference capability is **installed in `0` of 3 deployments** |
| **11** | Missing conditional route | re-ran the 6 routing rules | **CLEAN — 6 of 6 hold** |
| **12** | Wrong Inventory convergence | valuation facts under Product-Category policy; direct-shipment endpoints | **CLEAN** |
| **13** | Wrong Accounting convergence | cost binds to the same canonical event identity | **CLEAN** — `XMC-C-C6` + `BD-ACC-01` |
| **14** | Cross-tenant / company leakage | `MTI-D-04` ruled *no grant in v1*; `BD-ACC-02` untouched | **CLEAN** — and `CF3-P-04` **struck**, exception set empty and known |
| **15** | Retry / idempotency / reversal defect | `BD-ACC-01` unqualified: same-event retry must not duplicate | **CLEAN** — object ruled; `C-02` severity ruled at `SC-BD-09` |
| **16** | Unsupported statutory / compliance claim | swept for statutory assertions | **CLEAN — `0`.** Every Thai item carries `HOLD / EVIDENCE REQUIRED`; **`POH-D-02` explicitly withheld from Boss** on that ground |
| **17** | Veto discharge without issuer authority | byte-check + wording sweep | **CLEAN — `6` in force, `0` discharged, `0` self-discharged.** `SC-28` §3 **strengthens** the limb-2 blocker rather than easing it |
| **18** | Boss question SMEs Core could have closed | re-tested every open item for closability | **CLEAN — `0`.** `RC-D-03`/`RC-D-04` were closed by SMEs Core at `SC-14`/`SC-15`; the two escalations are a **records collision** and a **constitutional ambiguity**, neither closable by analysis |
| **19** | Progress-biased reclassification | five-test control | **CLEAN — `0`**; the one available upgrade (`E2E-04`) was **declined** |
| **20** | Any new material finding class | — | **1 FINDING** — `SC-F-15` is a new class: *an instrument contradicting itself on its own scope*, §3.3 |

---

## 3. The four findings

### 3.1 `SC-F-14` — a material omission in this executor's own appointment card *(class 3)*

`SC-EC07-02` §2 said no candidate was *"named, suggested, or hinted."* True of this executor's act — and it
**omitted that `Q-BOSS-02` §2 already names an eligibility class at Boss's hand**, including that
**Claude Opus 5 is `NOT ELIGIBLE`** where it authored the work under review.

**Cause: the card was written from the *concept* of `Q-BOSS-02` rather than from its *text*.** Secondary-source
inheritance, committed while writing the file whose purpose is to enable Boss's act.
**Corrected at `SC-27` §3**, with the §2-scope qualification stated.

### 3.2 `SC-I-06` — a dead instrument, caught by a control *(class 5)*

A multi-file sweep passed an unquoted shell variable to `grep`; the shell treated ten paths as **one
filename** and the tool returned *"File name too long"* and **`distinct: 0`**. **Read as a result, that zero
would have certified every citation in ten files without examining any of them.**

**Caught because the run printed the warning beside the zero.** Re-run per file in a loop: **9 distinct
object ids, all resolve, `deadbeef` correctly does not.**

> **Eight instrument failures have now been caught by controls across this session** — a dead alternation
> pattern, a substring/word-boundary pair, a regex complexity error read as a zero, a path-only output read
> as hits, a `grep -o` piped to a filter needing the line, a name-filter that excluded a file for containing
> the excluded token, this one, and the peer track's two. **Every one would have produced a plausible,
> publishable, wrong result.**

### 3.3 `SC-F-15` — the constitution contradicts itself on the advancement boundary *(classes 7 and 20)*

**§4:** *"No Module may advance to its next controlled **phase** or State…"*
**§11**, *"Effective immediately"*, restating the same Module rule: *"NO MODULE MAY ADVANCE TO **THE NEXT
CONTROLLED STATE**…"*

**Measured: `phase` occurs `1` time in 257 lines; `state` occurs `22`.** §2's application points, §3's own
sentence and §5's title all use **Module/State**.

> **This is the round's most consequential finding, and it cuts against the instruction this executor is
> operating under.** `FG-F-06 = Reading A` was partly grounded on §4's *"next controlled phase"* — **the
> constitution's sole occurrence of that word**, in a clause its own operative rule restates without it.
>
> **SMEs Core withdraws the strength previously attributed to that ground** (`SC-26` §3.1). It is not
> eliminated; it is materially weaker than presented. **Published because it is true, not because of which
> way it points.**

### 3.4 `SC-D-01` — a circularity inside the limb-2 path *(class 17, found while trying to falsify a peer's work)*

Limb 2 cannot be discharged in either direction **as written**; its **re-wording** is reserved to AAS+; its
**discharge** is an SMEs Core proof obligation. **SMEs Core cannot begin its obligation until AAS+ acts.**
**Recorded at `SC-28` §3 — it makes the handoff larger, not smaller.**

---

## 4. What falsification did NOT overturn

| Attacked | Survived |
|---|---|
| `23` / `F5 = 6` | **YES** — three shapes, primary-source re-confirmation |
| `0 of 3` tolerance-zero closed | **YES** — two shapes, firing control |
| `6` vetoes / `0` discharged | **YES** — both executions independently |
| The symmetric-blindness proof | **YES** — `git cat-file -e` both directions |
| `0` explicit supersessions | **YES** — firing positive control |
| Cross-module convergence, 6 of 6 | **YES** |

---

## 5. Checkpoint

> ## `CP-SA-SC-250 — SMEs CORE FINAL FALSIFICATION COMPLETE`
> **20 of 20 classes run · `4` findings, **`4` corrected before publication** · `1` finding weakens this
> executor's own governing instruction and is published anyway (`SC-F-15`) · `1` new material finding class ·
> `1` instrument failure caught by its control (`SC-I-06`), **`8` across the session** · `0` false
> independence · `0` tolerance-zero bypass · `0` progress-biased reclassification · `0` Boss questions
> SMEs Core could have closed · **`0` of `21` peer artefacts modified**, byte-verified.**

No Evidence = No Progress. Never Skip Gate. Falsify before Accept. Truth over Pass.
Boss remains the sole Final Approver.
