# SC-21 — REMAINING BOSS DECISION PACK

## `CP-SA-SC-150 — REMAINING BOSS DECISION PACK READY`

Session `[SMEPLUS-26-09-10-ACC-PHASE-SA-SMECORE-CONT-001]` · Head consumed `ce987553`
**Boss is the SOLE FINAL APPROVER. Nothing here is a ruling, a `PASS`, or a Phase SA closure.**

**Prompt §10's three preconditions, tested:** `RC-D-03` recommendation-complete (`SC-14`) · `RC-D-04`
recommendation-complete (`SC-15`) · both challenge-complete (`SC-16`, 0 returns open) · **Category 3 = 0**
(`SC-17`). **All met.**

---

## 0. ⚠ Read this before the six cards

> **The six decisions below are ready. Whether they may be ruled now is not SMEs Core's to say.**

`EC-05` is **OPEN**: two opposed Boss rulings on `FG-F-06` stand on this branch — `SC-BD-01` (**Reading B**,
on which **16 rulings already rest**) and `SC-CONTRA-01` (**Reading A**, under which the gate was not open).
The exit constitution: *"**No material contradiction may remain merely as an unresolved difference of
opinion.**"* `CF-D-01`: *"only Boss may state what a Boss ruling covers."*

**Under Reading B** these six may be ruled now, and doing so takes the ruled count from 16 to 22.
**Under Reading A** ruling them would extend an exposure from 16 decisions to 22.

> **This pack is therefore PREPARED AND PRESENTED, NOT OPENED FOR RULING.**
> **SMEs Core does not re-ask `FG-F-06`** — the underlying scope question is not re-put. What is put is a
> different and narrower question: **which of Boss's two records governs.** That is a disposition, and only
> Boss can make it.

---

## 1. `RC-D-03` — Private Company escalation criteria

| | |
|---|---|
| **Decision** | What objectively qualifies a tenant to leave the `SHARED SaaS POOL` for a dedicated (Private Company) operating model? |
| **SMEs Core recommendation** | **Objective-threshold model**, authored **as a pool-exit condition only** (`CF-I-08` forbids authoring behaviour *inside* the topology). Closed v1 trigger set: **regulatory mandate for physical isolation · data-residency obligation · unmeetable security/attestation obligation · unmeetable quantified performance/SLA class** — each **externally attributable**, never a preference. **Escalation is a platform decision**, recorded with trigger class, attribution and authority |
| **The `NEVER` list, ruled with the same force** | ① a wish for cross-company statutory posting/offsetting/settlement/aggregation/filing — **`BD-ACC-02` binds in every topology** ② a wish for cross-company visibility — `MTI-D-04` ruled **per-company export** ③ commercial willingness to pay alone ④ customer-specific customisation of platform-owned core (`MTI-D-03`) ⑤ tenant size/revenue/headcount alone |
| **Best alternative** | Keep escalation discretionary. **Consequence:** `CF-V-02` limb 2 — *"escalation criteria **stated**"* — would not be satisfied, so the veto stays active on that limb |
| **Challenge** | `SC-16` — 7 challenges, **3 accepted and corrected**. `SC-CH-02` added the `CF-I-08` distinction **without which the recommendation would itself have breached an active prohibition**; `SC-CH-04` added `NEVER` #1, closing a route to buy a ruling Boss has closed |
| **Consequence of the recommendation** | The isolation boundary stops being negotiable per customer; escalation is an exit, not a product variant, so no dual-topology branching enters the core |
| **Consequence of the alternative** | Every escalation defensible individually, the boundary indefensible in aggregate |
| **Gates** | **Not Pre-Test entry.** It is on **`CF-V-02` limb 2's** release path |
| **What SMEs Core cannot supply** | The **threshold values** inside the four classes — owners named at `SC-14` §9 (Thai statutory/legal · legal/commercial · operations · security/compliance). One is structurally unobtainable now: the pool's measured SLA ceiling needs a built pool and **`RC-V-01` bars implementation start**. **A business input, not a research gap** |
| **Atomic ID** | `RC-D-03` |

## 2. `RC-D-04` — mapping-layer ownership

| | |
|---|---|
| **Decision** | Who owns the cross-company read mapping/provenance layer (`CF-XCR-GAP-01`)? |
| **Scope, settled first** | **`CF-XCR-GAP-01`** — the class `SA_FINAL_03` L170 calls *"a required mapping/provenance relationship class … deliberately un-numbered"*. **NOT** the 12-boundary handoff contract (ruled at `SC-BD-02`) and **not** migration mapping |
| **SMEs Core recommendation** | **Split ownership, pre-assigned now, layer dormant in v1.** Semantics → **Accounting Core** (`BD-ACC-01` canonical identity); mechanics (store, version, publish, activate, retire) → **SaaS Foundation**; audit → Internal Control; consumption → Posting/reporting **read-only**. A requesting boundary may **propose only**. Tenant-scoped, Company-qualified, **append-only and versioned**, activation/retirement as **events not edits**. **v1 status: `SPECIFIED — DORMANT — NO SUBJECT`** |
| **The prohibition to rule** | **A mapping layer may never become a source of truth.** If a mapping can change what a figure *is*, `BD-ACC-01`'s canonical identity is no longer canonical |
| **Why it is not dissolved by `MTI-D-04`** | `SA_FINAL_02` L75: *"`RC-D-04` is **gated on `MTI-D-04`**"* — and Boss declined the grant. **But `CF-V-02` limb 1 requires three things**: *"`MTI-D-04` + **`RC-D-04` ruled and the mapping layer specified**"* (`SA_CORR5_09` L37). A dissolved `RC-D-04` leaves limb 1 **permanently unliftable** |
| **Best alternative** | Defer ownership with the grant (*"not applicable in v1"*). **Consequence:** limb 1 has no closure path, and a future grant arrives with an unowned layer — the `MTA-11` degradation pattern |
| **Challenge** | `SC-16` — 7 challenges, **3 accepted**. `SC-CH-09` **reversed the drafting instinct to dissolve it** and produced the dormant status; `SC-CH-10` kept `CF-XCR-GAP-01` **un-numbered**, preserving the isolation-completeness claim |
| **Gates** | **Not Pre-Test entry.** It is on **`CF-V-02` limb 1's** release path |
| **Atomic ID** | `RC-D-04` |

## 3. `POH-D-01` — declared departure from `BD-04`

**Recommendation:** confirm the departure, **declared per cost class** rather than granted generally.
**Alternative:** hold `BD-04` strictly, or grant a general exemption. **No Thai dependency. Rulable now.**
**Blocks:** later implementation only. `SC-18`.

## 4. `POH-D-03` — is `SETUP` time productive?

**Recommendation: productive.** Setup is a necessary condition of the output it precedes; excluding it pushes
a genuine production cost into the period as if it were idleness — the inverse of the failure `BLK-07` §6
rejected. **No Thai dependency identified — `NOT FOUND IN SEARCHED SCOPE`, not asserted absent.**
**Rulable now.** `SC-18`.

## 5. `POH-D-04` — are `IDLE` and `NO_DEMAND` one cause or two?

**Recommendation: two.** Different owners, different management responses — `IDLE` is operations,
`NO_DEMAND` is commercial. Collapsing them destroys the distinction at the point of measurement and no later
analysis recovers it. **`BD-02` already closes where unabsorbed overhead *goes*; this decides only how it is
*explained*.** **Rulable now.** `SC-18`.

## 6. `POH-D-05` — who owns the normal-capacity figure and its review cadence?

**Recommendation:** **Manufacturing/Operations proposes; Accounting Core approves** — the figure is an
absorption denominator with statutory consequences, so the body owning statutory posting approves it.
**Fixed periodic review, recorded basis per revision, revision as an event not an edit** — because
`MTA-11`'s pattern applies here too and **no cadence is designed anywhere in the corpus**.
**Indirect Thai dependency** (the cap's strength under `HOLD`) **does not block ownership and cadence.**
**Rulable now.** `SC-18`.

---

## 7. Deliberately OUTSIDE this pack

| Item | Why |
|---|---|
| **`POH-D-02`** — straight-line vs units-of-production | **Tax consequences unresearched; over-absorption cap strength under statutory `HOLD`** (`SC-SMT-11`). **The Thai panel is commissioned, NOT convened — no result exists and none is fabricated** (`SC-20` §3). **Recommendation deliberately WITHHELD** |
| The **16 ruled** decisions | Ruled at `SC-BD-02`…`SC-BD-09`. **Not re-asked** |
| **`FG-F-06`** | **Not re-asked.** Only the disposition between two records is put — §0 and §8 |
| `BOSS-ROUTE-01` · `BD-ACC-01/02/03A/03B` · `MTI-D-01/-02/-03` · `Q-BOSS-02` · `BD-02` · `BD-04` · `UAE-29` | Closed; **not re-asked absent material delta** |
| **`AAS-V-02` ratification** | Boss withheld it at `SC-BD-10`; it returns at his choosing, not by re-asking |
| **`POH-D-06` AAS+ concurrence** | **AAS+ authority**, not a Boss decision. Handoff at `SC-19` |

## 8. What SMEs Core asks

**Two authority inputs, neither of them team work:**

1. > **`EC-05` DISPOSITION — which record governs, `SC-BD-01` (Reading B) or `SC-CONTRA-01` (Reading A)?**
   > And, whichever governs, **what becomes of the 16 rulings already recorded** — SMEs Core does not
   > presume they are void and does not presume they are safe.
2. **AAS+ concurrence on `POH-D-06`** (`SC-19`) — issuer authority, outstanding.

**On the `EC-05` disposition, the six decisions above can be ruled immediately.** They are
recommendation-complete, challenge-complete, and independent in content of `FG-F-06`.

## 9. Checkpoint

> ## `CP-SA-SC-150 — REMAINING BOSS DECISION PACK READY, NOT OPENED`
> **6 decisions recommendation-complete and challenge-complete · `POH-D-02` deliberately excluded on
> evidence · Category 3 = `0` · 16 ruled and not re-asked · `FG-F-06` not re-asked · `EC-05` OPEN and
> Boss-owned · vetoes 6 in force / 0 discharged · independent passes `0`, none claimed · Phase SA NOT
> closed.**
