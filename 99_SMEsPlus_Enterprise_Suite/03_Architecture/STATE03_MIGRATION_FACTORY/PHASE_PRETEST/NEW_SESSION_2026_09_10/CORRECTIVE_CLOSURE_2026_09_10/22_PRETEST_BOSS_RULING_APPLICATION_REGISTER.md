# 22 — BOSS RULING APPLICATION REGISTER

## `CHECKPOINT G — 11 RULINGS APPLIED · 0 UPGRADED WITHOUT EVIDENCE`

Session: `[SMEPLUS-26-09-10-PHASE-PRETEST-BOSS-RESOLUTION-001]`
Branch head consumed: **`88003079`** · Boss: **SOLE FINAL APPROVER**
Ruling received: `2026-09-10` — SMEsPlus PHASE PRE-TEST BOSS CONSOLIDATED RULING

> **`0` rulings converted into runtime proof. `EC-04` `0/3`, `EC-07` `0/2` and `0 of 48` are UNCHANGED.**

---

## 1. `B4′` — twelve-boundary set **DECLARED**

| | |
|---|---|
| **Boss text** | *"DECLARE the proposed 12 explicitly named boundaries as the canonical Boss-declared closed denominator. **This is a NEW Boss authority declaration; it is NOT treated as inherited authority from `PT11-P-01`.**"* |
| Affected finding | `PT04-F-01` · `CC-F-02` α |
| Affected denominator | **boundary set: `NOT PROVABLE` → `12`, DECLARED** |
| Affected register | `02_` §2 · `PT-04` |
| Before | `HOLD — CANONICAL SET NOT PROVABLE`; `0` members enumerated by authority |
| **After** | **`12` members, named, on NEW Boss authority** |

**The canonical set, as declared:** `1` Sales→Inventory · `2` Sales→Manufacturing (make required) ·
`3` Sales→Purchase (dropship/buy/MTO) · `4` Manufacturing→Inventory · `5` Inventory→Accounting ·
`6` Sales→AR/Accounting · `7` Purchase→AP/Accounting · `8` Payment→Bank/Accounting · `9` Asset→Accounting ·
`10` Expense→Accounting · `11` Tax→Accounting/reporting · `12` Close→required subledgers/control sources.

### 1.1 `CC-F-13` — a NEW obligation the declaration creates

**Boss declared `12` **classes**. Two other registers remain at their own granularity:
`XMC-H-01`…`XMC-H-18` (`18` handoff rows) and `SA_CORR4_02` §5 (`10` contract rows).**

> **The declaration fixes the class denominator and does NOT map the `18` and the `10` onto it.**
> **`12 ↔ 18 ↔ 10` is now derivable and has not been derived.** Until it is, the per-boundary
> applicability declaration (`SC-11` §6 #5) can name its `12` targets but cannot say **which handoff rows
> each target covers**.

**Status:** **NEW SMEs Core obligation — `CORE-04`, OPEN.** Not a Boss item. Recorded rather than absorbed.

---

## 2. `B3′` — veto count **RATIFIED = 7**

| | |
|---|---|
| **Boss text** | *"RATIFY `CLASS C`. Canonical veto count = `7`. **This establishes membership/count only. No veto is discharged by this ruling.**"* |
| Before | `6` recorded · `1` unclassified in-force veto · `CC-F-06` contradiction |
| **After** | **`7` canonical · `0` discharged · `CC-F-06` RESOLVED** |
| Affected | `07_` · `PT07-F-01` · `SC-60`'s *"six vetoes"* carry-forward |

**`SC-60` states *"six vetoes active"*. That figure is now superseded by this ruling** — recorded here, and
`SC-60` itself is **not** modified (it is Boss's own record and remains Audit Lineage).

---

## 3. `CC-D-01` — classification **RULED**

| | |
|---|---|
| **(a)** | **OPTION 3** — KIND governs **admissibility**; CATEGORY governs **measurement**; *"a deterministic refusal rule is **mandatory** where CATEGORY requires a policy that KIND cannot legally/semantically use"* |
| **(b)** | **OPTION B** — Service carries *"an explicit recorded determination that no cost recognition arises where the applicable rule produces no recognized cost"* |
| **Boss constraint carried** | *"**`IR-17` partial evidence remains open and must not be converted to `PASS` by this ruling.**"* |

| Before | After |
|---|---|
| tie-break **undefined**; `BD-ACC-01` silent for services | **deterministic precedence ruled**; Service satisfies `ND-09` via its **second branch** |
| `2 of 6` service proofs undefined | **`0 of 6` undefined** — accounting treatment and classification authority both ruled |
| `IR-17` `PARTIAL` | **`IR-17` `PARTIAL` — UNCHANGED, explicitly** |

**Affected:** `X-18` · `E2E-08` · `PT-S-01` expected values · FD blocker 5 · `06_`.
**The refusal rule is a NEW Functional Design build obligation**, created by the ruling.

---

## 4. `B1` — six decisions **APPROVED**

| # | Decision | Applied |
|---:|---|---|
| 1 | `RC-D-03` | Model (a): objective-threshold · NEVER-list binding · **4** externally-attributable trigger classes · **closed for v1** |
| 2 | `RC-D-04` | Model (a): split ownership · pre-assigned · **dormant in v1** |
| 3 | **`POH-D-01`** | **APPROVED — impact applied to `X-16` and `X-17`** (§4.1) |
| 4 | `POH-D-03` | approved as recommended |
| 5 | `POH-D-04` | approved as recommended |
| 6 | `POH-D-05` | approved as recommended |

**`POH-D-02` — EXCLUDED. *"Thai statutory authority is still required."*** Unchanged, `PTE-4`.

**Open Boss decisions: `7` → `1`** (`POH-D-02`, withheld).

### 4.1 `X-16` / `X-17` — the ruling applied, and they do **NOT** become writable

**`B-6`, the gate on rows `16`/`17`, is a bundle:** `BLK-07`/`BLK-08` restatement · **veto limb 2** ·
`POH-D-01` · `POH-D-02` · `POH-D-06`.

| Component | Status after this ruling |
|---|---|
| `POH-D-06` | **RULED** (`SC-BD-06`) |
| **`POH-D-01`** | **RULED — by this ruling** |
| **`POH-D-02`** | **WITHHELD — Thai statutory** |
| **veto limb 2** | **OUTSTANDING — AAS+ issuer authority; *"cannot be discharged in either direction as written"*** |

> **`X-16` and `X-17` remain `GATED`.** `POH-D-01` was **one of four** bundle components; two remain, and
> **one of them cannot be discharged in either direction until AAS+ re-words it.**
>
> **This is the ruling most likely to be read as unblocking those scenarios. It does not.** Recording them
> as writable would be exactly the unevidenced upgrade the ruling's own closing prohibitions forbid.

**Readiness split: `19 / 2 / 1` — UNCHANGED.**

---

## 5. `B6` — `PTX-01`…`PTX-11` **ADOPTED**

| | |
|---|---|
| **Boss text** | *"ADOPT … Canonical denominator = `11`. **Do not double-count `RT-E15`. Adoption creates the control set only; it does not satisfy any `PTX` control.**"* |
| Before | constituted, **not adopted** |
| **After** | **ADOPTED — canonical Pre-Test exit-control set, denominator `11`** |
| **Satisfied** | **`0 of 11` — UNCHANGED** |

**The three states `08_` §4 kept apart remain apart:** instruction **discharged** · control set **adopted** ·
**`0 of 11` satisfied**.

---

## 6. `B2` — `AAS-V-02` ratification **GIVEN** · issuer act **OUTSTANDING**

| | |
|---|---|
| **Boss text** | *"RATIFY the `AAS-V-02` discharge act. **`RC-V-01` and all other unresolved veto controls remain effective.**"* |

### 6.1 `CC-F-14` — the ratification is given and the act it ratifies is not evidenced

**The chain, at primary text (`SC-04`):** *"`AAS-V-02` discharge ratification — **the issuer's act; Boss
ratifies**"* · *"**AAS+ performs the discharge act; Boss ratifies**"* · tallied as
***"`1 CONDITION SATISFIED — DISCHARGE ACT PENDING`"***.

**`SC-42` §1:** *"AAS+ input arrived since `093585d0`? — **NO — `0`**."*

| Half | State |
|---|---|
| Condition (`MTI-D-01`/`-02`/`-03` ruled) | **SATISFIED** |
| **Boss ratification** | **GIVEN — by this ruling** |
| **AAS+ issuer discharge act** | **NOT EVIDENCED — `0` AAS+ records exist** |

> **Applied as a STANDING RATIFICATION that takes effect on the issuer's act.** Recording `AAS-V-02` as
> discharged today would assert an AAS+ act that no evidence shows occurred — **fabricating an external
> authority's action, which the ruling's own prohibitions forbid.**

**Vetoes: `7` canonical · **`0` discharged** · `1` (`AAS-V-02`) ratification-complete, issuer-act pending.**

---

## 7. `B7′` · `B8′` · `B9′` · `B5′` · `B10′`

| Item | Applied | Before → After |
|---|---|---|
| **`B7′`** | Appointee: **Independent OpenAI GPT-5.6 Sol**, new clean session, different vendor/model, no transcript or session inheritance, no authoring role, **read-only on the post-ruling frozen baseline**; receives **only** Boss-approved scope, frozen SHA, canonical evidence paths and challenge instructions; **must not receive the executor's conclusion as authoritative evidence** | appointee **unnamed → NAMED**. **`EC-07` `0/2` UNCHANGED** — appointment is not a pass |
| **`B8′`** | **STRUCTURE A** — SMT performs the technical `E2E-04` re-grade; B-7 separately verifies/challenges it; *"the party performing the re-grade shall not certify its own independent verification"* | owner **divergent → RULED**; **`CC-F-08` RESOLVED**. **`E2E-04` still `NOT TRAVERSABLE`** — SMT has not re-graded |
| **`B9′`** | **ADMIT `MF-01` → `IR` and `AR`; ADMIT `MF-02` → `IR`; EXCLUDE `MF-03`** as a dimension. *"The unresolved `MF-01` counterpart-account issue remains open and requires evidence."* | **`IR` `18 → 20` · `AR` `29 → 30`**; enumeration **reconciled**; `MF-01` counterpart account **OPEN** |
| **`B5′`** | **NO RULING** — remains **PROVISIONAL** `19 / 2 / 1`, **B-7 attack target #1** | unchanged, by design |
| **`B10′`** | **Architecture disposition ACCEPTED** — `TERMINAL BY DESIGN` at the cross-module boundary | orphan outputs **`3 → 2`** (+ `1` orphan input) |

### 7.1 `MF-01` / `MF-02` applied to the convergence registers

| Register | Before | **After** | New member(s) |
|---|---:|---:|---|
| `IR` stock-affecting flows | `18` | **`20`** | `MF-01` certified opening balance · `MF-02` historical movement history |
| `AR` material business flows | `29` | **`30`** | `MF-01` |
| Status of the new members | — | **`NOT RECONCILED`** | `CC-F-11`: `MF-01` **creates value with no movement**, and **no existing valuation rule reaches it** |

> **Admission closes the ENUMERATION and opens a CONVERGENCE gap.** `IR` becomes `14 / 3 / 3` and `AR`
> `15 / 14 / 1` — **the reconciled counts do not rise; the denominators do.** A round that admitted members
> and left its reconciled totals unchanged would be reporting an improvement that did not occur.

---

## 8. Application summary

| Ruling | Denominator moved | Verification moved | Veto discharged | Runtime proof created |
|---|---|---|---|---|
| `B4′` | boundary `→ 12` | `0` | `0` | `0` |
| `B3′` | veto `6 → 7` | `0` | `0` | `0` |
| `CC-D-01` | — | `0` | `0` | `0` |
| `B1` | Boss decisions `7 → 1` | `0` | `0` | `0` |
| `B6` | `PTX = 11` adopted | `0` | `0` | `0` |
| `B2` | — | `0` | **`0`** | `0` |
| `B7′` | — | `0` | `0` | `0` |
| `B8′` | — | `0` | `0` | `0` |
| `B9′` | `IR 18→20`, `AR 29→30` | `0` | `0` | `0` |
| **Total** | **`5` denominators** | **`0 of 48`** | **`0`** | **`0`** |

> **`5` denominators moved. `0` verification items moved. `0` vetoes discharged. `0` runtime proof.**

---

## 9. Checkpoint

> ## `CHECKPOINT G — 11 RULINGS APPLIED`
>
> **`5` denominators moved · **`0 of 48` verification items moved · `0` vetoes discharged · `0` runtime
> proof created`** · `2` new findings: **`CC-F-13`** (the `12 ↔ 18 ↔ 10` mapping is now owed — new
> obligation `CORE-04`) and **`CC-F-14`** (`AAS-V-02`'s ratification is given while the issuer act is
> unevidenced — applied as a **standing** ratification, **not** a discharge) ·
> **`X-16`/`X-17` do NOT become writable** — `POH-D-01` was one of four bundle components and veto limb 2
> remains undischargeable as worded · `IR`/`AR` denominators rise while **reconciled counts do not**.**

