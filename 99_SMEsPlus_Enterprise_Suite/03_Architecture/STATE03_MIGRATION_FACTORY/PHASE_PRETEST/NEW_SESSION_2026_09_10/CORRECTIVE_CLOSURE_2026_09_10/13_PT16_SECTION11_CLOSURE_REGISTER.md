# 13 — `PT-16` §11 CLOSURE REGISTER

## `8 of 8 DISPOSED · 0 DISAPPEARED`

Session: `[SMEPLUS-26-09-10-PHASE-PRETEST-CORRECTIVE-CLOSURE-001]` · Boss: **SOLE FINAL APPROVER**

> **§16: every item receives `CLOSED` · `SUPERSEDED` · `DISPROVED` · `BOSS DECISION REQUIRED` ·
> `HOLD WITH EXACT BLOCKER`. *"No item may disappear from the register."***

---

## 1. The eight, read directly from `PT-16` §11

| # | Item as written | Work done this round | **Disposition** | Exact blocker / decision |
|---:|---|---|---|---|
| **1** | **Functional Design entry** — grant, or hold on the five gaps at §1 | all `5` gaps re-worked: `03_`…`06_` | **BOSS DECISION REQUIRED** | **`4 of 5` gaps remain.** Gap 2 partially closed (`1 of 4` outputs now `TERMINAL BY DESIGN`); gaps 1, 3, 4, 5 open — `15_` §2 |
| **2** | **`B1`** — the four presentable decisions (`POH-D-01`, `-03`, `-04`, `-05`) | re-verified against `SC-11` §2: `F5` `1 of 6` ruled, `5` open; `POH-D-02` withheld | **BOSS DECISION REQUIRED — UNCHANGED** | `4` ready and held. **`POH-D-01` additionally gates `X-16`/`X-17`** (`02_` β10/β11) — its value rose this round |
| **3** | **`B3`** — is the veto count **`6` or `7`**? | classified `A`/`B`/`C` from evidence — `07_` | **BOSS DECISION REQUIRED — SMEs Core determination now supplied** | **`CLASS C`, count `7`.** `A` and `B` disproved by measurement. **Renumbering is an issuer act**; figure carried at `6` |
| **4** | **`B4`** — adopt the proposed `12`-boundary set, or name another | tested against all authority — `02_` §2 | **BOSS DECISION REQUIRED · `HOLD — CANONICAL SET NOT PROVABLE`** | **`0` members enumerated by authority** (positive control `3`); `3` inconsistent lists `12`/`18`/`10`; the only `12` is a **prompt floor**. **Blocks `SC-11` §6 obligation `5`** |
| **5** | **`B5`** — commission the `PT10-F-01` re-derivation, or rule the pre-ruling split stands | **RE-DERIVED under §5's direction** — `01_` §3, `02_` §3 | **CLOSED — EXECUTED** | **`10/12` → `19 WRITABLE / 2 GATED / 1 NOT ESTABLISHED`.** Boss may still rule on **adopting** it |
| **6** | **`B6`** — adopt `PTX-01`…`PTX-11` as Pre-Test exit criteria | reconciled 1:1 against `RT-E15`; duplicate count prevented — `08_` | **BOSS DECISION REQUIRED — UNCHANGED** | denominator is **`11`, not `20`**; **`11 HOLD`, `0` discharged**. Adoption outstanding |
| **7** | **`B2`** — `AAS-V-02` ratification | re-verified: condition **SATISFIED**, ratification **NOT selected** at `SC-BD-10` | **BOSS DECISION REQUIRED — UNCHANGED** | a Boss **act**, not a decision. **Implementation start remains barred by `RC-V-01` regardless** |
| **8** | **Appoint `B-7`** | pack re-frozen and re-routed — `14_` | **HOLD WITH EXACT BLOCKER** | **Act approved at `SC-BD-10`; appointee UNNAMED.** `0` candidates named by SMEs Core (naming one would compromise independence). **`EC-07` `0 of 2`; `CP-PT-14` NOT REACHED** |

---

## 2. Movement summary

| Disposition | n | Items |
|---|---:|---|
| **CLOSED — EXECUTED** | **`1`** | `5` (`B5` re-derivation) |
| **BOSS DECISION REQUIRED** | **`6`** | `1`, `2`, `3`, `4`, `6`, `7` |
| **HOLD WITH EXACT BLOCKER** | **`1`** | `8` (B-7 appointee unnamed) |
| SUPERSEDED / DISPROVED | `0` | — |
| **Disappeared** | **`0`** ✔ | |

**`8 of 8` disposed.**

---

## 3. What actually moved — and what only looks like it moved

| Item | Real movement |
|---|---|
| **`5` (`B5`)** | **GENUINE — the only item this round could close by execution.** A Boss direction (§5) authorised the re-derivation `PT-10` had correctly declined to perform unbidden |
| **`3` (`B3`)** | **The determination moved, the decision did not.** `A`/`B` disproved, `C` established — **Boss still owns the renumbering** |
| **`4` (`B4`)** | **Moved AWAY from resolution.** `PT-16` offered a `12`-set to adopt; testing it against authority shows **no authority enumerates any set**, so the honest state is `NOT PROVABLE`. **A weaker position than `PT-16` implied, and the correct one** |
| **`1`** | `1 of 4` outputs closed; **`4 of 5` gaps remain** |
| `2`, `6`, `7`, `8` | **`0` movement** — all await a Boss act or an appointment |

> **One item closed by execution, one determination supplied, one item honestly downgraded, and four
> unmoved. `6 of 8` still require Boss.**

---

## 4. Checkpoint

> **`8 of 8` disposed, `0` disappeared · **`1` CLOSED by execution** (`B5`, under §5's direction) ·
> **`1` determination supplied** (`B3` = `CLASS C`) · **`1` honestly downgraded** (`B4` → `NOT
> PROVABLE`) · `6` remain `BOSS DECISION REQUIRED`, `1` `HOLD` on an unnamed appointee.**

