# 35 — `CORR1` REMAINING BOSS DECISIONS

## `CHECKPOINT G — BOSS DECISION SET MINIMIZED · 1 NEW ITEM FROM 4 CANDIDATES`

Session: `[SMEPLUS-26-09-10-PHASE-PRETEST-B7-RETURN-CORR1-001]` · Boss: **SOLE FINAL APPROVER**

> **§15: *"Do not manufacture a Boss decision if evidence resolves it."*
> **`4` candidate Boss items were tested. `3` were resolved below Boss and are NOT presented. `1` is
> irreducible and is presented.**

---

## 1. Minimization — the four candidates §15 anticipated, tested

| Candidate (§15's own list) | Resolved below Boss? | By what | Presented? |
|---|:--:|---|:--:|
| **Boundary denominator correction** if `H-13`/`-14`/`-17`/`-18` cannot map into the declared `12` | **NO** | `28_` §3 tested subcase (`A`), dimension (`B`) and insufficiency (`D`) for each; all four resolve to **`C`** on a named attribute. **The evidence is complete; what is missing is an authority act** | **YES — `BOSS-CORR1-01`** |
| **`E2E-04` role correction** if Structure A conflicts with authority | **YES** | `30_` — `B8′`'s operative clause is a **certification** bar in Boss's own words; `SC-03`/`SC-04` scope SMT's *output*, not its *work*. **Structure A is valid** | **NO** |
| **`MF-01` counterpart-account decision** if architecture cannot resolve it | **YES** | `29_` §6 — Architecture specified a Migration Opening Balance Control Account, net-zero at certification. **`2` residuals are a chart-of-accounts configuration act and a statutory question; neither is answerable by a Boss ruling** | **NO** |
| **Pre-Test gate phase-placement correction** if current criteria are circular | **YES** | `32_` — the criteria **are** circular, and the placement is **already Boss-approved**: `SC-40` answer B, `8C-CLARIFICATION-01` (`SC-54`), `SC-AUTH-02` = Reading C (`SC-51` §2, commit `39ea51c3`). **This round applies existing authority; asking again would re-put a closed question** | **NO** |

> **`3 of 4` resolved below Boss. Each was resolvable because the evidence already contained the answer —
> in one case a Boss ruling's own wording, in another a Boss-approved constitutional amendment this
> package had never applied to its own list.**

---

## 2. The open Boss decision set — `3`

### 2.1 `POH-D-02` — CARRIED, NOT RE-PUT

| | |
|---|---|
| Subject | Thai statutory tax consequences of straight-line vs units-of-production absorption |
| Status | **WITHHELD — excluded from `B1`**, `PTE-4` |
| Why not presented | **No Boss act can move it.** Statutory authority is required (`19_` §2, `36_`) |
| Blocks | `X-16`/`X-17` (`B-6`), scenarios `16`/`17` `GATED` |

### 2.2 `SC-SMT-01` — PRESENTED FOR THE FIRST TIME AS A DECISION

| | |
|---|---|
| **Item** | Reconciliation-control design for the **`Average`-costing original-cost reversal residual** |
| **Exact question** | Where does the difference go when `JT-05` reverses a return at **original** cost while the pool is carried at a **moving average**? |
| **Authority** | **Boss.** `SC-03` disposition: **`BOSS-ONLY DECISION`** — *"SMEs Core cannot close it: `09_JT05` §5 assigns the reconciliation-control design for the **AVCO admitted gap** to **Boss** at primary text"* |
| **Boss's own status of it** | `SC-BD-05` §8.1 — *"a **live obligation, not a closed condition** … **not** discharged by this ruling and must be tracked"* |
| **Why it is new here** | it was carried as an **obligation** (`SC-11` §6 #3; `PT-11` L109 `OPEN`) and never counted among *open Boss decisions*. **`22_` §4's *"`7` → `1`"* was low by this item** |
| **What it now gates** | **scenarios `8` and `9` are `GATED` on it** (`27_` §3). Under `Average`, a return-reversal case cannot state its expected accounting result |
| **Candidate destinations (for Boss, not chosen here)** | the existing **`Price Difference Account`** — one of the three permitted Product-Accounting overrides · a dedicated returns-variance control account · absorption into the pool by re-averaging on reversal |
| **SMEs Core recommendation** | **NONE OFFERED.** `SC-03` records that SMEs Core cannot close it; offering a preferred destination would be the reservation-discharge failure the programme has recorded |
| **Cost of leaving it open** | `2 of 22` scenarios remain `GATED`; `IR`/`AR` return convergence cannot complete for `Average` categories |

### 2.3 `BOSS-CORR1-01` — NEW

| | |
|---|---|
| **Item** | Boundary denominator correction |
| **Exact question** | Do `XMC-H-13` (Inventory→Sales/Purchase), `-14` (Quality→Inventory), `-17` (Service/Project→Accounting) and `-18` (Migration/replay→Inventory **and** Accounting) join the declared boundary denominator, or is each given a **declared exclusion with a reason**? |
| **Authority** | **Boss.** `B4′` was *"a NEW Boss authority declaration"*; only its issuer may amend its membership |
| **Evidence supplied** | `28_` — full `12 ↔ 18 ↔ 10` crosswalk; each of the four classified `A`/`B`/`C`/`D` on a named attribute; `2` further rows classified `B` and **not** proposed |
| **Why irreducible** | every non-Boss route was attempted and failed on evidence that is **complete and consistent**. This is an authority act, not a research gap |
| **Consequence if `12` stands** | `4` handoffs carrying `HOLD — EXACT GAP` sit outside the contracted and Pre-Test-tested surface — including `XMC-H-18`, onto which **`B9′` has already admitted `MF-01` and `MF-02`**. A denominator and an admission ruling point at different populations (`B7-F-08`) |
| **Consequence if `12 → 16`** | element-contract applicability, `HF-CTX-06`/`-11` attestation and Pre-Test Matrix scope extend to `16`; `CORE-04`'s mapping closes on `28_` plus the amendment |
| **Options** | **(a)** admit all four → `16` · **(b)** admit a named subset · **(c)** declare exclusions with reasons, leaving `12` · **(d)** rule the denominator is `18` and retire the `12`-class layer |
| **SMEs Core recommendation** | **(a)**, with `XMC-H-18` the least deferrable. **Stated, not applied — `0` boundaries were added by this round** |
| **Blocks** | exit condition `16` (`FAIL`); the per-boundary applicability declaration (`SC-11` §6 obl. `5`) |

---

## 3. SMEs Core obligations — status after this round

| ID | Obligation | Status |
|---|---|---|
| `CORE-01` | `RC-D-03`/`-04` recommendations | **DISCHARGED** before this round (`SC-14`/`SC-15`), `17_` §1 |
| `CORE-02` | migration-class flow rows | **DISCHARGED** — proposed, admitted by `B9′` |
| `CORE-03` | limb-2 re-wording | **CANNOT START** — AAS+ issuer authority (`36_` `X-02`) |
| `CORE-04` | map `12 ↔ 18 ↔ 10` | **DISCHARGED as a mapping** (`28_`); what remains is `BOSS-CORR1-01` |
| `CORE-05` | deterministic refusal rule | **SPECIFIED** (`32A_`); build obligation passes to Functional Design |
| `CORE-06` | `MF-01` counterpart account | **DISCHARGED at architecture level** (`29_` §6); `2` residuals routed to configuration and statutory |
| **`CORE-07`** | **NEW** — `HX-24` payload must carry `FIFO` layer granularity (`CORR1-F-02`) | **OPEN** |
| **`CORR1-F-03`** | **NEW** — `MF-01`/`MF-02` idempotency exposure on re-run | **OPEN**, routed to `X-22` / element `15` |

**Open to SMEs Core: `2` (`CORE-07`, `CORR1-F-03`). Blocked on an external body: `1` (`CORE-03`).**

---

## 4. Checkpoint

> ## `CHECKPOINT G — BOSS DECISION SET MINIMIZED`
>
> **`4` candidate Boss items tested; **`3` resolved below Boss** — one on a Boss ruling's own wording, one
> by an architecture act, one by applying a Boss-approved amendment the package had never applied to
> itself · **`1` new item presented: `BOSS-CORR1-01`** · open Boss decisions **`1 → 3`**, the correction
> being **`SC-SMT-01`**, an item classified `BOSS-ONLY DECISION` at primary text and counted only as an
> obligation until `B7-F-04` surfaced it by its consequence · **`0` recommendations offered where the
> owning record says SMEs Core may not recommend** · SMEs Core obligations: `2` open, `1` externally blocked.**
