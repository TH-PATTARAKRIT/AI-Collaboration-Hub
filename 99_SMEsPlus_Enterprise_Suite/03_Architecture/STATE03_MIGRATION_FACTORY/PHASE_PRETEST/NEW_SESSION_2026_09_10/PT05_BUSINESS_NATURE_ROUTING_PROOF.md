# PT-05 — ROUTING AND BUSINESS-NATURE PROOF

## `CP-PT-05 — ROUTING PROOF COMPLETE`

Session: `[SMEPLUS-26-09-10-PHASE-PRETEST-NEWSESSION-001]`
Branch: `architecture/account-phase-pretest-new-session-2026-09-10-001` · head consumed `2adf2682`
Executing body: **SMEs CORE** · Boss: **SOLE FINAL APPROVER**

> **Routing follows Business Nature, not module name.**
> **`0 of 22` verified · `E2E-04` `NOT TRAVERSABLE` · `6` vetoes in force.**

---

## 1. Result

| | |
|---|---|
| Business natures in the register | **`18`** — `BN-01`…`BN-18` |
| Current nature status | **`1 EVIDENCED — DESIGN; NOT LIVE` · `17 PARTIAL` · `0 HOLD`** |
| Natures at **`DETERMINED`** | **`0 of 18`** |
| Nature-DNA determinations carried | **`14`** — `ND-01`…`ND-14` |
| **Routing convergence rule sets found** | **`3` — CORR3's six (tested), the `06_` prompt's FIVE, and `SC-45`'s six** (`PT05-F-01`, corrected at `PT-13`) |
| Rule qualifications present in the tested register and absent from the summary | **`3`** |
| **Material findings** | **`3`** — `PT05-F-01`, `-F-02`, `-F-03` |
| Self-corrections of this session's own prior output | **`1`** — §7 |

---

## 2. `PT05-F-01` — MATERIAL: two different "six routing rules", and the summary is not the tested set

### Both sets, at primary text

| # | **SET A** — `SA_CORR3_08` §4.1, *"The mandatory routing rules, **tested**"* | **SET B** — `SC-45` §2 / `SC-30` §2, the closure summary |
|---:|---|---|
| 1 | Stock-affecting flow → Inventory — **"Holds for 18 enumerated flows; *the enumeration is short by at least one*"** | Stock-affecting → Inventory — **HOLDS** |
| 2 | Manufacture-required → Manufacturing — **"Holds as a route; *the trigger that selects make is undetermined*"** | Manufacture-required → Manufacturing — **HOLDS** |
| 3 | Procurement / dropship → Purchase — **"Holds, and *the entry is beneath Purchase's control floor*"** (`XMC-H-03`) | Procurement / dropship → Purchase — **HOLDS**, qualified instead by `SC-BD-08` |
| 4 | Every material flow → Accounting semantic — "29 of 29 determined; 14 carry a named open element" | same — **HOLDS** ✔ carried |
| 5 | One output may have multiple consumers; **a missing consumer is a defect** — **"*One defect found*: `XMC-H-09`"** | One output may feed multiple consumers — **HOLDS**, *"`XMC-H-09` remains the one recorded missing consumer"* |
| 6 | **"Routing follows Business Nature, not module name — Holds. *The resolution rule is still undetermined*"** | **`INPUT → PROCESS → OUTPUT → DOWNSTREAM ROUTING → NEXT MODULE INPUT` closes — HOLDS at route level** |

> ## ⚠ CORRECTED AT `PT-13` — READ THIS BEFORE §2's TABLE
>
> **The causal attribution below ("`SC-45` substitutes rule 6") is WRONG and was falsified by this
> session's own `PT-13`.** The governing `06_` prompt for the `SC-45` round mandates **FIVE** convergence
> rules — exactly `SC-45`'s rules 1–5. **`SC-45` followed its law and ADDED a sixth of its own; it never
> held the Business-Nature rule to drop.**
>
> **The corrected finding is worse for the programme, not better:** *"routing follows Business Nature, not
> module name"* was **lost at the PROMPT layer** between CORR3 and the `05_`/`06_` rounds — which removed
> the obligation to test it from **every subsequent round**, while it remains the central doctrine of this
> phase's master prompt §7.
>
> **Everything else in this finding stands unchanged:** the `3` stripped qualifications, rule 5's
> *"One defect found"* reported as `HOLDS`, `0` rules holding unqualified, and Set A governing `PT-12`.

### The three defects this comparison exposes

**1. Rule 6 is substituted, and the substituted-out rule is the master prompt's central routing doctrine.**
*"Routing follows Business Nature, not module name"* **does not appear as a rule row in `SC-45` or `SC-30`
at all** — while the master prompt §7 makes exactly this the question every scenario must answer. The
rule that was dropped is the one carrying *"the resolution **rule** is still undetermined"*.

**2. Three material qualifications are stripped between the tested register and the summary.**

| Qualification present in Set A | In Set B? |
|---|---|
| the stock-flow enumeration is **short by at least one** | **ABSENT** |
| the trigger that **selects make** is **undetermined** | **ABSENT** |
| the dropship entry is **beneath Purchase's control floor** | **ABSENT** (replaced by a different qualification) |

**3. A defect is re-presented as a holding rule.** Set A rule 5 reads ***"One defect found"***. Set B reads
***"HOLDS"*** with the defect demoted to a subordinate clause. **The rule's own text says a missing consumer
*is a defect*, so a register containing one cannot be reported as holding without saying so.**

### And a third variant: the governing prompts list **five**

The two single-session prompts (`05_`, `06_`) that govern the closure rounds enumerate **five** convergence
rules, not six. **Three different cardinalities — `5`, `6` (Set A), `6` (Set B) — over one doctrine.**

> **`SC-45`'s headline *"6 of 6 routing rules hold"* is therefore not a restatement of the tested result.**
> It is a **different six**, with three qualifications removed and one defect softened. **This is the
> programme's recorded *cite the corrections, not the headline table* defect, landing on the routing
> doctrine itself.**

| | |
|---|---|
| Status | **OPEN — MATERIAL** |
| Correct statement for Pre-Test | **`5 of 6` Set-A rules hold WITH A NAMED MATERIAL QUALIFICATION; `1` (rule 5) records a defect.** `0` rules hold unqualified |
| Binding on this session | **Set A governs `PT-12`.** `SC-45`'s six are not used as the routing denominator |
| Routed to | `PT-13`, **`PT-14` B-7** |

---

## 3. Business-nature routing — the register, with its supersession honoured

| Status | n | Natures |
|---|---:|---|
| `EVIDENCED — DESIGN; NOT LIVE` | **`1`** | `BN-06` |
| **`PARTIAL`** | **`17`** | `BN-01`…`BN-05`, `BN-07`…`BN-18` |
| `HOLD` | **`0`** | — |
| **`DETERMINED`** | **`0`** | — |
| Total | **`18`** ✔ | `BN-01`…`BN-18`, each exactly once |

**Supersession, honoured explicitly.** `SA05`'s own §3 status column (`0 / 11 / 7`) is superseded by
`SA_CORR2_03` §4 (`1 / 17 / 0`). `SA05` carries the instruction verbatim:

> *"their status column is **superseded and must not be quoted**."*

**And the reason the seven `HOLD` natures moved is itself load-bearing:** *"**None was closed by
research.** All seven were closed by reading evidence that already existed in a path set this register's
instrument could not reach."* **The natures did not improve; the instrument had been too narrow.**

> **`0 of 18` business natures are `DETERMINED`.** Every route in the Pre-Test matrix rests on a nature
> that is `PARTIAL` or design-only. **This is not a blocker to writing tests; it is a bound on what any
> passing test would mean.**

---

## 4. `PT05-F-02` — MATERIAL: the one scenario that improved rests on a superseded status column

### The claim

`SA15` `FINAL_CONTROLLED_V2`, the `E2E-07` re-grade to **`TRAVERSABLE`** — the **single positive movement**
in the final register — cites as its Blocker-2 basis:

> *"`SA05` `BN-07` → **`DETERMINED`**; `SA_CORR2_02` §3.4 `BN-07` residual → `RESOLVED — NO BOSS DECISION REQUIRED`"*

### What the cited sources actually say

| Source | `BN-07` status |
|---|---|
| **`SA05`** — the register cited | **`HOLD`** — and its status column is **superseded and "must not be quoted"** |
| **`SA_CORR2_03` §4** — the consolidated current register | **`PARTIAL`** |
| `SA_CORR2_02` §3.4 **half one** (resolution point) | **`DETERMINED, DESIGN`** |
| `SA_CORR2_02` §3.4 **half two** (which level carries cost) | **`PARTLY DETERMINED`** — *"and the finding is **worse than 'undetermined'**"* |

**`DETERMINED` is not the status of `BN-07` in any current register.** It is the status of **half one of
two**, and the citation attributes it to the one source that says `HOLD` and that forbids quoting its
status column.

### The half that did not travel — and it is a silent failure

`SA_CORR2_02` §3.4 half two, verbatim:

> *"For purchase price-difference correction, the filter that decides which valuation layers a bill line
> may correct **contains no kit predicate at all** — it compares the **bill line's own product**,
> unconditionally. Where the bill line's product differs from the ordering line's product, the layer set
> empties and **the correction is silently skipped**."*

**`E2E-07` is graded `TRAVERSABLE` and `SA17` records its required-inputs-not-yet-established as `none`
and attaches no bounded risk.** The named silent-skip defect on the kit's own cost correction appears in
**neither**.

### Fairness — what this finding does NOT claim

| Claim | Verdict |
|---|---|
| The `TRAVERSABLE` re-grade is wrong | **NOT CLAIMED.** The traversability blocker was the **resolution point**, and half one genuinely determines it |
| The re-grade should be reversed | **NOT CLAIMED** — and this session **has no authority to re-grade `SA15`** |
| The **citation** is defective | **CLAIMED** — it quotes a superseded status column and generalises a half to the whole |
| **A named silent-failure defect is absent from a row marked `none`** | **CLAIMED — this is the material half** |

| | |
|---|---|
| Status | **OPEN — MATERIAL** |
| Disposition | **`E2E-07` carries a bounded risk in `PT-12`: *kit price-difference correction is silently skipped when the bill-line product differs from the ordering-line product*.** `0` re-grades made |
| Routed to | `PT-13`, **`PT-14` B-7** — a challenger should test whether `TRAVERSABLE` + `none` survives this |

---

## 5. `PT05-F-03` — the routing enumeration is short by at least one, and the missing class is named

`SA_CORR3_08` §5.4 (`XMC-F-14`), verbatim:

> *"29 of 29 enumerated material flows have an explicitly determined accounting semantic; 14 carry a named
> open element; **the enumeration itself is short by at least one.**"*

**Instrument that established it:** pattern `migration|opening balance|cutover` over `SA06`, `SA07`,
`SA_CORR2_05`, `SA_CORR2_06` → **`0`, `0`, `0`, `0`**. **Positive control** `migration` → **`11` files**
across the two Phase SA packages. **The topic exists in the corpus and appears in none of the four flow
registers.**

**And the `29` carries an internal contradiction already published as `XMC-F-07`:**

| Statement | Where | Value |
|---|---|---:|
| count table `PARTIAL` | `SA_CORR2_06` line 74 | **`14`** |
| prose immediately below it | `SA_CORR2_06` lines 90–91 | **`13`** |
| its own checkpoint line | `SA_CORR2_06` line 206 | **`13`** |
| every downstream citation | `SA_CORR3_08`, `SA_CORR3_06`, `SC-30`, `SC-45` | **`14`** |

**Enumerating the `PARTIAL` rows gives `14`.** The register's own prose and checkpoint line are wrong;
downstream consumers are right. **A sibling defect `XMC-F-06` exists for the 18-flow inventory matrix,
whose checkpoint line still reads `13 / 4 / 1` against a published `14 / 3 / 1`.**

> **Two registers, each contradicting its own checkpoint line.** Both were caught, both are published, and
> **neither checkpoint line was repaired** — a *revision log is not a correction* instance. `PT-15` must
> re-check whether the text, not the disposition table, was ever fixed.

---

## 6. Routing by business nature — what routes, and on what authority

| Route trigger | Rule | State |
|---|---|---|
| Stock affected | → **Inventory** | Holds; enumeration **short by ≥1** (`PT05-F-03`) |
| Production required | → **Manufacturing** | Holds as a route; **the make-selection trigger is undetermined** |
| Procurement / replenishment / dropship | → **Purchase** | Holds; **entry sits beneath Purchase's control floor** (`XMC-H-03`) |
| Direct shipment (supplier → customer) | `BD-ACC-03A` product-category policy, **no route-specific exception** | **Boss-ruled `SC-BD-08` option (a)**; `F3` leaves the decision list. Location = **counterparty pair recorded as external endpoints with reason, not `N/A`** |
| Accounting consequence | → **Accounting** | `29 of 29` determined; `14` open elements; **enumeration short by ≥1** |
| Asset capitalization | → **Asset → Accounting** | **`XMC-H-09` GAP — the Equipment consumer has no evidenced route** |
| Statutory consequence | → **Tax** | company-scoped (`BD-ACC-02`); **`R-19 HARD` — neither module computes tax itself**; `PT-S-01` |

**On the reference system, held at arm's length:** `SC-BD-08` records the direct-shipment capability as
*"installed in **`0` of `3`** readable deployments, measured on two differently-shaped instruments"* — and
rules the SMEsPlus route **on SMEsPlus policy grounds**. **No vendor implementation became policy.**

**`XMC-F-03` caution carried:** the direct-shipment routing **template** sentence carries routing for
`BN-01`, `BN-04`, `BN-05`, `BN-06`, `BN-16` and the manufacture natures, and *"**no Phase SA register has
read it**"*.

---

## 7. Self-correction — `PT-01` and `PT-02` carry `0` business-nature identifiers

**Measured over this session's own published files.** `PT01` and `PT02` contain **`0`** `BN-nn` tokens
(the same instrument returns `XMC-F-03` at `PT01:199` and `ND-10` at `PT02:80`, so it fires).

> **A scenario population and an input matrix were published without once naming the business nature that
> routes each scenario** — while the governing doctrine is *routing follows Business Nature, not module
> name*. **The omission is this session's, not Phase SA's.**

| | |
|---|---|
| Corrected by | this checkpoint — `PT-05` establishes the `18`-nature register and its `1 / 17 / 0` status as the routing basis |
| Binding forward | **`PT-12` must carry a `BN-nn` on every material row**, or record why the row has no nature |
| Not corrected retroactively | `PT-01`/`PT-02` are **not rewritten**; the gap is recorded here and closed forward |

---

## 8. Checkpoint

> ## `CP-PT-05 — ROUTING PROOF COMPLETE, WITH THREE MATERIAL FINDINGS`
>
> **`PT05-F-01` MATERIAL — two different "six routing rules" exist. `SC-45`'s summary **substitutes rule 6**,
> dropping *"routing follows Business Nature, not module name"* entirely, **strips three material
> qualifications**, and reports as **HOLDS** a rule whose tested result reads ***"One defect found"***.
> The governing prompts list **five**. Correct statement: **`0` rules hold unqualified.** Set A governs `PT-12` ·
> **`PT05-F-02` MATERIAL — the single scenario that improved in the final register (`E2E-07` → `TRAVERSABLE`)
> cites a status column its own source marks *"superseded and must not be quoted"*, generalises a
> half-determination to the whole nature, and carries `none` outstanding while its business nature holds a
> named **silently-skipped** cost correction. `0` re-grades made; a bounded risk is attached instead** ·
> **`PT05-F-03` — the flow enumeration is short by at least one (migration flows, positive control `11` files),
> and two registers each contradict their own checkpoint line, unrepaired** ·
> **`0 of 18` business natures are `DETERMINED`** · `1 EVIDENCED / 17 PARTIAL / 0 HOLD`, supersession honoured ·
> **`1` self-correction: this session's own `PT-01`/`PT-02` carried `0` `BN-nn` identifiers.**
>
> **`0` ruled routes changed · `0` re-grades · `0` classifications improved · `0 of 22` verified.**

Next checkpoint: `PT-06 — Accounting + Inventory Universal Convergence`.

No Evidence = No Progress. Never Skip Gate. Truth over Pass. Routing follows Business Nature, not module name.
Boss remains the sole Final Approver.
