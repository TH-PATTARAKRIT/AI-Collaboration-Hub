# 11 — BOSS DECISION MINIMIZATION

## `CHECKPOINT G — 4 CANDIDATES TESTED · 1 IRREDUCIBLE BOSS ITEM`

Session: `[SMEPLUS-26-09-10-PHASE-PRETEST-RECOVERY-NEWSESSION-001]` · Boss: **SOLE FINAL APPROVER**

> **§9: *"Do not send every uncertainty to Boss."* Each candidate tested for whether Boss authority is
> genuinely required.**

---

## 1. The four candidates

| Candidate | Genuinely Boss's? | Determination |
|---|---|---|
| **Boundary denominator correction** (`4` handoffs outside the declared `12`) | **NO — NOT YET** | **`B4′` already declared the set on NEW Boss authority.** The `4` gap-carrying handoffs are a **coverage** finding, not a denominator error. **Architecture/PMO must first determine whether each of `-13`, `-14`, `-17`, `-18` needs a boundary class, an `N/A` with reason, or a different contract.** Asking Boss to re-declare before that analysis exists would put a SMEs Core position in Boss's mouth — the `SC-BD-F-01` defect. **→ SMEs Core obligation `CORE-07`, not a Boss item** |
| **`SC-SMT-01`** — the `Average`-costing return residual | **NO — ALREADY BOSS-OWNED AND ALREADY OPEN** | `SC-11` §6 obligation `3` records it: owner **Boss**, on SMEs Core input, created by `SC-BD-05`. **It is not a new item; it is an existing open obligation that this round newly showed gates `X-08`/`X-09`.** Re-asking would be a repeated question without material delta — **prohibited**. **→ carried, not re-asked** |
| **`POH-D-02`** | **NO — NOT ASKABLE** | **Thai statutory evidence absent. No Boss act can move it.** `PTE-4`, excluded from `B1` by Boss's own ruling. **→ external, unchanged** |
| **Gate phase-placement correction** | **YES — IRREDUCIBLE** | §2 |

---

## 2. The one irreducible item

### `R-D-01` — PHASE PLACEMENT OF `EC-04`, `EC-07`, THE `48`-ITEM VERIFICATION, AND `E2E-04`'s `D`/`I` LIMBS

| | |
|---|---|
| **Why Boss** | A phase-placement change **alters what a gate means**. `09_` shows `4` conditions are placed against their own governing authority — but **moving them is an amendment to the Pre-Test exit set**, and only Boss amends a gate |
| **Why SMEs Core cannot** | re-placing a criterion this session is measured against would be **the challenged party editing its own exam** |
| **Why AAS+/PMO cannot** | `EC-04`'s closure point is fixed by **`8C-CLARIFICATION-01`**, a Boss-approved constitutional amendment; `EC-07`'s by **`SC-AUTH-02` = Reading C**, a Boss ruling |
| **What is asked** | ratify that `EC-04` closes at the **State gate**, `EC-07` at the **Module and State gates**, the `48`-item verification at **Build/Test**, and `E2E-04`'s `D`/`I` limbs at **Functional Design exit / Build** — **or** direct that they remain Pre-Test exit conditions |
| **Honest consequence** | **Re-placement does NOT unblock Pre-Test.** Conditions `3`, `4`, `16`, `17` fail at the correct gate. **The exit set would become `13`, satisfied `7 of 13`, and the verdict would remain `HOLD`** |

### 2.1 Why raise it at all, if it changes no verdict

> **Because leaving it unraised means every future Pre-Test round re-measures itself against `3` criteria
> that cannot be satisfied at this gate by construction — and reports failures that are mis-placements.**
> **It corrects the instrument, not the result.**

---

## 3. What was resolved without Boss

| Resolved by | Items |
|---|---|
| **SMEs Core** | `18 of 18` B-7 findings verified · readiness re-derived · `12 ↔ 18` crosswalk · `MF-01`/`MF-03` re-verified · `17` recounted · `15` wrong-session artefacts classified · `2` wrong-session conclusions corrected |
| **Architecture** | `MF-03` = `EXECUTION MODE` on evidence, not on an unbuilt control |
| **Audit control** | `AAS-V-02` **NOT DISCHARGED**; `X-14` reversed from *"executed"* to `OUTSTANDING` |
| **PMO** | `4` Boss candidates tested; `3` returned to their proper owners |

**`3` of `4` candidates removed from the Boss set.**

---

## 4. Checkpoint

> ## `CHECKPOINT G — BOSS SET MINIMIZED`
>
> **`4` candidates tested → **`1` irreducible Boss item** (`R-D-01`, gate phase placement) · `1` returned
> as a **new SMEs Core obligation** (`CORE-07`) · `1` identified as an **existing open Boss obligation**
> that must not be re-asked · `1` confirmed **not askable** (statutory) ·
> **and the honest note: `R-D-01` corrects the instrument and changes no verdict.**

