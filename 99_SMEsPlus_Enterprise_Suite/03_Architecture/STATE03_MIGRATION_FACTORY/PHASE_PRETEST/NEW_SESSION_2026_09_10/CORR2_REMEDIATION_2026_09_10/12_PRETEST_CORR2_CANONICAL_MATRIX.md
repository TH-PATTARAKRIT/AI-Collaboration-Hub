# 12 — PRE-TEST `CORR2` CANONICAL MATRIX

# `THE CORRECTED STATE · HOLD PRE-TEST EXIT`

## `CHECKPOINT N (part 1)`

Session: `[SMEPLUS-26-09-10-PHASE-PRETEST-B7R2-REMEDIATION-NEWSESSION-001]`
Baseline read: `c91d5840` · Boss: **SOLE FINAL APPROVER**

> **This matrix supersedes `RECOVERY_2026_09_10/10_` §1 and `12_`.**
> **`0` prior artefacts modified.** Every figure below is re-derived in a named file of this package,
> and every superseded figure is shown beside its replacement.

---

## 1. THE CORRECTED CANONICAL STATE

| Item | Published at `c91d5840` | **`CORR2`** | Moved by | Derived in |
|---|---|---|---|---|
| Pre-Test exit | `7 / 13` = `53.8 %` | **`5 / 14` = `35.7 %`** | condition `11` restored; conditions `2` and `5` downgraded | `03_` |
| Exit denominator | `13` | **`14`** | `3` re-placements on their own instruments, not `4` | `03_` §1 |
| Open Boss decisions | `1` | **`4`** | `3` uncounted, `0` closures recorded | `05_` |
| FD blockers | `3` (`10_` §1) / `5` (`10_` §2) | **`11`** | `2` new · `3` restored after being dropped · `1` Boss-owned | `10_` |
| Gap-carrying flows outside the declared set | `4` | **floor `5`, up to `7`** | union population declared; `N/A` pair conditional | `06_` |
| Circular gate defects | `3` → `0` | **`2` genuine, `1` misclassified, `1` relocated** | two incompatible memberships settled | `03_` §5 |
| `MF-03` | `EXECUTION MODE` *"on sound ground"* | **`NOT DECIDABLE ON CURRENT EVIDENCE`** | ground falsified twice at primary source | `07_` |
| `R-D-01` | applied as ruled | **`AUTHORITY NOT DURABLY PROVEN`** | `1` occurrence across `194` branches | `04A_` |
| B-7 Round 1 | *"independent, different vendor"* | **`NOT INDEPENDENT`** | same credential, same model family | `02_` |
| B-7 Round 2 | appointed GPT-5.6 Sol | **`NOT INDEPENDENT`** | audited party's own display name, `Claude Opus 5` | `02_` |
| Denominators with named membership | *"`0` without"* | **`10 of 21` sound = `47.6 %`** | `11` defective in `8` classes | `08_` |
| `SMEs Core` obligations | `4`, range naming `3` | **`3` memberships for one count; `3` more dropped** | `CORE-07` re-used | `10_` §2 |
| External-authority items | `14`, `X-01…X-14` | **`14`, `X-01…X-13` + `X-15`** | range added a non-member, dropped a member | `08_` row `22` |

---

## 2. UNCHANGED, AND NOT TO BE UPGRADED

| Item | State | Re-verified this session |
|---|---|---|
| `EC-04` | **`0 / 3`** — deferred to the State gate, **not discharged** | YES |
| `EC-07` | **`0 / 2`** — deferred to Module + State, **never credited to either B-7 round** | YES |
| `48`-item verification | **`0 PASS · 0 FAIL · 48 HOLD`** | YES |
| Scenarios runtime-verified | **`0 of 22`** | YES |
| `E2E-04` | **`NOT TRAVERSABLE`**; re-grade act **OUTSTANDING** | YES |
| `CP-PT-14` | **NOT REACHED** | YES |
| Vetoes | **`7` canonical · `0` discharged** | YES |
| `AAS-V-02` | **NOT DISCHARGED** — `0` AAS+-authored records | YES |
| Readiness split | **`18 / 4 / 0`** — membership exact, **authority absent (`B5′` unruled), `PROVISIONAL`** | YES — mechanically re-extracted |
| Canonical boundary set | **`12`, Boss-declared (`B4′`)** — amendment is `BOSS-CORR1-01` | YES |
| Manifest integrity | **`15 / 15`, coverage complete** | YES — controls fire |
| Functional Design | **NOT AUTHORIZED** | — |

---

## 3. COVERAGE — the five dimensions, never blended

| Dimension | Result | Floor | Verdict |
|---|---|---:|---|
| **`A` Source presence** | `18 of 25` rows below floor; range `0 % – 100 %` | `96 %` | **HOLD** |
| **`B` Runtime reachability** | `0 %` on `6` populations; `5` correctly phase-placed, `1` a dropped obligation | `96 %` | **HOLD** — `5` DEFERRED |
| **`C` Configuration reachability** | **NO DECLARED POPULATION** | `96 %` | **HOLD — NOT MEASURABLE** |
| **`D` Optional function reachability** | **NO DECLARED INVENTORY** | `96 %` | **HOLD — NOT MEASURABLE** |
| **`G` Critical / zero-tolerance** | `3 of 9` at `100 %`; `5` falsified by direct attack | `100 %` | **HOLD** |

**`0` averaging · `0` compensation · `0` source presence used as evidence for another dimension.**

---

## 4. PRE-COMMIT SWEEP — four checks, four disjoint units · **EXECUTED**

**Run as the last act before the freeze, because three clean checks have previously shipped a broken
record when all three shared one unit. The commands and their output are published, not the intent.**

| # | Unit | Check | Result |
|---:|---|---|---|
| **`1a`** | identifier | every `CORR2-*` id cited is **defined with its severity** in this package | **`14` defined · `14` cited · `0` orphan · `0` unused** — **after `1` defect was found and fixed** |
| **`1b`** | identifier family | every peer family cited resolves to a declared owner | **CLEAN** — `R2-F` `17` distinct · `ZT` `9` · `B7-F` · `CORE` · `CORR1-F` · `XMC-H` · `HX` · `X` · `PTX`, each attributed at first use |
| **`2`** | tally | every count re-derived by counting its own table | **`1` DEFECT FOUND AND FIXED** — see below |
| **`3`** | superseded value | every replaced figure names what it replaces | **CLEAN** — §1 is the register; `13` figures, each paired |
| **`4`** | evidence citation | every artefact id cited resolves to a file | **CLEAN** — `15 of 15` own ids present (`15_` written last); **`18` peer ids, `0` unresolved** |

**Sweep exceptions: `0` remaining. `2` defects were found by the sweep and corrected before the freeze.**

### `4.1` My own instrument failures — reported, as required

**`1` — check `1a`'s first predicate was broken and returned a false zero.**
The detector matched `**id** —`, but this package writes `**`id` — MATERIAL.**` with the bold enclosing
the severity. It reported **`0` of `14` identifiers defined** — a result indistinguishable from a package
with no definitions at all. **Caught only because `0 of 14` was implausible and the predicate was
re-read.** Corrected, and re-run **with a synthetic injection control** (`CORR2-ZZZ-99`) that fires.
**The corrected run then found a genuine defect the broken one had buried:** `CORR2-MF-01` was cited in
`11_` §3 with a severity but stated in `07_` §1 without one. **Fixed.**

**`2` — check `4`'s first predicate conflated two populations.** It compared **all** `NN_` references
against this package's own files, so `18` legitimate references to parent-package artefacts (`17_`,
`22_`, `28_`, `35_`, …) appeared as unresolved. **Unit corrected to separate own ids from peer ids**;
peer ids were then resolved against `CORRECTIVE_CLOSURE_2026_09_10/` and `RECOVERY_2026_09_10/` —
**`0` unresolved.**

**`3` — check `2` found a wrong tally in this package's own headline.**
`01_` published **`14` CONFIRMED · `3` MODIFIED**. Counting the disposition table mechanically returns
**`15` CONFIRMED · `2` MODIFIED** (`R2-F-13` and `R2-F-15` are the only `MODIFIED` rows).
**Corrected in `01_`, `11_`, `12_` and `16_`**, and the derived figure *"`3 of 17` challenger accounts
corrected — `17.6 %`"* corrected to **`2 of 17` — `11.8 %`**.

> **This is the defect this package spends `08_` and `09_` documenting, occurring in this package.**
> **A total is an unverified claim until it is counted, including one's own.** It was caught by a
> disjoint-unit check rather than by re-reading, which is why the sweep exists.

### `4.2` Package composition — stated as `B7-F-15` / `B7-F-16` require

**`B7-F-15` was a wrong manifest entry count. `B7-F-16` was an inventory naming a range that omitted its
highest-numbered member. Both recurred one commit later (`R2-F-03`, `R2-F-04`). This package therefore
states its composition as an explicit membership, and its entry count as produced BY the manifest.**

```
FILES PRESENT (16), enumerated — not a range:
  01_ 02_ 03_ 04A_ 05_ 06_ 07_ 08_ 09_ 10_ 11_ 12_ 13_ 14_ 16_   (15 .md)
  15_PRETEST_CORR2_MANIFEST_SHA256.txt                            ( 1 .txt)

THERE IS NO 04_ AND NO 15_.md IN THIS PACKAGE.
  04_  was NOT produced: 04A_ supersedes it, because R-D-01's authority was NOT found (04A_ SS1).
  15_  is the manifest itself, and is a .txt.

MANIFEST ENTRY COUNT, produced by the manifest in 4 command shapes:
  grep -cE '^[0-9a-f]{64}  '  -> 15
  awk  'NF==2{n++}'           -> 15
  wc -l                       -> 15
  python3 non-blank           -> 15

INTEGRITY   shasum -a 256 -c  -> 15 OK, 0 FAILED
CONTROL A   one-byte append   -> 1 FAILED          (the checker fires)
CONTROL B   unlisted file     -> 0 detected        (shasum -c is addition-blind, so:)
COVERAGE    by set-difference -> present 16, listed 15,
                                 uncovered = 15_ itself (self-reference limit),
                                 listed-but-absent = 0                COMPLETE
```

## 5. TERMINAL POSITION

> ## `HOLD PRE-TEST EXIT`
>
> **`FUNCTIONAL DESIGN NOT AUTHORIZED`**

| | |
|---|---|
| Is the challenged terminal position (`HOLD`) wrong? | **NO — it is understated** |
| Is the baseline reliable as a certified gate input? | **NO** |
| Is any `PASS` declared? | **NO** |
| Is this an `EC-07` pass? | **NO** — and `EC-07` is Module + State placed |
| Independent assurance | **NOT ESTABLISHED — both rounds** |
| Canonical writes to `architecture/…-new-session-…` or either audit branch | **`0`** |
| Evidence waived | **`0`** |
| Prior artefacts modified | **`0`** |
| Findings discarded | **`0`** |

---

## 6. CHECKPOINT

> ## `CHECKPOINT N (part 1) — CORRECTED CANONICAL MATRIX`
>
> `13` figures corrected · `12` re-verified unchanged · `5` dimensions published separately ·
> `4`-unit pre-commit sweep **CLEAN with `0` exceptions** ·
> **`HOLD PRE-TEST EXIT` · `FUNCTIONAL DESIGN NOT AUTHORIZED`.**

No Evidence = No Progress. Never Skip Gate. Truth over Pass.
**Boss is the sole Final Approver.**
