# 16 — B-7 ROUND `2R1` INDEPENDENCE RERUN — NEW SESSION PROMPT

## `CHECKPOINT O`

**This file is a prompt to be executed in a DIFFERENT session, by a DIFFERENT party.
It is NOT executed here.**

Session to open: `[SMEPLUS-26-09-10-PHASE-PRETEST-B7-ROUND2R1-INDEPENDENT-001]`
Executor: **OPENAI GPT-5.6 SOL**
Boss: **SOLE FINAL APPROVER**

> **This is not Round 3.** The intended Independent Round 2 has not validly occurred. `d878a603`
> executed every target but is classified **`B7R2-SECOND-LINE-CHALLENGE`**. **This rerun is Round `2R1`.**

---

# §0. STOP CONDITION — READ BEFORE ANYTHING ELSE

**Your FIRST act, before opening any evidence file, is to publish your own identity.**

```
=====================================================================
EXECUTOR IDENTITY CARD  —  PUBLISH BEFORE READING ANY EVIDENCE
=====================================================================
VENDOR                 : ____________________   (required: OpenAI)
MODEL                  : ____________________   (required: GPT-5.6 Sol)
SESSION ID             : [SMEPLUS-26-09-10-PHASE-PRETEST-B7-ROUND2R1-INDEPENDENT-001]
TRANSCRIPT INHERITED   : ____________________   (required: NONE)
GIT AUTHOR NAME        : ____________________   (must NOT be "TH.PATTARAKRIT SOLUTION
                                                 SERVICE CO., LTD." or "SMEs Core")
GIT AUTHOR EMAIL       : ____________________   (must NOT be jabsung.s@gmail.com
                                                 and must NOT be scgl.thailand@gmail.com)
MODEL ATTRIBUTION      : ____________________   (must NOT be "Claude Opus 5")
CHANNEL                : audit/b7-round2r1-independent-2026-09-10
=====================================================================
```

**If any required field cannot be truthfully completed:**

> # `STOP: INDEPENDENCE NOT PROVEN`
> # `DO NOT EXECUTE AUDIT`

**Publish the stop and nothing else.** A second-line challenge executed under a false independence claim
has already cost this programme two rounds. **A refusal is a better outcome than a third.**

**Why this section exists — the measured facts, which you must verify yourself:**

```
$ git log -1 --format='%an <%ae>%n%(trailers)' d878a603      # Round 2
   TH.PATTARAKRIT SOLUTION SERVICE CO., LTD. <scgl.thailand@gmail.com>
   Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>     <- audited party's own name and model

$ git log -1 --format='%an <%ae>%n%(trailers)' 5bd36d62      # Round 1
   B7 Independent Challenger <jabsung.s@gmail.com>
   Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>

$ git log --format='%ae' origin/architecture/account-phase-pretest-new-session-2026-09-10-001 -12
   jabsung.s@gmail.com   x12                                 <- identical address, 12 of 12
```

**Neither round is independent. Verify this before you accept anything else in the package.**

---

# §1. MANDATE

| | |
|---|---|
| **Read-only baseline** | the SHA frozen at `15_` of `CORR2_REMEDIATION_2026_09_10` — **resolve it yourself and say how** |
| **Canonical branch** | `architecture/account-phase-pretest-b7r2-remediation-2026-09-10-001` — **DO NOT WRITE TO IT** |
| **Your channel** | `audit/b7-round2r1-independent-2026-09-10` — **your only write target** |
| **Conclusion inheritance** | **NONE.** `d878a603`'s `HOLD`, `5bd36d62`'s `HOLD` and `CORR2`'s `HOLD` are **claims to be attacked**, not premises |
| **Your verdict as `EC-07`** | **NO.** `EC-07` attaches at the Module and State gates, not here |
| **Allowed verdicts** | **`PASS` · `FAIL` · `HOLD`** — nothing else |

**Forbidden verdict wording:** `MOSTLY PASS`, `CONDITIONAL PASS`, `NEAR READY`,
`SUBSTANTIALLY COMPLETE`, or any qualifier attached to `PASS`.

**`PASS` requires, with no exception:** every applicable non-critical dimension **`≥ 96 %`** · every
Critical / Zero-Tolerance dimension **`= 100 %`** · no unresolved critical gap · no unresolved material
contradiction · valid denominators · no missing required evidence at its correct phase · no unsupported
`N/A` · no semantic invention required downstream.

---

# §2. ATTACK SCOPE — `26` TARGETS

Each target requires a **recorded falsification attempt**. Report attempts that **fail** as well as
those that succeed: **a published failed attack is evidence.**

| # | Target | What to attack |
|---:|---|---|
| `1` | Baseline integrity | manifest counts, coverage by **set-difference** (`shasum -c` cannot detect additions), `0` canonical writes |
| `2` | **Independence apparatus** | your own identity card; both prior rounds; whether `CORR2`'s remedy is sufficient |
| `3` | Pre-Test exit denominator | `CORR2` says `14`. **Re-derive from named authority. Do not start from `13`, `14` or `17`** |
| `4` | `R-D-01` authority | search wider than `194` branches if you can. **Publish your positive control** |
| `5` | Boss decision population | `CORR2` says `4`. Test for a fifth, and test each for a closure act |
| `6` | `12 ↔ 18 ↔ 10` | re-derive independently. **`CORR2` claims `9 of 10` and `11 of 12`** |
| `7` | Outside-boundary flows | `CORR2` claims a **floor of `5`, up to `7`**. Attack the floor and the ceiling |
| `8` | `MF-03` | `CORR2` says **`NOT DECIDABLE`**. Attack that too — is a classification available that `CORR2` missed? |
| `9` | Denominator matrix | `CORR2` claims `10 of 21` sound. **Audit `CORR2`'s own `21`** |
| `10` | Readiness matrix | `18 / 4 / 0` — membership, arithmetic, and the absent `B5′` authority |
| `11` | `AAS-V-02` | re-measure with your own instruments and **declare your `PATH SET`** — three parties have published three different denominators (`103`, `104`, `114`) and none declared the same path set |
| `12` | FD blockers | `CORR2` says `11`. Test for a twelfth, and test the `3` "restored" ones |
| `13` | **All `18` Round-1 findings** and their remediation | |
| `14` | **All `17` Round-2 findings** and their `CORR2` disposition — **including the `2` `CORR2` MODIFIED** | |
| `15` | All `5` coverage dimensions | are the dimensions themselves the right ones? |
| `16` | Every dimension `< 96 %` | is any of them actually *worse* than published? |
| `17` | Every claimed Critical `= 100 %` | **`ZT-01`, `ZT-02`, `ZT-07`. Attack all three** |
| `18` | Source-vs-runtime substitution | find any place `CORR2` uses source presence as evidence for another dimension |
| `19` | Configuration reachability | `CORR2` says **no population exists**. Prove it wrong |
| `20` | Optional reachability | same |
| `21` | Unsupported `N/A` | including `CORR2`'s own treatment of `XMC-H-15`/`-16` |
| `22` | Hidden denominator members | any population where a member was excluded without an authority |
| `23` | Stale / superseded evidence | including whether `CORR2` itself carries a superseded figure |
| `24` | Circular proof | any claim resting on itself |
| `25` | Circular gate | `CORR2` says `2` genuine, `1` misclassified, `1` relocated |
| `26` | New material contradictions | anything the `3` prior rounds missed |

**For every applicable coverage claim, verify all six:**
`DENOMINATOR` · `NUMERATOR` · `MEMBERSHIP` · `%` · `THRESHOLD` · `EVIDENCE`.

---

# §3. METHOD CONTROLS — the traps this programme has actually fallen into

**These are not advice. Each names a defect that reached a published register here.**

| # | Control |
|---:|---|
| `1` | **Declare `POPULATION` + `UNIT` + `PATTERN` + `PATH SET` + `AUTHORITY` for every count.** None may be author-chosen |
| `2` | **A range expression is not an enumeration.** `X-01…X-14` concealed a non-member and a missing member |
| `3` | **Every negative needs a firing positive control.** `R-D-01`'s absence is only meaningful because `BOSS-RESOLUTION-001` returns `9` |
| `4` | **Validate every count with a second command of a different shape.** The manifest count was verified in `4` |
| `5` | **`NO EVIDENCE FOUND ≠ DOES NOT EXIST.`** State which of the two you measured |
| `6` | **The evidence base is itself a claim.** `HX-24`/`HX-25`'s defining register is off-baseline on **two identical copies** — `CORR2` §1 of `07_`. Check for a third |
| `7` | **Widen the path set before publishing a negative.** `CORR2` searched all `194` branches for `R-D-01`. Searching only the frozen tree would have been a narrower claim |
| `8` | **A summary may locate a source; it may never be the evidence.** Read `22_` §1, `SA_CORR3_08` L213–230 and `SA_CORR4_02` §5 at primary text |
| `9` | **Cite a round's corrections section, never its headline table** |
| `10` | **Recency is not authority.** A later artefact may still be wrong; `CORR1`'s `28_` claimed a discharge the recovery's `05_` correctly downgraded |
| `11` | **An identifier may be re-used.** `CORE-07` means two different obligations one commit apart |
| `12` | **Audit your own disposition table.** A revision log is not a correction. `CORR2` `11_` §4 audits its own; audit it again |
| `13` | **Count *open*, record *askable* separately.** Conflating them is how `3` Boss decisions vanished |
| `14` | **Report your own instrument failures.** Round 2 reported `3`; `CORR2` reported its own tally error and the fix |
| `15` | **A total is an unverified claim.** Re-count every tally in `CORR2` mechanically. **`CORR2` published a wrong tally in `08_` §3 and `09_` §2 and corrected both — check that the corrections are complete** |

---

# §4. HARD PROHIBITIONS

**DO NOT:** write to any canonical branch · modify or delete any challenged artefact · declare `PASS`
without every floor met · treat your verdict as `EC-07` · discharge any veto · re-grade `E2E-04` ·
amend any Boss-declared denominator · invent runtime, configuration or statutory evidence ·
accept any `< 96 %` applicable dimension or any `< 100 %` critical control · inherit any conclusion ·
**execute this audit if your identity card cannot be truthfully completed.**

---

# §5. REQUIRED OUTPUT

Publish to `audit/b7-round2r1-independent-2026-09-10` only:

```
00_EXECUTOR_IDENTITY_CARD.md          <- FIRST, before any evidence is read
01_B7_ROUND2R1_INDEPENDENT_VERDICT.md
02_B7_ROUND2R1_MANIFEST_SHA256.txt    <- must cover EVERY file you publish, and
                                         state its own entry count as produced BY the manifest
```

**The verdict must state, separately:** verdict (`PASS`/`FAIL`/`HOLD`) · targets executed of `26` ·
falsification attempts recorded · attempts succeeded / **failed** · findings by severity ·
**your own instrument failures** · **claims you could not test, and why** · every coverage dimension with
its six fields · whether every applicable dimension met its floor (**`YES`/`NO`**) · whether every
critical control is at `100 %` (**`YES`/`NO`**) · canonical writes (**must be `0`**) ·
whether this is an `EC-07` pass (**`NO`**).

---

# §6. WHAT YOU ARE INHERITING — stated so you can attack it

**`CORR2`'s claims, offered as targets:**

| Claim | Where |
|---|---|
| Pre-Test exit `5 / 14` = `35.7 %` | `03_` |
| Exit denominator `14`, **independent of `R-D-01`** | `03_` §1 |
| Neither B-7 round is independent | `02_` |
| `R-D-01` has no ruling artefact across `194` branches | `04A_` |
| `4` open Boss decisions, `0` closures | `05_` |
| `9 of 10` · `11 of 12`; `Purchase → Inventory` outside both registers; class `12` uncovered | `06_` |
| `MF-03` `NOT DECIDABLE`; `8 of 11` properties differ | `07_` |
| `10 of 21` denominators sound | `08_` |
| `18 of 25` source rows below floor; `2` dimensions unmeasurable; `6 of 9` critical below `100 %` | `09_` |
| `11` FD blockers; `3` obligations dropped; `CORE-07` collision | `10_` |
| `2` of Round 2's `17` accounts required correction | `01_` |

**`CORR2` was executed by Claude Opus 5 and is itself NOT independent assurance.
Treat every line above as a claim by an interested party.**

---

> ## `IF INDEPENDENCE CANNOT BE PROVEN: STOP. DO NOT EXECUTE AUDIT.`

No Evidence = No Progress. Never Skip Gate. Truth over Pass. Falsify before Accept.
**Boss is the SOLE FINAL APPROVER.**
