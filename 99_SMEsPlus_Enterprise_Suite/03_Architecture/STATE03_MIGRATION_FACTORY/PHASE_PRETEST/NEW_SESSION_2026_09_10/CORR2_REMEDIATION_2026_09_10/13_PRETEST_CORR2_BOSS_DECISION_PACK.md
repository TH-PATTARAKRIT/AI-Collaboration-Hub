# 13 — PRE-TEST `CORR2` BOSS DECISION PACK

# `2 DECISIONS ASKABLE · 2 CARRIED OPEN AND NOT ASKABLE`

## `CHECKPOINT M — BOSS DECISION SET MINIMIZED`

Session: `[SMEPLUS-26-09-10-PHASE-PRETEST-B7R2-REMEDIATION-NEWSESSION-001]`
Baseline: `c91d5840` · Boss: **SOLE FINAL APPROVER**

> **`§28`: Boss receives only irreducible Boss-authority decisions.** `05_` §4 tested `8` candidates and
> resolved `7` below Boss. **Nothing in this pack is evidence search, arithmetic, crosswalk derivation,
> denominator counting, stale-evidence correction, statutory interpretation or reachability testing.**

---

## 1. THE POPULATION — `4` open, `2` askable this turn

| # | ID | Askable now? | Why |
|---:|---|---|---|
| `1` | **`BOSS-CORR2-RD01`** | **YES — NEW** | only an authority can ratify its own act |
| `2` | **`BOSS-CORR1-01`** | **YES** | only `B4′`'s issuer may amend its membership; **the evidence it waited for is now supplied** |
| `3` | `SC-SMT-01` | **NO** | open, Boss-owned, **no material delta** — re-asking is prohibited. **Carried as OPEN** |
| `4` | `POH-D-02` | **NO** | Thai statutory evidence absent; **no Boss act can move it**. **Carried as OPEN** |

**Both non-askable items remain in the open count.** `05_` §3 records why removing them was the defect.

---

## 2. `BOSS-CORR2-RD01` — RATIFY, VARY OR WITHDRAW `R-D-01`

### The question

**`R-D-01` re-placed `4` Pre-Test exit criteria and produced the headline `7 / 13`.
No ruling artefact for it exists anywhere in the repository.**

### The evidence

```
'RECOVERY-BOSS-RULING'  across ALL 194 remote branches, whole trees   ->  1 occurrence
   and that occurrence is a session id in the header of the
   challenged party's own application file (14_ line 6)
POSITIVE CONTROL — 'BOSS-RESOLUTION-001', the prior ruling round     ->  9 files,
   including a dedicated capture artefact (21_) and an application register (22_)
```

**What is NOT in question:** the phase-placement *analysis*. `SC-54` cl. `2` and cl. `3` and `SA17` §2b
place `EC-04`, `EC-07` and the `48` items at downstream gates **by their own terms**, verified verbatim.
**Those three re-placements stand under every option below.**

**What IS in question:** whether the authority act occurred, and what happens to condition `11`.

### The options

| | Option | Denominator | Consequence |
|---|---|---:|---|
| **(a)** | **RATIFY as executed** — all `4` re-placed, condition `11` leaves | `13` | **`E2E-04`'s `G`-limb re-grade act becomes owed at no gate.** `14_` L92 already calls it *"live, **unowned by any gate the ruling moved**"*. **An obligation would be extinguished by arithmetic** |
| **(b)** | **RATIFY IN PART · RECOMMENDED** — `9`, `10`, `12` re-placed on their own instruments; **condition `11` retained**, `D`/`I` limbs recorded downstream, **`G` limb live at Pre-Test** | **`14`** | Matches `SC-54`/`SA17`; matches `09_` §2.4 (*"correctly placed and simply outstanding"*); matches `14_` L88–92; **matches the package's own resume state L227 — `7 of 14`, `3` re-placed** |
| **(c)** | **WITHDRAW** — nothing re-placed | `17` | Re-imposes `2` genuine circular gate defects that no instrument places at Pre-Test |

### Why option (b)

**Three independent records already say `3` re-placements, not `4`:** the governing instruments name
three criteria; `09_` §2.4 places condition `11`'s `G` limb at Pre-Test; and the package's own designated
carrier published `7 of 14` before `14_` overrode it without reconciliation.

**Under every option Pre-Test remains `HOLD`** — conditions `3`, `4`, `16`, `17` fail at their own gate,
and **no option moves any dimension to `96 %`.** *(`03_` §4 publishes all three sensitivities.)*

### Also required, whichever option is ruled

**`SC-54` cl. `5` (`NO DUMPING`) was never applied** (`R2-F-07`). Its execution-dependency evidence is
present in substance for `EC-04` and the `48` items and **absent for `E2E-04`'s `D` limb**, which is a
design artefact rather than an execution artefact. **Under option (a) or (b), that limb's move needs
either cl. `5` evidence or an express exemption.**

---

## 3. `BOSS-CORR1-01` — THE `B4′` BOUNDARY DENOMINATOR

### Status

**Open. `0` closure evidence tree-wide. Absent from the recovery package entirely** — `9` files in
`CORRECTIVE_CLOSURE`, `3` lines in the resume state, **`0` in `RECOVERY_2026_09_10/`** (`05_` §3).

### The evidence Boss was waiting for is now supplied

`22_` §1.1 recorded the precondition: *"`12 ↔ 18 ↔ 10` **is now derivable and has not been derived**.
Until it is, the per-boundary applicability declaration (`SC-11` §6 #5) can name its `12` targets but
**cannot say which handoff rows each target covers**."*

**`06_` derives it.** The measurement, with its population declared:

| Measured | Result |
|---|---|
| `12 ↔ 18` | **exact `1 : 1`** on `XMC-H-01`…`-12`, verified label by label at the Boss text |
| `12 ↔ 10` | **`9 of 10` rows map · `11 of 12` classes reached** · `2` rows span multiple classes |
| Outside the declared set, **`XMC-H` population** | `4` gap-carrying — `-13`, `-14`, `-17`, `-18` |
| Outside the declared set, **union of both registers** | **`5`** — the four above **plus `Purchase → Inventory`** (contract row `4`), which is **in neither the `12` nor the `18`** |
| Configuration-defeasible `N/A` | **`+2`** — `-15`, `-16`; the primary source ordered their non-durability *"in the row"* and the recovery's registers dropped it |
| **So the count is** | **a FLOOR of `5`, up to `7` — not the fixed `4` published** |
| Declared class `12` (Close → subledgers) | **reached by NO contract row.** `XMC-H-12`: *"there is **no accounting-period object**"* |

### The options

| | Option | Effect |
|---|---|---|
| **(a)** | **`12` stands unchanged** | `5`–`7` gap-carrying flows remain outside the contract and Pre-Test denominators. `SC-11` §6 #5 stays **`STILL BLOCKED`**. `B7-F-08` (`B9′` admits flows onto the excluded `XMC-H-18`) stays **OPEN** |
| **(b)** | **`12 → 16`** — add `XMC-H-13`, `-14`, `-17`, `-18` | the option `28_` §4 already modelled. **Resolves `B7-F-08`.** Leaves `Purchase → Inventory` outside |
| **(c)** | **`12 → 17`** — `(b)` **plus `Purchase → Inventory`** | closes the measured union. **Goods receipt against a purchase order becomes a declared boundary** |
| **(d)** | **`12` stands, and the `5`–`7` are declared expressly out of scope**, with reasons | makes the exclusion **supported** rather than silent. `§24`: an unsupported exclusion counts as UNRESOLVED |

**No recommendation is made on membership.** `B4′` is a Boss declaration and its granularity is a scope
act. **What this session supplies is the measurement that was owed, and the observation that
`Purchase → Inventory` — goods receipt — is presently outside every tested register.**

**Note for the record:** `CORE-07` as cited in this context is the **recovery's** `CORE-07`. `CORR1`
defined `CORE-07` as the `FIFO` layer-granularity obligation. **The identifier was re-used** (`10_` §2).
**`10_` §5 renames the boundary obligation `CORE-08`;** Boss is asked to note the rename, not to rule it.

---

## 4. CARRIED OPEN, NOT ASKED

| ID | Status | Why not asked | Consequence while open |
|---|---|---|---|
| **`SC-SMT-01`** | **OPEN — Boss-owned** | `SC-11` §6 obl. `3` already records it: owner **Boss**, on SMEs Core input. **Re-asking without material delta is prohibited** | **gates readiness rows `X-08` and `X-09`**; a return-reversal case for an `Average`-costed category *"cannot state its expected accounting result"*; **FD blocker `11`** |
| **`POH-D-02`** | **OPEN — NOT ASKABLE** | Thai statutory evidence absent; `PTE-4`, excluded from `B1` by Boss's own ruling. **No Boss act can move it** | external dependency; **carried, unchanged** |

> **Both are counted `OPEN`. Askability is a separate field and is never an exclusion** (`05_` §6).

---

## 5. WHAT BOSS IS NOT ASKED

| Not asked | Resolved by | Where |
|---|---|---|
| The exit denominator | instrument reading — `SC-54`, `SA17`; independent of `R-D-01` | `03_` §1 |
| The `12 ↔ 18 ↔ 10` crosswalk | derived; **`CORE-04`/`CORE-07` mapping discharged** | `06_` |
| `MF-03`'s classification | re-derived → **`NOT DECIDABLE`**; not escalated | `07_` |
| `10_`'s `11` defective memberships | re-derived | `08_` |
| FD blocker count | recomputed → `11` | `10_` |
| Circular-gate-defect membership | settled at primary text | `03_` §5 |
| Independence remediation | apparatus design | `02_` §10, `16_` |
| Coverage percentages | measured | `09_` |

**`8` resolved below Boss · `2` asked · `2` carried.**

---

## 6. THE RULING BLOCK

```
=====================================================================
BOSS ONE-TURN RULING BLOCK — PRE-TEST CORR2
=====================================================================

BOSS-CORR2-RD01   R-D-01 ratification
                  (a) RATIFY AS EXECUTED        -> exit denominator 13
                  (b) RATIFY IN PART            -> exit denominator 14   [RECOMMENDED]
                  (c) WITHDRAW                  -> exit denominator 17
                  and: SC-54 cl.5 evidence for E2E-04's D limb, or an express exemption
   RULING: ______________________________________________

BOSS-CORR1-01     B4' twelve-boundary set
                  (a) 12 STANDS
                  (b) 12 -> 16   (add XMC-H-13, -14, -17, -18)
                  (c) 12 -> 17   (b, plus Purchase -> Inventory)
                  (d) 12 STANDS, and the 5-7 are declared out of scope WITH REASONS
   RULING: ______________________________________________

NOTED, NOT RULED  SC-SMT-01 and POH-D-02 remain OPEN and are not re-asked.
                  CORE-07 identifier collision: the boundary obligation is renamed CORE-08.

WHICHEVER IS RULED: Pre-Test remains HOLD. No option reaches any 96% floor.
                    Functional Design is NOT authorized.
=====================================================================
```

---

## 7. CHECKPOINT

> ## `CHECKPOINT M — BOSS DECISION SET MINIMIZED`
>
> **`4` open · `2` askable · `2` carried** · `8` candidates resolved below Boss ·
> `1` new Boss item, and only because **no party below Boss can ratify a Boss act** ·
> **`BOSS-CORR1-01`'s precondition evidence is supplied** — the derivation it waited for is executed ·
> `0` denominators amended by this session · `0` recommendations on `B4′` membership ·
> **`0` repeated questions without material delta.**

No Evidence = No Progress. Never Skip Gate. Truth over Pass. Boss is the SOLE FINAL APPROVER.
