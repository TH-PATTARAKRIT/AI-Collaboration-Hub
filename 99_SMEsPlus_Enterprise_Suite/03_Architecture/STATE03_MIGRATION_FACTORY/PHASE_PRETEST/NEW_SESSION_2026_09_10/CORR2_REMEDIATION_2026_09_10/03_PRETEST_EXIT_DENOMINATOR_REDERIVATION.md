# 03 — PRE-TEST EXIT DENOMINATOR RE-DERIVATION

# `DENOMINATOR = 14 · SATISFIED = 5 · 35.7 %`

## `CHECKPOINT D — RE-DERIVED FROM NAMED AUTHORITY, NOT FROM 13 OR 14`

Session: `[SMEPLUS-26-09-10-PHASE-PRETEST-B7R2-REMEDIATION-NEWSESSION-001]`
Baseline: `c91d5840` · Boss: **SOLE FINAL APPROVER**

> **`§14` forbids beginning from `13` or from `14`. Neither was assumed.** The population is re-derived
> from the master-prompt `17`, and each member's **inclusion is decided by the criterion's own governing
> instrument**, read at primary text.

---

## 1. THE DECISIVE STRUCTURAL FINDING — the denominator does not depend on `R-D-01`

`R-D-01`'s authority is **UNEVIDENCED** (`04A_`). That would ordinarily place the whole `17 → 13` move at
issue. **It does not, and the reason matters:**

| Re-placed condition | Rests on | Durable? |
|---|---|---|
| `9` `EC-04` | **`SC-54` cl. 3**, verbatim: *"`EC-04` is closed by EXECUTED RUNTIME PROOF plus INDEPENDENT REPRODUCTION, evidenced **no later than the STATE 8-Criteria Exit Gate**"* | **YES — external instrument, on the tree** |
| `10` `EC-07` | **`SC-54` cl. 2**: *"An internal `PHASE` transition inside a State is **NOT** an eight-criteria gate"*; cl. 4 places `Phase SA → Pre-Test` as internal | **YES — external instrument, on the tree** |
| `12` `48`-item verification | **`SA17` §2b**: *"nothing may be read as testing tenant isolation until an implementation exists"* | **YES — external instrument, on the tree** |
| `11` `E2E-04` `D`/`I` limbs | **no external instrument** — `09_` §2.4 constructs the limb split; `8C-CLARIFICATION-01` governs `EC-01`…`EC-08` and `E2E-04` is not an `EC` | **NO — executor's own reading (`R2-F-05`)** |

> ## **Conditions `9`, `10` and `12` leave Pre-Test on the authority of their own instruments, whether or
> ## not `R-D-01` is ever ratified. `R-D-01` is only load-bearing for condition `11` — the one
> ## re-placement that has no external instrument.**

**Consequence, and it minimises the Boss set (`§28`):** the denominator is **`14` under both readings**.
Reading an instrument that says where a criterion attaches is **analysis correcting a mis-registration**,
not a scope act. **The denominator question is resolvable below Boss and is resolved here.**
`04A_` still requires ratification of the **act**; it does not change the **number**.

---

## 2. THE SEVENTEEN, RE-DERIVED

Legend — **Limb:** `S` semantic/architecture · `D` functional design · `I` implementation/runtime ·
`G` governance/independent assurance.

| # | Obligation | Limb | Governing authority | Original gate | `R-D-01` treatment | **Current gate** | **Status** | Evidence | **Incl.** | Reason |
|---:|---|---|---|---|---|---|---|---|:---:|---|
| **1** | Canonical `12`-gate set established | `S` | **Boss `B4′`** (`22_` §1) | Pre-Test | — | **Pre-Test** | **SATISFIED — QUALIFIED** | `12` members named verbatim at `22_` §1; verified `1:1` onto `XMC-H-01…12` (`06_`) | **YES** | Pre-Test-placed; established. *Established ≠ sufficient* — coverage qualified by `06_` |
| **2** | Gate state freshly re-derived | `S`+`G` | master prompt §19 | Pre-Test | — | **Pre-Test** | **`FAIL` — DOWNGRADED from `SATISFIED — QUALIFIED`** | `18/4/0` re-derived and **exact** (`04_`, re-verified `01_` §3) — **but its sole cure was *"an independent party has attacked it"***, falsified at `02_`; and `10_` records its authority as **`NONE — B5′ unruled`**, independently verified **`NO`** | **YES** | **`B7-F-13`'s circularity is NOT cured. Over-determined: `B5′` unruled, so the figure is `PROVISIONAL` in any case** |
| **3** | Outputs have consumers / terminal classification | `S` | `PT-04`, `B10′` | Pre-Test | — | **Pre-Test** | **`FAIL`** | `2` outputs + `1` orphan input remain `D ORPHAN` | **YES** | Pre-Test-placed, genuinely failing |
| **4** | Ship → … → downstream complete | `S` | `PT-04` | Pre-Test | — | **Pre-Test** | **`FAIL`** | `O-1`, `O-3` open | **YES** | Pre-Test-placed, genuinely failing |
| **5** | Material-flow enumeration reconciled | `S` | Boss `B9′` | Pre-Test | — | **Pre-Test** | **`FAIL` — DOWNGRADED from `SATISFIED — QUALIFIED`** | **three independent grounds fail:** `IR 20` membership unsupported (**max `6`** distinct `IR-nn` in the path set) · `AR 30` unsupported (**max `10`**) · **`MF-03`'s exclusion falsified** (`07_`) · already qualified by `B7-F-08` | **YES** | **An enumeration whose members are not named is not reconciled** |
| **6** | Classification tie-break deterministic | `S` | Boss `CC-D-01` (`22_` §3) | Pre-Test | — | **Pre-Test** | **SATISFIED** | (a) Opt 3 · (b) Opt B ruled; refusal rule specified at `32A_` | **YES** | Closed on a Boss act |
| **7** | Veto count reconciled | `G` | Boss `B3′` | Pre-Test | — | **Pre-Test** | **SATISFIED** | `7` ratified; second-line attack **failed**; re-measured `01_` §3 | **YES** | Closed on a Boss act |
| **8** | `RT-E15` / `PTX` reconciled | `S` | Boss `B6` | Pre-Test | — | **Pre-Test** | **SATISFIED** | `1:1`; denominator `11`; **`PTX-01`…`-11` enumerated in `2` files — membership proved, not asserted** | **YES** | Closed on a Boss act, membership verified |
| **9** | `EC-04` complete | `I` | **`SC-54` cl. 3** | Pre-Test | **→ STATE 8-Criteria Exit Gate** | **State gate** | `0 / 3` — deferred, **not discharged** | runtime proof cannot exist pre-build | **NO** | **Re-placed by its own instrument.** `CGD-01` genuinely resolved: the receiving gate is downstream of the build the criterion requires |
| **10** | `EC-07` complete | `G` | **`SC-54` cl. 2 / `SC-AUTH-02` Reading C** | Pre-Test | **→ MODULE + STATE gates** | **Module + State** | `0 / 2` — deferred, **not discharged** | neither B-7 round is a clean independent pass (`02_`) | **NO** | **Re-placed by its own instrument.** Pre-Test is an internal transition, expressly not an eight-criteria gate |
| **11** | `E2E-04` traversable | `S`+`D`+`I`+**`G`** | `SC-01` §6.2; Boss `E2E-04` clause; `B8′` | Pre-Test | `D`→FD Exit · `I`→Build/Test · **`G` limb NOT moved** | **Pre-Test (`G` limb)** | **`FAIL`** | `S` limb **closed**. **`G` limb — the re-grade act — `OUTSTANDING`.** `14_` L88 places it at *"the appropriate architecture / **Pre-Test** control point"*; `09_` §2.4: *"**correctly placed and simply outstanding**"*; `14_` L92: *"**stays live, unowned by any gate the ruling moved**"* | **YES** | **A Pre-Test-placed obligation is live. The condition cannot leave the denominator while a limb of it is owed at this gate** |
| **12** | `48`-item verification complete | `I` | **`SA17` §2b** | Pre-Test | **→ BUILD / TEST GATE** | **Build/Test** | `0 PASS · 0 FAIL · 48 HOLD` | nothing testable pre-implementation | **NO** | **Re-placed by its own instrument.** `CGD-03` genuinely resolved |
| **13** | `CP-PT-14` resolved | `G` | `PT-14` | Pre-Test | — | **Pre-Test** | **`FAIL`** | authority sufficient; **NOT REACHED** — consumption is of a challenge against a superseded package | **YES** | Pre-Test-placed, failing |
| **14** | `8` `PT-16` §11 items disposed | `S` | `PT-16` §11 | Pre-Test | — | **Pre-Test** | **SATISFIED** | `8 of 8`, `0` disappeared | **YES** | Closed |
| **15** | B-7 completed independently | `G` | master prompt §19; `B7′` | Pre-Test | — | **Pre-Test** | **`FAIL` — RE-OPENED, OVER-DETERMINED** | **(i)** Round 1 read `c94839e8`, which this baseline supersedes (`08_` row `15`) · **(ii)** **neither round is independent** (`02_`) | **YES** | Pre-Test-placed. **Two independent reasons now** |
| **16** | No unresolved material contradiction | `S` | master prompt §19 | Pre-Test | — | **Pre-Test** | **`FAIL`** | `B7-F-08` **OPEN** (`B9′` admits flows onto `XMC-H-18` that `B4′` excludes); **and `06_` adds a second** — declared class `12` covered by no contract row while `05_` publishes the crosswalk as complete | **YES** | Pre-Test-placed, failing |
| **17** | No missing business semantic invented | `S` | master prompt §19 | Pre-Test | — | **Pre-Test** | **`FAIL`** | **`5`** FD blockers remain (`10_`), not `3` | **YES** | Pre-Test-placed, failing |

---

## 3. TALLY — arithmetic executed and shown

```
POPULATION : the 17 master-prompt exit conditions
UNIT       : exit condition
PATTERN    : inclusion decided by the condition's OWN governing instrument, read at primary text
PATH SET   : NEW_SESSION_2026_09_10/** at c91d5840, plus the named external instruments SC-54, SA17
AUTHORITY  : master prompt §19 (population) · SC-54, SA17, B4′, B3′, B6, B9′, CC-D-01 (per member)

RE-PLACED OUT (own instrument)      {9, 10, 12}                     = 3
DENOMINATOR                         17 - 3                          = 14
SATISFIED                           {1, 6, 7, 8, 14}                = 5
FAILING                             {2, 3, 4, 5, 11, 13, 15, 16, 17} = 9
CHECK                               5 + 9                           = 14  ✓
PERCENTAGE                          5 / 14                          = 35.71 %
```

**`0` of the re-placed three were satisfied — the numerator was structurally incapable of rising.**

---

## 4. RECONCILIATION AGAINST EVERY PUBLISHED FIGURE

| Figure | Source | Where it differs |
|---|---|---|
| `7 / 17` = `41.2 %` | `08_`, `31_` | pre-re-placement |
| **`7 / 14` = `50.0 %`** | **resume state L227** — *"`7 of 14` Pre-Test-owned; **`3` re-placed**"* | **the package's own carrier already re-placed exactly `3` and retained condition `11`. This session's denominator reproduces it independently** |
| `7 / 13` = `53.8 %` | `14_` §2, and the headline | **removes condition `11` as a fourth** — the only re-placement with no external instrument |
| `7 / 14` = `50.0 %` | `d878a603` `R2-F-06` | restores `11`; retains conditions `2` and `5` as satisfied |
| `6 / 14` = `42.9 %` | `d878a603` §15, conditional | withdraws condition `2`'s cure |
| **`5 / 14` = `35.7 %`** | **this file** | **also withdraws condition `5`** — `IR`/`AR` membership unsupported and `MF-03`'s exclusion falsified (`07_`, `08_`) |

> **The published `53.8 %` is `18.1` points above the instrument this session can support, and the gap is
> entirely membership and ground — `0` obligations were closed to create it.**

**Sensitivity, stated so no reader over-reads a single number:**

| If | Then |
|---|---|
| `R-D-01` is **withdrawn** by Boss | denominator **`17`**, satisfied **`5`** = `29.4 %` |
| `R-D-01` is **ratified** as executed (`4` removed) | denominator **`13`** — **but condition `11`'s `G` limb is then owed at no gate at all**, which `14_` L92 itself calls *"unowned"*. **This session does not recommend it** |
| condition `2`'s cure were restored (independence evidenced) | **`6 / 14` = `42.9 %`** |
| conditions `2` and `5` both restored | **`7 / 14` = `50.0 %`** |

**Under every reading the figure is far below the `96 %` floor. No reading reaches Pre-Test exit.**

---

## 5. CIRCULAR GATE DEFECTS — membership settled

`R2-F-17` established that `09_` publishes two incompatible sets for one count of `3`. **Settled here:**

| Condition | `09_` identifier verdict | `09_` §4.1/§5 verdict | **Canonical** |
|---|---|---|---|
| `9` | `CGD-01` — **CIRCULAR** | in the set | **CIRCULAR — resolved by re-placement to the State gate** |
| `10` | `CGD-02` — *"**Not strictly circular**"* by its own §2.2 | **not** in the set | **NOT CIRCULAR — a phase-placement error, resolved by re-placement** |
| `12` | `CGD-03` — **CIRCULAR** | in the set | **CIRCULAR — resolved by re-placement to Build/Test** |
| `11`-`D` | not an identifier; §2.4 says *"**not a defect**"* | in the set | **RELOCATED, NOT RESOLVED** — `D` limb moves to *"Functional Design Exit"*, a gate Pre-Test exit itself authorises entry to. Not a loop, but a gate-**entry** control converted into a gate-**exit** control |

> **`2` genuine circular gate defects (`9`, `12`), both resolved by re-placement.
> `1` phase-placement error (`10`), resolved.
> `1` relocation that is not a resolution (`11`-`D`) — and it is the limb with no external instrument.**
>
> **`14_` §5's *"`3` → `0`"* is not supported: the count was `2`, and `11`-`D` did not resolve.**

---

## 6. WHAT THIS RE-DERIVATION DOES NOT DO

| Not done | Why |
|---|---|
| Amend `B4′`'s `12` | Boss-reserved. `13_` carries evidence only |
| Waive any re-placement | `0` waived; each rests on a named instrument or is refused |
| Rewrite `08_`, `14_`, `31_` or the resume state | superseding artefact, `0` prior files modified |
| Treat condition `11`'s `G` limb as discharged | it is `OUTSTANDING` and counted as failing |
| Declare any condition satisfied that the package declared failing | **`0` upgrades. Every movement in this file is a downgrade** |

---

## 7. CHECKPOINT

> ## `CHECKPOINT D — EXIT DENOMINATOR RE-DERIVED`
>
> **Denominator `14`** — derived from named instruments, **not** from `13` and **not** from `14` ·
> **`3` re-placed on their own external authority, independent of `R-D-01`** ·
> **condition `11` restored** — a Pre-Test-placed `G` limb is live and the package says so twice ·
> **`5 of 14` = `35.7 %`** · `2` conditions downgraded on evidence (`2`, `5`), **`0` upgraded** ·
> circular gate defects **`3` → `2` genuine, `1` misclassified, `1` relocated** ·
> **the denominator does not depend on the unevidenced ruling — one fewer question for Boss.**

No Evidence = No Progress. Never Skip Gate. Truth over Pass.
**Boss is the sole Final Approver.**
