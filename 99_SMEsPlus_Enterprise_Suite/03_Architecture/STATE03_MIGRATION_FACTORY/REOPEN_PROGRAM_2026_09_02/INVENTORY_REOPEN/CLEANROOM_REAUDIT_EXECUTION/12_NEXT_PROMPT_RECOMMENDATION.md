# 12 — Next Prompt Recommendation

In priority order, per `10_REMEDIATION_ACTION_REGISTER.md` and `11_BOSS_FINAL_GATE_PACKAGE.md` §3–4:

1. **Boss written decision on CORR-007B git-history containment** (`10` item 1). This is not a prompt for an AI session — it is a Boss-only decision among the four named options. Nothing else in this program should proceed to Team B/C authorization for the CORR-007B evidence chain until this is decided.
2. **Boss written ratification of this session's `C-05` tie-breaking read** (`10` item 3) — a short, direct decision: accept, reject, or request a further independent pass on `02_CORR007B_C05_CLEAN_ROOM_REAUDIT.md`.
3. **A narrow, single-file remediation session** for menu deliverable `10` (warehouse/location/route/rule map) — rewrite §2 per `10` item 2. Small enough to be a short, bounded prompt; does not require the full checkpoint structure of this session.
4. **Boss written ruling on `U-07`** (which 9-Veto Charter governs) — foundational for every subsequent AI-Audit pass in this program, this one included.
5. **A dedicated, deeper independent-verification pass on files `18` (migration/data-quality) and `19` (security/permission)** if Boss wants them elevated from `SAFE_FOR_AI_AUDIT_ONLY` to `SAFE_FOR_TEAM_B_AFTER_BOSS_APPROVAL` — optional, not required by anything found in this session.
6. Only after 1–4 above are resolved: **TBRAC real-user validation** of the Thai naming register (`17`) and the (once rewritten) warehouse/location structure — this was already next in line per the menu package's own `26_NEXT_PROMPT_RECOMMENDATION.md`, and nothing in this re-audit changes that sequencing.

**Explicitly not recommended by this session:** any Team B design session, any Team C architecture session, any Development session, any merge to `SMEsPlus`, or any history rewrite of the CORR-007B branch performed unilaterally by an AI session. Item 1's option (c) (history purge) is named as a technical possibility in `10`, not as a recommendation — it is a destructive, shared-state operation that only Boss should authorize, with full awareness of what it does to any existing clone or fork of this branch.

No Evidence = No Progress. Never Skip Gate. Boss = Sole Final Approver.
