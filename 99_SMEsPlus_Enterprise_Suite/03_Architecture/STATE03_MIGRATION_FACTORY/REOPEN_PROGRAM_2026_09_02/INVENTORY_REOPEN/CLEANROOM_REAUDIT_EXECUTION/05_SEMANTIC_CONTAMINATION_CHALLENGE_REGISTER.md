# 05 — Semantic Contamination Challenge Register

Challenge posed per item: has benchmark (Odoo-family) behavior become SMEsPlus target design, schema, workflow, or UI approval by stealth — i.e. through meaning or structure, not literal tokens?

| # | Item | Challenge finding | Classification |
|---|---|---|---|
| 1 | 29 menu deliverables' process maps (files `06`–`16`) | Content is consistently framed as "the benchmark does X" / "SMEsPlus must decide Y", not "SMEsPlus does X." Sampled sections use SMEsPlus's own generic notation and repeatedly attach open gaps (`GAP-MD-01..31`) rather than closed conclusions. No section read asserts a benchmark UI layout or field set as SMEsPlus's own screen design. | `SAFE_CLEAN_ROOM_LEARNING` |
| 2 | Warehouse/location structure (file `10`) | Per `03_MENU_PACKAGE_MECHANICAL_LEAKAGE_SCAN.md` §5, the five-node `WH/Stock`/`WH/Input`/`WH/Quality`/`WH/Output`/`WH/Packing` structure is the benchmark's own default location scaffold, carried over as if it were the candidate SMEsPlus structure, ahead of any Thai warehouse validation. This is the one item in this register where benchmark *structure*, not just vocabulary, risks becoming a de facto design default. | `NEEDS_WORDING_REWRITE` (structural, not code-level — matches `03`'s classification) |
| 3 | Thai naming register (`17`) | Candidate names explicitly framed as proposals for Boss/Team B to decide, not adopted labels; every row `UNVALIDATED`. No stealth-adoption pattern found. | `SAFE_CLEAN_ROOM_LEARNING` |
| 4 | Product classification (Stockable/Consumable/Service; `INV-FP-13`) | Both the reopen (`13` item 4, `UNKNOWN`) and the menu package's lineage register (`01` row 51) treat this as an open target-routing hypothesis, explicitly not generalized from the benchmark's `type`/`is_storable`/`consu`/`service`/`combo` split. Correct posture, independently re-confirmed here against the same source text verified in `01_MANDATORY_EVIDENCE_INTAKE_REGISTER.md`. | `SAFE_CLEAN_ROOM_LEARNING` |
| 5 | CORR-007B files `08`/`09`, current branch surface | Independently re-scanned in `02_CORR007B_C05_CLEAN_ROOM_REAUDIT.md` §2.2 — zero prescriptive "must use the reference model" language remains. | `SAFE_CLEAN_ROOM_LEARNING` (surface only — see item 6) |
| 6 | CORR-007B files `08`/`09`, pre-remediation git history (`ac9e1e40`, `0eb78c68`) | Contains confirmed verbatim Odoo source reproduction (`02` §2.1) and is not access-restricted (`02` §2.3). This is the single highest-risk semantic-contamination surface identified anywhere in this re-audit — the risk is exposure in the exact path a future Team B author would encounter if they searched repository history without being told to stop at the branch tip. | `NEEDS_QUARANTINE` |
| 7 | Menu package's own use of CORR-007B evidence (`01` rows 76–77) | Explicitly states file `08` "not opened; content taken from reopen `14`/`20` summaries" and file `09` "Only Layer 1 text used." Correctly declines the higher-risk file even in remediated form, and never touches the pre-remediation history. | `SAFE_CLEAN_ROOM_LEARNING` |
| 8 | `U-07` — rival "9 Veto Challenge Council" definitions found inside CORR-007B's own branch | Genuinely unresolved authority question, material because it affects whether this session's own 9 Veto/9 Special Team/4 Overlay pass (`07`–`09`) is measured against the correct, Boss-ratified charter. This session follows the same convention the menu package adopted (`01` row 91): the ratified Charter `SMEPLUS-GOV-9VETO-001` governs; the rival document is noted, not followed. | `BOSS_ONLY_REVIEW` |
| 9 | Statutory Thai claims surfaced anywhere in the reviewed packages | Consistently marked `HOLD / EVIDENCE REQUIRED` or routed to the Accounting-Tax track wherever sampled. No instance found where a statutory claim was stated as settled. | `SAFE_CLEAN_ROOM_LEARNING` (posture correct; substance remains `HOLD` by design) |

## Summary

Of the nine items challenged: seven `SAFE_CLEAN_ROOM_LEARNING`, one `NEEDS_WORDING_REWRITE` (item 2, structural carry-over in file `10`), one `NEEDS_QUARANTINE` (item 6, unrestricted git history), one `BOSS_ONLY_REVIEW` (item 8, charter authority). No item is classified `FAIL / FROZEN`: nothing found has actually propagated into a downstream SMEsPlus design, schema, or workflow document that has been merged or authorized (confirmed in `06_DOWNSTREAM_RELIANCE_CLASSIFICATION_MATRIX.md`). Both flagged items are live exposure/carry-over risks, not confirmed contamination of an approved deliverable.
