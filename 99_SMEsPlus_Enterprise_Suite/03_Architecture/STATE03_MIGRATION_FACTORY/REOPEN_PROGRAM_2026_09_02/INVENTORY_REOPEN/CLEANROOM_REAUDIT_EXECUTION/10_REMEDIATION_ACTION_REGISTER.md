# 10 — Remediation Action Register

Every item below traces to a specific finding earlier in this package. None is executed by this session; each names its owner and the decision required.

| # | Item | Source finding | Required action | Owner | Blocking? |
|---|---|---|---|---|---|
| 1 | CORR-007B pre-remediation git history (`ac9e1e40`, `0eb78c68`) is reachable by any standard repository clone, not restricted to Boss/PMO/AI-Audit as the Layer 2 model assumes | `02` §2.3, `05` item 6, `06` | **Boss decision required among named options (this session executes none):** (a) accept the current risk explicitly, treating "usage-policy quarantine" as sufficient and formally documenting that decision; (b) restrict repository read access more narrowly than current program-wide access; (c) a history-rewrite/purge of the two commits (technically possible via `git filter-repo` + coordinated force-push) — **this is a destructive, hard-to-reverse, shared-state operation that this session explicitly does not perform and recommends only with full awareness that it rewrites branch history other clones may already hold**; (d) leave history intact and instead add an explicit, prominent warning label at the top of `17_CORR007B_CLEAN_ROOM_REMEDIATION_RECORD.md` directing any future reader away from the pre-remediation commits by SHA. | Boss | **Yes — blocks unconditional `SAFE_FOR_TEAM_B_AFTER_BOSS_APPROVAL` status for the CORR-007B evidence chain.** |
| 2 | Menu deliverable `10` (warehouse/location/route/rule map) carries the benchmark's default five-node location scaffold and slash-path notation forward as if it were validated SMEsPlus structure | `03` §5, `05` item 2, `08` lens 8 | Rewrite §2 of file `10` to (a) replace the `WH/xxx` path notation with prose role descriptions, and (b) explicitly mark the five-node set itself as benchmark-derived and unvalidated, pending TBRAC field input on what Thai SME warehouses actually use. Narrow, mechanical, does not require Team B authorization to perform. | Author of the menu package (or PMO on its behalf) | No — narrow, correctable; does not block the rest of the menu package. |
| 3 | This session's own tie-breaking read of CORR-007B files `08`/`09` (the action `10_CLEANROOM_IP_PROVENANCE_VETO_FINDINGS.md` §7 called for) has now been performed, but has not yet been formally ratified by Boss | `02` §5 | Boss reviews `02_CORR007B_C05_CLEAN_ROOM_REAUDIT.md` and issues a written ruling on whether this session's independent finding (both defect types confirmed resolved on the branch surface) is accepted as the single non-conflicting record `10` §7 required. | Boss | Yes — until ratified, the branch-surface `SAFE_FOR_TEAM_B_AFTER_BOSS_APPROVAL` label in `06` remains conditional, not final. |
| 4 | `U-07` — two non-cross-referencing "9 Veto Challenge Council" definitions exist in the repository, both claiming Boss approval | `05` item 8, `07` lens 8 (carried, not newly discovered by this session) | Boss issues a written ruling on which charter document governs, or formally reconciles the two. This session followed the ratified Charter `SMEPLUS-GOV-9VETO-001` by convention, consistent with the menu package's own choice, but did not resolve the underlying conflict — that is outside any AI session's authority. | Boss | No new blocking effect from this session (already carried from the reopen); affects the *evaluation standard* used by any future AI-Audit pass, including this one. |
| 5 | Migration/data-quality register (`18`) and security/permission register (`19`) were read for framing only in this session, not independently re-derived to the same depth as the CORR-007B and file-`10` findings | `06` (labels these `SAFE_FOR_AI_AUDIT_ONLY` rather than a stronger label) | If Boss wants these two files elevated to `SAFE_FOR_TEAM_B_AFTER_BOSS_APPROVAL`, a dedicated follow-up pass with the same depth of independent verification used elsewhere in this package should be run against them specifically. | PMO / next AI-Audit session | No — conservative labeling already applied; not a blocker, an option. |

## Priority for Boss

1. **Item 1** — the git-history containment decision. This is the single highest-priority action in this register: it is the specific, named condition this session's `C-05` verdict (`02` §5, "SURFACE REMEDIATED / HISTORY QUARANTINE REQUIRED") depends on to ever become "CLOSED."
2. **Item 3** — formal ratification of this session's tie-breaking read, since it is the specific deliverable this session was commissioned to produce.
3. **Item 4** — `U-07`, a foundational authority question underneath every AI-Audit pass in this program, this one included.
4. **Item 2** — narrow, low-effort, high-traceability-value fix.
5. **Item 5** — optional depth increase, not a defect.
