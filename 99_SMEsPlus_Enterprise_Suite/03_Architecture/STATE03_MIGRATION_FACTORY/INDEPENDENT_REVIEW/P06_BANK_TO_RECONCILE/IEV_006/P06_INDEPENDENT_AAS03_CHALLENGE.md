# P06_INDEPENDENT_AAS03_CHALLENGE.md

**Prompt:** `[SMEPLUS-26-09-06-G02-P06-B2R-INDEPENDENT-EXTERNAL-CORRECTION-VERIFICATION-006]` · §5 Stage E
**Track:** INDEPENDENT EXTERNAL VERIFICATION · frozen surface `1b018c1`
**Classification:** LAYER 2 — AUDIT QUARANTINE

---

## 0. How independence was constructed, and its limit

**Each of the four challengers ran in a separate context.** Each received only: the frozen extract path, the file counts by directory, a minimal statement that the package corrects its own claims in place under dated markers, its own remit, and three known instrument hazards. **None saw the audited package's narrative, any count, any prior register, or any other challenger's brief or conclusions.**

**They could not coordinate and did not.** Where two reached the same finding independently — the blocker count, the prescribed command's path set — that agreement is evidence. Where one reached a finding alone, it was re-executed by the main verifier before adoption.

**Limit:** same model family; the main verifier wrote all four briefs and therefore chose the remits; the main verifier adjudicates. **This is separation, not proof of independence.**

## 1. Expert 1 — Leader Functional Design (semantic and handoff consistency)

**SUPPORTED.** The repair arithmetic reconciles (21 + 3 = 24). `34_`:51's tally recounted independently from the 20 `F-` rows: 10/4/2/3/1 — exact. The assignment-site vs branch unit distinction is real and consistently applied at all four sites. `35_`:97/:99, `36_`:58, `56_`:70/:78 are coherent as written. `21_`:42 and the publication-snapshot marker are the correct instrument for a figure that was true when written.

**CONTRADICTED — 8 material.** The blocker count 65 vs **67** in four repairs including both P11 packs · *"three vetoes"* / *"four active vetoes"* against **seven** · *"16 recorded author errors"* against **23** · a **third** surviving ordinal at `34_`:32 in the file whose correction claims two · **≥13** surviving *"filtered"* statements, eight of them in the file that produced the correction · `18_`:188 retaining the retired `B-44` half four lines below the narrowing · `36_`:42-43 gating on *"P08's absence"* · `18_`:7/:212 completion language against a HOLD terminal state · and **`41_`:6's five line citations all wrong by exactly 3, a defect the repair round manufactured by inserting the pointer into the file it points at**.

**NARROWED.** `01_`:115's insertion orphaned its sentence tail — *"With no bank statement in existence**."* is subjectless with an unclosed emphasis, and it carried the finding's force.

**MISSING EVIDENCE.** `18_`:214 claims 75 open items *"counted by a command executed after the final file was written"* and prints no command or path set — `P06-OQ-*` returns **68** over the root and **75** only with the subdirectories. No repair states which command produced `65`; had one been printed with its date, the defect would have been visible on the page.

**MATERIAL: YES ×8.**

## 2. Expert 2 — Leadership Database Design (denominator, population, identity)

**RETURNED AFTER PUBLICATION, AND ADJUDICATED.** **[Q-P06-01 / XRD-001, 2026-09-07]**

> **SUPERSEDED, retained as lineage.** This section previously read: *"**REPORT NOT RECEIVED AT PUBLICATION** … its remit is materially covered by Experts 1 and 4 … **This is recorded as a gap, not as a clean result.** A fourth challenger's report is outstanding, and **no finding in this package depends on it**."*

**Expert 2 returned twelve material defects, seven absent from the published eighteen** — `IEV-D-19` … `IEV-D-25` plus `IEV-D-26`. Its findings are set out in `P06_INDEPENDENT_VERIFICATION_ADDENDUM_E2.md` and each was re-executed by the main verifier before adoption.

**SUPPORTED.** Every reproducible count in the package reproduces, several per file — the round-5 census (30/15) is path-set-insensitive; round 6 reconciles 47 − 7 = 40; round 7 reconciles to exactly 21 across 14 files under its declared unit; `46_`'s 6/17/20/3/7/2 = 55 covers `B-01`…`B-55` with zero duplicates and zero omissions; no identifier is cited-but-undefined.

**CONTRADICTED — 12 material.** Chief: `13_`:95 (*"Open-item population … 66"*, measured **68**) — an untouched row one line below the repaired `:94`, same command, same claim class, **in no round's population and not in this audit's**. And `56_`:99's published archive pattern **cannot fire**: BRE matches 0 of 2 real spellings, ERE matches 0 of 5.

**MATERIAL: YES ×12.**

**And the gap itself was a finding.** The disposition that closed it — *"no finding in this package depends on it"* — was **falsified by its own author**. `IEV-I-05`.

## 3. Expert 3 — Lead Integration & Localization (outbound boundary truth)

**SUPPORTED.** The `X-08` family is clean across eleven statements and seven files; none asserts closure. No peer-owned open item is converted to closed — every `P06-B-27` statement is permissive. `54_` is the strongest outbound file in the package: three P07 BLOCKING dependencies accepted, all three reported defective, `DUP-01`/`DUP-02` explicitly refused. `52_` does not adjudicate P02. `70_` §7 and the per-`HO` *"MUST NOT ASSUME"* clauses are the correct outbound form. Time-bounded negatives at `12_`:84/:134, `11_`:85, `09_`:34-35, `53_`:15 are correct as written and must not be swept.

**CONTRADICTED — 11 material.** Chief among them: **`34_`:44 asserts a peer acceptance and cites P06's own sentence as the evidence** — the quoted string occurs twice on the tree, at `09_`:80 as P06's own consequence and here as P11's intake, while `35_`:158 and `37_`:60 call the same rows *net-new* and *"will land"*. And **`P06_AAS_PLUS_CONSOLIDATION.md`:50 states `HO-03`/`HO-04` "are labelled so"; the handoff pack's only delivery word is *"re-delivered"*, and two governance statements certify the labelling "verified by grep"**.

Also: `36_`:42-43 · `40_`:17/:35 · `37_`'s seven-versus-eight with P08 excluded · `52_`:86's *"sweep of all nine peer branches"* against `OQ-128`'s record that P01 was never consumed · `09_`:13/:98's withdrawn terminal-process framing · `18_`:80's *"no sibling package was read"* · `35_`:148's unqualified *"delivered"* · `12_`:133's *"Branches read (7)"* listing eight and omitting P08.

**NARROWED.** *"routed to P11 as `P11-C-08`"* drops the word *candidate* at `53_`:113 and `67_`:21.

**MATERIAL: YES ×11.**

## 4. Expert 4 — Lead Code & UI Architect (source, version, instrument)

**SUPPORTED.** **The source layer is sound.** `is_matched` — 4 top-level branches at `:436`/`:439`/`:442`/`:445`, 5 assignment sites at `:438`/`:441`/`:444`/`:450`/`:452` — exact. Every sampled v18 and v19 line number exact. The `account_move.py:1159-1160` SQL quotation reproduces byte-for-byte. All eight enumerative version counts verify: 791 / 1422 addons, 2 / 514 l10n, `addons_archive` 961 / 904, 791 + 961 = 1752. Two of three re-tested cross-version rows are byte-identical across generations. Zero real broken-emphasis instances.

**CONTRADICTED — 5 material.** `01_`:122 cites `:437` for the circular site; it is `:438`, and the same file gets it right twice · `01_`:122's *"two branches by configuration"* against its own correction's *"one"* — branch 3 is **amount**-gated · **`51_` publishes the retired *"filtered subset"* in three places with no correction marker at all, and rests `B-55` on *"791 versus 1422"* — a comparison that inverts on the corrected denominator of 1752** · the blocker count 65 vs 67 · the prescribed command's path set omitting `G02_VERIFICATION_2026_09_06/`.

**CONTRADICTED — against this verifier.** **`VER-E-04` does not reproduce**: the documented regex returns its hits under `/usr/bin/grep` with **exit status 0**. This verifier's first wording of that row was too generous and has been amended. *An auditor's charitable reading is itself an instrument, and it failed in the package's favour.*

**NARROWED.** *"circularly"* overstates — there is no compute cycle; `_compute_state` never depends on `is_matched`. And `:421` carries a stronger unmeasured clause: `or not liquidity.account_id.reconcile` sets `'paid'` regardless of residual whenever the liquidity account is non-reconcilable.

**MATERIAL: YES ×5.**

## 5. Dissent preserved

| Point | Position | Counter-position |
|---|---|---|
| `34_`:40/:99 *"the answer is delivered"* | **E1: material** — the word is used for a handoff the package holds undelivered | **Main verifier, after re-execution: NARROWED.** Both instances qualify in the same clause (*"delivered; the dependency is NOT closed"*, *"delivered, not accepted"*). **E1 over-calls; the unqualified instances are `35_`:148 and the `HO-04` heading, carried separately** |
| Severity of `01_`:115's orphaned tail | **E1: partly material** — the clause carrying the finding's force is now subjectless | **E4: not material** — the substantive correction is right and reproduces against source |
| `56_`:53's mandated form | **E1: a prescription, not a record** — it instructs every future writer to use the withdrawn term | no counter-position offered |
| Whether the source layer is trustworthy | **E4: yes, emphatically** — the citation layer is in good order | **E1/E3 do not contest it**; the defects are in claims *about the package*, not about the ERP |

**No challenger recommends discharging `AASP-VETO-07`. Three of the four state or imply the opposite.**

## 6. Convergence — the finding that carries the most weight

**Two challengers, in separate contexts, independently reached the blocker-count defect and the prescribed-command defect.** Neither could see the other. The main verifier then re-executed both at three declared scopes and confirmed **67**, and ran the prescribed command verbatim to get **25 / 18**.

**Three independent derivations of the same two defects is the strongest evidentiary result in this audit**, and both defects were introduced by the repair round itself.
