# A2 Runtime Access Gate

A2 blind must receive the same MODULE+QID set but no A1 answers. Before launch it requires a separately verified runtime/config observation profile identifying safe credentials/transport, read-only versus controlled-transaction authority, evidence capture path, reset/recovery rules and context separation. V7 runtime cleanliness alone does not satisfy this evidence-access contract.
